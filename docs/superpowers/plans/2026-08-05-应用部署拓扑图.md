# 应用部署拓扑图 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给每个已部署应用加一张手绘盒+箭头部署拓扑图（应用容器层 → 标 env 变量名的边 → 中间件实例层），纯前端、零新依赖、不改后端。

**Architecture:** 沿用前端既有范式：拓扑拼装抽 `lib/topology.ts` 纯函数（vitest node env，对标 `deps-ui.ts`），`TopologySection` 内联组件消费。数据从既有接口（`app.instances`+`deps`+`catalog.instances`）客户端拼装。

**Tech Stack:** Next.js 16 + React 19 + TypeScript 5 + Tailwind 4 + vitest 4（node env，纯函数）；pnpm workspace（包名 `frontend`，目录 `platform/frontend`）。

## Global Constraints

- **纯前端，严禁改后端**：复用 `GET .../apps/:id/detail`、`GET .../apps/:id/deps`、`GET .../deps/catalog`。
- **依赖 P5 的 `lib/deps-ui.ts`**（`export type Dep`/`CatalogInstance`/`DepsCatalog`）——已在分支 base（feat/app-topology 基于 feat/unified-dep-supply）。
- **vitest 仅纯函数/node env**：`vitest.config.ts` `environment:"node"`、无 jsdom/RTL。组件无单测，靠 `tsc --noEmit` + `next build` + .28 手动 e2e。
- **命令**（仓库根 `D:/Projects/智源-ANP平台`）：单测 `pnpm --filter frontend test`｜类型快检 `pnpm --filter frontend exec tsc --noEmit`｜构建 `pnpm --filter frontend build`。
- **commit 规范**：conventional commits scope `topology`；中文 body 可，每行 ≤100 字符；husky/commitlint + lint-staged(prettier) 自动跑；末尾带 `Co-Authored-By: Claude <noreply@anthropic.com>`。
- **分支**：`feat/app-topology`（已在其上）。
- **prettier 陷阱**：commit plan/spec 时 prettier 会改坏 .md 里的 tsx 代码围栏（如 `{expr}` → `{expr;}` 多分号，致语法错）。task brief 是意图基准；每步跑 `tsc --noEmit` 兜底；遇此语法异常做最小修正保语义并在报告披露。

---

## File Structure

| 文件                                          | 责任                                                                                                            | 任务   |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------ |
| `platform/frontend/lib/topology.ts`           | **新增**：`KIND_ENV_LABEL`/`kindEnvLabel`/`appBoxes`/`buildDepBoxes` + `AppBox`/`DepBox`/`AppInstanceLike` 类型 | Task 1 |
| `platform/frontend/lib/topology.test.ts`      | **新增**：纯函数 vitest 单测                                                                                    | Task 1 |
| `platform/frontend/app/applications/page.tsx` | **新增**：`TopologySection` 内联组件 + 卡片内接线（DepsSection 之后）                                           | Task 2 |

---

## Task 1: `lib/topology.ts` 纯函数 + 类型（TDD）

**Files:**

- Create: `platform/frontend/lib/topology.ts`
- Test: `platform/frontend/lib/topology.test.ts`

**Interfaces:**

- Consumes: `import type { CatalogInstance, Dep } from "./deps-ui"`（P5 已建）
- Produces: `kindEnvLabel(kind:string):string`、`appBoxes(instances):AppBox[]`、`buildDepBoxes(deps, instances):DepBox[]`；类型 `AppBox`/`DepBox`/`AppInstanceLike`（均 export）

- [ ] **Step 1: 写失败测试 `lib/topology.test.ts`**

```ts
// topology 纯函数测试（vitest node env）。跑法: pnpm --filter frontend test
import { describe, it, expect } from "vitest";
import { kindEnvLabel, appBoxes, buildDepBoxes, type AppInstanceLike } from "./topology";
import type { CatalogInstance, Dep } from "./deps-ui";

const ins = (o: Partial<AppInstanceLike> & { env: string }): AppInstanceLike => ({
  env: o.env,
  status: o.status ?? "running",
  url: o.url ?? "",
  host_port: o.host_port ?? 0,
  version: o.version ?? 0,
  image: o.image,
});

const cinst = (id: string, kind: string, host = "10.10.0.28", port = 5432): CatalogInstance => ({
  id,
  kind,
  name: id + "-name",
  supply_mode: "shared",
  host,
  port,
});

const dep = (kind: string, extra: Partial<Dep> = {}): Dep => ({
  kind,
  strategy: extra.strategy ?? "shared",
  status: extra.status ?? "bound",
  instance: extra.instance,
  token: extra.token,
  error: extra.error,
});

describe("kindEnvLabel", () => {
  it("已知 kind 命中 map", () => {
    expect(kindEnvLabel("pg")).toBe("DATABASE_URL");
    expect(kindEnvLabel("redis")).toBe("REDIS_ADDR");
    expect(kindEnvLabel("milvus")).toBe("MILVUS_ADDR");
  });
  it("未知 kind 兜底 KIND_ADDR", () => {
    expect(kindEnvLabel("foo")).toBe("FOO_ADDR");
  });
});

describe("appBoxes", () => {
  it("多 env 派生 + test 排在 prod 前", () => {
    const boxes = appBoxes([
      ins({
        env: "prod",
        host_port: 9200,
        url: "http://10.10.0.28:9200",
        version: 3,
        status: "running",
      }),
      ins({
        env: "test",
        host_port: 9100,
        url: "http://10.10.0.28:9100",
        version: 2,
        status: "building",
      }),
    ]);
    expect(boxes.map((b) => b.env)).toEqual(["test", "prod"]);
    expect(boxes[0]).toMatchObject({
      env: "test",
      port: 9100,
      version: 2,
      status: "building",
      host: "10.10.0.28:9100",
    });
  });
  it("url → host 解析；异常 url 兜底空串", () => {
    expect(appBoxes([ins({ env: "prod", url: "http://h:1" })])[0].host).toBe("h:1");
    expect(appBoxes([ins({ env: "prod", url: "not-a-url" })])[0].host).toBe("");
    expect(appBoxes([ins({ env: "prod", url: "" })])[0].host).toBe("");
  });
  it("空/undefined → []", () => {
    expect(appBoxes(undefined)).toEqual([]);
    expect(appBoxes([])).toEqual([]);
  });
  it("未列出的 env 排末（不崩）", () => {
    const boxes = appBoxes([ins({ env: "staging" }), ins({ env: "test" })]);
    expect(boxes.map((b) => b.env)).toEqual(["test", "staging"]);
  });
});

describe("buildDepBoxes", () => {
  it("bound dep 交叉引用 catalog 取 host/port/name", () => {
    const boxes = buildDepBoxes(
      [dep("pg", { instance: "sv1", status: "bound" })],
      [cinst("sv1", "pg", "10.10.0.28", 5432)]
    );
    expect(boxes[0]).toMatchObject({
      kind: "pg",
      status: "bound",
      envLabel: "DATABASE_URL",
      host: "10.10.0.28",
      port: 5432,
      name: "sv1-name",
      error: "",
    });
  });
  it("declared dep（无 instance）→ host 空/port 0", () => {
    const boxes = buildDepBoxes([dep("redis", { status: "declared" })], []);
    expect(boxes[0].host).toBe("");
    expect(boxes[0].port).toBe(0);
    expect(boxes[0].envLabel).toBe("REDIS_ADDR");
  });
  it("failed dep → 带 error", () => {
    const boxes = buildDepBoxes([dep("milvus", { status: "failed", error: "配额超限" })], []);
    expect(boxes[0].status).toBe("failed");
    expect(boxes[0].error).toBe("配额超限");
    expect(boxes[0].envLabel).toBe("MILVUS_ADDR");
  });
  it("dep.instance 找不到 catalog 项 → 空 host 不崩", () => {
    const boxes = buildDepBoxes(
      [dep("pg", { instance: "ghost", status: "bound" })],
      [cinst("other", "pg")]
    );
    expect(boxes[0].host).toBe("");
    expect(boxes[0].port).toBe(0);
  });
  it("空 deps → []", () => {
    expect(buildDepBoxes([], [cinst("sv1", "pg")])).toEqual([]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "D:/Projects/智源-ANP平台" && pnpm --filter frontend test`
Expected: FAIL（`./topology` 模块不存在 → 解析失败）。

- [ ] **Step 3: 写最小实现 `lib/topology.ts`**

```ts
// topology.ts 应用部署拓扑拼装的纯函数（kind→envLabel、应用盒、依赖盒）。
// 后端契约：appdeploy.DepDeclaration / CatalogInstance（见 ./deps-ui）。
// 测试见 topology.test.ts；组件消费见 app/applications/page.tsx (TopologySection)。
import type { CatalogInstance, Dep } from "./deps-ui";

// kind → 注入的 env 变量名（mwsupply 注入源，单源 map；新增 kind 同步此处）。
export const KIND_ENV_LABEL: Record<string, string> = {
  pg: "DATABASE_URL",
  redis: "REDIS_ADDR",
  milvus: "MILVUS_ADDR",
};

export function kindEnvLabel(kind: string): string {
  return KIND_ENV_LABEL[kind] ?? kind.toUpperCase() + "_ADDR";
}

// 应用容器盒（每个已部署 env 一个）。
export type AppBox = {
  env: string; // test / prod
  status: string; // running / building / failed / stopped ...
  host: string; // 从 url 派生（无 url 则空）
  port: number; // host_port
  version: number;
  image: string;
};

// app.instances 的形状（与 page.tsx 的 Instance 兼容；只取拓扑所需字段）。
export type AppInstanceLike = {
  env: string;
  status: string;
  url: string;
  host_port: number;
  version: number;
  image?: string;
};

const ENV_ORDER = ["test", "prod"];
function envRank(env: string): number {
  const i = ENV_ORDER.indexOf(env);
  return i === -1 ? ENV_ORDER.length : i;
}

function hostFromURL(url: string): string {
  if (!url) return "";
  try {
    return new URL(url).host;
  } catch {
    return "";
  }
}

// 从 app.instances 派生应用盒（仅含已部署 env；按 env 排序 test 前 prod 后）。
export function appBoxes(instances: AppInstanceLike[] | undefined): AppBox[] {
  return (instances ?? [])
    .map((i) => ({
      env: i.env,
      status: i.status,
      host: hostFromURL(i.url),
      port: i.host_port,
      version: i.version,
      image: i.image ?? "",
    }))
    .sort((a, b) => envRank(a.env) - envRank(b.env));
}

// 中间件依赖实例盒。
export type DepBox = {
  kind: string;
  strategy: string; // shared / bind_existing / dedicated
  status: string; // bound / declared / failed
  envLabel: string; // kindEnvLabel(kind)
  host: string; // 解析到的实例 host（bound 时从 catalog；无则空）
  port: number;
  name: string; // 实例名（bound 时）
  error: string; // failed 时的 last_error
};

// 由 deps + catalog 拼依赖盒：dep.instance(id) 交叉引用 catalog.instances 取 host/port/name。
// bound → 有实例信息；declared/failed → host/port 空盒（failed 带 error）。
export function buildDepBoxes(deps: Dep[], instances: CatalogInstance[]): DepBox[] {
  return deps.map((d) => {
    const inst = d.instance ? instances.find((c) => c.id === d.instance) : undefined;
    return {
      kind: d.kind,
      strategy: d.strategy,
      status: d.status,
      envLabel: kindEnvLabel(d.kind),
      host: inst?.host ?? "",
      port: inst?.port ?? 0,
      name: inst?.name ?? "",
      error: d.error ?? "",
    };
  });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd "D:/Projects/智源-ANP平台" && pnpm --filter frontend test`
Expected: PASS（新增 topology 用例全绿 + 既有回归全绿）。

- [ ] **Step 5: 提交**

```bash
cd "D:/Projects/智源-ANP平台" && git add platform/frontend/lib/topology.ts platform/frontend/lib/topology.test.ts && git commit -m "feat(topology): 拓扑拼装纯函数 + 类型" --trailer "Co-Authored-By: Claude <noreply@anthropic.com>"
```

（husky 自动 prettier；`--trailer` 不识别则 heredoc `-F -` 把 trailer 放 body。）

---

## Task 2: `TopologySection` 组件 + 接线

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx`
  - import 块加 `import { appBoxes, buildDepBoxes } from "@/lib/topology";`
  - 新增 `TopologySection` 内联组件（与 `DepsSection`/`ArtifactSection` 同级，置于 `DepsSection` 之前或之后）
  - 应用卡片内 `<DepsSection>` 之后加 `<TopologySection>` 接线

**Interfaces:**

- Consumes（Task 1）：`appBoxes`/`buildDepBoxes` from `@/lib/topology`；既有 `Dep`/`DepsCatalog` from `@/lib/deps-ui`；`API_BASE_URL` from `@/lib/api`
- Produces：无对外导出（`TopologySection` 为本文件内联组件，`ApplicationsPage` 内消费）

- [ ] **Step 1: 加 import**

第 5-9 行 import 区，加：

```ts
import { appBoxes, buildDepBoxes } from "@/lib/topology";
```

- [ ] **Step 2: 加 `TopologySection` 内联组件**

在 `DepsSection` 函数之前（与 `ArtifactSection`/`InstancesPanel` 同级）插入：

```tsx
// 部署拓扑：应用容器层 → 标 env 变量名的边 → 中间件实例层（手绘盒，两层）。
// 仅已部署应用（有 instance）渲染；external 不渲染（由父级条件保证）。
// deps/catalog 自 fetch（同 DepsSection 范式，best-effort）。
function TopologySection({ psID, appID, app }: { psID: string; appID: string; app: App }) {
  const [deps, setDeps] = useState<Dep[]>([]);
  const [catalog, setCatalog] = useState<DepsCatalog | null>(null);
  useEffect(() => {
    Promise.all([
      fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appID}/deps`).then((r) => r.json()),
      fetch(`${API_BASE_URL}/project-spaces/${psID}/deps/catalog`).then((r) => r.json()),
    ])
      .then(([d, c]) => {
        setDeps((d as Envelope<Dep[]>)?.data ?? []);
        setCatalog((c as Envelope<DepsCatalog>)?.data ?? null);
      })
      .catch(() => {});
  }, [psID, appID]);

  const boxes = appBoxes(app.instances);
  const depBoxes = buildDepBoxes(deps, catalog?.instances ?? []);
  if (boxes.length === 0) return null;

  const appBoxCls = (s: string) =>
    s === "running"
      ? "border-success/60 bg-success/10"
      : s === "building"
        ? "border-warn/60 bg-warn/10"
        : s === "failed"
          ? "border-danger/60 bg-danger/10"
          : "border-border bg-surface-2";
  const depBoxCls = (s: string) =>
    s === "bound"
      ? "border-success/60 bg-success/10"
      : s === "failed"
        ? "border-danger/60 bg-danger/10"
        : "border-warn/60 bg-warn/10"; // declared

  return (
    <div className="mt-3 rounded border border-border p-3">
      <div className="mb-2 text-sm font-semibold">🕸️ 部署拓扑</div>
      {/* 应用容器层 */}
      <div className="mb-1 text-xs text-text-muted">应用容器</div>
      <div className="flex flex-wrap gap-2">
        {boxes.map((b) => (
          <div key={b.env} className={`rounded border px-2 py-1 text-xs ${appBoxCls(b.status)}`}>
            <div className="font-medium">
              {app.name} · {b.env}
            </div>
            <div className="text-text-muted">
              :{b.port} · v{b.version} · {b.status}
            </div>
            {app.app_kind && (
              <div className="text-text-muted">
                {app.app_kind}
                {app.network_mode === "host" ? " · host 网络" : ""}
              </div>
            )}
          </div>
        ))}
      </div>
      {/* env 注入边标签 */}
      {depBoxes.length > 0 && (
        <div className="my-2 flex flex-wrap items-center gap-2 text-[11px] text-text-muted">
          <span>↓ 注入环境变量</span>
          {depBoxes.map((d) => (
            <span key={d.kind} className="rounded bg-surface-2 px-1.5 py-0.5 font-mono">
              {d.envLabel}
            </span>
          ))}
        </div>
      )}
      {/* 中间件实例层 */}
      <div className="mb-1 text-xs text-text-muted">中间件依赖</div>
      {depBoxes.length === 0 ? (
        <div className="text-xs text-text-muted">无中间件依赖</div>
      ) : (
        <div className="flex flex-wrap gap-2">
          {depBoxes.map((d) => (
            <div key={d.kind} className={`rounded border px-2 py-1 text-xs ${depBoxCls(d.status)}`}>
              <div className="font-medium">{d.kind}</div>
              <div className="text-text-muted">
                {d.host ? `${d.host}:${d.port}` : "(未供给)"} · {d.strategy} · {d.status}
              </div>
              {d.error && <div className="truncate text-danger">{d.error}</div>}
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 卡片内接线**

应用卡片里，现有 `<DepsSection psID={psID} appID={a.id} />` 与 `<NetworkModeSection .../>` 所在块（page.tsx 末段 `a.deploy_mode !== "external"` 条件内）之后加：

```tsx
{
  a.deploy_mode !== "external" && a.instances && a.instances.length > 0 && (
    <TopologySection psID={psID} appID={a.id} app={a} />
  );
}
```

- [ ] **Step 4: 类型快检**

Run: `cd "D:/Projects/智源-ANP平台" && pnpm --filter frontend exec tsc --noEmit`
Expected: exit 0（`AppBox`/`DepBox` 字段、`app.instances` 与 `AppInstanceLike` 兼容、`Envelope`/`App` 在文件作用域内）。

- [ ] **Step 5: 提交**

```bash
cd "D:/Projects/智源-ANP平台" && git add platform/frontend/app/applications/page.tsx && git commit -m "feat(topology): TopologySection 部署拓扑组件 + 接线" --trailer "Co-Authored-By: Claude <noreply@anthropic.com>"
```

（无 vitest——组件改动，靠 tsc + build + Task 3 e2e。）

---

## Task 3: 整体验证 + .28 部署

**Files:** 无（验证 + 部署）

- [ ] **Step 1: 全量单测**

Run: `cd "D:/Projects/智源-ANP平台" && pnpm --filter frontend test`
Expected: PASS（新增 topology 用例 + 既有全绿）。

- [ ] **Step 2: 全量构建**

Run: `cd "D:/Projects/智源-ANP平台" && pnpm --filter frontend build`
Expected: exit 0（27 路由静态预渲染）。

- [ ] **Step 3: 部署到 .28**（增量：scp 新增/改动文件 + 重建 frontend 容器）

```bash
cd "D:/Projects/智源-ANP平台"
SCPB="scp -i $HOME/.ssh/miscode -o PubkeyAcceptedAlgorithms=+ssh-rsa"
$SCPB platform/frontend/lib/topology.ts platform/frontend/lib/topology.test.ts root@10.10.0.28:/opt/anp/platform/frontend/lib/
$SCPB platform/frontend/app/applications/page.tsx root@10.10.0.28:/opt/anp/platform/frontend/app/applications/
# 重建 frontend 容器（容器内 pnpm build，几分钟）
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa -i "$HOME/.ssh/miscode" root@10.10.0.28 'cd /opt/anp && nohup docker-compose -f deploy/docker-compose.prod.yml up --build -d frontend >/tmp/fe.log 2>&1 &'
```

- [ ] **Step 4: 等重建完成 + 验证上线**

轮询 `docker ps --filter name=deploy_frontend_1` 至 `Up N seconds/minutes`（新建）；HTTP `:8088` 200；bundle grep 命中新字符串：

```bash
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa -i "$HOME/.ssh/miscode" root@10.10.0.28 '
  curl -s -o /dev/null -w "fe: %{http_code}\n" http://10.10.0.28:8088/
  docker exec deploy_frontend_1 sh -c "grep -rl 部署拓扑 /app/.next 2>/dev/null | head -2; echo ---; grep -rl DATABASE_URL /app/.next 2>/dev/null | head -2"'
```

- [ ] **Step 5: .28 手动 e2e**（浏览器 Ctrl+Shift+R 强刷 http://10.10.0.28:8088，admin 登录）
- [ ] 已部署应用见「🕸️ 部署拓扑」：应用容器盒（test/prod，status 着色）+ env 标签边（DATABASE_URL 等）+ 中间件盒（kind:host:port strategy·status）
- [ ] 仅 test 部署的应用 → 只画 test 应用盒
- [ ] failed 依赖盒带 error（若有）
- [ ] 未部署应用（无 instance）→ 无拓扑区
- [ ] external 应用 → 无拓扑区（无容器）

- [ ] **Step 6: 记账本 + 推送**

把结果回写 `.superpowers/sdd/progress.md`（topology 段）；`git push -u origin feat/app-topology`（独立 PR）。

---

## 自审（writing-plans skill）

**1. Spec 覆盖**：§4.1 纯函数+类型→T1；§4.2 组件+布局→T2 Step 2；§4.3 接线→T2 Step 3（自包含 fetch 方案 a）；§4.4 行为→T3 e2e 勾选；§5 测试→T1 vitest + T3 build/e2e。无遗漏。未部署不画、external 不渲染、多 env 同图、真实 env 标签——全部落 task。

**2. 占位符扫描**：无 TBD；每步含完整代码 + 命令 + 预期。

**3. 类型一致**：T1 产出 `AppBox`/`DepBox`/`AppInstanceLike`/`appBoxes`/`buildDepBoxes`/`kindEnvLabel`；T2 消费名一致；`app.instances`（page.tsx `Instance[]`）与 `AppInstanceLike` 字段（env/status/url/host_port/version/image?）兼容；`Envelope`/`App` 在 page.tsx 作用域内（既有）。前后一致。
