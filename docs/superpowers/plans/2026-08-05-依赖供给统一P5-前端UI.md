# 依赖供给统一 P5 前端 UI 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 pg 在依赖选择器可见可选、可绑定实例（含 pg）可见、dedicated 实例数配额第 5 维可见可配——纯前端，不改后端。

**Architecture:** 沿用前端既有范式：可测纯函数抽到 `lib/`（vitest node env，对标 `devstep.ts`↔`devstep.test.ts`），组件内联消费。`DepsSection` 加只读「可绑定实例」面板（`catalog.instances` 按 kind 分组）+ 编辑态 `bind_existing` 高亮同 kind 组（`Set<string>`，标记不裁剪）；`QuotaPage` 利用既有 `DIMENSIONS` 数据驱动加第 5 维。

**Tech Stack:** Next.js 16 + React 19 + TypeScript 5 + Tailwind 4 + vitest 4（node env，纯函数）；pnpm workspace（包名 `frontend`，目录 `platform/frontend`）。

## Global Constraints

- **纯前端，严禁改后端**：四类 API（`GET .../deps/catalog`、`GET/PUT .../deps`、`GET/PUT .../project-spaces/:id/quota`）均已就绪并经 .28 e2e 验证。
- **vitest 仅纯函数/node env**：`vitest.config.ts` 明确无 jsdom/RTL，`environment:"node"`，`include:["lib/**/*.test.ts", ...]`。组件无单测，靠 `tsc --noEmit` + `next build` + .28 手动 e2e。
- **测试/构建命令**（仓库根 `D:/Projects/智源-ANP平台`）：
  - 单测：`pnpm --filter frontend test`（即 `vitest run`，备选 `cd platform/frontend && pnpm test`）
  - 类型快检：`pnpm --filter frontend exec tsc --noEmit`
  - 构建：`pnpm --filter frontend build`
- **commit 规范**：conventional commits，scope `p5`（与 spec commit `docs(p5)` 一致），中文 body 可，每行 ≤100 字符，husky/commitlint + lint-staged(prettier) 会自动跑。commit message 末尾必须带 `Co-Authored-By: Claude <noreply@anthropic.com>`。
- **分支**：`feat/unified-dep-supply`（已在其上，不直接推 main）。

### 与 spec 的一处偏离（已确认合理）

spec §4.1 列了 `filterInstancesByKind`，但 §3「标记不裁剪」+ §4.2 的 `Set<string>` 高亮方案使它成为**死代码**（`InstancesPanel` 用 `groupInstancesByKind` 分组 + `highlightKinds.has(kind)` 判定高亮，无需按 kind 过滤）。按 YAGNI，本计划**不实现 `filterInstancesByKind`**，只做 `groupInstancesByKind`。

---

## File Structure

| 文件                                          | 责任                                                                                            | 任务   |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------ |
| `platform/frontend/lib/deps-ui.ts`            | **新增**：deps 相关类型（`Dep`/`CatalogInstance`/`DepsCatalog`）+ 纯函数 `groupInstancesByKind` | Task 1 |
| `platform/frontend/lib/deps-ui.test.ts`       | **新增**：`groupInstancesByKind` vitest 单测                                                    | Task 1 |
| `platform/frontend/app/applications/page.tsx` | 删内联 deps 类型改 import；`DepsSection` 加 `InstancesPanel` + `highlightKinds` 派生 + 渲染     | Task 2 |
| `platform/frontend/app/quota/page.tsx`        | `Quota`/`Usage`/`Dim`/`form` 加 dedicated 字段；`DIMENSIONS` 加第 5 项；文案 4→5                | Task 3 |

---

## Task 1: `lib/deps-ui.ts` 纯函数 + 类型（TDD）

**Files:**

- Create: `platform/frontend/lib/deps-ui.ts`
- Test: `platform/frontend/lib/deps-ui.test.ts`

**Interfaces:**

- Consumes: 无（自包含类型定义）
- Produces: `groupInstancesByKind(instances: CatalogInstance[]): Map<string, CatalogInstance[]>`；类型 `Dep`、`CatalogInstance`、`DepsCatalog`（均 `export`，供 Task 2 `import type`）

- [ ] **Step 1: 写失败测试 `lib/deps-ui.test.ts`**

```ts
// groupInstancesByKind 纯函数测试（vitest node env）。
// 跑法: pnpm --filter frontend test
import { describe, it, expect } from "vitest";
import { groupInstancesByKind, type CatalogInstance } from "./deps-ui";

const inst = (kind: string, name: string, port = 6379): CatalogInstance => ({
  id: `${kind}-${name}`,
  kind,
  name,
  supply_mode: "bind_existing",
  host: "10.10.0.28",
  port,
});

describe("groupInstancesByKind", () => {
  it("多 kind 混合按 kind 分组", () => {
    const groups = groupInstancesByKind([
      inst("redis", "a"),
      inst("pg", "x", 5432),
      inst("milvus", "m", 19530),
      inst("redis", "b"),
    ]);
    expect(groups.get("redis")?.map((i) => i.name)).toEqual(["a", "b"]);
    expect(groups.get("pg")?.map((i) => i.name)).toEqual(["x"]);
    expect(groups.get("milvus")?.map((i) => i.name)).toEqual(["m"]);
  });

  it("保持 catalog 首次出现顺序（Map 保插入序，UI 分组依此渲染）", () => {
    const groups = groupInstancesByKind([
      inst("milvus", "m1", 19530),
      inst("pg", "x1", 5432),
      inst("redis", "r1"),
    ]);
    expect([...groups.keys()]).toEqual(["milvus", "pg", "redis"]);
  });

  it("空数组 → 空 Map", () => {
    expect(groupInstancesByKind([]).size).toBe(0);
  });

  it("同 kind 多实例归同组", () => {
    const groups = groupInstancesByKind([
      inst("pg", "x1", 5432),
      inst("pg", "x2", 5433),
      inst("pg", "x3", 5434),
    ]);
    expect(groups.size).toBe(1);
    expect(groups.get("pg")?.length).toBe(3);
  });

  it("实例原对象引用保留（不深拷贝，UI 直接用 host/port 等字段）", () => {
    const a = inst("redis", "a");
    const groups = groupInstancesByKind([a]);
    expect(groups.get("redis")?.[0]).toBe(a);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter frontend test`
Expected: FAIL，`groupInstancesByKind` / `./deps-ui` 模块不存在（解析失败）。

- [ ] **Step 3: 写最小实现 `lib/deps-ui.ts`**

```ts
// deps-ui.ts 依赖勾选器相关类型 + 只读实例分组的纯函数。
// 后端契约：appdeploy.DepDeclaration / CatalogInstance / DepsCatalog。
// 测试见 deps-ui.test.ts；组件消费见 app/applications/page.tsx (DepsSection)。

// 依赖声明（后端 appdeploy.DepDeclaration）：kind/strategy 用户编，status/instance/token 供给结果。
export type Dep = {
  kind: string;
  strategy: string;
  status: string;
  instance?: string;
  token?: string;
  error?: string;
};

// catalog 实例（后端 appdeploy.CatalogInstance）。
export type CatalogInstance = {
  id: string;
  kind: string;
  name: string;
  supply_mode: string;
  host: string;
  port: number;
};

// 依赖勾选器 catalog（后端 appdeploy.DepsCatalog）。
export type DepsCatalog = {
  kinds: string[];
  strategies: { name: string; desc: string }[];
  instances: CatalogInstance[];
};

// groupInstancesByKind 按 kind 分组实例，保持 catalog 出现顺序（Map 保插入序）。
// UI 只读「可绑定实例」面板用：每组渲染一个 kind 区块（含 pg）。
export function groupInstancesByKind(instances: CatalogInstance[]): Map<string, CatalogInstance[]> {
  const out = new Map<string, CatalogInstance[]>();
  for (const ins of instances) {
    const arr = out.get(ins.kind);
    if (arr) {
      arr.push(ins);
    } else {
      out.set(ins.kind, [ins]);
    }
  }
  return out;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter frontend test`
Expected: PASS（5 个用例全绿）。

- [ ] **Step 5: 提交**

```bash
git add platform/frontend/lib/deps-ui.ts platform/frontend/lib/deps-ui.test.ts
git commit -m "feat(p5): deps-ui 纯函数 groupInstancesByKind + 类型"
```

提交时 husky 会跑 prettier（lint-staged），无需手动格式化。

---

## Task 2: `DepsSection` 加可绑定实例面板 + 编辑态高亮

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx`
  - import 块（第 5-9 行附近）：加 `import type { CatalogInstance, Dep, DepsCatalog } from "@/lib/deps-ui";`
  - 删内联 `type Dep`/`type DepsCatalog`（约第 54-75 行，含两段注释）
  - `DepsSection` 内（第 306 行 `const list = editingDeps ?? deps;` 之后）：派生 `highlightKinds`
  - `DepsSection` render（依赖列表 `list.map` 之后、「+ 添加依赖」按钮块之前）：插 `<InstancesPanel>`
  - 新增 `InstancesPanel` 内联组件（与 `ArtifactSection`/`NetworkModeSection`/`DepsSection` 同级，建议置于 `DepsSection` 之前）

**Interfaces:**

- Consumes（来自 Task 1）：`import type { CatalogInstance, Dep, DepsCatalog } from "@/lib/deps-ui"`；`import { groupInstancesByKind } from "@/lib/deps-ui"`
- Produces：无对外导出（`InstancesPanel` 为本文件内联组件，`DepsSection` 内部消费）

- [ ] **Step 1: 切换 import，删内联类型**

第 5-9 行 import 块，在 `import { API_BASE_URL } from "@/lib/api";` 之后加一行：

```ts
import type { CatalogInstance, Dep, DepsCatalog } from "@/lib/deps-ui";
import { groupInstancesByKind } from "@/lib/deps-ui";
```

删除约第 54-75 行的两段内联类型定义（`type Dep = {...}` 及其上方注释 `// 中间件依赖声明...`；`type DepsCatalog = {...}` 及其上方注释 `// 依赖勾选器选项...`）。删除后 page.tsx 内对 `Dep`/`DepsCatalog` 的所有引用改由 import 提供（`DepsSection` 签名、`editingDeps` state、`saveDeps` 参数等均用 `Dep`/`DepsCatalog`，无需改名）。

- [ ] **Step 2: 类型快检（确认 import 切换无误）**

Run: `pnpm --filter frontend exec tsc --noEmit`
Expected: 通过（无 `Cannot find name 'Dep'` 之类报错）。若报错，检查删除范围是否误删或漏改引用。

- [ ] **Step 3: 加 `InstancesPanel` 内联组件**

在 `DepsSection` 函数**之前**插入（与 `ArtifactSection` 同级）：

```tsx
// 可绑定实例面板（只读）：渲染 catalog.instances，按 kind 分组（含 pg）。
// 编辑态 highlightKinds 收集 strategy=bind_existing 的 kind → 命中组高亮提示「将绑定此类实例」。
// 只读展示：后端声明无 instance 字段，bind_existing 供给时按 kind 隐式匹配——故标记不裁剪。
function InstancesPanel({
  instances,
  highlightKinds,
}: {
  instances: CatalogInstance[];
  highlightKinds: Set<string>;
}) {
  const groups = groupInstancesByKind(instances);
  return (
    <div className="mt-2 rounded border border-border bg-bg p-2 text-xs">
      <div className="mb-1 text-text-muted">
        可绑定实例（部署机已运行的中间件；bind_existing 将按 kind 绑定）
      </div>
      <div className="space-y-1">
        {[...groups.entries()].map(([kind, insts]) => {
          const on = highlightKinds.has(kind);
          return (
            <div
              key={kind}
              className={`rounded px-1.5 py-1 ${on ? "border border-accent/60 bg-accent/10" : ""}`}
            >
              <div className="flex items-center gap-1">
                <code className="text-text">{kind}</code>
                <span className="text-text-muted">· {insts.length} 个</span>
                {on && <span className="text-accent">← 将绑定此类</span>}
              </div>
              <div className="mt-0.5 space-y-0.5 pl-2">
                {insts.map((i) => (
                  <div key={i.id} className="text-text-muted">
                    {i.name} · {i.host}:{i.port} · {i.supply_mode}
                  </div>
                ))}
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 在 `DepsSection` 内派生 `highlightKinds` 并渲染面板**

在 `DepsSection` 内、`const list = editingDeps ?? deps;`（第 306 行）之后加：

```ts
// 编辑态收集所有 strategy=bind_existing 的 kind（可能多个），命中组在面板高亮。
// 非编辑态（editingDeps=null）→ 空 Set，全不高亮。
const highlightKinds = new Set(
  (editingDeps ?? []).filter((d) => d.strategy === "bind_existing").map((d) => d.kind)
);
```

在 `DepsSection` 的 return 内、依赖列表 `{list.length === 0 ? (...) : (list.map(...))}` 块**之后**、「+ 添加依赖」按钮块 `{editingDeps && (<button>...+ 添加依赖...</button>)}` **之前**插入：

```tsx
{
  catalog && catalog.instances.length > 0 && (
    <InstancesPanel instances={catalog.instances} highlightKinds={highlightKinds} />
  );
}
```

- [ ] **Step 5: 类型快检**

Run: `pnpm --filter frontend exec tsc --noEmit`
Expected: 通过。

- [ ] **Step 6: 提交**

```bash
git add platform/frontend/app/applications/page.tsx
git commit -m "feat(p5): DepsSection 加可绑定实例面板 + bind_existing 高亮"
```

（提交后 husky 跑 prettier；本任务无 vitest 单测——组件渲染不在纯函数测试范围内，靠 tsc + 下游 build + Task 4 的 .28 e2e 覆盖。）

---

## Task 3: `QuotaPage` 加第 5 维 dedicated 实例数

**Files:**

- Modify: `platform/frontend/app/quota/page.tsx`
  - `Quota` 类型（约第 9-16 行）：加 `max_dedicated_instances`
  - `Usage` 类型（约第 17-23 行）：加 `used_dedicated_instances`
  - `Dim` 类型的两个 `Pick` 联合（约第 52-64 行）：补字段
  - `DIMENSIONS` 数组（约第 66-77 行）：加第 5 项
  - `form` state 初值（约第 83-88 行）+ `load()` 回填（约第 123-128 行）：加字段
  - 顶部文案（约第 233-235 行）：「4 维度」→「5 维度」

**Interfaces:**

- Consumes: 无（独立页面，后端 P4 字段已就绪：`quota.max_dedicated_instances`、`usage.used_dedicated_instances`、PUT 接受 `max_dedicated_instances`）
- Produces: 无对外导出

- [ ] **Step 1: 扩展 `Quota` 类型**

约第 9-16 行 `type Quota = {...}`，在 `max_capability_calls_per_day: number;` 之后加一行：

```ts
max_dedicated_instances: number;
```

- [ ] **Step 2: 扩展 `Usage` 类型**

约第 17-23 行 `type Usage = {...}`，在 `used_capability_today: number;` 之后加一行：

```ts
used_dedicated_instances: number;
```

- [ ] **Step 3: 扩展 `Dim` 类型的两个 `Pick` 联合**

约第 52-64 行，`key` 的 `Pick<Quota, ...>` 联合加 `"max_dedicated_instances"`，`usedKey` 的 `Pick<Usage, ...>` 联合加 `"used_dedicated_instances"`：

```ts
type Dim = {
  key: keyof Pick<
    Quota,
    | "max_apps"
    | "max_databases"
    | "max_total_db_mb"
    | "max_capability_calls_per_day"
    | "max_dedicated_instances"
  >;
  label: string;
  unit: string;
  icon: string;
  usedKey: keyof Pick<
    Usage,
    | "used_apps"
    | "used_databases"
    | "used_db_size_mb"
    | "used_capability_today"
    | "used_dedicated_instances"
  >;
};
```

- [ ] **Step 4: `DIMENSIONS` 加第 5 项**

约第 66-77 行 `DIMENSIONS` 数组，在「今日 AI 调用」项之后加：

```ts
  {
    key: "max_dedicated_instances",
    label: "专属实例数",
    unit: "个",
    icon: "🖥️",
    usedKey: "used_dedicated_instances",
  },
```

- [ ] **Step 5: `form` state 初值 + `load()` 回填加字段**

约第 83-88 行 `useState({...})`，在 `max_capability_calls_per_day: 0,` 之后加 `max_dedicated_instances: 0,`。

约第 123-128 行 `load()` 内 `setForm({...})`，在 `max_capability_calls_per_day: r.data.quota.max_capability_calls_per_day,` 之后加：

```ts
          max_dedicated_instances: r.data.quota.max_dedicated_instances,
```

`save()`（约第 158-174 行）**无需改**——它 `JSON.stringify(form)`，form 已含新字段，全传给后端 PUT（`*int` + `validate:omitempty` 区分未改字段，P4 handler 已验证 code 0）。

- [ ] **Step 6: 顶部文案 4→5 维度**

约第 233-235 行 `<p>`：

```tsx
<p className="mb-4 text-sm text-text-muted">
  5 维度强制：<b>应用数 / 库数 / 库总大小 / 每日 AI 调用 / 专属实例数</b>。建资源前
  check，超限返回友好错误。
</p>
```

- [ ] **Step 7: 类型快检**

Run: `pnpm --filter frontend exec tsc --noEmit`
Expected: 通过（`Dim` 联合补字段后，`DIMENSIONS` 新条目 `key`/`usedKey` 类型吻合；若遗漏 `Pick` 字段会在此报错）。

- [ ] **Step 8: 提交**

```bash
git add platform/frontend/app/quota/page.tsx
git commit -m "feat(p5): 配额页加第 5 维 dedicated 实例数"
```

（无 vitest 单测——第 5 维是声明式数据，靠 tsc + build + Task 4 的 .28 e2e 覆盖。）

---

## Task 4: 整体验证（单测 + 构建 + .28 手动 e2e）

**Files:** 无（验证门）

- [ ] **Step 1: 全量单测**

Run: `pnpm --filter frontend test`
Expected: PASS（Task 1 的 `deps-ui.test.ts` 5 用例 + 既有 `lib/*.test.ts` 全绿，零回归）。

- [ ] **Step 2: 全量构建**

Run: `pnpm --filter frontend build`
Expected: 构建成功（Next 16 + tsc，类型扩展的强校验兜底；无 TS / lint 阻断错误）。

- [ ] **Step 3: .28 手动 e2e（依赖页）**

前置：本地 `pnpm --filter frontend dev` 指向 .28 后端（`NEXT_PUBLIC_API_BASE_URL` 指向 .28，或 .28 已部署前端）；admin 登录。

核对清单：

- [ ] 应用页任一 managed 应用 → 依赖区下拉 kind 含 **pg**（与 redis/milvus 并列）
- [ ] 部署机有注册实例时（如 .28 redis@:6381、pg 实例）→ 依赖区下方见「可绑定实例」面板，按 kind 分组（redis/pg/milvus 各组 + host:port + supply_mode）
- [ ] 点「编辑」，把某依赖 strategy 选 **bind_existing** → 对应 kind 实例组高亮（边框 + 「← 将绑定此类」）；选其他 strategy → 该组不高亮
- [ ] 同时对多个 kind 选 bind_existing → 多组同时高亮
- [ ] 保存 → 退出编辑态，高亮消失；面板仍显示（只读）
- [ ] 无实例时面板不渲染（不显空态）

- [ ] **Step 4: .28 手动 e2e（配额页）**

核对清单：

- [ ] 配额页见第 5 张卡片「🖥️ 专属实例数」（`used / max` + 进度条 + 满/接近上限提示）
- [ ] admin 编辑表单见「🖥️ 专属实例数上限」输入框
- [ ] 改 `max_dedicated_instances` → 保存 → 回显成功（code 0）；重载值持久
- [ ] `used_dedicated_instances` 随该 ps dedicated 部署数量变化（若有 dedicated 应用）

- [ ] **Step 5: 记录验证结果**

把 Step 1-4 的实测结果（命令输出摘要 + e2e 勾选）回写本任务，作为 P5 完成证据；更新 `.superpowers/sdd/progress.md`（P5 段）。

---

## 自审（writing-plans skill 要求）

**1. Spec 覆盖**：§4.1 类型+`groupInstancesByKind`→Task 1（`filterInstancesByKind` 按偏离说明舍弃）；§4.2 实例面板+高亮→Task 2；§4.3 quota 第 5 维→Task 3；§5 测试→Task 1 vitest + Task 4 build/e2e；§4.4 行为→Task 4 e2e 勾选清单。无遗漏。

**2. 占位符扫描**：无 TBD/TODO；每个代码步骤含完整代码；命令含预期输出。

**3. 类型一致**：Task 1 产出 `groupInstancesByKind`、`Dep`、`CatalogInstance`、`DepsCatalog`；Task 2 消费时 import 名一致（`CatalogInstance` 用于 `InstancesPanel` props、`groupInstancesByKind` 用于分组）；Task 3 的 `max_dedicated_instances`/`used_dedicated_instances` 在 Quota/Usage/Dim/DIMENSIONS/form/load 处处同名。前后一致。
