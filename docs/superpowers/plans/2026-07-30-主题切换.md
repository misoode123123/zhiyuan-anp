# 主题切换（亮/暗）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 ANP 前端加亮/暗两主题 + 右上角切换开关 + localStorage 持久化 + 首次跟随系统，通用组件全适配暗色，浅色回归不破。

**Architecture:** 语义 CSS 变量（8 + 3 状态色）+ Tailwind v4 `@theme inline` 映射成 token 类；`<html>` 加 `.dark` class 切换；`<head>` inline script 防 FOUC；`ThemeProvider` context 管 theme + localStorage；三层适配（框架骨架 / 全局表单兜底 / 页面卡片 token 替换）。

**Tech Stack:** Next.js 16 App Router + React 19 + Tailwind v4（零配置，`@theme inline`）+ TypeScript + vitest（node 环境，纯函数测试）。

## Global Constraints

- 浅色值在 `:root`，暗色值在 `.dark`；`color-scheme` 由 class 驱动（`:root{color-scheme:light}` `.dark{color-scheme:dark}`）。
- localStorage key = `anp.theme`，值 `'light'`/`'dark'`；不存 `'system'`，系统态由"未设 key"表示（首次跟随 `prefers-color-scheme`）。
- inline script 必须在 React hydration 前同步执行（防 FOUC），不能用 effect。
- 旧变量 `--background`/`--foreground` 保留为 `--bg`/`--text` 别名，不破坏现有引用。
- vitest 是 node 环境纯函数测试（无 jsdom/RTL）；只有 `resolveTheme` 这类纯函数可单测，React 组件（ThemeProvider/ThemeToggle）靠 `pnpm tsc` + 人工验。
- 暗色用蓝灰暗（呼应浅色蓝灰）：bg `#1a1d21` / surface `#25292e` / text `#e4e6eb`。
- 不含：opencode iframe 主体、图表/可视化配色、品牌装饰色。
- 每个任务结束 `pnpm tsc` 必过；整体 `pnpm build` 26 页必过。

## 文件结构

- `app/globals.css`（改）— 语义变量 + `@theme inline` 映射 + 表单兜底 + color-scheme
- `lib/theme.tsx`（新）— `resolveTheme` 纯函数 + `ThemeProvider`/`useTheme` + `applyTheme`
- `lib/theme.test.ts`（新）— `resolveTheme` 单测
- `app/layout.tsx`（改）— inline script 防 FOUC + body className 改 token + ThemeProvider 包裹
- `app/_components/theme-toggle.tsx`（新）— 太阳/月亮图标开关
- `app/_components/shell.tsx`（改）— 接入 ThemeToggle + 侧栏/顶栏改 token
- `app/_components/*.tsx`（改）— tabs/tab-bar/user-switcher/workspace-switcher/notif-bell/stepper/placeholder/change-output 改 token
- `app/workspace/*.tsx` + `app/workspace/views/*.tsx`（改）— 工作台骨架改 token（不含 opencode iframe）
- `app/**/page.tsx`（改）— 20+ 页面卡片机械 token 替换

---

## Task 1: globals.css — 主题变量 + Tailwind 映射 + 表单兜底

**Files:**

- Modify: `platform/frontend/app/globals.css`

**Interfaces:**

- Produces: 8 语义变量 + 3 状态色（浅色 `:root` / 暗色 `.dark`）、Tailwind token 类（`bg-bg`/`bg-surface`/`text-text`/`border-border` 等）、原生表单自动跟主题。

- [ ] **Step 1: 重写 globals.css**

`platform/frontend/app/globals.css`:

```css
@import "tailwindcss";

:root {
  color-scheme: light;
  --bg: #f1f3f5;
  --surface: #ffffff;
  --surface-2: #f3f4f6;
  --text: #171717;
  --text-muted: #6b7280;
  --border: #e5e7eb;
  --accent: #2563eb;
  --accent-fg: #ffffff;
  --danger: #cf222e;
  --success: #2da44e;
  --warn: #d29922;
  /* 旧变量别名，保留不破坏现有引用 */
  --background: var(--bg);
  --foreground: var(--text);
}

.dark {
  color-scheme: dark;
  --bg: #1a1d21;
  --surface: #25292e;
  --surface-2: #2d3239;
  --text: #e4e6eb;
  --text-muted: #9ca3af;
  --border: #33373c;
  --accent: #3b82f6;
  --accent-fg: #ffffff;
  --danger: #f87171;
  --success: #3fb950;
  --warn: #e3b341;
}

@theme inline {
  --color-bg: var(--bg);
  --color-surface: var(--surface);
  --color-surface-2: var(--surface-2);
  --color-text: var(--text);
  --color-text-muted: var(--text-muted);
  --color-border: var(--border);
  --color-accent: var(--accent);
  --color-accent-fg: var(--accent-fg);
  --color-danger: var(--danger);
  --color-success: var(--success);
  --color-warn: var(--warn);
  --color-background: var(--background);
  --color-foreground: var(--foreground);
}

body {
  background: var(--bg);
  color: var(--text);
  font-family: Arial, Helvetica, sans-serif;
}

/* 全局表单兜底：所有页面原生表单元素自动跟主题，零页面改动 */
input,
select,
textarea {
  background: var(--surface);
  color: var(--text);
  border-color: var(--border);
}
input::placeholder,
textarea::placeholder {
  color: var(--text-muted);
}
```

- [ ] **Step 2: tsc 验证**

Run: `cd platform/frontend && pnpm tsc`
Expected: exit 0（纯 CSS 改动，不影响 TS）。

- [ ] **Step 3: Commit**

```bash
git add platform/frontend/app/globals.css
git commit -m "feat(theme): globals.css 主题变量体系 + Tailwind 映射 + 表单兜底"
```

---

## Task 2: lib/theme.tsx — resolveTheme 纯函数 + ThemeProvider

**Files:**

- Create: `platform/frontend/lib/theme.tsx`
- Test: `platform/frontend/lib/theme.test.ts`

**Interfaces:**

- Produces: `type Theme`、`THEME_STORAGE_KEY`、`resolveTheme(stored, systemDark)` 纯函数、`ThemeProvider` 组件、`useTheme()` hook、`applyTheme(theme)`。
- 后续 Task 3（layout）用 `ThemeProvider`；Task 4（toggle）用 `useTheme`。

- [ ] **Step 1: 写失败测试**

`platform/frontend/lib/theme.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { resolveTheme, THEME_STORAGE_KEY } from "./theme";

describe("resolveTheme", () => {
  it("stored 值优先于系统", () => {
    expect(resolveTheme("dark", false)).toBe("dark");
    expect(resolveTheme("light", true)).toBe("light");
  });

  it("无 stored 跟随系统", () => {
    expect(resolveTheme(null, true)).toBe("dark");
    expect(resolveTheme(null, false)).toBe("light");
  });

  it("非法 stored 当作无 stored", () => {
    expect(resolveTheme("", true)).toBe("dark");
    expect(resolveTheme("system", false)).toBe("light");
  });
});

describe("THEME_STORAGE_KEY", () => {
  it("固定值", () => {
    expect(THEME_STORAGE_KEY).toBe("anp.theme");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform/frontend && pnpm test -- theme`
Expected: FAIL（`./theme` 未定义）。

- [ ] **Step 3: 实现 lib/theme.tsx**

`platform/frontend/lib/theme.tsx`:

```tsx
"use client";

import { createContext, useCallback, useContext, useEffect, useState } from "react";

export type Theme = "light" | "dark";

export const THEME_STORAGE_KEY = "anp.theme";

// resolveTheme 纯函数：stored 优先，否则跟随系统。inline script 与 ThemeProvider 共用。
export function resolveTheme(stored: string | null, systemDark: boolean): Theme {
  if (stored === "light" || stored === "dark") return stored;
  return systemDark ? "dark" : "light";
}

// applyTheme 把主题应用到 <html> + 持久化。仅浏览器端调。
export function applyTheme(theme: Theme) {
  const root = document.documentElement;
  root.classList.toggle("dark", theme === "dark");
  try {
    localStorage.setItem(THEME_STORAGE_KEY, theme);
  } catch {}
}

// readInitialTheme 挂载时读 <html> 当前 class（inline script 已设）作初始值，避免 SSR/CSR 不一致。
function readInitialTheme(): Theme {
  if (typeof document === "undefined") return "light";
  return document.documentElement.classList.contains("dark") ? "dark" : "light";
}

interface ThemeContextValue {
  theme: Theme;
  toggle: () => void;
}

const ThemeContext = createContext<ThemeContextValue | null>(null);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<Theme>(readInitialTheme);

  // 系统实时跟随：用户未手动选（无 localStorage）时，系统切换实时跟。
  useEffect(() => {
    const stored =
      typeof localStorage !== "undefined" ? localStorage.getItem(THEME_STORAGE_KEY) : null;
    if (stored) return; // 用户已手动选，不跟随系统
    const mql = window.matchMedia("(prefers-color-scheme: dark)");
    const onChange = (e: MediaQueryListEvent) => {
      const t: Theme = e.matches ? "dark" : "light";
      document.documentElement.classList.toggle("dark", t === "dark");
      setTheme(t);
    };
    mql.addEventListener("change", onChange);
    return () => mql.removeEventListener("change", onChange);
  }, []);

  const toggle = useCallback(() => {
    setTheme((prev) => {
      const next: Theme = prev === "dark" ? "light" : "dark";
      applyTheme(next);
      return next;
    });
  }, []);

  return <ThemeContext.Provider value={{ theme, toggle }}>{children}</ThemeContext.Provider>;
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme 必须在 ThemeProvider 内使用");
  return ctx;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform/frontend && pnpm test -- theme`
Expected: 4 个用例 PASS。

- [ ] **Step 5: tsc 验证**

Run: `cd platform/frontend && pnpm tsc`
Expected: exit 0。

- [ ] **Step 6: Commit**

```bash
git add platform/frontend/lib/theme.tsx platform/frontend/lib/theme.test.ts
git commit -m "feat(theme): resolveTheme 纯函数 + ThemeProvider/useTheme"
```

---

## Task 3: layout.tsx — inline script 防 FOUC + body token + Provider 包裹

**Files:**

- Modify: `platform/frontend/app/layout.tsx`

**Interfaces:**

- Consumes: Task 2 的 `ThemeProvider`、`THEME_STORAGE_KEY`、`resolveTheme`。
- Produces: SSR 时 `<html>` class 已设好（防 FOUC）+ body 走 token + 应用被 Provider 包裹。

- [ ] **Step 1: 改 layout.tsx**

`platform/frontend/app/layout.tsx`:

```tsx
import type { Metadata } from "next";
import "./globals.css";
import { Shell } from "./_components/shell";
import { ThemeProvider, THEME_STORAGE_KEY } from "@/lib/theme";

export const metadata: Metadata = {
  title: "智源 ANP",
  description: "企业 AI 原生研发平台",
};

// 防 FOUC：SSR 时同步执行，读 localStorage + 系统偏好，在 React hydration 前设好 <html>.dark。
// 必须内联在 HTML 里同步跑，不能用 effect（否则 hydration 前闪浅色）。
const THEME_INLINE_SCRIPT = `(function(){try{var t=localStorage.getItem(${JSON.stringify(
  THEME_STORAGE_KEY
)});var d=t?t==='dark':matchMedia('(prefers-color-scheme: dark)').matches;document.documentElement.classList.toggle('dark',d);}catch(e){}})();`;

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="zh-CN" className="h-full antialiased">
      <head>
        <script dangerouslySetInnerHTML={{ __html: THEME_INLINE_SCRIPT }} />
      </head>
      <body className="min-h-full bg-bg text-text">
        <ThemeProvider>
          <Shell>{children}</Shell>
        </ThemeProvider>
      </body>
    </html>
  );
}
```

注意：body className 从 `bg-neutral-50 text-neutral-900` 改为 `bg-bg text-text`（走变量）。`<head>` 里直接放 `<script>` 在 Next.js App Router 会渲染到 HTML head 并同步执行。

- [ ] **Step 2: tsc + build 验证**

Run: `cd platform/frontend && pnpm tsc && pnpm build`
Expected: tsc exit 0；build 26 页成功（`/` 首页等）。

- [ ] **Step 3: 人工验防 FOUC（可选，部署后做）**

刷新页面观察是否闪浅色。本期靠 tsc+build，人工验留 Task 8。

- [ ] **Step 4: Commit**

```bash
git add platform/frontend/app/layout.tsx
git commit -m "feat(theme): layout inline script 防 FOUC + body 走 token + Provider 包裹"
```

---

## Task 4: ThemeToggle 组件 + shell 顶栏接入

**Files:**

- Create: `platform/frontend/app/_components/theme-toggle.tsx`
- Modify: `platform/frontend/app/_components/shell.tsx`

**Interfaces:**

- Consumes: Task 2 的 `useTheme`。

- [ ] **Step 1: 新建 theme-toggle.tsx**

`platform/frontend/app/_components/theme-toggle.tsx`:

```tsx
"use client";

import { useTheme } from "@/lib/theme";

// 主题切换开关：太阳/月亮图标，点击 toggle。放 shell 顶栏 user-switcher 旁。
export function ThemeToggle() {
  const { theme, toggle } = useTheme();
  const isDark = theme === "dark";
  return (
    <button
      onClick={toggle}
      title={isDark ? "切换到亮色" : "切换到暗色"}
      aria-label="切换主题"
      className="flex h-8 w-8 items-center justify-center rounded-md text-text-muted hover:bg-surface-2 hover:text-text"
    >
      {isDark ? (
        // 月亮
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
        </svg>
      ) : (
        // 太阳
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <circle cx="12" cy="12" r="5" />
          <line x1="12" y1="1" x2="12" y2="3" />
          <line x1="12" y1="21" x2="12" y2="23" />
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64" />
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78" />
          <line x1="1" y1="12" x2="3" y2="12" />
          <line x1="21" y1="12" x2="23" y2="12" />
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36" />
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22" />
        </svg>
      )}
    </button>
  );
}
```

- [ ] **Step 2: shell.tsx 接入 ThemeToggle**

`app/_components/shell.tsx`：在 import 区加 `import { ThemeToggle } from "./theme-toggle";`。

在顶栏 `<div className="flex items-center gap-1">`（约 shell.tsx:76，NotifBell 旁）加 ThemeToggle：

把：

```tsx
<div className="flex items-center gap-1">
  <NotifBell />
  {/* 移动端关闭按钮 */}
  <button onClick={() => setSidebarOpen(false)} className="text-neutral-400 lg:hidden">
    ×
  </button>
</div>
```

改为：

```tsx
<div className="flex items-center gap-1">
  <ThemeToggle />
  <NotifBell />
  {/* 移动端关闭按钮 */}
  <button onClick={() => setSidebarOpen(false)} className="text-text-muted lg:hidden">
    ×
  </button>
</div>
```

- [ ] **Step 3: tsc + build 验证**

Run: `cd platform/frontend && pnpm tsc && pnpm build`
Expected: tsc exit 0；build 26 页成功。

- [ ] **Step 4: Commit**

```bash
git add platform/frontend/app/_components/theme-toggle.tsx platform/frontend/app/_components/shell.tsx
git commit -m "feat(theme): ThemeToggle 开关组件 + shell 顶栏接入"
```

---

## Task 5: 框架骨架适配 token（_components）

**Files:**

- Modify: `platform/frontend/app/_components/shell.tsx`
- Modify: `platform/frontend/app/_components/tabs.tsx`
- Modify: `platform/frontend/app/_components/tab-bar.tsx`
- Modify: `platform/frontend/app/_components/user-switcher.tsx`
- Modify: `platform/frontend/app/_components/workspace-switcher.tsx`
- Modify: `platform/frontend/app/_components/notif-bell.tsx`
- Modify: `platform/frontend/app/_components/stepper.tsx`
- Modify: `platform/frontend/app/_components/placeholder.tsx`
- Modify: `platform/frontend/app/_components/change-output.tsx`

**Interfaces:**

- Produces: 框架骨架组件在暗色下可读。

- [ ] **Step 1: 机械替换规则**

对上述每个文件，按以下规则替换 Tailwind 类（old → new）：

| old                                   | new                                   |
| ------------------------------------- | ------------------------------------- |
| `bg-white`                            | `bg-surface`                          |
| `bg-neutral-50`                       | `bg-bg`                               |
| `bg-neutral-100`                      | `bg-surface-2`                        |
| `hover:bg-neutral-100`                | `hover:bg-surface-2`                  |
| `text-neutral-900`                    | `text-text`                           |
| `text-neutral-700`                    | `text-text`                           |
| `text-neutral-500`                    | `text-text-muted`                     |
| `text-neutral-400`                    | `text-text-muted`                     |
| `border-neutral-200`                  | `border-border`                       |
| `border-neutral-300`                  | `border-border`                       |
| `bg-[#f7f8fa]`                        | `bg-surface`                          |
| `bg-[#f3f3f3]`                        | `bg-surface-2`                        |
| `bg-[#e8e8e8]` / `hover:bg-[#e8e8e8]` | `bg-surface-2` / `hover:bg-surface-2` |
| `bg-[#f0f0f0]` / `hover:bg-[#f0f0f0]` | `bg-surface-2` / `hover:bg-surface-2` |
| `bg-[#eaeaea]`                        | `bg-surface-2`                        |
| `bg-blue-600` / `bg-blue-500`         | `bg-accent`                           |
| `text-blue-600` / `text-blue-500`     | `text-accent`                         |
| `bg-[#cf222e]`                        | `bg-danger`                           |
| `text-[#cf222e]`                      | `text-danger`                         |
| `bg-[#2da44e]` / `text-[#1a7f37]`     | `bg-success` / `text-success`         |
| `bg-[#d29922]` / `text-[#bf8700]`     | `bg-warn` / `text-warn`               |

> 保留：logo 装饰色 `text-blue-600` 里的 "ANP" 字样（品牌色，不改）。若不确定某色是否品牌色，保留原样（暗色下品牌蓝仍可读）。

- [ ] **Step 2: 逐文件替换**

对 9 个文件逐一应用 Step 1 规则。重点：

- `shell.tsx`：侧栏 `bg-[#f7f8fa]` → `bg-surface`、`border-neutral-200` → `border-border`、`hover:bg-neutral-100` → `hover:bg-surface-2`、`text-neutral-400` → `text-text-muted`、`border-b border-neutral-200` → `border-b border-border`。
- `tabs.tsx`/`tab-bar.tsx`：标签栏背景 `bg-white`→`bg-surface`、激活态/文字色按规则。
- `user-switcher.tsx`/`workspace-switcher.tsx`/`notif-bell.tsx`：下拉 `bg-white`→`bg-surface`、文字 `text-neutral-*`→`text-text`/`text-text-muted`、边框按规则。
- `stepper.tsx`/`placeholder.tsx`/`change-output.tsx`：背景/文字/边框按规则。

- [ ] **Step 3: tsc + build 验证**

Run: `cd platform/frontend && pnpm tsc && pnpm build`
Expected: tsc exit 0；build 26 页成功。

- [ ] **Step 4: Commit**

```bash
git add platform/frontend/app/_components/
git commit -m "feat(theme): 框架骨架组件适配 token(暗色可读)"
```

---

## Task 6: workspace 编码工作台适配 token

**Files:**

- Modify: `platform/frontend/app/workspace/activity-bar.tsx`
- Modify: `platform/frontend/app/workspace/sidebar.tsx`
- Modify: `platform/frontend/app/workspace/diff-drawer.tsx`
- Modify: `platform/frontend/app/workspace/project-docs.tsx`
- Modify: `platform/frontend/app/workspace/workspace-toolbar.tsx`
- Modify: `platform/frontend/app/workspace/views/requirements-view.tsx`
- Modify: `platform/frontend/app/workspace/views/source-control-view.tsx`
- Modify: `platform/frontend/app/workspace/views/releases-view.tsx`
- Modify: `platform/frontend/app/workspace/views/files-view.tsx`

**Interfaces:**

- Produces: 编码工作台左面板（活动栏/侧栏/diff/各视图）在暗色下可读。不含 opencode iframe 主体。

- [ ] **Step 1: 应用 Task 5 Step 1 的替换规则**

对上述 9 个文件应用同样的 old→new 规则。重点：

- `activity-bar.tsx`：`bg-white`→`bg-surface`、`border-[#e5e5e5]`→`border-border`、`text-[#171717]`→`text-text`、`text-[#9ca3af]`→`text-text-muted`、`hover:bg-[#f3f3f3]`→`hover:bg-surface-2`、`hover:text-[#525252]`→`hover:text-text`、`border-l-[#007acc]`→`border-l-accent`、`bg-[#cf222e]`→`bg-danger`。
- `sidebar.tsx`：`bg-[#f3f3f3]`→`bg-surface-2`、`border-[#d0d0d0]`→`border-border`、`text-[#cf222e]`→`text-danger`。
- `diff-drawer.tsx`：背景/文字/边框按规则；diff 内容的 `+`/`-` 行色（绿/红）保留或用 `bg-success/10`、`bg-danger/10`（若原来是 `bg-[#dafbe1]`→`bg-success/10`，`bg-[#ffebe9]`→`bg-danger/10`）。
- 各 view：列表/hover/徽章色按规则。
- **不动**：opencode iframe（`workspace-frame.tsx` 里的 `<iframe>`）。

- [ ] **Step 2: tsc + build 验证**

Run: `cd platform/frontend && pnpm tsc && pnpm build`
Expected: tsc exit 0；build 26 页成功（含 `/workspace`）。

- [ ] **Step 3: Commit**

```bash
git add platform/frontend/app/workspace/
git commit -m "feat(theme): workspace 编码工作台适配 token(不含 opencode iframe)"
```

---

## Task 7: 页面卡片 token 替换 part 1（admin + 核心业务页）

**Files:**

- Modify: `platform/frontend/app/admin/config/page.tsx`
- Modify: `platform/frontend/app/admin/logs/page.tsx`
- Modify: `platform/frontend/app/admin/users/page.tsx`
- Modify: `platform/frontend/app/applications/page.tsx`
- Modify: `platform/frontend/app/requirements/page.tsx`
- Modify: `platform/frontend/app/requirements/chat/page.tsx`
- Modify: `platform/frontend/app/release/page.tsx`
- Modify: `platform/frontend/app/testing/page.tsx`
- Modify: `platform/frontend/app/approvals/page.tsx`
- Modify: `platform/frontend/app/login/page.tsx`
- Modify: `platform/frontend/app/page.tsx`（首页）

**Interfaces:**

- Produces: 11 个页面的卡片/表单/表格在暗色下可读。

- [ ] **Step 1: 应用 Task 5 Step 1 的替换规则**

对上述 11 个文件应用同样的 old→new 规则。每文件重点处理 `bg-white`→`bg-surface`、`text-neutral-*`→`text-text`/`text-text-muted`、`border-neutral-200`→`border-border`、`hover:bg-neutral-100`→`hover:bg-surface-2`、状态色按规则。

> 逐文件改完跑一次 tsc 确认没把类名敲错。

- [ ] **Step 2: tsc + build 验证**

Run: `cd platform/frontend && pnpm tsc && pnpm build`
Expected: tsc exit 0；build 26 页成功。

- [ ] **Step 3: Commit**

```bash
git add platform/frontend/app/admin/ platform/frontend/app/applications/page.tsx platform/frontend/app/requirements/ platform/frontend/app/release/page.tsx platform/frontend/app/testing/page.tsx platform/frontend/app/approvals/page.tsx platform/frontend/app/login/page.tsx platform/frontend/app/page.tsx
git commit -m "feat(theme): 页面卡片 token 替换 part1(admin+核心业务页)"
```

---

## Task 8: 页面卡片 token 替换 part 2（其余页面）+ 验收 + 部署

**Files:**

- Modify: `platform/frontend/app/capabilities/page.tsx`
- Modify: `platform/frontend/app/compute/page.tsx`
- Modify: `platform/frontend/app/databases/page.tsx`
- Modify: `platform/frontend/app/dev/page.tsx`
- Modify: `platform/frontend/app/docs/page.tsx`
- Modify: `platform/frontend/app/ops/page.tsx`
- Modify: `platform/frontend/app/quota/page.tsx`
- Modify: `platform/frontend/app/security/page.tsx`
- Modify: `platform/frontend/app/team/page.tsx`
- Modify: `platform/frontend/app/governance/page.tsx`

**Interfaces:**

- Produces: 全部页面暗色可读；主题切换功能完整上线。

- [ ] **Step 1: 应用 Task 5 Step 1 的替换规则**

对上述 10 个文件应用同样规则。**不动图表/可视化配色**（compute/performance 页的图表色保留，暗色下若不可读留下期）。

- [ ] **Step 2: tsc + build 验证**

Run: `cd platform/frontend && pnpm tsc && pnpm build`
Expected: tsc exit 0；build 26 页成功。

- [ ] **Step 3: 跑全量测试**

Run: `cd platform/frontend && pnpm test`
Expected: 全部 PASS（含 `lib/theme.test.ts`）。

- [ ] **Step 4: Commit + 推 origin main**

```bash
git add platform/frontend/app/capabilities/ platform/frontend/app/compute/ platform/frontend/app/databases/ platform/frontend/app/dev/ platform/frontend/app/docs/ platform/frontend/app/ops/ platform/frontend/app/quota/ platform/frontend/app/security/ platform/frontend/app/team/ platform/frontend/app/governance/
git commit -m "feat(theme): 页面卡片 token 替换 part2(其余页面)"
git push origin main
```

- [ ] **Step 5: 部署 .28（纯前端，scp + 重建 frontend）**

按 `deploy/README.md` 增量部署：scp 改动的 `app/` + `lib/` 文件到 `/opt/anp` → 手动 `docker-compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up -d --build frontend`（后台 + 轮询）。

- [ ] **Step 6: 生产验收**

```bash
# 镜像时间戳确认重建
ssh -i ~/.ssh/miscode root@10.10.0.28 "docker images deploy_frontend --format '{{.CreatedAt}}'"
# 8088 首页
curl -s -o /dev/null -w '%{http_code}' http://10.10.0.28:8088/
```

浏览器开 http://10.10.0.28:8088 ，Ctrl+Shift+R 强刷：

1. 右上角有主题开关，点击亮↔暗切换，图标随之变。
2. 刷新无闪烁。
3. 切换后 localStorage 记住，再次访问保持。
4. 清 localStorage 后刷新跟随系统主题。
5. 暗色下侧栏/顶栏/卡片/表单/表格/按钮/文字/边框均可读。
6. 浅色下与改造前视觉一致（回归不破）。

- [ ] **Step 7: 更新记忆**

更新 `ai-native-platform.md` 记忆：新增"亮/暗主题切换"能力，记录变量体系 + 防 FOUC + 三层适配 + 边界（不含 opencode iframe/图表色）。

---

## Self-Review

**1. Spec coverage:**

- §3 变量体系 + Tailwind 映射 → Task 1
- §4.1 防 FOUC inline script → Task 3
- §4.2 ThemeProvider + 系统跟随 → Task 2
- §4.3 ThemeToggle 开关 → Task 4
- §5.1 框架骨架适配 → Task 5
- §5.2 全局表单兜底 → Task 1
- §5.3 页面卡片批量替换 → Task 7, 8
- §5.4 workspace 适配（不含 opencode iframe）→ Task 6
- §6 测试（resolveTheme 单测）→ Task 2
- §8 验收标准 → Task 8 Step 6
- §9 部署 → Task 8 Step 5

**2. Placeholder scan:** 无 TBD/TODO；页面替换用"规则表 + 文件清单"（可执行的机械任务，非占位）；每步有验证命令。

**3. Type consistency:** `Theme`/`THEME_STORAGE_KEY`/`resolveTheme`/`ThemeProvider`/`useTheme`/`applyTheme` 跨 Task 2-4 一致；token 类名（bg-surface/text-text/border-border/bg-surface-2/text-text-muted/bg-accent/text-accent/bg-danger/text-danger/bg-success/text-success/bg-warn/text-warn/bg-bg）在 Task 1 定义、Task 5-8 使用一致。
