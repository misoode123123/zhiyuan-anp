# 应用部署界面 tab 化改版 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/applications` 从长单列流改为「应用总览条 + 单应用 tab 工作区」，tab 内 test/prod 部署实例左右并排卡（各自吸附本环境操作 + 按 env 过滤的部署历史时间线），同时把 2369 行 page.tsx 拆成行数受控的模块（单文件纪律首次落地样板）。

**Architecture:** 纯逻辑进 `_lib/`（类型/谓词、tab 状态机、总览格子推导、数据 hook + 动作 hook），视图进 `_components/`（总览条 / tab 面板 / 环境并排卡 / 五栏详情 / 导入向导 / 四个既有自包含区块原样平移），`page.tsx` 退化为壳（工具条 + 注册表单 + 总览条 + tab 容器编排）。既有大块**平移不重写**，评审按 diff 等价口径。

**Tech Stack:** Next 16 App Router + React 19 + TS + Tailwind（既有自定义色板 bg-surface/text-warn/text-accent 等）；vitest 4（node 项目跑纯函数、jsdom 项目跑组件，`vitest.config.ts` projects 模式）。

**分支:** `feat/app-deploy-tabs`（已从 main 861f753 切出）。conventional commits（中文 body 可，body 每行 ≤100 字符）。

## Global Constraints

- **行数预算（验收硬指标，`wc -l` 核对）**：`page.tsx` ≤450；`app-tab-panel.tsx`/`detail-panels.tsx`/`use-app-actions.ts` ≤400；`env-deploy-card.tsx` ≤300；`overview-bar.tsx` ≤150；纯逻辑拆 `_lib/*.ts`。
- **行为零变化**：平移块（DevWizard/ArtifactSection/DepsSection/NetworkModeSection/导入向导/五栏 Detail/env 编辑/各 async 动作函数）以 diff 等价为评审口径（`git diff -M` 以 rename/移动为主）。**仅允许的三处交互变化（spec §2/§4 明示）**：
  1. tab 化导航：总览条点击=开/切 tab，面板 ✕ 关闭（关闭后激活转移相邻 tab）；
  2. 环境并排卡吸附**本环境**操作按钮（test=构建部署+日志、prod=上线，均含「固定引擎重试」）+ deploy_history 时间线按 env 过滤迁入卡内（五栏 Detail 部署栏的时间线块随之移除）；
  3. Detail 打开 tab 时自动拉取（「详情」按钮 toggle 删除），失败 toast + 面板内重试按钮。
- **不改后端 API、零新增请求类型**：总览条数据 = 既有 `load()` 全量列表；时间线数据 = 既有 `/apps/{id}/detail` 的 `deploy_history`（前端按 env 过滤）。tab 激活随全局轮询刷新 detail（仅激活 tab，spec §4）。
- **质量门**：既有 vitest 用例全绿（当前 47 个，含新增后总数以实际输出记录）、`npx tsc --noEmit` 0 错误、`npm run build` 通过、`npm run lint` 0 新增 warning（既有 warning 基线不动）。
- **fetch 现状保持**：本页既有裸 fetch 不带 Authorization——平移时不改（存量问题另行 follow-up，不在本计划）。
- **spec 偏差记录（已按 main 分支事实修正 scope，写计划时确认）**：
  1. spec §3 文件树中的 `topology-section.tsx`（「TopologySection 原样迁出」）**移出范围**——main 不存在 topology 代码（`lib/topology.ts`/`TopologySection` 只在未合并的 `feat/app-topology` 分支）；该分支将来合并时组件可直接放入 `app-tab-panel` 的 sections 区。
  2. spec §2 mockup 中 tab 面板头部列了 `[拓扑图]`——同上，main 无此区块，改版后 tab 面板不含拓扑位。
- **发现的死代码（平移时删除，评审确认）**：`page.tsx:931-950` 的 `promote(id)` 函数无任何调用点（现用 `promoteWithNode`），随 T5 迁移时删除不搬。

## 文件结构与行数预算总表

```
app/applications/
├── page.tsx                    # 壳：工具条+注册表单+总览条+tab容器+hooks 编排  ≤450
├── _lib/
│   ├── types.ts                # 全部类型（自 page.tsx:12-142 迁出）+NodeInfo/ChangeSummary 提升
│   ├── predicates.ts           # STATUS_COLOR/isDockerKind/nodeMatchesEnv/healthBadge/historyForEnv
│   ├── app-tabs.ts             # tab 状态机纯函数
│   ├── overview.ts             # buildOverviewCells 总览格子推导
│   ├── use-app-data.ts         # 数据 hook：spaces/psID/apps/nodes/stats/changes+双轮询 ~240
│   ├── use-app-actions.ts      # 动作 hook：展开态+全部操作函数                      ≤400
│   ├── app-tabs.test.ts        # vitest（node project）
│   └── overview.test.ts        # vitest（含 historyForEnv 断言）
└── _components/
    ├── overview-bar.tsx        # 总览条（受控）                                    ≤150
    ├── env-deploy-card.tsx     # 环境并排卡+按 env 时间线                          ≤300
    ├── detail-panels.tsx       # 五栏 Detail+登记变更+版本历史（平移）             ≤400
    ├── app-tab-panel.tsx       # 单应用工作区（横幅+状态头+卡区+面板编排）          ≤400
    ├── import-wizard.tsx       # 导入向导自包含（state+逻辑+JSX 整体平移）          ~360
    └── sections.tsx            # DevWizard/Artifact/Deps/NetworkMode 平移          ~350
```

依赖顺序：T1 类型/谓词/tab 状态机 → T2 总览推导 → T3/T4 自包含区块平移 → T5 hooks 平移 → T6 新 UI 组件 → T7 Detail 平移 → T8 tab 面板 → T9 壳化收口 → T10 全套验证。

---

### Task 1: `_lib/types.ts` + `_lib/predicates.ts` + `_lib/app-tabs.ts`（tab 状态机 TDD）

**Files:**

- Create: `app/applications/_lib/types.ts`
- Create: `app/applications/_lib/predicates.ts`
- Create: `app/applications/_lib/app-tabs.ts`
- Test: `app/applications/_lib/app-tabs.test.ts`
- Modify: `vitest.config.ts`（node project include 增一条）
- Modify: `app/applications/page.tsx:12-182`（本地类型/常量/纯函数删除，改 import）

**Interfaces:**

- Consumes: 无（首任务）。
- Produces（后续所有任务依赖）:
  - `types.ts` 导出：`Envelope<T>`、`PS`、`Instance`、`EnvVar`、`AppStats`、`App`、`Dep`、`DepsCatalog`、`Req`、`Detail`（含 `Detail["deploy_history"]`）、`NodeInfo`（新命名，原 page.tsx:531-542 内联类型）、`ChangeSummary`（新命名 `{ id: string; status: string; output?: string; created_at?: string }`，原 appChanges 内联类型）
  - `predicates.ts` 导出：`STATUS_COLOR: Record<string, string>`、`isDockerKind(kind: string): boolean`、`nodeMatchesEnv(n: { id: string; env?: string } | undefined, targetEnv: string): boolean`、`healthBadge(status: string): { text: string; cls: string }`、`historyForEnv(history: Detail["deploy_history"], env: string): Detail["deploy_history"]`
  - `app-tabs.ts` 导出：`type TabState = { ids: string[]; activeId: string }`、`const EMPTY_TABS: TabState`、`openTab(t: TabState, appId: string): TabState`、`selectTab(t: TabState, appId: string): TabState`、`closeTab(t: TabState, appId: string): TabState`、`pruneTabs(t: TabState, liveIds: string[]): TabState`

- [ ] **Step 1: 扩 vitest node project 的 include**

现有 node project 只收根 `lib/**` 与根 `__tests__/**` 的 `.test.ts`——`app/**/_lib/` 下的纯函数测试两个 project 都不收（components project 只收 `.test.tsx`）。在 `vitest.config.ts` 的 node project 改：

```ts
        test: {
          name: "node",
          environment: "node",
          include: [
            "lib/**/*.test.ts",
            "__tests__/**/*.test.ts",
            // 应用页内 _lib 纯函数（applications tab 化改版）：node 环境跑
            "app/**/_lib/**/*.test.ts",
          ],
        },
```

- [ ] **Step 2: 写失败测试 `_lib/app-tabs.test.ts`**

```ts
// tab 状态机纯函数测试：开合/去重/选中转移/删除应用清理。
import { describe, it, expect } from "vitest";
import { EMPTY_TABS, openTab, selectTab, closeTab, pruneTabs, type TabState } from "./app-tabs";

describe("openTab", () => {
  it("新应用：追加到末尾并激活", () => {
    const t = openTab(EMPTY_TABS, "app_a");
    expect(t).toEqual({ ids: ["app_a"], activeId: "app_a" });
  });
  it("已打开应用：不重复追加，仅激活，顺序不变", () => {
    let t = openTab(EMPTY_TABS, "app_a");
    t = openTab(t, "app_b");
    t = openTab(t, "app_a");
    expect(t).toEqual({ ids: ["app_a", "app_b"], activeId: "app_a" });
  });
});

describe("selectTab", () => {
  it("已打开：切换激活", () => {
    const t: TabState = { ids: ["app_a", "app_b"], activeId: "app_a" };
    expect(selectTab(t, "app_b").activeId).toBe("app_b");
  });
  it("未打开：原样返回（入口统一走 openTab）", () => {
    const t: TabState = { ids: ["app_a"], activeId: "app_a" };
    expect(selectTab(t, "app_zz")).toBe(t);
  });
});

describe("closeTab", () => {
  const t: TabState = { ids: ["app_a", "app_b", "app_c"], activeId: "app_b" };
  it("关非激活 tab：激活不变", () => {
    const r = closeTab(t, "app_a");
    expect(r).toEqual({ ids: ["app_b", "app_c"], activeId: "app_b" });
  });
  it("关激活 tab：激活转移给右侧", () => {
    expect(closeTab(t, "app_b")).toEqual({ ids: ["app_a", "app_c"], activeId: "app_c" });
  });
  it("关最右激活 tab：激活转移给左侧", () => {
    const r = closeTab({ ids: ["app_a", "app_b"], activeId: "app_b" }, "app_b");
    expect(r).toEqual({ ids: ["app_a"], activeId: "app_a" });
  });
  it("关唯一 tab：回空态", () => {
    expect(closeTab({ ids: ["app_a"], activeId: "app_a" }, "app_a")).toEqual(EMPTY_TABS);
  });
});

describe("pruneTabs（应用删除后清理）", () => {
  it("ids 过滤 + activeId 失效时兜底最后一个", () => {
    const t: TabState = { ids: ["app_a", "app_b"], activeId: "app_a" };
    expect(pruneTabs(t, ["app_b"])).toEqual({ ids: ["app_b"], activeId: "app_b" });
  });
  it("无变化：返回原引用（避免无谓重渲染）", () => {
    const t: TabState = { ids: ["app_a"], activeId: "app_a" };
    expect(pruneTabs(t, ["app_a", "app_x"])).toBe(t);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npx vitest run app/applications/_lib/app-tabs.test.ts`
Expected: FAIL（Cannot find module './app-tabs'）

- [ ] **Step 4: 写 `_lib/types.ts`、`_lib/predicates.ts`、`_lib/app-tabs.ts`**

`types.ts`：把 page.tsx:12-142 的类型**原样复制**（`Envelope`/`PS`/`Instance`/`EnvVar`/`AppStats`/`App`/`Dep`/`DepsCatalog`/`Req`/`Detail`，注释随行），每个加 `export`；另追加两个提升命名：

```ts
// 部署节点（原 page.tsx 内联 useState 泛型提升命名）
export type NodeInfo = {
  id: string;
  name: string;
  host: string;
  status: string;
  app_count?: number;
  env?: string;
  os_type?: string;
  connect_type?: string;
};
// 变更摘要（appChanges 轮询行内类型提升命名；与 Detail.changes 字段子集兼容）
export type ChangeSummary = { id: string; status: string; output?: string; created_at?: string };
```

`predicates.ts`：把 `STATUS_COLOR`（:144-152）、`isDockerKind`（:159-160）、`nodeMatchesEnv`（:163-167）、`healthBadge`（:171-182）**原样复制**加 export（注释随行），文件头加 import，末尾追加新函数：

```ts
import type { Detail } from "./types";

//（以上四个原样迁出的导出）

// 按 env 过滤部署历史（P3 时间线进环境卡用）：保持原顺序（后端已按时间倒序）。
export function historyForEnv(
  history: Detail["deploy_history"],
  env: string
): Detail["deploy_history"] {
  return history.filter((h) => h.env === env);
}
```

`app-tabs.ts` 完整内容：

```ts
// 应用 tab 状态机（纯函数，视图层不直接改数组）：
// 开=去重+激活；选中=已打开才生效（入口统一走 openTab）；关=激活转移相邻（优先右侧）；
// prune=应用被删后清理失效 id。所有函数不修改入参。
export type TabState = { ids: string[]; activeId: string };

export const EMPTY_TABS: TabState = { ids: [], activeId: "" };

export function openTab(t: TabState, appId: string): TabState {
  if (t.ids.includes(appId)) return { ids: t.ids, activeId: appId };
  return { ids: [...t.ids, appId], activeId: appId };
}

export function selectTab(t: TabState, appId: string): TabState {
  if (!t.ids.includes(appId)) return t;
  return { ...t, activeId: appId };
}

export function closeTab(t: TabState, appId: string): TabState {
  const idx = t.ids.indexOf(appId);
  if (idx < 0) return t;
  const ids = t.ids.filter((id) => id !== appId);
  let activeId = t.activeId;
  if (t.activeId === appId) {
    activeId = ids[idx] ?? ids[idx - 1] ?? ""; // 优先右侧，其次左侧，全关回空
  }
  return { ids, activeId };
}

export function pruneTabs(t: TabState, liveIds: string[]): TabState {
  const ids = t.ids.filter((id) => liveIds.includes(id));
  if (ids.length === t.ids.length) return t;
  const activeId = ids.includes(t.activeId) ? t.activeId : (ids[ids.length - 1] ?? "");
  return { ids, activeId };
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `npx vitest run app/applications/_lib/app-tabs.test.ts`
Expected: PASS（11 passed）

- [ ] **Step 6: page.tsx 换 import（编译等价）**

删除 page.tsx:12-182 的本地类型/常量/纯函数（保留 `DevWizard` 等组件），顶部改为：

```tsx
import { API_BASE_URL } from "@/lib/api";
import type { Artifact } from "@/lib/api-types-manual";
import { devStep } from "@/lib/devstep";
import { logger } from "@/lib/logger";
import { toast } from "@/lib/toast";
import { ChangeOutput } from "@/app/_components/change-output";
import type {
  App,
  AppStats,
  ChangeSummary,
  Dep,
  DepsCatalog,
  Detail,
  Envelope,
  EnvVar,
  NodeInfo,
  PS,
  Req,
} from "./_lib/types";
import {
  STATUS_COLOR,
  healthBadge,
  historyForEnv,
  isDockerKind,
  nodeMatchesEnv,
} from "./_lib/predicates";
```

注：`historyForEnv` 本任务 page.tsx 尚未用到（T6 才用），先不 import（避免 unused 报错），只 import 现有引用的五个。`NodeInfo`/`ChangeSummary` 同理——page 内联类型 :531-542 与 :567-569 本任务**暂不替换**（保持最小 diff，T5 换 hook 时一并替换为命名类型）。

- [ ] **Step 7: 全量验证**

Run: `npx tsc --noEmit && npx vitest run`
Expected: tsc 0 错误；vitest 全绿（原 47 + 新 11 = 58，以实际输出记录数）

- [ ] **Step 8: Commit**

```bash
git add app/applications/_lib vitest.config.ts app/applications/page.tsx
git commit -m "refactor(applications): 拆出 _lib 类型/谓词 + tab 状态机（TDD）"
```

---

### Task 2: `_lib/overview.ts`（总览格子推导 TDD）

**Files:**

- Create: `app/applications/_lib/overview.ts`
- Test: `app/applications/_lib/overview.test.ts`

**Interfaces:**

- Consumes: `App`（Task 1 `types.ts`）。
- Produces:
  - `type CellEnvState = "ok" | "bad" | "none"`
  - `type OverviewCell = { id: string; name: string; status: string; isExternal: boolean; isImporting: boolean; importingHint: string; version: number; showVersion: boolean; test: CellEnvState; prod: CellEnvState }`
  - `buildOverviewCells(apps: App[]): OverviewCell[]`（T6 overview-bar 消费）

- [ ] **Step 1: 写失败测试 `_lib/overview.test.ts`**

```ts
// 总览格子推导测试：managed 双环境态 / failed / 无实例 / external / 版本显隐。
import { describe, it, expect } from "vitest";
import { buildOverviewCells } from "./overview";
import type { App } from "./types";

const base: App = {
  id: "app_a",
  name: "A",
  repo_dir: "/data/repos/a",
  internal_port: 8080,
  image: "img",
  container_name: "c",
  host_port: 9103,
  url: "http://x",
  version: 3,
  status: "running",
  last_error: "",
  build_log: "",
  deploy_mode: "managed",
  external_url: "",
  app_kind: "web",
  network_mode: "bridge",
  updated_at: "2026-08-19T00:00:00Z",
  instances: [
    {
      env: "test",
      status: "running",
      url: "u",
      version: 3,
      host_port: 9103,
      image: "i",
      updated_at: "t",
    },
    {
      env: "prod",
      status: "running",
      url: "u",
      version: 2,
      host_port: 9201,
      image: "i",
      updated_at: "t",
    },
  ],
};

describe("buildOverviewCells", () => {
  it("managed 双环境 running → ok/ok，版本显示", () => {
    const [c] = buildOverviewCells([base]);
    expect(c.test).toBe("ok");
    expect(c.prod).toBe("ok");
    expect(c.showVersion).toBe(true);
    expect(c.version).toBe(3);
    expect(c.isExternal).toBe(false);
  });
  it("test 实例存在但非 running → bad", () => {
    const app = {
      ...base,
      instances: base.instances!.map((i) => (i.env === "test" ? { ...i, status: "failed" } : i)),
    };
    expect(buildOverviewCells([app])[0].test).toBe("bad");
  });
  it("无实例 → none/none", () => {
    const app = { ...base, instances: undefined };
    const [c] = buildOverviewCells([app]);
    expect(c.test).toBe("none");
    expect(c.prod).toBe("none");
  });
  it("external：isExternal=true，版本不显示，环境态 none", () => {
    const app = { ...base, deploy_mode: "external", version: 0, instances: undefined };
    const [c] = buildOverviewCells([app]);
    expect(c.isExternal).toBe(true);
    expect(c.showVersion).toBe(false);
    expect(c.test).toBe("none");
  });
  it("importing：isImporting=true 且带进度提示", () => {
    const app = { ...base, status: "importing", last_error: "正在 clone..." };
    const [c] = buildOverviewCells([app]);
    expect(c.isImporting).toBe(true);
    expect(c.importingHint).toBe("正在 clone...");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run app/applications/_lib/overview.test.ts`
Expected: FAIL（Cannot find module './overview'）

- [ ] **Step 3: 写 `_lib/overview.ts`**

```ts
// 总览条格子推导（纯函数）：App+instances → 一格展示模型。
// 环境迷你态：ok=实例 running / bad=实例存在但非 running / none=无实例。
import type { App } from "./types";

export type CellEnvState = "ok" | "bad" | "none";

export type OverviewCell = {
  id: string;
  name: string;
  status: string; // App.status 原值（running/building/...）
  isExternal: boolean;
  isImporting: boolean;
  importingHint: string; // importing 时 last_error 进度文案
  version: number;
  showVersion: boolean; // external 或 version=0 不显示
  test: CellEnvState;
  prod: CellEnvState;
};

function envState(app: App, env: "test" | "prod"): CellEnvState {
  const ins = app.instances?.find((i) => i.env === env);
  if (!ins) return "none";
  return ins.status === "running" ? "ok" : "bad";
}

export function buildOverviewCells(apps: App[]): OverviewCell[] {
  return apps.map((a) => ({
    id: a.id,
    name: a.name,
    status: a.status,
    isExternal: a.deploy_mode === "external",
    isImporting: a.status === "importing",
    importingHint: a.last_error || "",
    version: a.version,
    showVersion: a.deploy_mode !== "external" && a.version > 0,
    test: envState(a, "test"),
    prod: envState(a, "prod"),
  }));
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run app/applications/_lib/overview.test.ts`
Expected: PASS（5 passed）

- [ ] **Step 5: 全量验证 + Commit**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 0 错误，全绿

```bash
git add app/applications/_lib/overview.ts app/applications/_lib/overview.test.ts
git commit -m "feat(applications): _lib 总览格子推导 buildOverviewCells（TDD）"
```

---

### Task 3: `_components/sections.tsx`（四个自包含区块原样平移）

**Files:**

- Create: `app/applications/_components/sections.tsx`
- Modify: `app/applications/page.tsx:186-525`（删 DevWizard/ArtifactSection/DepsSection/NetworkModeSection 四个定义，改 import）

**Interfaces:**

- Consumes: `App`/`Dep`/`DepsCatalog`/`Envelope`（Task 1）、`API_BASE_URL`/`Artifact`/`devStep`/`toast`。
- Produces:
  - `export function DevWizard({ app }: { app: App })`（签名不变）
  - `export function ArtifactSection({ psID, appID, appKind }: { psID: string; appID: string; appKind: string })`
  - `export function DepsSection({ psID, appID }: { psID: string; appID: string })`
  - `export function NetworkModeSection({ psID, appID, mode }: { psID: string; appID: string; mode: string })`

- [ ] **Step 1: 平移（剪切粘贴，零改动）**

新建 `_components/sections.tsx`，文件头：

```tsx
"use client";

// 应用页自包含区块组件（自 page.tsx 原样平移，行为零变化）：
// DevWizard 开发向导 / ArtifactSection 构建产物 / DepsSection 中间件依赖 / NetworkModeSection 网络模式。
import { useEffect, useState } from "react";
import { API_BASE_URL } from "@/lib/api";
import type { Artifact } from "@/lib/api-types-manual";
import { devStep } from "@/lib/devstep";
import { toast } from "@/lib/toast";
import { healthBadge } from "../_lib/predicates";
import type { App, Dep, DepsCatalog, Envelope } from "../_lib/types";
```

然后把 page.tsx:186-246（DevWizard）、:250-317（ArtifactSection）、:322-478（DepsSection）、:482-525（NetworkModeSection）**整段剪切**过来，四个组件各加 `export`，函数体一行不改（注释随行）。page.tsx 对应段删除，使用处（:1573、:2341、:2346-2347）改为：

```tsx
import {
  ArtifactSection,
  DepsSection,
  DevWizard,
  NetworkModeSection,
} from "./_components/sections";
```

page.tsx 头部如 `useState`/`devStep`/`Artifact` 等仅被平移段使用的 import 需同步清理（保留仍被使用的 `useEffect`/`useRef`/`useState`/`useRouter`）。

- [ ] **Step 2: 验证平移等价**

Run: `git diff -M --stat && npx tsc --noEmit && npx vitest run`
Expected: diff stat 显示 page.tsx 大减、sections.tsx 新增；tsc 0 错误；vitest 全绿

- [ ] **Step 3: Commit**

```bash
git add app/applications/_components/sections.tsx app/applications/page.tsx
git commit -m "refactor(applications): DevWizard/产物/依赖/网络模式四区块平移至 sections.tsx"
```

---

### Task 4: `_components/import-wizard.tsx`（导入向导自包含平移）

**Files:**

- Create: `app/applications/_components/import-wizard.tsx`
- Modify: `app/applications/page.tsx`（删向导 state :570-587 中的向导 8 个 + JSX :1255-1491 + 向导函数 :711-832）

**Interfaces:**

- Consumes: `App`/`Detail`/`Envelope`（Task 1）、`API_BASE_URL`、`toast`。
- Produces:
  - `export function ImportWizard({ psID, onDone }: { psID: string; onDone: () => void })`——onDone = 导入终态或刷新列表时调（原 `load(psID)` 的等价钩子，由壳传入）
  - `export function useImportWizard()`（同文件导出 hook）：返回 `{ wizard, openWizard, closeWizard }`，`wizard` 为 `ReactNode | null`——**不采用**此形态，实际形态见下。

  实际接口（避免 JSX 生成器反模式，向导自持 state）：

```tsx
export type ImportWizardHandle = {
  open: () => void; // 复位步骤并打开（原「导入已有项目」按钮行为）
};
export function ImportWizard({
  psID,
  onDone,
  onAppsLoaded,
}: {
  psID: string;
  onDone: () => void; // 终态/失败时壳刷新列表（原 load(psID)）
  onAppsLoaded: (r: Envelope<App & { id: string }>) => void; // 透传 POST 响应给壳 toast（如需）
}): ImportWizardHandle;
```

设计说明：`ImportWizard` 自持全部 8 个 state（importOpen/importStep/importSource/importForm/importFile/importing/importProgress + importCancelRef），`forwardRef`+`useImperativeHandle` 暴露 `open()`。壳只放一个 `<ImportWizard ref={wizRef} psID={psID} onDone={() => load(psID)} onAppsLoaded={undefined}/>` 与触发按钮。`onAppsLoaded` 若平移中发现原逻辑不需要回传壳，**允许简化为删掉该 prop**（等价优先）。

- [ ] **Step 1: 平移**

新建 `_components/import-wizard.tsx`，文件头：

```tsx
"use client";

// 导入已有项目向导（自 page.tsx 原样平移，行为零变化）：
// 3 步（选来源/填信息/执行进度）；startImport 提交、pollImportDetail 轮询 detail 至终态，
// last_error 当进度文案；importCancelRef 防旧 tick 竞态（注释随原样保留）。
import { useRef, useState, forwardRef, useImperativeHandle } from "react";
import { API_BASE_URL } from "@/lib/api";
import { toast } from "@/lib/toast";
import type { App, Detail, Envelope } from "../_lib/types";
```

平移内容（**逐段剪切，函数体零改动**，仅外层包装变化）：

- page.tsx:570-587 的向导 8 个 state + `importCancelRef`（:587）→ 组件内 useState/useRef（原注释保留）
- `resetImportWizard`/`closeImportWizard`/`startImport`/`pollImportDetail`（:711-832）→ 组件内函数；其中 `load(psID)` 两处调用（:812、:818 附近）改为 `onDone()`；`pollImportDetail` 的 `setImportProgress` 等 setter 不变
- JSX :1255-1491（绿色边框向导整块）→ 组件 return；`importOpen && (...)` 条件改为组件内 `if (!importOpen) return null; return (...)`

包装：

```tsx
function ImportWizardInner(
  { psID, onDone }: { psID: string; onDone: () => void },
  ref: React.Ref<ImportWizardHandle>
) {
  //（8 个 state + importCancelRef + 4 个函数，原样）
  useImperativeHandle(ref, () => ({
    open: () => {
      resetImportWizard();
      setImportOpen(true);
    },
  }));
  if (!importOpen) return null;
  return (
    <div className="mb-4 rounded-lg border-2 border-success bg-success/40 p-3 text-sm">
      {/*（:1258-1490 原样）*/}
    </div>
  );
}
export const ImportWizard = forwardRef(ImportWizardInner);
```

page.tsx 侧：删除上述 state/函数/JSX；「导入已有项目」按钮 onClick 改为 `wizRef.current?.open()`；`importing` 在向导外的引用处（apps.map 内 `disabled={isImporting}` 用的 `a.status === "importing"` 是 App 字段**不受影响**——原向导 `importing` state 只在向导 JSX 内用，确认后清理）。

- [ ] **Step 2: 验证平移等价**

Run: `git diff -M --stat && npx tsc --noEmit && npx vitest run`
Expected: page.tsx 再减 ~270 行；tsc 0 错误；vitest 全绿

- [ ] **Step 3: Commit**

```bash
git add app/applications/_components/import-wizard.tsx app/applications/page.tsx
git commit -m "refactor(applications): 导入向导自包含平移至 import-wizard.tsx"
```

---

### Task 5: hooks 平移（`_lib/use-app-data.ts` + `_lib/use-app-actions.ts`）

**Files:**

- Create: `app/applications/_lib/use-app-data.ts`
- Create: `app/applications/_lib/use-app-actions.ts`
- Modify: `app/applications/page.tsx:527-1106`（state 与函数区大幅缩减）

**Interfaces:**

- Consumes: Task 1 全部类型/谓词。
- Produces（T8/T9 壳与面板直接消费）:

```ts
// use-app-data.ts
export function useAppData(psID: string): {
  apps: App[];
  nodes: NodeInfo[];
  appStats: Record<string, AppStats>;
  appChanges: Record<string, ChangeSummary[]>;
  reload: (id: string) => void; // 原 load()
  loadChanges: (id: string) => void;
  refreshClosedLoop: (
    appID: string,
    detailFor: string,
    setDetail: (d: Detail | null) => void
  ) => void;
};
// use-app-actions.ts
export type ExpandedKey = "logs" | "reqs" | "env" | "detail";
export function useAppActions(deps: {
  psID: string;
  apps: App[];
  nodes: NodeInfo[];
  appChanges: Record<string, ChangeSummary[]>;
  reload: (id: string) => void;
  loadChanges: (id: string) => void;
  refreshClosedLoop: (
    appID: string,
    detailFor: string,
    setDetail: (d: Detail | null) => void
  ) => void;
}): {
  // 展开态（per-app 单开互斥，原样）
  openPanel: ExpandedKey | "";
  setOpenPanel: (k: ExpandedKey | "") => void;
  logs: string;
  setLogs: (s: string) => void;
  appReqs: Req[];
  setAppReqs: (r: Req[]) => void;
  appEnvs: EnvVar[];
  setAppEnvs: (e: EnvVar[]) => void;
  envForm: { key: string; value: string; is_secret: boolean };
  setEnvForm: (f: { key: string; value: string; is_secret: boolean }) => void;
  regBusy: boolean;
  // 动作（原签名不变；detail 展开态迁移后 showDetail 由 T7/T8 的 tab 逻辑接管，见下）
  deployMsg: Record<string, string>;
  act: (
    id: string,
    action: "deploy" | "stop" | "start",
    env?: string,
    nodeID?: string,
    engine?: "fixed" | "ai"
  ) => Promise<void>;
  stopOrStart: (id: string, action: "stop" | "start") => Promise<void>; // = act(id, action)（语义化薄别名，可选）
  promoteWithNode: (id: string, nodeID: string) => Promise<void>;
  saveEnv: (id: string) => Promise<void>;
  removeEnv: (id: string, key: string) => Promise<void>;
  remove: (id: string) => Promise<void>;
  showLogs: (id: string) => Promise<void>;
  showReqs: (id: string) => Promise<void>;
  showEnv: (id: string) => Promise<void>;
  approveChange: (appID: string, chgID: string) => Promise<void>;
  rejectChange: (appID: string, chgID: string) => Promise<void>;
  releaseChange: (appID: string, chgID: string) => Promise<void>;
  mergeChange: (appID: string, chgID: string, reqID?: string) => Promise<void>;
  deployCommit: (appID: string, sha: string) => Promise<void>;
};
```

设计说明（等价优先的取舍）：

- 原 `showDetail`/`detailFor`/`detail` 三个**不进 hook**——tab 化后 Detail 属于 tab 面板生命周期（T8 在 AppTabPanel 内管理，打开即拉取），hook 只留 `refreshClosedLoop` 接收注入的 `detailFor/setDetail`。
- 原 `promote(id)`（:931-950，无调用点死代码）**删除不迁**。
- 原 `openWorkspace`（:953-955，一行 router.push）**不迁**，留 T8 面板内联（它用 `router`+`wsTool` 局部 state）。
- `register`（注册表单）与向导外逻辑留在壳（T9）。

- [ ] **Step 1: 写 `use-app-data.ts`（平移）**

文件头 + 内容（state/函数体逐行剪切自 page.tsx，仅 `load` 改名 `reload` 并外提；三个 effect 原样）：

```ts
"use client";

// 数据 hook（自 page.tsx 平移，行为零变化）：spaces 在壳内（选空间属页面级），
// 这里管 apps/nodes/appStats/appChanges + 双轮询（3s building 刷新 / 30s stats+changes）。
import { useEffect, useState } from "react";
import { API_BASE_URL } from "@/lib/api";
import { logger } from "@/lib/logger";
import { toast } from "@/lib/toast";
import type { App, AppStats, ChangeSummary, Detail, Envelope, NodeInfo } from "./types";

export function useAppData(psID: string) {
  const [apps, setApps] = useState<App[]>([]);
  const [nodes, setNodes] = useState<NodeInfo[]>([]);
  const [appStats, setAppStats] = useState<Record<string, AppStats>>({});
  const [appChanges, setAppChanges] = useState<Record<string, ChangeSummary[]>>({});
  const [deployMsg, setDeployMsg] = useState<Record<string, string>>({});
  //（load/loadStats/loadChanges/两个 useEffect 自 page.tsx:600-675 原样平移，
  //  内部 setApps/setNodes/setAppStats/setAppChanges/setDeployMsg 与 fetch/toast/logger 全不变；
  //  原 3s interval 里 toast.success + logger.info 的 deployMsg 清理逻辑原样。
  //  对外：load 更名 reload 返回；loadChanges 返回；deployMsg 一并从此 hook 返回
  //  （T8 面板横幅消费）：return { apps, nodes, appStats, appChanges, deployMsg, reload, loadChanges, refreshClosedLoop }）
  // refreshClosedLoop（原 :1019-1026）：签名加 detailFor/setDetail 参数注入（原闭包读 detailFor 改参数）。
  return {
    apps,
    nodes,
    appStats,
    appChanges,
    deployMsg,
    reload: load,
    loadChanges,
    refreshClosedLoop: (
      appID: string,
      detailFor: string,
      setDetail: (d: Detail | null) => void
    ) => {
      loadChanges(appID);
      if (detailFor === appID) {
        fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appID}/detail`)
          .then((r) => r.json())
          .then((r: Envelope<Detail>) => {
            if (r.code === 0) setDetail(r.data ?? null);
          });
      }
    },
  };
}
```

注：上面注释块即平移指令——implementer 按 page.tsx 实际行号区段（:600-675 原样）剪切，不允许改写逻辑；`refreshClosedLoop` 原 async 函数体 :1019-1026 的逻辑与上方等价（catch 省略差异以原文件为准：原函数无 try/catch，此处保持无）。

- [ ] **Step 2: 写 `use-app-actions.ts`（平移）**

同法：文件头 import 后，把 page.tsx 的展开态 state（logsFor→openPanel 模式见下）、:956-1005 的 env/reqs/logs 函数、:836-929 的 act/promoteWithNode、:1027-1095 的闭环四函数+registerChange 的 regBusy、:1096-1105 deployCommit 逐段平移。展开态改造（唯一的结构变化，等价依据：原每面板一组 state 都是「单开互斥」）：

```ts
// 原 logsFor/reqsFor/envFor/detailFor 四组 per-app 展开态，tab 化后同一时刻只有一个应用
// 面板存在 → 收敛为单值 openPanel: "logs"|"reqs"|"env"|"detail"|""。原 showLogs/showReqs/showEnv
// 的 toggle 语义（同 id 再点关闭）改为：同 key 再点关闭，异 key 切换。
```

`act`/`promoteWithNode` 里的 `alert(...)` 校验、`setDeployMsg`、`load(psID)`→`deps.reload(psID)` 等逐行对应（仅把闭包里的 `psID`/`nodes`/`apps`/`appChanges` 换成 `deps.` 前缀，函数体其余零改动）。

- [ ] **Step 3: page.tsx 接线（本任务先原地消费，UI 不变）**

page.tsx 删掉已迁 state/函数，组件体改为：

```tsx
const data = useAppData(psID);
const { apps, nodes, appStats, appChanges, deployMsg, reload, loadChanges, refreshClosedLoop } =
  data;
const actions = useAppActions({
  psID,
  apps,
  nodes,
  appChanges,
  reload,
  loadChanges,
  refreshClosedLoop,
});
const [spaces, setSpaces] = useState<PS[]>([]); // 壳保留
//（detail/detailFor/showDetail 本任务暂留 page.tsx 原位——T7/T8 再迁，避免一步跨两任务）
```

JSX 中 `logsFor === a.id` → `actions.openPanel === "logs"`，`envFor === a.id` → `actions.openPanel === "env"`，`reqsFor === a.id` → `actions.openPanel === "reqs"`（detail 判断暂留原样）；所有 `act(`/`showEnv(`/`remove(`/`deployCommit(` 等调用加 `actions.` 前缀。

- [ ] **Step 4: 验证**

Run: `npx tsc --noEmit && npx vitest run && npm run lint`
Expected: 0 错误全绿；lint 无新增 warning（page.tsx 行数此时应 ~1450 以下）

- [ ] **Step 5: Commit**

```bash
git add app/applications/_lib/use-app-data.ts app/applications/_lib/use-app-actions.ts app/applications/page.tsx
git commit -m "refactor(applications): 数据/动作逻辑平移至 _lib hooks（展开态收敛单值）"
```

---

### Task 6: `_components/overview-bar.tsx` + `_components/env-deploy-card.tsx`（新 UI）

**Files:**

- Create: `app/applications/_components/overview-bar.tsx`
- Create: `app/applications/_components/env-deploy-card.tsx`

**Interfaces:**

- Consumes: `OverviewCell`/`buildOverviewCells`（Task 2）、`STATUS_COLOR`/`historyForEnv`（Task 1）、`Instance`/`AppStats`/`Detail`（Task 1）。
- Produces:

```tsx
// overview-bar.tsx（受控组件，≤150 行）
export function OverviewBar({
  cells,
  activeId,
  onSelect,
}: {
  cells: OverviewCell[];
  activeId: string;
  onSelect: (appId: string) => void;
});
// env-deploy-card.tsx（≤300 行）
export function EnvDeployCard({
  app,
  env,
  instance,
  stats,
  history,
  deployMsg,
  busy,
  selectedNode,
  onDeploy,
  onPromote,
  onRetryFixed,
  onLogs,
}: {
  app: App;
  env: "test" | "prod";
  instance?: Instance;
  stats?: AppStats;
  history: Detail["deploy_history"]; // 该应用全量历史（卡内 historyForEnv 过滤）
  deployMsg?: string;
  busy: boolean; // importing 等禁用态
  selectedNode: string;
  onDeploy: () => void; // test：act(id,"deploy","test",selectedNode)
  onPromote: () => void; // prod：promoteWithNode(id, selectedNode)
  onRetryFixed: () => void; // 失败重试：act(id,"deploy",env,selectedNode,"fixed")
  onLogs: () => void; // 构建日志（test 卡）
});
```

- [ ] **Step 1: 写 `overview-bar.tsx`**

```tsx
"use client";

// 应用总览条（spec §2）：每应用一格，横向滚动；点击=开/切 tab；激活格高亮边框。
// 纯受控展示，数据推导在 _lib/overview.ts（cells 由壳传入）。
import { STATUS_COLOR } from "../_lib/predicates";
import type { OverviewCell } from "../_lib/overview";

const ENV_DOT: Record<string, string> = {
  ok: "bg-success", // T✓/P✓ 绿点
  bad: "bg-danger", // 实例存在但非 running
  none: "bg-surface-2", // 无实例
};

function EnvDot({ label, state }: { label: string; state: "ok" | "bad" | "none" }) {
  return (
    <span
      className="flex items-center gap-0.5 text-[10px] text-text-muted"
      title={`${label}: ${state}`}
    >
      <span className={`inline-block h-2 w-2 rounded-full ${ENV_DOT[state]}`} />
      {label}
    </span>
  );
}

export function OverviewBar({
  cells,
  activeId,
  onSelect,
}: {
  cells: OverviewCell[];
  activeId: string;
  onSelect: (appId: string) => void;
}) {
  return (
    <div className="mb-3 flex gap-2 overflow-x-auto pb-1" data-testid="overview-bar">
      {cells.map((c) => (
        <button
          key={c.id}
          onClick={() => onSelect(c.id)}
          className={`shrink-0 rounded-lg border px-3 py-1.5 text-left text-xs transition-colors ${
            c.id === activeId
              ? "border-accent bg-accent/10"
              : "border-border bg-surface hover:border-accent/50"
          }`}
          title={c.isImporting ? `导入中：${c.importingHint}` : c.status}
        >
          <div className="flex items-center gap-2">
            <span className={`rounded px-1 py-0.5 ${STATUS_COLOR[c.status] ?? "bg-surface-2"}`}>
              {c.isImporting ? "导入中" : c.status}
            </span>
            <span className="font-medium">{c.name}</span>
            {c.showVersion && <span className="text-text-muted">v{c.version}</span>}
          </div>
          {!c.isExternal && (
            <div className="mt-1 flex items-center gap-3">
              <EnvDot label="T" state={c.test} />
              <EnvDot label="P" state={c.prod} />
            </div>
          )}
        </button>
      ))}
      {cells.length === 0 && (
        <span className="text-xs text-text-muted">暂无应用（在上方注册或导入）</span>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 写 `env-deploy-card.tsx`**

结构（完整代码以此描述+下方骨架为准，徽章/URL/健康/待上线变更/时间线五块**逐块取自 page.tsx 现有实现等价重排**，样式类沿用原值）：

```tsx
"use client";

// 环境并排卡（spec §2）：tab 内 test/prod 左右各一张。
// 组成（自原 page.tsx :1794-1890 平移重排）：
//   头：🧪测试test / 🚀生产prod 徽章 + 实例状态徽章（STATUS_COLOR）+ vN；
//   体：实例 URL 链接（无实例=提示文案「未部署/未上线」）+ prod 健康（stats.health/cpu/mem，:1851-1873 同款）；
//       + prod 待上线变更数（appChanges approved 列表，:1851-1885 同款，由壳过滤后传 count/summary 亦可——
//         为控行数：prod 卡只显示「待上线变更 N 条」摘要行 + title 悬浮明细，不再逐条列（spec §2 卡内紧凑）；
//         逐条列表保留在五栏 Detail 的「研发」栏（原 :2066-2086 平移不动），信息不丢失）；
//   时间线：historyForEnv(history, env) → P3 deploy_history 折叠列表（:2125-2173 原样平移进卡）；
//   按钮（吸附本环境，spec §2 允许变化②）：test=[构建部署(test·master)][日志]；prod=[🚀上线(prod)]；
//       实例 failed 时本卡显示 [🔧用固定引擎重试]（:1541-1559 语义，按钮文案/提示原样）。
import { STATUS_COLOR, historyForEnv } from "../_lib/predicates";
import type { App, AppStats, Detail, Instance } from "../_lib/types";

export function EnvDeployCard(props: {
  app: App;
  env: "test" | "prod";
  instance?: Instance;
  stats?: AppStats;
  history: Detail["deploy_history"];
  deployMsg?: string;
  busy: boolean;
  onDeploy: () => void;
  onPromote: () => void;
  onRetryFixed: () => void;
  onLogs: () => void;
}) {
  const { app, env, instance, stats, history, deployMsg, busy } = props;
  const hist = historyForEnv(history, env);
  const label = env === "prod" ? "🚀 生产 prod" : "🧪 测试 test";
  return (
    <div className="rounded bg-bg p-2 text-xs" data-testid={`env-card-${env}`}>
      <div className="flex items-center gap-2">
        <span
          className={`rounded px-1.5 py-0.5 font-medium ${env === "prod" ? "bg-accent/10 text-accent" : "bg-warn/10 text-warn"}`}
        >
          {label}
        </span>
        {instance && (
          <span
            className={`rounded px-1.5 py-0.5 ${STATUS_COLOR[instance.status] ?? "bg-surface-2"}`}
          >
            {instance.status}
          </span>
        )}
        {instance && instance.version > 0 && (
          <span className="text-text-muted">v{instance.version}</span>
        )}
      </div>
      {deployMsg && <div className="mt-1 text-accent">{deployMsg}</div>}
      {instance?.url ? (
        <a
          href={instance.url}
          target="_blank"
          rel="noreferrer"
          className="mt-1 block truncate text-accent hover:underline"
        >
          {instance.url}
        </a>
      ) : (
        <div className="mt-1 text-text-muted">
          {env === "prod" ? "未上线（点「上线」部署）" : "未部署（发布或「构建部署」）"}
        </div>
      )}
      {env === "prod" && stats?.deployed && (
        <div className="mt-1 flex flex-wrap items-center gap-2 text-[11px]">
          <span className="text-text-muted">健康</span>
          <span
            className={(stats.health === "up" ? "text-success" : "text-danger") + " font-medium"}
          >
            {stats.health}
          </span>
          {stats.cpu && (
            <span className="text-text-muted">
              CPU {stats.cpu} · 内存 {stats.mem}
            </span>
          )}
        </div>
      )}
      {/* prod 待上线变更摘要（approved 数）+ prod 无 prod-stats 时与原 :1851-1885 的待上线块等价的紧凑行 */}
      {/* 时间线：<details> 折叠 + historyForEnv 列表（原 :2127-2172 JSX 平移，dh 字段渲染一字不改） */}
      {/* 按钮行：见组件头注释的吸附规则（disabled={busy}，title 文案取原值） */}
    </div>
  );
}
```

implementer 注意：`{/* ... */}` 注释处即平移目标——对应源码行号已标（:2127-2172 时间线 / 按钮文案 :1651-1670 / 重试 :1541-1559）；「待上线变更摘要」为唯一新写小块（一行 + title），其余全部等价平移。落笔后 `wc -l` 核 ≤300。

- [ ] **Step 3: 验证 + Commit**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 0 错误全绿（两组件本任务暂无消费者，tsc 通过即接线就绪；实际渲染验证在 T9 后）

```bash
git add app/applications/_components/overview-bar.tsx app/applications/_components/env-deploy-card.tsx
git commit -m "feat(applications): 总览条 + 环境并排卡组件（时间线按 env 过滤进卡）"
```

---

### Task 7: `_components/detail-panels.tsx`（五栏 Detail + 登记变更 + 版本历史平移）

**Files:**

- Create: `app/applications/_components/detail-panels.tsx`
- Modify: `app/applications/page.tsx`（删 :1980-2339 五栏 Detail 块 → 改用组件）

**Interfaces:**

- Consumes: Task 5 hooks 全部动作、`ChangeOutput`、Task 1 类型。
- Produces:

```tsx
export function DetailPanels({
  app,
  detail,
  appStats,
  regFor,
  setRegFor,
  regReq,
  setRegReq,
  regNote,
  setRegNote,
  regBusy,
  envList,
  envForm,
  setEnvForm,
  logs,
  appReqs,
  openPanel,
  onApprove,
  onReject,
  onRelease,
  onMerge,
  onRegisterChange,
  onDeployCommit,
  onSaveEnv,
  onRemoveEnv,
}: {
  /* 字段类型 = Task 1 类型 + Task 5 hook 返回类型；回调签名与 Task 5 Produces 一致 */
});
```

设计说明：本组件是**纯展示编排**（受控），承接原 `detailFor === a.id && detail`（:1980-2340）+ logs/reqs/env 三个展开块（:1896-1979）+ 登记变更面板（:2266-2317）+ 版本历史（:2318-2338）的**等价平移**：

- 五栏网格 `grid md:grid-cols-5`（需求/研发/部署/运行/依赖）逐栏平移；其中「部署」栏的 deploy_history 折叠块**删除**（时间线已按 env 进环境卡，T6 完成变化②），实例列表/needs/发布保留；
- `detail` 为 null 时组件返回 null（tab 面板控制加载/重试态，T8）。

- [ ] **Step 1: 平移**

文件头 `"use client"` + import（`ChangeOutput`、类型、无直接 fetch）。组件体 = 上述区段逐段剪切，仅做三类机械替换：`a.id`→`app.id`、`approveChange(`→`onApprove(` 等回调改名、state setter 换 props。渲染结构/className/文案零改动。

- [ ] **Step 2: page.tsx 临时接线（UI 暂不变——仍在旧列表位置渲染）**

原位置替换为：

```tsx
<DetailPanels
  app={a}
  detail={detailFor === a.id ? detail : null}
  appStats={appStats[a.id]}
  regFor={regFor}
  setRegFor={setRegFor}
  regReq={regReq}
  setRegReq={setRegReq}
  regNote={regNote}
  setRegNote={setRegNote}
  regBusy={actions.regBusy}
  envList={appEnvs}
  envForm={envForm}
  setEnvForm={setEnvForm}
  logs={logs}
  appReqs={appReqs}
  openPanel={
    actions.openPanel === "logs"
      ? "logs"
      : actions.openPanel === "reqs"
        ? "reqs"
        : actions.openPanel === "env"
          ? "env"
          : ""
  }
  onApprove={actions.approveChange}
  onReject={actions.rejectChange}
  onRelease={actions.releaseChange}
  onMerge={actions.mergeChange}
  onRegisterChange={/* registerChange 需要 regReq/regNote 闭包 —— 由壳薄包装：
    (appID: string) => {} 不适用；改为 onRegisterChange: (appID: string, reqID: string, note: string) => Promise<void>
    hook 内 registerChange 签名同步为三参（Task 5 的 Produces 若已两参，此处统一三参，回改 Task 5 文件签名即可） */}
  onDeployCommit={actions.deployCommit}
  onSaveEnv={actions.saveEnv}
  onRemoveEnv={actions.removeEnv}
/>
```

注：`registerChange` 三参签名 `(appID: string, reqID: string, note: string)` 是本任务对 Task 5 接口的**修订**——hook 不再闭包 regReq/regNote（它们是面板局部输入），组件直接从 props 读值传参。执行时以三参为准（Task 5 文件同步改）。

- [ ] **Step 3: 验证**

Run: `git diff -M --stat && npx tsc --noEmit && npx vitest run`
Expected: page.tsx 降至 ~1200 行；0 错误全绿

- [ ] **Step 4: Commit**

```bash
git add app/applications/_components/detail-panels.tsx app/applications/page.tsx app/applications/_lib/use-app-actions.ts
git commit -m "refactor(applications): 五栏详情/登记变更/版本历史平移至 detail-panels.tsx"
```

---

### Task 8: `_components/app-tab-panel.tsx`（单应用工作区）

**Files:**

- Create: `app/applications/_components/app-tab-panel.tsx`

**Interfaces:**

- Consumes: Task 3 sections、Task 5 两 hooks 的动作（经 props 注入）、Task 6 EnvDeployCard、Task 7 DetailPanels、Task 1 `TabState` 无关（tab 状态在壳）。
- Produces:

```tsx
export function AppTabPanel(props: {
  app: App;
  psID: string;
  nodes: NodeInfo[];
  selectedNode: string;
  wsTool: string;
  setWsTool: (t: string) => void;
  appStats?: AppStats;
  appChanges: ChangeSummary[];
  data: ReturnType<typeof useAppData>; // 整个数据 hook 返回值（reload/deployMsg 等）
  actions: ReturnType<typeof useAppActions>; // 整个动作 hook 返回值
  onClose: () => void; // ✕ 关闭 tab
}): ReactNode;
```

- [ ] **Step 1: 写组件（编排为主，区块来自既有件）**

结构（对应 spec §2 tab 区）：

```tsx
"use client";

// 单应用工作区（spec §2）：状态横幅（构建中/失败/导入中 spinner 行，原 :1498-1572 平移）
// + 状态头（名字/徽章/导入来源徽章/repo 行，原 :1573-1772 精简——操作按钮迁入环境卡与工具行）
// + 环境并排区：external=探活卡（原 :1773-1792 平移）；managed=2×EnvDeployCard（grid sm:grid-cols-2）
// + 工具行：编码工具选择+编码按钮+变量+停止/启动+需求+日志+删除（原 :1622-1744 的 ml-auto 按钮组平移，
//   「构建部署/上线」两按钮不再在此——吸附环境卡；「详情」按钮删除——面板常驻 Detail）
// + DevWizard（!external && !importing）+ DetailPanels + Artifact/Deps/NetworkMode 三区块
// Detail 生命周期：挂载即拉（useEffect 首拉 + 随 data 轮询刷新仅激活 tab；失败 toast+重试按钮，spec §4）。
```

要点（implementer 按 page.tsx 已标行号平移，行为等价）：

1. **Detail 自管**：面板内 `const [detail, setDetail] = useState<Detail | null>(null)` + `detailFor` 即 `app.id`；挂载 useEffect 拉 `/apps/{id}/detail`（带 Authorization 不需要——与页内现状一致）；失败 `toast.error` + 渲染重试按钮（spec §4 允许变化③）。`refreshClosedLoop` 调用从 `actions.refreshClosedLoop(app.id, app.id, setDetail)` 走（Task 5 签名）。
2. **按钮吸附**：`onDeploy={() => actions.act(app.id, "deploy", "test", selectedNode)}`；`onPromote={() => actions.promoteWithNode(app.id, selectedNode)}`；`onRetryFixed` 取原 :1541-1559 的「最近 failed 实例 env」逻辑（在卡间判断：`app.instances?.filter(failed).sort(updated_at desc)[0]?.env || "test"`，prod 卡默认 env 固定 "prod"、test 卡固定 "test"——重试按钮只在对应卡 instance.status==="failed" 时渲染）；`onLogs={() => actions.showLogs(app.id)}`。
3. **wsTool**：工具选择器 state 经 props 提升到壳（多 tab 共享一个全局工具偏好，与现状单值等价——现状本就是全局一个 wsTool）。
4. **external 探活卡**：原 :1773-1792 JSX 平移（条件 `appStats?.deployed`）。
5. `openWorkspace`：面板内 `const router = useRouter()` 内联一行（原 :953-955）。

- [ ] **Step 2: 验证（编译级）**

Run: `npx tsc --noEmit`
Expected: 0 错误（尚无消费者）

- [ ] **Step 3: Commit**

```bash
git add app/applications/_components/app-tab-panel.tsx
git commit -m "feat(applications): 单应用 tab 面板（环境并排卡+工具行+详情常驻编排）"
```

---

### Task 9: page.tsx 壳化收口（tab 状态接线 + 删旧列表）

**Files:**

- Modify: `app/applications/page.tsx`（全量重排为壳）

**Interfaces:**

- Consumes: T2 `buildOverviewCells`、T1 `openTab/selectTab/closeTab/pruneTabs/EMPTY_TABS`、T4 ImportWizard、T6 OverviewBar、T8 AppTabPanel、T5 hooks。
- Produces: 最终 `/applications` 页面。

- [ ] **Step 1: 壳化**

ApplicationsPage 最终结构：

```tsx
"use client";
//（imports：hooks/组件/_lib）
export default function ApplicationsPage() {
  const [spaces, setSpaces] = useState<PS[]>([]);
  const [psID, setPsID] = useState("");
  const [selectedNode, setSelectedNode] = useState("");
  const [appKind, setAppKind] = useState("web");
  const [form, setForm] = useState({
    name: "",
    internal_port: 8080,
    deploy_mode: "managed" as "managed" | "external",
    external_url: "",
  });
  const [wsTool, setWsTool] = useState("opencode");
  const [tabs, setTabs] = useState<TabState>(EMPTY_TABS);
  const wizRef = useRef<ImportWizardHandle>(null);
  // spaces 首拉 effect（原 :590-598 原样）
  const data = useAppData(psID);
  const actions = useAppActions({
    psID,
    apps: data.apps,
    nodes: data.nodes,
    appChanges: data.appChanges,
    reload: data.reload,
    loadChanges: data.loadChanges,
    refreshClosedLoop: data.refreshClosedLoop,
  });
  const activeApp = data.apps.find((a) => a.id === tabs.activeId);

  // 应用删除后清 tab（pruneTabs 引用不变则不 setState，轮询安全）
  useEffect(() => {
    setTabs((t) =>
      pruneTabs(
        t,
        data.apps.map((a) => a.id)
      )
    );
  }, [data.apps]);

  return (
    <div>
      {/* ① 工具条：标题+空间选择+节点选择（原 :1109-1160 原样）+ 说明（:1161-1165） */}
      {/* ② 注册表单（原 :1167-1253 原样，含 register 函数） */}
      {/* ③ ImportWizard（onDone={() => data.reload(psID)}） */}
      {/* ④ OverviewBar：cells={buildOverviewCells(data.apps)} activeId={tabs.activeId}
              onSelect={(id) => setTabs((t) => openTab(t, id))} */}
      {/* ⑤ tab 页签行：tabs.ids.map(名字+✕)，click=selectTab、✕=closeTab（小 tab 头，一行式） */}
      {/* ⑥ 工作区：activeApp ? <AppTabPanel ... onClose={() => setTabs((t) => closeTab(t, activeApp!.id))} />
              : 空态提示（apps.length===0 的原 :2353-2359 文案 + 非空未选提示「点击上方应用格子打开工作区」） */}
      {/* ⑦ 底部说明卡（原 :2362-2366 原样） */}
    </div>
  );
}
```

同时删除：旧 `apps.map` 大列表（:1493-2352 全部——内容已由 T3-T8 组件承接）、`detail/detailFor/showDetail`（已进 T8）、`promote`（死代码）。

- [ ] **Step 2: 新增组件冒烟测试（jsdom）**

Create: `app/applications/__tests__/page.test.tsx`

```tsx
// 壳冒烟（jsdom）：总览条渲染 + 点格开 tab + 关 tab 回空态。fetch 全 mock（installFetchMock）。
import { describe, it, expect } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import ApplicationsPage from "../page";
import { installFetchMock, ok } from "@/lib/test-utils";
import type { App, Envelope } from "../_lib/types";

const app: App = {
  id: "app_smoke",
  name: "冒烟应用",
  repo_dir: "/data/repos/s",
  internal_port: 8080,
  image: "img",
  container_name: "c",
  host_port: 9103,
  url: "http://x",
  version: 1,
  status: "running",
  last_error: "",
  build_log: "",
  deploy_mode: "managed",
  external_url: "",
  app_kind: "web",
  network_mode: "bridge",
  updated_at: "2026-08-19T00:00:00Z",
  instances: [
    {
      env: "test",
      status: "running",
      url: "u",
      version: 1,
      host_port: 9103,
      image: "i",
      updated_at: "t",
    },
  ],
};

function install() {
  const m = installFetchMock();
  m.mockImplementation((input: unknown) => {
    const url = String(input);
    if (url.endsWith("/project-spaces"))
      return Promise.resolve(ok([{ id: "ps_default", name: "默认", slug: "default" }]));
    if (url.endsWith("/deploy-nodes")) return Promise.resolve(ok([]));
    if (url.includes("/apps/app_smoke/"))
      return Promise.resolve(
        ok({
          application: app,
          requirements: [],
          changes: [],
          releases: [],
          commits: [],
          instances: app.instances!,
          sessions: [],
          tasks: [],
          routes: [],
          deps: [],
          deploy_history: [],
        })
      );
    if (url.endsWith("/apps")) return Promise.resolve(ok([app]));
    if (url.endsWith("/stats?env=prod"))
      return Promise.resolve(ok({ health: "up", stats: {}, deployed: false }));
    return Promise.resolve(ok({}));
  });
  return m;
}

describe("ApplicationsPage tab 化壳", () => {
  it("总览条渲染应用格；点击开 tab 显示面板；✕ 关闭回空态", async () => {
    install();
    render(<ApplicationsPage />);
    await waitFor(() => screen.getByTestId("overview-bar"));
    fireEvent.click(screen.getByText("冒烟应用"));
    await waitFor(() => screen.getByTestId("env-card-test")); // 面板渲染（test 环境卡）
    fireEvent.click(screen.getByTitle("关闭 tab"));
    await waitFor(() => expect(screen.queryByTestId("env-card-test")).toBeNull());
  });
});
```

注：`getByTitle("关闭 tab")` 依赖 AppTabPanel 页签行的 ✕ 按钮 `title="关闭 tab"`——T9 壳页签行按钮需带此属性（若实现用 aria-label，测试同步即可，二选一在实现时对齐）。

- [ ] **Step 3: 行数预算核对 + 全套验证**

Run: `wc -l app/applications/page.tsx app/applications/_lib/*.ts app/applications/_components/*.tsx | sort -n`
Expected: page.tsx ≤450；app-tab-panel/detail-panels/use-app-actions ≤400；env-deploy-card ≤300；overview-bar ≤150；import-wizard/sections ~360/~350（平移件允许 ±10%，超出则再拆）
Run: `npx tsc --noEmit && npx vitest run && npm run lint && npm run build`
Expected: 0 错误；全部测试绿（含新增壳冒烟）；lint 0 新增 warning；build 通过

- [ ] **Step 4: Commit**

```bash
git add app/applications
git commit -m "feat(applications): 页面壳化——总览条+应用tab工作区+环境并排卡上线"
```

---

### Task 10: 收尾验证（.28 部署前置自检 + 手测清单）

**Files:** 无新文件；产出核验记录。

- [ ] **Step 1: 全量回归**

Run: `cd platform/frontend && npx tsc --noEmit && npx vitest run && npm run lint && npm run build`
Expected: 全绿（测试数以输出为准记录进 commit message 或 PR 描述）

- [ ] **Step 2: git 历史整理核对**

Run: `git log --oneline main..HEAD && git diff -M --stat main..HEAD`
Expected: 9 个左右 commit（T1-T9）；diff stat 显示 page.tsx 2369→≤450，新文件符合行数预算；`-M` 下平移文件被识别为 rename/copy 为主

- [ ] **Step 3: 浏览器手测清单（本地 `npm run dev` 或直接交 .28 部署后走）**

以下每条对应 spec §5 验收，逐条过：

1. 总览条：每应用一格（状态色点 T/P、版本、external 无环境点）；点击=开 tab；重复点击已开 tab=仅激活不重复；
2. tab：多 tab 并存、切换、✕ 关闭（关激活 tab 时激活转移相邻）；
3. 环境并排卡：test 卡（状态徽章/vN/URL/构建部署按钮/日志）、prod 卡（上线按钮/健康/待上线变更摘要）；实例 failed 时本卡出现「🔧用固定引擎重试」；
4. 时间线：卡内 `<details>` 折叠、按 env 过滤（test 卡只见 env=test 行）、在途行「部署中…」黄字、ⓘ 悬浮 error_summary；
5. Detail 常驻：开 tab 自动拉取；失败 toast+重试按钮；审批/发布/合并/登记变更/部署此版本操作全通；
6. external 应用：探活卡（无环境卡）；导入中应用：spinner+进度、按钮禁用；
7. 既有回归：导入向导三步全流程、DevWizard 进度条、变量增删、产物构建下载、依赖编辑保存、网络模式切换、注册表单（managed/external）、构建完成 toast；
8. `/ops/deploy-stats` 与其他页面不受影响（仅本页改版）。

- [ ] **Step 4: Commit（如有微调）+ 汇报**

```bash
git add -A && git commit -m "chore(applications): 改版收尾微调（手测清单回归）"
```

向用户汇报：分支 `feat/app-deploy-tabs` 就绪、行数预算达标表、测试结果、待 .28 部署 + 浏览器 e2e。

---

## Self-Review 记录

1. **Spec 覆盖**：§2 总览条→T2+T6；tab 开合/激活渲染→T1+T8+T9；环境并排卡+吸附操作+env 过滤时间线→T6；external 探活→T8；§3 文件结构+行数预算→文件结构总表+T9 Step3 核对；§4 数据流（零新增请求/仅激活 tab 刷新/失败重试）→T5+T8；§5 测试验收（vitest/tsc/build/lint/diff 等价/e2e 清单）→各任务 Step+T10。拓扑两处偏差已在 Global Constraints 记录并移出范围。
2. **占位符扫描**：T6/T8 含「平移目标+源行号」式指令（非 TBD——源码在 page.tsx 指定行号处完整存在，implementer 剪切即得）；T7 registerChange 三参修订已显式写明回改 Task 5；无 TODO/TBD 残留。
3. **类型一致性**：`TabState`/`openTab` 等（T1 定义，T9 消费一致）；`OverviewCell`/`buildOverviewCells`（T2→T6→T9）；`historyForEnv`（T1 定义，T6 消费）；hooks 返回类型经 `ReturnType<typeof useAppData/useAppActions>` 贯通（T5→T8）；`registerChange` 三参签名在 T7 显式修订并要求回改 T5 文件（执行顺序 T5→T7 无冲突）。
