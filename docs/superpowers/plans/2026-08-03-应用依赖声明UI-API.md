# 应用依赖声明 UI/API 实现计划（P6 deps-ui）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 mwsupply 补 HTTP API + 前端「依赖」section，让开发者不手编 `.anp/deps.yaml` 就能声明中间件依赖；声明存平台 DB（`appdeploy_service_binding` 表），下次部署生效。

**Architecture:** 复用 `appdeploy_service_binding`（已具 `UNIQUE(app_id,service_kind)`+`declared` 态）做声明载体——**零新表/零新列/零迁移**。MWReconciler 读源从文件换 DB binding；PutDeps diff（removed/changed→释放资源，added→写 declared）；导入种子（不覆盖 UI 声明）。DTO 定义在 appdeploy（避免 appdeploy↔mwsupply 循环依赖），MWReconciler 接口扩展。

**Tech Stack:** Go（gin / sqlx / pgx），PostgreSQL（anp_test 库），Next.js App Router（TypeScript）。

## Global Constraints

- **PG only，禁 SQLite**（memory `no-sqlite-pg-only`）。单测连 `anp_test` PG（`testutil.TestDB`），全量回归 `go test -p 1` 串行（memory `go-test-serial-p1`，避免污染 anp_test）。
- **包依赖方向**：`mwsupply` import `appdeploy`（EnvWriter/Store）；`appdeploy` **不 import** `mwsupply`（接口在 appdeploy 定义、mwsupply 实现，经 main.go `SetMwReconciler` 注入）。任何 mwsupply→appdeploy 类型引用 OK；反向禁止。
- **零迁移、零新表、零新列、零新依赖**。
- 本机 GOPATH 可能被污染：跑 go 命令前缀 `GOPATH=C:/Users/yxt/go`（memory `gopath-pollution-windows`）。
- commit 消息尾加 `Co-Authored-By: Claude <noreply@anthropic.com>`；本仓库直接提交 main（项目惯例）。
- 删除 app 子表必带 `app_id ... ON DELETE CASCADE`（memory `appdeploy-env-no-cascade`）——本计划复用既有 binding 表已满足，无需新表。

## 文件结构

| 文件                                          | 责任                                                                                                                                                                                                                                       |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `internal/mwsupply/store.go`                  | +`DeclareIfAbsent`（种子，ON CONFLICT DO NOTHING）、+`DeclareBinding`（upsert declared，重置结果字段）                                                                                                                                     |
| `internal/mwsupply/supply.go`                 | `Reconcile` 改读 binding（去 repoDir）；抽 `supplyAll(deps)`；+`SeedFromManifest`；+`ReleaseDep`；+`injectedEnvKeys`；+`ListDeps`/`SetDeps`/`DepsCatalog`（接口实现）                                                                      |
| `internal/mwsupply/connstr.go`                | `EnvKeyFor` 已有；本计划引用，不改                                                                                                                                                                                                         |
| `internal/appdeploy/deps.go`（新建）          | DTO：`DepDeclaration` / `DepsCatalog` / `StrategyOption` / `CatalogInstance`                                                                                                                                                               |
| `internal/appdeploy/handler.go`               | MWReconciler 接口扩展（`Reconcile` 去 repoDir；+`ListDeps`/`SetDeps`/`DepsCatalog`/`SeedFromManifest`）；+`GetDeps`/`PutDeps`/`GetDepsCatalog` handler + 3 路由；deploy 调用点去 repoDir；`runImport`/`runImportZip` 调 `SeedFromManifest` |
| `internal/appdeploy/handler_http_test.go`     | +fake MWReconciler；GetDeps/PutDeps/catalog/校验 400/导入种子接线测试                                                                                                                                                                      |
| `internal/mwsupply/supply_test.go`            | 既有 Reconcile 测试迁 `supplyAll`；+Reconcile 读 binding / SeedFromManifest / ReleaseDep / SetDeps 测试                                                                                                                                    |
| `internal/mwsupply/store_test.go`             | +DeclareIfAbsent / DeclareBinding 测试                                                                                                                                                                                                     |
| `platform/frontend/app/applications/page.tsx` | 详情面板 +「依赖」section                                                                                                                                                                                                                  |
| `cmd/server/main.go`                          | 仅当接口签名变更致编译错时同步（Reconciler 装配不变，预期无需改；任务中验证）                                                                                                                                                              |

---

## Task 1: mwsupply store — DeclareIfAbsent + DeclareBinding

声明写入层：种子（不覆盖）+ 显式声明（upsert declared，重置结果字段）。

**Files:**

- Modify: `internal/mwsupply/store.go`（末尾追加）
- Test: `internal/mwsupply/store_test.go`

**Interfaces:**

- Produces: `(*Store).DeclareIfAbsent(ctx, appID, psID, kind, strategy) error`、`(*Store).DeclareBinding(ctx, appID, psID, kind, strategy) error`

- [ ] **Step 1: 写失败测试**（追加到 `store_test.go`）

```go
// TestStore_DeclareIfAbsent_absent 无 binding → 种 declared；已有 → 不覆盖。
func TestStore_DeclareIfAbsent(t *testing.T) {
	s, db := newTestStore(t)
	ctx := context.Background()
	app := mkAppRow(t, db, "dia_a", "ps_1")

	if err := s.DeclareIfAbsent(ctx, app, "ps_1", "redis", ModeShared); err != nil {
		t.Fatalf("DeclareIfAbsent: %v", err)
	}
	b, _ := s.GetBinding(ctx, app, "redis")
	if b == nil || b.Status != StatusDeclared || b.Strategy != ModeShared || b.EnvKey != "REDIS_ADDR" {
		t.Fatalf("应种 declared/shared/REDIS_ADDR，得 %+v", b)
	}
	// 已存在（用户 UI 改成 dedicated+bound）→ DeclareIfAbsent 不覆盖
	if err := s.UpsertBinding(ctx, &ServiceBinding{AppID: app, ProjectSpaceID: "ps_1",
		ServiceKind: "redis", Strategy: ModeDedicated, EnvKey: "REDIS_ADDR", Status: StatusBound}); err != nil {
		t.Fatalf("upsert bound: %v", err)
	}
	if err := s.DeclareIfAbsent(ctx, app, "ps_1", "redis", ModeShared); err != nil {
		t.Fatalf("DeclareIfAbsent 二次: %v", err)
	}
	b2, _ := s.GetBinding(ctx, app, "redis")
	if b2.Strategy != ModeDedicated || b2.Status != StatusBound {
		t.Fatalf("DeclareIfAbsent 不应覆盖已存在声明，得 %+v", b2)
	}
}

// TestStore_DeclareBinding_upsertDeclared upsert declared，重置 instance/token；幂等。
func TestStore_DeclareBinding(t *testing.T) {
	s, db := newTestStore(t)
	ctx := context.Background()
	app := mkAppRow(t, db, "dcb_a", "ps_1")

	if err := s.DeclareBinding(ctx, app, "ps_1", "milvus", ModeShared); err != nil {
		t.Fatalf("DeclareBinding: %v", err)
	}
	b, _ := s.GetBinding(ctx, app, "milvus")
	if b == nil || b.Status != StatusDeclared || b.Strategy != ModeShared ||
		b.EnvKey != "MILVUS_ADDR" || b.ServiceInstanceID != "" || b.IsolationToken != "" {
		t.Fatalf("应 declared/shared/MILVUS_ADDR + 空结果字段，得 %+v", b)
	}
	// 模拟已供给（bound + instance + token）后重新声明 → 重置回 declared、清结果
	if err := s.UpsertBinding(ctx, &ServiceBinding{AppID: app, ProjectSpaceID: "ps_1",
		ServiceKind: "milvus", Strategy: ModeShared, ServiceInstanceID: "svinst-milvus-shared-28",
		IsolationToken: "appabc123def_", EnvKey: "MILVUS_ADDR", Status: StatusBound}); err != nil {
		t.Fatalf("upsert bound: %v", err)
	}
	if err := s.DeclareBinding(ctx, app, "ps_1", "milvus", ModeDedicated); err != nil {
		t.Fatalf("DeclareBinding 改策略: %v", err)
	}
	b2, _ := s.GetBinding(ctx, app, "milvus")
	if b2.Status != StatusDeclared || b2.Strategy != ModeDedicated ||
		b2.ServiceInstanceID != "" || b2.IsolationToken != "" {
		t.Fatalf("重新声明应 declared+新策略+清结果，得 %+v", b2)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run 'TestStore_DeclareIfAbsent|TestStore_DeclareBinding' -v`
Expected: FAIL（`s.DeclareIfAbsent undefined`）

- [ ] **Step 3: 实现**（追加到 `store.go`）

```go
// DeclareIfAbsent 种声明（仅当该 (app,kind) 无 binding 时）；ON CONFLICT DO NOTHING。
// 导入种子用：不覆盖用户已 UI 设的声明。
func (s *Store) DeclareIfAbsent(ctx context.Context, appID, psID, kind, strategy string) error {
	if strategy == "" {
		strategy = ModeBindExisting
	}
	_, err := s.db.ExecContext(ctx,
		`INSERT INTO appdeploy_service_binding (id, app_id, project_space_id, service_kind, strategy, env_key, status)
		 VALUES ($1,$2,$3,$4,$5,$6,$7)
		 ON CONFLICT (app_id, service_kind) DO NOTHING`,
		"svb_"+uuid.NewString()[:20], appID, psID, kind, strategy, EnvKeyFor(kind), StatusDeclared)
	return err
}

// DeclareBinding upsert 声明（status=declared，重置 instance/token/env_key）。
// PutDeps 的 added/changed 用：结果字段清空，待下次部署供给。
func (s *Store) DeclareBinding(ctx context.Context, appID, psID, kind, strategy string) error {
	if strategy == "" {
		strategy = ModeBindExisting
	}
	return s.UpsertBinding(ctx, &ServiceBinding{
		AppID: appID, ProjectSpaceID: psID, ServiceKind: kind,
		Strategy: strategy, EnvKey: EnvKeyFor(kind), Status: StatusDeclared,
	})
}
```

> 注：`UpsertBinding` 的 `ON CONFLICT(app_id,service_kind) DO UPDATE` 会把 `service_instance_id`/`isolation_token` 设为 excluded（空→`NULLIF('','')`=NULL），即重置结果字段——正是 DeclareBinding 所需。

- [ ] **Step 4: 跑测试验证通过**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run 'TestStore_DeclareIfAbsent|TestStore_DeclareBinding' -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add internal/mwsupply/store.go internal/mwsupply/store_test.go
git commit -m "feat(mwsupply): store.DeclareIfAbsent/DeclareBinding 声明写入层 (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: mwsupply — injectedEnvKeys + EnvWriter.DeleteEnv + ReleaseDep

声明移除/变更时释放单依赖资源：dedicated docker rm + 删 instance + 删 platform env 键 + 删 binding。

**Files:**

- Modify: `internal/mwsupply/supply.go`（EnvWriter 接口 +DeleteEnv；+`injectedEnvKeys`；+`ReleaseDep`）
- Test: `internal/mwsupply/supply_test.go`

**Interfaces:**

- Consumes: Task 1 的 `DeleteBinding`/`DeleteInstance`（已有）；`r.env.DeleteEnv`（appdeploy.Store 已有，本任务加到 EnvWriter 接口）
- Produces: `(*Reconciler).ReleaseDep(ctx, b *ServiceBinding)`（供 Task 6 SetDeps 用）

- [ ] **Step 1: 写失败测试**（追加到 `supply_test.go`）

```go
// TestReleaseDep_dedicatedRedis bound dedicated redis → RmForce + 删 instance + 删 env(3 键) + 删 binding。
// 直接种一条 bound dedicated binding + instance（不依赖供给流程，故本任务可独立编译）。
func TestReleaseDep_dedicatedRedis(t *testing.T) {
	r, appStore, db, _, dk := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "relrd", RepoDir: "/x", InternalPort: 8080}
	_ = appStore.Create(ctx, a)
	inst := &ServiceInstance{ID: "svinst-redis-ded-rel", Kind: "redis", Name: "mwredis-rel",
		SupplyMode: ModeDedicated, Host: "testdeploy", Port: 9600, AuthRef: "p",
		ContainerName: "mwredis-rel", Status: "active"}
	_ = NewStore(db).CreateInstance(ctx, inst)
	_ = NewStore(db).UpsertBinding(ctx, &ServiceBinding{AppID: a.ID, ProjectSpaceID: "ps_1",
		ServiceKind: "redis", Strategy: ModeDedicated, ServiceInstanceID: inst.ID,
		EnvKey: "REDIS_ADDR", Status: StatusBound})
	_ = appStore.UpsertEnv(ctx, a.ID, "REDIS_ADDR", "testdeploy:9600", false, "platform")
	_ = appStore.UpsertEnv(ctx, a.ID, "REDIS_PASSWORD", "p", true, "platform")
	binds, _ := NewStore(db).ListBindingsByApp(ctx, a.ID)
	b := binds[0]
	dk.rmCalls = nil

	r.ReleaseDep(ctx, &b)
	if len(dk.rmCalls) != 1 || dk.rmCalls[0] != "mwredis-rel" {
		t.Fatalf("应 RmForce mwredis-rel，得 %v", dk.rmCalls)
	}
	if got, _ := NewStore(db).GetInstance(ctx, inst.ID); got != nil {
		t.Fatalf("instance 行应删，得 %+v", got)
	}
	for _, k := range []string{"REDIS_ADDR", "REDIS_DB", "REDIS_PASSWORD"} {
		if v, _ := appStore.GetEnvValue(ctx, a.ID, k); v != "" {
			t.Fatalf("env %q 应删，得 %q", k, v)
		}
	}
	if got, _ := NewStore(db).GetBinding(ctx, a.ID, "redis"); got != nil {
		t.Fatalf("binding 行应删，得 %+v", got)
	}
}

// TestReleaseDep_shared 只删 env + binding（无 docker rm）；token 随行释放。
func TestReleaseDep_shared(t *testing.T) {
	r, appStore, db, _, dk := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "relsh", RepoDir: "/x", InternalPort: 8080}
	_ = appStore.Create(ctx, a)
	_ = NewStore(db).ClaimSharedToken(ctx, a.ID, "ps_1", "redis", "svinst-redis-shared-28", "7", "REDIS_ADDR")
	_ = appStore.UpsertEnv(ctx, a.ID, "REDIS_ADDR", "10.10.0.28:6381", false, "platform")
	_ = appStore.UpsertEnv(ctx, a.ID, "REDIS_DB", "7", false, "platform")
	binds, _ := NewStore(db).ListBindingsByApp(ctx, a.ID)
	b := binds[0]
	dk.rmCalls = nil

	r.ReleaseDep(ctx, &b)
	if len(dk.rmCalls) != 0 {
		t.Fatalf("shared 不应 RmForce，得 %v", dk.rmCalls)
	}
	if v, _ := appStore.GetEnvValue(ctx, a.ID, "REDIS_ADDR"); v != "" {
		t.Fatalf("REDIS_ADDR 应删，得 %q", v)
	}
	if got, _ := NewStore(db).GetBinding(ctx, a.ID, "redis"); got != nil {
		t.Fatalf("binding 应删，得 %+v", got)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run TestReleaseDep -v`
Expected: FAIL（`r.ReleaseDep undefined`，且 EnvWriter 缺 DeleteEnv）

- [ ] **Step 3: 实现 EnvWriter +DeleteEnv**（改 `supply.go` 顶部 EnvWriter 接口）

```go
// EnvWriter 写/删应用 env（由 appdeploy.Store 实现，避免 mwsupply→appdeploy 循环依赖）。
type EnvWriter interface {
	UpsertEnv(ctx context.Context, appID, key, value string, isSecret bool, source string) error
	DeleteEnv(ctx context.Context, appID, key string) error // P6：声明移除时删注入的 platform env
}
```

- [ ] **Step 4: 实现 injectedEnvKeys + ReleaseDep**（追加到 `supply.go`，紧邻 `Cleanup` 之后）

```go
// injectedEnvKeys 某 kind 供给时注入的全部 env 键（ReleaseDep 删除用）。
func injectedEnvKeys(kind string) []string {
	switch kind {
	case "redis":
		return []string{"REDIS_ADDR", "REDIS_DB", "REDIS_PASSWORD"}
	case "milvus":
		return []string{"MILVUS_ADDR", "MILVUS_COLLECTION_PREFIX"}
	default:
		return []string{strings.ToUpper(kind) + "_ADDR"}
	}
}

// ReleaseDep 释放单个依赖的资源（声明移除/变更时；best-effort，不报错）：
// dedicated → docker rm 容器/栈 + 删 instance 行；三类都删注入的 platform env 键 + 删 binding 行。
func (r *Reconciler) ReleaseDep(ctx context.Context, b *ServiceBinding) {
	// dedicated：docker rm + 删 instance（复用 Cleanup 的 per-binding rm 逻辑）
	if b.Strategy == ModeDedicated && b.ServiceInstanceID != "" {
		if inst, ie := r.store.GetInstance(ctx, b.ServiceInstanceID); ie == nil && inst != nil && inst.ContainerName != "" {
			switch inst.Kind {
			case "milvus":
				if err := r.docker.RmMilvusStack(ctx, inst.ContainerName); err != nil && r.log != nil {
					r.log.Warn("ReleaseDep milvus 栈清理失败 (best-effort)", zap.String("app", b.AppID), zap.Error(err))
				}
			default:
				if err := r.docker.RmForce(ctx, inst.ContainerName); err != nil && r.log != nil {
					r.log.Warn("ReleaseDep 容器清理失败 (best-effort)", zap.String("app", b.AppID), zap.Error(err))
				}
			}
			_ = r.store.DeleteInstance(ctx, inst.ID)
		}
	}
	// 删注入的 platform env 键
	for _, key := range injectedEnvKeys(b.ServiceKind) {
		_ = r.env.DeleteEnv(ctx, b.AppID, key)
	}
	// 删 binding 行（dedicated 已删 instance，无 FK RESTRICT 阻碍；shared/bind_existing 直接删）
	_ = r.store.DeleteBinding(ctx, b.ID)
}
```

- [ ] **Step 5: 跑测试验证通过**（须 Task 3 已合并，否则按 Step 1 注释改前置）

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run TestReleaseDep -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add internal/mwsupply/supply.go internal/mwsupply/supply_test.go
git commit -m "feat(mwsupply): ReleaseDep 释放单依赖资源 + EnvWriter.DeleteEnv (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: mwsupply — 抽 supplyAll + 迁移既有 Reconcile 测试（纯重构）

把 Reconcile 的「按声明循环供给」抽成 `supplyAll(ctx,appID,psID,deps)`，把 25 个既有 Reconcile 测试从「写文件 + Reconcile」改成直调 `supplyAll`。**零行为变更**：供给逻辑、env 注入、binding 状态、flush/docker 调用全部不变。Reconcile 本身暂保留原签名（读文件→supplyAll），下一任务才换读源。目的是把「读源切换」与「测试迁移」解耦，先保 P1-P5 护栏全绿。

**Files:**

- Modify: `internal/mwsupply/supply.go`（抽 `supplyAll`；`Reconcile` 改调它）
- Modify: `internal/mwsupply/supply_test.go`（25 处机械替换）

**Interfaces:**

- Produces: `(*Reconciler).supplyAll(ctx, appID, psID string, deps []DepService)`（unexported，同包测试用）

- [ ] **Step 1: 抽 supplyAll**（改 `supply.go` 的 `Reconcile`）

把现有：

```go
func (r *Reconciler) Reconcile(ctx context.Context, appID, psID, repoDir string) error {
	m, err := LoadDepsManifest(repoDir)
	if err != nil || m == nil {
		return nil
	}
	for _, dep := range m.Services {
		if dep.Kind == "" {
			continue
		}
		r.supplyOne(ctx, appID, psID, dep)
	}
	return nil
}
```

改为：

```go
// Reconcile 读 repoDir 的 .anp/deps.yaml → supplyAll（读源在 Task 4 换 DB）。
func (r *Reconciler) Reconcile(ctx context.Context, appID, psID, repoDir string) error {
	m, err := LoadDepsManifest(repoDir)
	if err != nil || m == nil {
		return nil
	}
	r.supplyAll(ctx, appID, psID, m.Services)
	return nil
}

// supplyAll 按声明列表逐个供给（best-effort，幂等）。供给逻辑核心，被 Reconcile 与测试共用。
func (r *Reconciler) supplyAll(ctx context.Context, appID, psID string, deps []DepService) {
	for _, dep := range deps {
		if dep.Kind == "" {
			continue
		}
		r.supplyOne(ctx, appID, psID, dep)
	}
}
```

- [ ] **Step 2: 机械替换 25 处测试调用**

替换规则（每处 2 行 → 1 行）：

before:

```go
dir := writeManifest(t, "services:\n  - kind: <K>\n    strategy: <S>\n")
_ = r.Reconcile(ctx, <appID>, "<ps>", dir)
```

after:

```go
_ = r.supplyAll(ctx, <appID>, "<ps>", []DepService{{Kind: "<K>", Strategy: "<S>"}})
```

bind_existing（无 strategy 行）before:

```go
dir := writeManifest(t, "services:\n  - kind: <K>\n")
_ = r.Reconcile(ctx, <appID>, "<ps>", dir)
```

after（strategy 留空，supplyOne 内默认 bind_existing）:

```go
_ = r.supplyAll(ctx, <appID>, "<ps>", []DepService{{Kind: "<K>"}})
```

多 kind 用多元素切片，例：`[]DepService{{Kind: "redis"}, {Kind: "milvus"}}`。

需替换的测试函数清单（`supply_test.go`）：

- `TestReconcile_bindExisting`（redis+milvus 两 kind，注意此处 `_ = r.Reconcile(ctx, a.ID, "ps_1", dir)` + 前面 `dir := writeManifest(t, "services:\n  - kind: redis\n  - kind: milvus\n")` → `_ = r.supplyAll(ctx, a.ID, "ps_1", []DepService{{Kind: "redis"}, {Kind: "milvus"}})`，删 dir 行）
- `TestReconcile_idempotent`（二次调用也要改；保留两次 supplyAll 调用验幂等）
- `TestReconcile_missingInstanceKind`
- `TestReconcile_noManifest`（无清单→空：改为 `_ = r.supplyAll(ctx, a.ID, "ps_1", nil)`，删 `t.TempDir()`；断言 0 binding 不变）
- `TestReconcile_sharedRedis`（内联 `mk` 闭包里 `_ = r.Reconcile(ctx, a.ID, "ps_1", dir)` → `_ = r.supplyAll(ctx, a.ID, "ps_1", []DepService{{Kind: "redis", Strategy: "shared"}})`；删外部 `dir :=`）
- `TestReconcile_shared_idempotent`、`TestReconcile_shared_flushFailBestEffort`、`TestReconcile_shared_poolExhaust`
- `TestReconcile_dedicatedRedis`、`TestReconcile_dedicated_idempotent`、`TestReconcile_dedicated_poolExhaust`、`TestReconcile_dedicated_runFail`、`TestReconcile_dedicated_readyFail_bestEffort`
- `TestReconcile_cleanup_dedicated`、`TestReconcile_cleanup_skipsSharedAndBindExisting`（这两个前置供给改 supplyAll；`r.Cleanup` 调用不变）
- `TestReconcile_dedicatedMilvus`、`TestReconcile_dedicatedMilvus_idempotent`、`TestReconcile_dedicatedMilvus_poolExhaust`、`TestReconcile_dedicatedMilvus_stackFail`、`TestReconcile_dedicatedMilvus_readyTimeout_bestEffort`、`TestReconcile_cleanup_dedicatedMilvus`
- `TestReconcile_sharedMilvus`、`TestReconcile_sharedMilvus_idempotent`、`TestReconcile_sharedMilvus_noInstance`

> `writeManifest` 助手保留（Task 4 SeedFromManifest 测试仍用）。替换后这些测试不再调 `r.Reconcile`，故 Task 4 改 Reconcile 签名时它们不受影响。

- [ ] **Step 3: 跑全部 mwsupply 测试验证零回归**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -v`
Expected: 全部 PASS（除 Task 1/2 新增的尚待实现项若已合入也 PASS；ReleaseDep 测试若未到 Task 2 实现可暂跳过 `-run 'TestReleaseDep'`）

- [ ] **Step 4: 提交**

```bash
git add internal/mwsupply/supply.go internal/mwsupply/supply_test.go
git commit -m "refactor(mwsupply): 抽 supplyAll + 既有 Reconcile 测试迁移 (P6 deps-ui 零行为变更)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: mwsupply — Reconcile 读 binding + 去 repoDir + SeedFromManifest

读源切换（文件→DB）+ 导入种子。Reconcile 改读 `ListBindingsByApp`；去 repoDir（接口/方法/调用点）；新增 `SeedFromManifest`。

**Files:**

- Modify: `internal/mwsupply/supply.go`
- Modify: `internal/appdeploy/handler.go`（MWReconciler 接口 `Reconcile` 去 repoDir + 新增 `SeedFromManifest`；deploy 调用点 :1631 去 buildDir；+`SetMwReconciler` 不变）
- Test: `internal/mwsupply/supply_test.go`（+Reconcile 读 binding、+SeedFromManifest）

**Interfaces:**

- Consumes: Task 1 `DeclareIfAbsent`；Task 3 `supplyAll`；`ListBindingsByApp`（已有）
- Produces: `(*Reconciler).Reconcile(ctx, appID, psID)`（3 参）、`(*Reconciler).SeedFromManifest(ctx, appID, psID, repoDir) error`

- [ ] **Step 1: 写失败测试**（追加到 `supply_test.go`）

```go
// TestReconcile_readsBindings Reconcile 从 DB binding 声明供给（不再读文件）。
func TestReconcile_readsBindings(t *testing.T) {
	r, appStore, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "rbapp", RepoDir: "/x", InternalPort: 8080}
	_ = appStore.Create(ctx, a)
	// 不写文件；直接种一条 declared binding
	_ = NewStore(db).DeclareBinding(ctx, a.ID, "ps_1", "redis", ModeShared)
	if err := r.Reconcile(ctx, a.ID, "ps_1"); err != nil {
		t.Fatalf("reconcile: %v", err)
	}
	ra, _ := appStore.GetEnvValue(ctx, a.ID, "REDIS_ADDR")
	if ra != "10.10.0.28:6381" {
		t.Fatalf("REDIS_ADDR 应 10.10.0.28:6381，得 %q", ra)
	}
	binds, _ := NewStore(db).ListBindingsByApp(ctx, a.ID)
	if len(binds) != 1 || binds[0].Status != StatusBound {
		t.Fatalf("应 1 binding bound，得 %+v", binds)
	}
}

// TestSeedFromManifest 有 deps.yaml → 种 declared；已声明 → 不覆盖。
func TestSeedFromManifest(t *testing.T) {
	r, _, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "seedapp", RepoDir: "/x", InternalPort: 8080}
	_ = appdeploy.NewStore(db).Create(ctx, a)
	dir := writeManifest(t, "services:\n  - kind: redis\n    strategy: shared\n")

	if err := r.SeedFromManifest(ctx, a.ID, "ps_1", dir); err != nil {
		t.Fatalf("SeedFromManifest: %v", err)
	}
	b, _ := NewStore(db).GetBinding(ctx, a.ID, "redis")
	if b == nil || b.Status != StatusDeclared || b.Strategy != ModeShared {
		t.Fatalf("应种 declared/shared，得 %+v", b)
	}
	// 用户改 dedicated 后再 seed → 不覆盖
	_ = NewStore(db).DeclareBinding(ctx, a.ID, "ps_1", "redis", ModeDedicated)
	if err := r.SeedFromManifest(ctx, a.ID, "ps_1", dir); err != nil {
		t.Fatalf("SeedFromManifest 二次: %v", err)
	}
	b2, _ := NewStore(db).GetBinding(ctx, a.ID, "redis")
	if b2.Strategy != ModeDedicated {
		t.Fatalf("SeedFromManifest 不应覆盖已声明，得 %+v", b2)
	}
}

// TestSeedFromManifest_noFile 无 deps.yaml → 不报错、不种。
func TestSeedFromManifest_noFile(t *testing.T) {
	r, _, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "seednone", RepoDir: "/x", InternalPort: 8080}
	_ = appdeploy.NewStore(db).Create(ctx, a)
	if err := r.SeedFromManifest(ctx, a.ID, "ps_1", t.TempDir()); err != nil {
		t.Fatalf("无文件不应报错: %v", err)
	}
	binds, _ := NewStore(db).ListBindingsByApp(ctx, a.ID)
	if len(binds) != 0 {
		t.Fatalf("无文件应 0 binding，得 %d", len(binds))
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run 'TestReconcile_readsBindings|TestSeedFromManifest' -v`
Expected: FAIL（编译错：Reconcile 仍是 4 参；SeedFromManifest undefined；接口不匹配）

- [ ] **Step 3: 改 supply.go —— Reconcile 读 binding + SeedFromManifest**

把 Task 3 的 `Reconcile`（读文件）替换为：

```go
// Reconcile 读该 app 的 binding 声明（DB）→ 逐个供给 → 写 env + binding。
// 幂等（binding 已 bound 且同实例则复用 token 不 flush）。总不返回错（best-effort，不阻塞部署）。
func (r *Reconciler) Reconcile(ctx context.Context, appID, psID string) error {
	binds, err := r.store.ListBindingsByApp(ctx, appID)
	if err != nil {
		return nil // best-effort：读声明失败不阻塞部署
	}
	deps := make([]DepService, 0, len(binds))
	for _, b := range binds {
		if b.ServiceKind == "" {
			continue
		}
		deps = append(deps, DepService{Kind: b.ServiceKind, Strategy: b.Strategy})
	}
	r.supplyAll(ctx, appID, psID, deps)
	return nil
}

// SeedFromManifest 读 repoDir/.anp/deps.yaml → 对每个声明种 declared binding（不覆盖已存在）。
// 导入时调：opencode 适配/手编的 .anp/deps.yaml 由此进入平台 DB。best-effort，无文件=空清单不报错。
func (r *Reconciler) SeedFromManifest(ctx context.Context, appID, psID, repoDir string) error {
	m, err := LoadDepsManifest(repoDir)
	if err != nil || m == nil {
		return nil
	}
	for _, dep := range m.Services {
		if dep.Kind == "" {
			continue
		}
		_ = r.store.DeclareIfAbsent(ctx, appID, psID, dep.Kind, dep.Strategy)
	}
	return nil
}
```

- [ ] **Step 4: 改 appdeploy/handler.go —— 接口 Reconcile 去 repoDir + 加 SeedFromManifest**

把：

```go
type MWReconciler interface {
	Reconcile(ctx context.Context, appID, psID, repoDir string) error
	Cleanup(ctx context.Context, appID string) error
}
```

改为：

```go
type MWReconciler interface {
	Reconcile(ctx context.Context, appID, psID string) error
	Cleanup(ctx context.Context, appID string) error
	SeedFromManifest(ctx context.Context, appID, psID, repoDir string) error
}
```

**同步更新既有 `fakeMWReconciler`**（`handler_http_test.go:422`，否则 appdeploy 测试包编译崩——接口变了 fake 必须跟上）：

```go
type fakeMWReconciler struct{}

func (f *fakeMWReconciler) Reconcile(_ context.Context, _, _ string) error { return nil }
func (f *fakeMWReconciler) Cleanup(_ context.Context, _ string) error       { return nil }
func (f *fakeMWReconciler) SeedFromManifest(_ context.Context, _, _, _ string) error { return nil }
```

- [ ] **Step 5: 改 deploy 调用点（handler.go :1631 附近）**

把：

```go
_ = h.mwReconciler.Reconcile(ctx, a.ID, a.ProjectSpaceID, buildDir)
```

改为：

```go
_ = h.mwReconciler.Reconcile(ctx, a.ID, a.ProjectSpaceID)
```

- [ ] **Step 6: 跑测试验证通过 + 全包零回归**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ ./internal/appdeploy/ -v`
Expected: PASS（注意：appdeploy 包若有别处调 4 参 Reconcile 需一并改；grep 确认）

- [ ] **Step 7: 提交**

```bash
git add internal/mwsupply/supply.go internal/appdeploy/handler.go internal/mwsupply/supply_test.go
git commit -m "feat(mwsupply): Reconcile 读 DB binding(去 repoDir) + SeedFromManifest 导入种子 (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 5: appdeploy DTO + 接口 ListDeps/DepsCatalog + mwsupply 实现（读路径）

定义跨包 DTO（appdeploy 包，避免循环）+ 接口加只读方法 + mwsupply 实现 ListDeps（binding→DTO）/ DepsCatalog（查 active 实例）。

**Files:**

- Create: `internal/appdeploy/deps.go`
- Modify: `internal/appdeploy/handler.go`（MWReconciler 接口 +`ListDeps`/`DepsCatalog`）
- Modify: `internal/mwsupply/store.go`（+`ListActiveInstances`）
- Modify: `internal/mwsupply/supply.go`（+`ListDeps`/`DepsCatalog` 实现）
- Test: `internal/mwsupply/supply_test.go`（+ListDeps/DepsCatalog）

**Interfaces:**

- Produces: `appdeploy.DepDeclaration`、`appdeploy.DepsCatalog`、`appdeploy.StrategyOption`、`appdeploy.CatalogInstance`；`(*Reconciler).ListDeps(ctx, appID) ([]appdeploy.DepDeclaration, error)`、`(*Reconciler).DepsCatalog(ctx, psID) (appdeploy.DepsCatalog, error)`

- [ ] **Step 1: 写失败测试**（追加到 `supply_test.go`）

```go
// TestListDeps binding 行 → DepDeclaration（kind/strategy/status/instance/token）。
func TestListDeps(t *testing.T) {
	r, _, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "ldapp", RepoDir: "/x", InternalPort: 8080}
	_ = appdeploy.NewStore(db).Create(ctx, a)
	_ = NewStore(db).DeclareBinding(ctx, a.ID, "ps_1", "redis", ModeShared)
	_ = r.supplyAll(ctx, a.ID, "ps_1", []DepService{{Kind: "redis", Strategy: ModeShared}})

	decls, err := r.ListDeps(ctx, a.ID)
	if err != nil {
		t.Fatalf("ListDeps: %v", err)
	}
	if len(decls) != 1 || decls[0].Kind != "redis" || decls[0].Strategy != ModeShared ||
		decls[0].Status != StatusBound || decls[0].Instance == "" || decls[0].Token == "" {
		t.Fatalf("应 1 条 redis/shared/bound+instance+token，得 %+v", decls)
	}
}

// TestDepsCatalog 返回 kinds/strategies + ps 级与平台级 active 实例。
func TestDepsCatalog(t *testing.T) {
	r, _, _, _, _ := newReconcilerTest(t)
	cat, err := r.DepsCatalog(context.Background(), "ps_1")
	if err != nil {
		t.Fatalf("DepsCatalog: %v", err)
	}
	if len(cat.Kinds) != 2 || cat.Kinds[0] != "redis" || cat.Kinds[1] != "milvus" {
		t.Fatalf("kinds 应 [redis,milvus]，得 %v", cat.Kinds)
	}
	if len(cat.Strategies) != 3 {
		t.Fatalf("strategies 应 3 个，得 %d", len(cat.Strategies))
	}
	// 平台级 redis/milvus 种子在 instances
	kinds := map[string]bool{}
	for _, ins := range cat.Instances {
		kinds[ins.Kind] = true
	}
	if !kinds["redis"] || !kinds["milvus"] {
		t.Fatalf("instances 应含 redis+milvus，得 %v", kinds)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run 'TestListDeps|TestDepsCatalog' -v`
Expected: FAIL（appdeploy.DepDeclaration undefined；r.ListDeps undefined）

- [ ] **Step 3: 建 appdeploy/deps.go（DTO）**

```go
package appdeploy

// DepDeclaration 应用对单个中间件的依赖声明 + 供给结果（UI/API 用）。
type DepDeclaration struct {
	Kind     string `json:"kind"`               // redis / milvus
	Strategy string `json:"strategy"`           // bind_existing / shared / dedicated
	Status   string `json:"status"`             // declared / bound / failed
	Instance string `json:"instance,omitempty"` // 供给的 service_instance_id（bound 时）
	Token    string `json:"token,omitempty"`    // 隔离 token（shared：db号/前缀）
	Error    string `json:"error,omitempty"`    // failed 时的 last_error
}

// StrategyOption 策略选项（catalog 用，UI 渲染）。
type StrategyOption struct {
	Name string `json:"name"`
	Desc string `json:"desc"`
}

// CatalogInstance 可见中间件实例（catalog 用）。
type CatalogInstance struct {
	ID         string `json:"id"`
	Kind       string `json:"kind"`
	Name       string `json:"name"`
	SupplyMode string `json:"supply_mode"`
	Host       string `json:"host"`
	Port       int    `json:"port"`
}

// DepsCatalog 依赖勾选器所需选项集合。
type DepsCatalog struct {
	Kinds      []string           `json:"kinds"`
	Strategies []StrategyOption   `json:"strategies"`
	Instances  []CatalogInstance  `json:"instances"`
}
```

- [ ] **Step 4: 接口加 ListDeps/DepsCatalog（handler.go）**

```go
type MWReconciler interface {
	Reconcile(ctx context.Context, appID, psID string) error
	Cleanup(ctx context.Context, appID string) error
	SeedFromManifest(ctx context.Context, appID, psID, repoDir string) error
	ListDeps(ctx context.Context, appID string) ([]DepDeclaration, error)
	DepsCatalog(ctx context.Context, psID string) (DepsCatalog, error)
}
```

**同步给 `fakeMWReconciler` 加 stub**（handler_http_test.go，appdeploy 测试包编译）：

```go
func (f *fakeMWReconciler) ListDeps(_ context.Context, _ string) ([]DepDeclaration, error) {
	return nil, nil
}
func (f *fakeMWReconciler) DepsCatalog(_ context.Context, _ string) (DepsCatalog, error) {
	return DepsCatalog{}, nil
}
```

- [ ] **Step 5: store.go +ListActiveInstances**

```go
// ListActiveInstances 列某 ps 可见的 active 实例（项目级 + 平台级 NULL）。
func (s *Store) ListActiveInstances(ctx context.Context, psID string) ([]ServiceInstance, error) {
	var list []ServiceInstance
	err := s.db.SelectContext(ctx, &list,
		`SELECT `+instCols+` FROM appdeploy_service_instance
		 WHERE status='active' AND (project_space_id=$1 OR project_space_id IS NULL)
		 ORDER BY kind, supply_mode`, psID)
	return list, err
}
```

- [ ] **Step 6: supply.go +ListDeps/+DepsCatalog 实现**

```go
// ListDeps 该 app 的依赖声明（binding → DepDeclaration）。
func (r *Reconciler) ListDeps(ctx context.Context, appID string) ([]appdeploy.DepDeclaration, error) {
	binds, err := r.store.ListBindingsByApp(ctx, appID)
	if err != nil {
		return nil, err
	}
	out := make([]appdeploy.DepDeclaration, 0, len(binds))
	for _, b := range binds {
		out = append(out, appdeploy.DepDeclaration{
			Kind: b.ServiceKind, Strategy: b.Strategy, Status: b.Status,
			Instance: b.ServiceInstanceID, Token: b.IsolationToken, Error: b.LastError,
		})
	}
	return out, nil
}

// DepsCatalog 勾选器选项：固定 kinds/strategies + 可见 active 实例。
func (r *Reconciler) DepsCatalog(ctx context.Context, psID string) (appdeploy.DepsCatalog, error) {
	insts, err := r.store.ListActiveInstances(ctx, psID)
	if err != nil {
		return appdeploy.DepsCatalog{}, err
	}
	cis := make([]appdeploy.CatalogInstance, 0, len(insts))
	for _, ins := range insts {
		cis = append(cis, appdeploy.CatalogInstance{
			ID: ins.ID, Kind: ins.Kind, Name: ins.Name, SupplyMode: ins.SupplyMode,
			Host: ins.Host, Port: ins.Port,
		})
	}
	return appdeploy.DepsCatalog{
		Kinds: []string{"redis", "milvus"},
		Strategies: []appdeploy.StrategyOption{
			{Name: ModeBindExisting, Desc: "绑定部署机已运行的服务（最省，导入最常见）"},
			{Name: ModeShared, Desc: "ANP 共享实例 + 每 app 隔离 token（db号/前缀）"},
			{Name: ModeDedicated, Desc: "每 app 专属容器（隔离最强，资源独占）"},
		},
		Instances: cis,
	}, nil
}
```

> `supply.go` 已 import `appdeploy`（EnvWriter），新增引用 `appdeploy.DepDeclaration` 等无需新 import。

- [ ] **Step 7: 跑测试验证通过**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run 'TestListDeps|TestDepsCatalog' -v`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add internal/appdeploy/deps.go internal/appdeploy/handler.go internal/mwsupply/store.go internal/mwsupply/supply.go internal/mwsupply/supply_test.go
git commit -m "feat(mwsupply): ListDeps/DepsCatalog 读路径 + appdeploy DTO (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 6: mwsupply SetDeps（diff + 释放 + 声明）+ 接口 SetDeps

PutDeps 的核心：diff 现 binding vs 新声明，removed/changed→ReleaseDep，added/changed→DeclareBinding，unchanged→不动。

**Files:**

- Modify: `internal/appdeploy/handler.go`（接口 +`SetDeps`）
- Modify: `internal/mwsupply/supply.go`（+`SetDeps`）
- Test: `internal/mwsupply/supply_test.go`（+SetDeps 各分支）

**Interfaces:**

- Consumes: Task 1 `DeclareBinding`；Task 2 `ReleaseDep`
- Produces: `(*Reconciler).SetDeps(ctx, appID, psID string, decls []appdeploy.DepDeclaration) error`

- [ ] **Step 1: 写失败测试**（追加到 `supply_test.go`）

```go
// TestSetDeps_added 新增声明 → declared binding（不立即供给）。
func TestSetDeps_added(t *testing.T) {
	r, _, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "sdadd", RepoDir: "/x", InternalPort: 8080}
	_ = appdeploy.NewStore(db).Create(ctx, a)
	err := r.SetDeps(ctx, a.ID, "ps_1", []appdeploy.DepDeclaration{
		{Kind: "redis", Strategy: ModeShared},
	})
	if err != nil {
		t.Fatalf("SetDeps: %v", err)
	}
	b, _ := NewStore(db).GetBinding(ctx, a.ID, "redis")
	if b == nil || b.Status != StatusDeclared || b.Strategy != ModeShared {
		t.Fatalf("应 declared/shared，得 %+v", b)
	}
}

// TestSetDeps_unchanged 同 kind 同 strategy → 不动（保 bound，不重供、不释放）。
func TestSetDeps_unchanged(t *testing.T) {
	r, _, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "sdunc", RepoDir: "/x", InternalPort: 8080}
	_ = appdeploy.NewStore(db).Create(ctx, a)
	_ = r.supplyAll(ctx, a.ID, "ps_1", []DepService{{Kind: "redis", Strategy: ModeShared}})
	before, _ := NewStore(db).GetBinding(ctx, a.ID, "redis")
	_ = r.SetDeps(ctx, a.ID, "ps_1", []appdeploy.DepDeclaration{{Kind: "redis", Strategy: ModeShared}})
	after, _ := NewStore(db).GetBinding(ctx, a.ID, "redis")
	if after.Status != StatusBound || after.IsolationToken != before.IsolationToken {
		t.Fatalf("unchanged 应保 bound+同 token，before=%+v after=%+v", before, after)
	}
}

// TestSetDeps_removed 删除声明 → dedicated docker rm + 删 instance/env/binding。
func TestSetDeps_removed(t *testing.T) {
	r, appStore, db, _, dk := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "sdrem", RepoDir: "/x", InternalPort: 8080}
	_ = appStore.Create(ctx, a)
	_ = r.supplyAll(ctx, a.ID, "ps_1", []DepService{{Kind: "redis", Strategy: ModeDedicated}})
	dk.rmCalls = nil
	_ = r.SetDeps(ctx, a.ID, "ps_1", nil) // 清空
	if len(dk.rmCalls) != 1 {
		t.Fatalf("应 RmForce 1 次，得 %d", len(dk.rmCalls))
	}
	if got, _ := NewStore(db).GetBinding(ctx, a.ID, "redis"); got != nil {
		t.Fatalf("binding 应删，得 %+v", got)
	}
	if v, _ := appStore.GetEnvValue(ctx, a.ID, "REDIS_ADDR"); v != "" {
		t.Fatalf("REDIS_ADDR 应删，得 %q", v)
	}
}

// TestSetDeps_changed 策略变 → 释放旧资源 + 声明新（declared）。
func TestSetDeps_changed(t *testing.T) {
	r, appStore, db, _, dk := newReconcilerTest(t)
	ctx := context.Background()
	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "sdchg", RepoDir: "/x", InternalPort: 8080}
	_ = appStore.Create(ctx, a)
	_ = r.supplyAll(ctx, a.ID, "ps_1", []DepService{{Kind: "redis", Strategy: ModeDedicated}})
	dk.rmCalls = nil
	_ = r.SetDeps(ctx, a.ID, "ps_1", []appdeploy.DepDeclaration{{Kind: "redis", Strategy: ModeShared}})
	if len(dk.rmCalls) != 1 {
		t.Fatalf("策略变更应释放旧 dedicated 容器，得 %d 次 RmForce", len(dk.rmCalls))
	}
	b, _ := NewStore(db).GetBinding(ctx, a.ID, "redis")
	if b == nil || b.Status != StatusDeclared || b.Strategy != ModeShared {
		t.Fatalf("应变 declared/shared，得 %+v", b)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run TestSetDeps -v`
Expected: FAIL（r.SetDeps undefined；接口缺方法）

- [ ] **Step 3: 接口加 SetDeps（handler.go）**

在 MWReconciler 接口加一行：

```go
	SetDeps(ctx context.Context, appID, psID string, decls []DepDeclaration) error
```

**同步给 `fakeMWReconciler` 加 stub**（handler_http_test.go）：

```go
func (f *fakeMWReconciler) SetDeps(_ context.Context, _, _ string, _ []DepDeclaration) error { return nil }
```

- [ ] **Step 4: supply.go +SetDeps 实现**

```go
// SetDeps 整体替换声明（PutDeps 核心，best-effort）：
// removed（现 binding 不在新声明）/ changed（同 kind 策略变）→ ReleaseDep 释放资源；
// added（新声明不在现 binding）/ changed → DeclareBinding 写 declared；
// unchanged（同 kind 同策略）→ 不动（保 bound，不重供）。
func (r *Reconciler) SetDeps(ctx context.Context, appID, psID string, decls []appdeploy.DepDeclaration) error {
	cur, _ := r.store.ListBindingsByApp(ctx, appID)
	curMap := map[string]ServiceBinding{}
	for _, b := range cur {
		curMap[b.ServiceKind] = b
	}
	newStrategy := map[string]string{}
	for _, d := range decls {
		newStrategy[d.Kind] = d.Strategy
	}
	// removed / changed → 释放
	for kind, b := range curMap {
		ns, ok := newStrategy[kind]
		if !ok || ns != b.Strategy {
			r.ReleaseDep(ctx, &b)
		}
	}
	// added / changed → 声明
	for kind, strategy := range newStrategy {
		b, exists := curMap[kind]
		if !exists || b.Strategy != strategy {
			_ = r.store.DeclareBinding(ctx, appID, psID, kind, strategy)
		}
	}
	return nil
}
```

- [ ] **Step 5: 跑测试验证通过**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/ -run TestSetDeps -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add internal/appdeploy/handler.go internal/mwsupply/supply.go internal/mwsupply/supply_test.go
git commit -m "feat(mwsupply): SetDeps diff(释放+声明) + 接口 SetDeps (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 7: appdeploy handlers — GetDeps/PutDeps/GetDepsCatalog + 路由 + 导入种子接线

HTTP 层：3 handler + 3 路由 + 校验 + 导入处调 SeedFromManifest。handler 用 fake MWReconciler 测 HTTP 契约（diff 正确性已在 Task 6 mwsupply 包测）。

**Files:**

- Modify: `internal/appdeploy/handler.go`（+`GetDeps`/`PutDeps`/`GetDepsCatalog`；Register +3 路由；`runImport`/`runImportZip` 调 SeedFromManifest）
- Modify: `internal/appdeploy/handler_http_test.go`（+fake MWReconciler；deps handler 测试）

**Interfaces:**

- Consumes: Task 4/5/6 的 MWReconciler 接口方法；`appdeploy.Store.Get`（取 app 的 psID，已有）
- Produces: HTTP `GET/PUT /project-spaces/:id/apps/:aid/deps`、`GET /project-spaces/:id/deps/catalog`

- [ ] **Step 1: 写失败测试**（追加到 `handler_http_test.go`）

先把既有 `fakeMWReconciler`（T4–T6 累加的空 struct + stub 方法）**替换**为带记录字段版：

```go
// fakeMWReconciler 替换 T4–T6 的空 stub 版，加记录字段供 deps handler 测 HTTP 契约
// （diff 正确性在 mwsupply 包测；handler_http_test.go 是 package appdeploy，DTO 不加包限定词）。
type fakeMWReconciler struct {
	seeded      []string              // SeedFromManifest 调用的 appID
	setDepsArgs []map[string]string   // SetDeps 收到的 kind→strategy
	listDeps    []DepDeclaration
	catalog     DepsCatalog
}

func (f *fakeMWReconciler) Reconcile(ctx context.Context, appID, psID string) error { return nil }
func (f *fakeMWReconciler) Cleanup(ctx context.Context, appID string) error         { return nil }
func (f *fakeMWReconciler) SeedFromManifest(ctx context.Context, appID, psID, repoDir string) error {
	f.seeded = append(f.seeded, appID)
	return nil
}
func (f *fakeMWReconciler) ListDeps(ctx context.Context, appID string) ([]DepDeclaration, error) {
	return f.listDeps, nil
}
func (f *fakeMWReconciler) SetDeps(ctx context.Context, appID, psID string, decls []DepDeclaration) error {
	f.setDepsArgs = append(f.setDepsArgs, nil)
	m := map[string]string{}
	for _, d := range decls {
		m[d.Kind] = d.Strategy
	}
	f.setDepsArgs[len(f.setDepsArgs)-1] = m
	return nil
}
func (f *fakeMWReconciler) DepsCatalog(ctx context.Context, psID string) (DepsCatalog, error) {
	return f.catalog, nil
}
```

测试：

```go
// TestHandler_GetDeps_ok 返回声明列表。
func TestHandler_GetDeps_ok(t *testing.T) {
	h, _ := newHTTPHandler(t)
	h.SetMwReconciler(&fakeMWReconciler{listDeps: []DepDeclaration{
		{Kind: "redis", Strategy: "shared", Status: "bound"},
	}})
	r := newRouterWith(h)
	a := seedApp(t, h, "ps_1", "d1", "/tmp/d1")
	code, resp := doReq(t, r, http.MethodGet, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/deps", nil)
	if code != 200 {
		t.Fatalf("状态码 %d", code)
	}
	data, _ := resp["data"].([]interface{})
	if len(data) != 1 {
		t.Fatalf("应 1 条声明，得 %v", data)
	}
}

// TestHandler_PutDeps_ok 整体替换 → 调 SetDeps。
func TestHandler_PutDeps_ok(t *testing.T) {
	h, _ := newHTTPHandler(t)
	fw := &fakeMWReconciler{}
	h.SetMwReconciler(fw)
	r := newRouterWith(h)
	a := seedApp(t, h, "ps_1", "d2", "/tmp/d2")
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/deps",
		[]map[string]string{{"kind": "redis", "strategy": "shared"}})
	if code != 200 {
		t.Fatalf("状态码 %d", code)
	}
	if len(fw.setDepsArgs) != 1 || fw.setDepsArgs[0]["redis"] != "shared" {
		t.Fatalf("应调 SetDeps(redis=shared)，得 %v", fw.setDepsArgs)
	}
}

// TestHandler_PutDeps_badKind 非法 kind → 400。
func TestHandler_PutDeps_badKind(t *testing.T) {
	h, _ := newHTTPHandler(t)
	h.SetMwReconciler(&fakeMWReconciler{})
	r := newRouterWith(h)
	a := seedApp(t, h, "ps_1", "d3", "/tmp/d3")
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/deps",
		[]map[string]string{{"kind": "mongodb", "strategy": "shared"}})
	if code != 400 {
		t.Fatalf("非法 kind 应 400，得 %d", code)
	}
}

// TestHandler_PutDeps_badStrategy 非法 strategy → 400。
func TestHandler_PutDeps_badStrategy(t *testing.T) {
	h, _ := newHTTPHandler(t)
	h.SetMwReconciler(&fakeMWReconciler{})
	r := newRouterWith(h)
	a := seedApp(t, h, "ps_1", "d4", "/tmp/d4")
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/deps",
		[]map[string]string{{"kind": "redis", "strategy": "magic"}})
	if code != 400 {
		t.Fatalf("非法 strategy 应 400，得 %d", code)
	}
}

// TestHandler_GetDepsCatalog_ok 返回 catalog。
func TestHandler_GetDepsCatalog_ok(t *testing.T) {
	h, _ := newHTTPHandler(t)
	h.SetMwReconciler(&fakeMWReconciler{catalog: DepsCatalog{Kinds: []string{"redis", "milvus"}}})
	r := newRouterWith(h)
	code, resp := doReq(t, r, http.MethodGet, "/api/v1/project-spaces/ps_1/deps/catalog", nil)
	if code != 200 {
		t.Fatalf("状态码 %d", code)
	}
	data, _ := resp["data"].(map[string]interface{})
	kinds, _ := data["kinds"].([]interface{})
	if len(kinds) != 2 {
		t.Fatalf("应 2 kinds，得 %v", kinds)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run 'TestHandler_GetDeps|TestHandler_PutDeps' -v`
Expected: FAIL（404：路由未注册）

- [ ] **Step 3: 实现 handler（追加到 `handler.go`）**

```go
// GetDeps 列应用依赖声明。
func (h *Handler) GetDeps(c *gin.Context) {
	psID, aid := c.Param("id"), c.Param("aid")
	if a, _ := h.store.Get(c.Request.Context(), psID, aid); a == nil || a.ID == "" {
		httpx.Err(c, 404, 40420, "应用不存在")
		return
	}
	decls, err := h.mwReconciler.ListDeps(c.Request.Context(), aid)
	if err != nil {
		httpx.Err(c, 500, 50020, err.Error())
		return
	}
	httpx.OK(c, decls)
}

// PutDeps 整体替换应用依赖声明（校验 kind/strategy → SetDeps）。
func (h *Handler) PutDeps(c *gin.Context) {
	psID, aid := c.Param("id"), c.Param("aid")
	a, _ := h.store.Get(c.Request.Context(), psID, aid)
	if a == nil || a.ID == "" {
		httpx.Err(c, 404, 40420, "应用不存在")
		return
	}
	var body []DepDeclaration
	if err := c.ShouldBindJSON(&body); err != nil {
		httpx.Err(c, 400, 40001, "invalid body: "+err.Error())
		return
	}
	seen := map[string]bool{}
	for _, d := range body {
		if d.Kind != "redis" && d.Kind != "milvus" {
			httpx.Err(c, 400, 40001, "kind 非法: "+d.Kind)
			return
		}
		if d.Strategy != "bind_existing" && d.Strategy != "shared" && d.Strategy != "dedicated" {
			httpx.Err(c, 400, 40001, "strategy 非法: "+d.Strategy)
			return
		}
		if seen[d.Kind] {
			httpx.Err(c, 400, 40001, "重复 kind: "+d.Kind)
			return
		}
		seen[d.Kind] = true
	}
	if err := h.mwReconciler.SetDeps(c.Request.Context(), aid, a.ProjectSpaceID, body); err != nil {
		httpx.Err(c, 500, 50020, err.Error())
		return
	}
	httpx.OK(c, gin.H{"ok": true})
}

// GetDepsCatalog 依赖勾选器选项（kinds/strategies/可见实例）。
func (h *Handler) GetDepsCatalog(c *gin.Context) {
	psID := c.Param("id")
	cat, err := h.mwReconciler.DepsCatalog(c.Request.Context(), psID)
	if err != nil {
		httpx.Err(c, 500, 50020, err.Error())
		return
	}
	httpx.OK(c, cat)
}
```

> 范式照搬自 `DeleteEnv`/`Detail`：`httpx.Err(c, <status>, <bizcode>, msg)`（40420=应用不存在 / 40001=参数非法 / 50020=内部错）、`httpx.OK(c, data)`（包成 `{code:0,data:...}`）；app 查 `h.store.Get(ctx, psID, aid)`（**3 参**）+ `a==nil||a.ID==""` 判 404。`httpx` 已 import（既有 handler 用）。

- [ ] **Step 4: 注册路由（Register 内）**

在 `r.DELETE("/project-spaces/:id/apps/:aid/env/:key", h.DeleteEnv)` 之后加：

```go
	r.GET("/project-spaces/:id/apps/:aid/deps", h.GetDeps)
	r.PUT("/project-spaces/:id/apps/:aid/deps", h.PutDeps)
	r.GET("/project-spaces/:id/deps/catalog", h.GetDepsCatalog)
```

- [ ] **Step 5: 导入种子接线（runImport / runImportZip）**

两函数都是 goroutine，内有本地 `ctx`（`context.WithTimeout(context.Background(), 30*time.Minute)`）与 `repoDir` 变量。在 `UpdateImportDone(ctx, psID, appID, repoDir)` 成功块**之后**、`provisioner` 块**之前**（runImport 约 :1204 后；runImportZip 约 :1319 后），插入：

```go
	// 导入种子：仓库 .anp/deps.yaml → declared binding（best-effort，不覆盖 UI 声明，失败不阻塞导入）。
	if h.mwReconciler != nil {
		_ = h.mwReconciler.SeedFromManifest(ctx, appID, psID, repoDir)
	}
```

（用本地 `ctx` 与 `repoDir`，不是 `c.Request.Context()`——这两函数无 `c`。两处插入点结构相同，照搬。）

- [ ] **Step 6: 跑测试验证通过**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run 'TestHandler_GetDeps|TestHandler_PutDeps|TestHandler_GetDepsCatalog' -v`
Expected: PASS

- [ ] **Step 7: 跑全包 + mwsupply 零回归**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ ./internal/mwsupply/`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add internal/appdeploy/handler.go internal/appdeploy/handler_http_test.go
git commit -m "feat(appdeploy): GetDeps/PutDeps/GetDepsCatalog + 导入种子接线 (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 8: 前端 — 应用管理页「依赖」section

在 applications 详情面板（与 env/instances 并列）加「依赖」section：列声明、增删改、catalog 驱动下拉、PUT 保存。

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx`

**Interfaces:**

- Consumes: 后端 `GET/PUT /project-spaces/:id/apps/:aid/deps`、`GET /project-spaces/:id/deps/catalog`（Task 7）
- 既有范式：`API_BASE_URL`（`@/lib/api`）、`Envelope<T>`、详情面板 fetch `/apps/:aid/detail`

- [ ] **Step 1: 加类型 + state（page.tsx 顶部类型区与选中 app 的 state 区）**

类型区加：

```ts
type Dep = {
  kind: string;
  strategy: string;
  status: string;
  instance?: string;
  token?: string;
  error?: string;
};
type DepsCatalog = {
  kinds: string[];
  strategies: { name: string; desc: string }[];
  instances: {
    id: string;
    kind: string;
    name: string;
    supply_mode: string;
    host: string;
    port: number;
  }[];
};
```

选中 app 的 state 区加：

```ts
const [deps, setDeps] = useState<Dep[]>([]);
const [catalog, setCatalog] = useState<DepsCatalog | null>(null);
const [editingDeps, setEditingDeps] = useState<Dep[] | null>(null); // null=查看态，非 null=编辑态
```

- [ ] **Step 2: 加 fetch（选中 app 时，挨着既有 detail fetch）**

```ts
async function loadDeps(appId: string, psID: string) {
  const [d, c] = await Promise.all([
    fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appId}/deps`, {
      headers: { Authorization: `Bearer ${token}` },
    }).then((r) => r.json()),
    fetch(`${API_BASE_URL}/project-spaces/${psID}/deps/catalog`, {
      headers: { Authorization: `Bearer ${token}` },
    }).then((r) => r.json()),
  ]);
  setDeps((d as Envelope<Dep[]>)?.data ?? []);
  setCatalog((c as Envelope<DepsCatalog>)?.data ?? null);
}
```

（`token` 取既有鉴权 token 的来源；`psID` 取选中 app 的 project_space_id。在选中 app 的 effect 里调 `loadDeps`。）

- [ ] **Step 3: 加保存（PUT 整体替换）**

```ts
async function saveDeps(appId: string, psID: string, next: Dep[]) {
  const r = await fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appId}/deps`, {
    method: "PUT",
    headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
    body: JSON.stringify(next.map((d) => ({ kind: d.kind, strategy: d.strategy }))),
  });
  const j = await r.json();
  if (r.ok && j.code === 0) {
    toast("依赖已保存，下次部署生效");
    setEditingDeps(null);
    await loadDeps(appId, psID);
  } else {
    toast(j.message || "保存失败");
  }
}
```

- [ ] **Step 4: 加「依赖」section JSX（详情面板，挨着 env/instances section）**

```tsx
{
  /* 依赖 section */
}
<section className="border-t pt-3 mt-3">
  <div className="flex items-center justify-between mb-2">
    <h3 className="font-semibold">中间件依赖</h3>
    {editingDeps ? (
      <div className="flex gap-2">
        <button onClick={() => setEditingDeps(null)}>取消</button>
        <button onClick={() => saveDeps(selectedApp.id, selectedApp.project_space_id, editingDeps)}>
          保存
        </button>
      </div>
    ) : (
      <button onClick={() => setEditingDeps(deps.length ? deps : [])}>编辑</button>
    )}
  </div>
  <p className="text-xs text-gray-500 mb-2">依赖在下次部署/重新部署时注入生效。</p>
  {(editingDeps ?? deps).map((d, i) => {
    const list = editingDeps ?? deps;
    const set = (patch: Partial<Dep>) =>
      setEditingDeps(list.map((x, j) => (j === i ? { ...x, ...patch } : x)));
    return (
      <div key={i} className="flex gap-2 items-center mb-1">
        <select
          value={d.kind}
          onChange={(e) => set({ kind: e.target.value })}
          disabled={!editingDeps}
        >
          {(catalog?.kinds ?? []).map((k) => (
            <option key={k} value={k}>
              {k}
            </option>
          ))}
        </select>
        <select
          value={d.strategy}
          onChange={(e) => set({ strategy: e.target.value })}
          disabled={!editingDeps}
        >
          {(catalog?.strategies ?? []).map((s) => (
            <option key={s.name} value={s.name}>
              {s.name}
            </option>
          ))}
        </select>
        {!editingDeps && (
          <span className="text-xs text-gray-500">
            {d.status}
            {d.token ? ` · ${d.token}` : ""}
          </span>
        )}
        {editingDeps && (
          <button onClick={() => setEditingDeps(list.filter((_, j) => j !== i))}>删除</button>
        )}
      </div>
    );
  })}
  {editingDeps && (
    <button
      onClick={() =>
        setEditingDeps([
          ...editingDeps,
          { kind: catalog?.kinds?.[0] ?? "redis", strategy: "bind_existing", status: "declared" },
        ])
      }
    >
      + 添加依赖
    </button>
  )}
</section>;
```

（变量名 `selectedApp`/`token`/`toast` 按既有代码实际名照齐；JSX class 风格按既有。）

- [ ] **Step 5: 本机类型检查/构建**

Run: `cd platform/frontend && pnpm exec tsc --noEmit`
Expected: 无类型错

- [ ] **Step 6: 提交**

```bash
git add platform/frontend/app/applications/page.tsx
git commit -m "feat(frontend): 应用管理页「中间件依赖」section (P6 deps-ui)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 9: .28 端到端 + 文档/记忆闭环

真前端驱动后端验证全链路（替代历次 e2e 手编 deps.yaml）；更新 spec §14 e2e 结论；写实现记录 + 记忆闭环 + 下个 session 开场白。

**Files:**

- 部署：scp 源码 + .28 重建（memory `deploy-prod-10.10.0.28`）
- 改：`docs/superpowers/specs/2026-08-03-应用依赖声明UI-API-design.md`（+§14 e2e 结论）
- 建：`docs/2026-08-03-应用依赖声明UI-API-实现记录.md`

- [ ] **Step 1: 部署到 .28**

```bash
# 源码（排除 data/.env.prod），scp + 重建 backend+frontend
tar --force-local -czf /tmp/anp.tar.gz --exclude=node_modules --exclude=.next --exclude=.git --exclude=data --exclude='deploy/.env.prod' -C "D:/Projects/智源-ANP平台" .
scp -i ~/.ssh/miscode /tmp/anp.tar.gz root@10.10.0.28:/root/
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp && tar -xzf /root/anp.tar.gz && docker-compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up -d --build backend frontend"
```

验证三项（memory `deploy-prod-10.10.0.28` push-prod 假成功教训）：`grep SetDeps /opt/anp/platform/backend/internal/mwsupply/supply.go`（源码真更新）、`docker ps` backend/frontend CreatedAt（新创建）、`schema_migrations` 仍 000033（本期零迁移）。

- [ ] **Step 2: e2e — UI 声明 redis shared → deploy → 验注入**

登 `admin/admin123` → 应用管理页选一 python app → 「依赖」编辑 → 加 redis=shared → 保存 → 工作台 deploy(test) → 验：

- `docker exec deploy_pg_dev psql -U anp -d anp_dev -tAc "SELECT env_key,source FROM appdeploy_env WHERE app_id='<aid>' AND env_key LIKE 'REDIS%'"` → 含 `REDIS_ADDR/REDIS_DB`(source=platform)，无 PASSWORD（shared redis 无 auth）
- 容器内 `nc -z 10.10.0.28 6381` = OK
- `appdeploy_service_binding` 一行 `strategy=shared/status=bound`

- [ ] **Step 3: e2e — UI 声明 milvus shared → deploy → 验注入**

同上加 milvus=shared → deploy → 验 `MILVUS_ADDR`(=10.10.0.28:19530) + `MILVUS_COLLECTION_PREFIX`(`app<12hex>_`) source=platform；`nc -z 10.10.0.28 19530` OK。

- [ ] **Step 4: e2e — 导入种子**

导入一个带 `.anp/deps.yaml`（`services:[{kind:redis}]`）的 app → 验 `appdeploy_service_binding` 自动种 `strategy=bind_existing/status=declared`（导入种子生效）→ deploy → bound。

- [ ] **Step 5: e2e — UI 删除依赖 → 验清理**

UI 删 redis 依赖（dedicated 的更显著：先加 redis=dedicated deploy，再删）→ 保存 → 验 dedicated 容器 `docker rm`、`REDIS_*` env 行清、binding 行清。

- [ ] **Step 6: 更新 spec §14 + 写实现记录**

spec 末尾加 §14 e2e 结论（照 P5 spec §14 风格）。建 `docs/2026-08-03-应用依赖声明UI-API-实现记录.md` 记变更/零迁移/e2e 结论/教训。

- [ ] **Step 7: 提交 + 更新记忆 + 下个 session 开场白**

```bash
git add docs/superpowers/specs/2026-08-03-应用依赖声明UI-API-design.md docs/2026-08-03-应用依赖声明UI-API-实现记录.md
git commit -m "docs(mwsupply): P6 deps-ui .28 e2e 结论 + 实现记录

Co-Authored-By: Claude <noreply@anthropic.com>"
```

更新记忆：新建 `deps-ui-closed.md`（type:project）+ MEMORY.md 索引一行。给「下个 session 开场白」（memory `handoff-prompt-for-next-session`）。
