# host 网络门禁 实现计划（PRD §7 中期-运行态 ④）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 appdeploy 应用加 `network_mode`（bridge 默认 / host 需授权），host 模式容器以 `--network host` 运行；set-time（改 host）+ deploy-time（部署 host 应用）双 RBAC 角色门禁（`app.net.host`，gatekeeper/admin）。做完即关闭 PRD §7 中期-运行态里程碑。

**Architecture:** `appdeploy_application` 加 `network_mode` 列（migration `000034`）。新 op `app.net.host`（permission.go）。`deployer.Deploy` 把 host flag 提到分支前（与 app_kind 正交），web/service 在 host 模式下跳 `-p`/端口分配、`ins.HostPort=internalPort`（复用既有 appgw `UpsertRoute`/URL 语义零改动）。`PutNetworkMode` handler（set-time gate）+ Deploy/Promote/DeployCommit 三入口加 deploy-time gate。前端自包含 `NetworkModeSection` 选择器（403 toast 替代前置置灰）。

**Tech Stack:** Go（gin / sqlx / pgx），PostgreSQL（anp_test 库），Next.js App Router（TypeScript）。

**关联 spec：** [2026-08-03-host网络门禁-design.md](../specs/2026-08-03-host网络门禁-design.md)

## Global Constraints

- **PG only，禁 SQLite**（memory `no-sqlite-pg-only`）。单测连 `anp_test` PG（`testutil.TestDB`，`newHTTPHandler` 内自动跑迁移 → 新迁移 `000034` 自动上），全量回归 `go test -p 1` 串行（memory `go-test-serial-p1`）。
- 本机 GOPATH 可能被污染：跑 go 命令前缀 `GOPATH=C:/Users/yxt/go`（memory `gopath-pollution-windows`）。
- commit 消息尾加 `Co-Authored-By: Claude <noreply@anthropic.com>`；本仓库直接提交 main（项目惯例）。
- **迁移编号 `000034`**（当前最新 `000033_mwsupply_shared_milvus`）。迁移文件命名 `000034_app_network_mode.{up,down}.sql`，放 `platform/backend/internal/db/migrations/pg/`（`//go:embed migrations/pg/*.sql` 自动拾取，backend 启动自动 migrate，无需手跑）。
- **`network_mode` 与 `app_kind`/`deploy_mode` 正交**：任意 app_kind（含 headless）都可 host；deploy_mode=external 应用不经 `buildAndDeploy`，host 对其无意义（前端对 external 应用不渲染 NetworkModeSection，同 deps-ui 门控）。
- host 是特权操作：`app.net.host` 不分 test/prod，均 gatekeeper/admin。
- 前端 `platform/frontend`：raw `fetch(${API_BASE_URL}/...)` 不带显式 auth header（走 `X-User` 拦截器/cookie，同既有 DepsSection）；`toast` 用 `@/lib/toast` 的 `toast.success/toast.error`。**前端 AGENTS.md 警告「This is NOT the Next.js you know」**——本计划只加一个 React 组件 + fetch（同既有 DepsSection 范式），不碰 Next.js 特性；若遇 Next API 报错先查 `node_modules/next/dist/docs/`。

## 文件结构

| 文件                                                                     | 责任                                                                                                                                                                                      |
| ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `internal/db/migrations/pg/000034_app_network_mode.up.sql` / `.down.sql` | `appdeploy_application` 加/删 `network_mode` 列                                                                                                                                           |
| `internal/appdeploy/model.go`                                            | `Application` +`NetworkMode` 字段                                                                                                                                                         |
| `internal/appdeploy/store.go`                                            | `appCols()` +`network_mode`；+`UpdateNetworkMode(ctx,appID,mode)`                                                                                                                         |
| `internal/auth/permission.go`                                            | `OpRoles` +`"app.net.host": {gatekeeper, admin}`                                                                                                                                          |
| `internal/appdeploy/deployer.go`                                         | `Deploy` host flag 提前 + web/service host 分支（跳 `-p`/端口分配，`ins.HostPort=internalPort`）                                                                                          |
| `internal/appdeploy/handler.go`                                          | +`PutNetworkMode` handler + 1 路由（set-time gate）；Deploy/Promote/DeployCommit +deploy-time host gate                                                                                   |
| `internal/appdeploy/handler_http_test.go`                                | +`newRouterWithRoles` helper；+`TestStore_UpdateNetworkMode`；+`TestHandler_PutNetworkMode_*`（200/403/400/404）；+`TestDeploy_HostApp_*`/`TestDeployCommit_HostApp_*`（deploy-time 403） |
| `internal/appdeploy/deployer_test.go`                                    | +`TestDeploy_Host_NoPortMap_NetworkHost`                                                                                                                                                  |
| `platform/frontend/app/applications/page.tsx`                            | `App` 类型 +`network_mode`；+`NetworkModeSection` 组件 + 渲染                                                                                                                             |

---

## Task 1: 数据层 — migration 000034 + Application.NetworkMode + store

`network_mode` 列 + Go 模型字段 + `appCols` 读取 + `UpdateNetworkMode` 写入。后续所有任务依赖此任务的 `a.NetworkMode`。

**Files:**

- Create: `internal/db/migrations/pg/000034_app_network_mode.up.sql`
- Create: `internal/db/migrations/pg/000034_app_network_mode.down.sql`
- Modify: `internal/appdeploy/model.go:31`（`Application` struct）
- Modify: `internal/appdeploy/store.go:22-24`（`appCols()`）+ 末尾追加 `UpdateNetworkMode`
- Test: `internal/appdeploy/handler_http_test.go`（+`TestStore_UpdateNetworkMode`）

**Interfaces:**

- Produces: `Application.NetworkMode string`（db:`network_mode`）、`(*Store).UpdateNetworkMode(ctx, appID, mode string) error`；`appCols()` 含 `network_mode` → 所有 `Get/List/GetByName/GetByAppID` 自动返回该字段

- [ ] **Step 1: 建迁移文件**

`internal/db/migrations/pg/000034_app_network_mode.up.sql`：

```sql
-- host 网络门禁（PRD §7 中期-运行态 ④）：Application 加 network_mode 维度。
-- bridge 默认；host 需 gatekeeper/admin 角色开启+部署（op app.net.host）。与 app_kind/deploy_mode 正交。
ALTER TABLE appdeploy_application
    ADD COLUMN IF NOT EXISTS network_mode TEXT NOT NULL DEFAULT 'bridge';
-- 值: bridge(默认) / host
```

`internal/db/migrations/pg/000034_app_network_mode.down.sql`：

```sql
ALTER TABLE appdeploy_application DROP COLUMN IF EXISTS network_mode;
```

- [ ] **Step 2: 写失败测试**（追加到 `handler_http_test.go`）

```go
// TestStore_UpdateNetworkMode 写 network_mode + 读回；新 app 默认 bridge。
func TestStore_UpdateNetworkMode(t *testing.T) {
	h, _ := newHTTPHandler(t)
	ctx := context.Background()
	a := seedApp(t, h, "ps_1", "nwm", "/tmp/nwm")

	// 默认 bridge（列 DEFAULT 'bridge'，Create 不显式写 → DB 默认）
	got, _ := h.store.Get(ctx, "ps_1", a.ID)
	if got.NetworkMode != "bridge" {
		t.Fatalf("默认应 bridge，得 %q", got.NetworkMode)
	}
	// 改 host → 读回
	if err := h.store.UpdateNetworkMode(ctx, a.ID, "host"); err != nil {
		t.Fatalf("UpdateNetworkMode: %v", err)
	}
	got2, _ := h.store.Get(ctx, "ps_1", a.ID)
	if got2.NetworkMode != "host" {
		t.Fatalf("应 host，得 %q", got2.NetworkMode)
	}
}
```

- [ ] **Step 3: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run TestStore_UpdateNetworkMode -v`
Expected: 编译失败（`got.NetworkMode undefined`、`h.store.UpdateNetworkMode undefined`）

- [ ] **Step 4: 实现 — model.go 加字段**

在 `Application` struct 的 `AppKind` 行（`model.go:31`）后加一行：

```go
	AppKind        string        `json:"app_kind" db:"app_kind"`                 // 应用类型 web/desktop/mobile/cli/service/headless
	NetworkMode   string        `json:"network_mode" db:"network_mode"`          // bridge(默认) / host(需 gatekeeper/admin，op app.net.host)
```

- [ ] **Step 5: 实现 — store.go appCols 加列**

把 `appCols()`（`store.go:22-24`）的返回串，在 `COALESCE(app_kind,'web') AS app_kind,` 之后插入 `COALESCE(network_mode,'bridge') AS network_mode,`。完整新返回串：

```go
func appCols() string {
	return `id, project_space_id, name, COALESCE(repo_dir,'') AS repo_dir, internal_port, COALESCE(image,'') AS image, COALESCE(container_name,'') AS container_name, host_port, COALESCE(url,'') AS url, version, status, COALESCE(last_error,'') AS last_error, COALESCE(build_log,'') AS build_log, COALESCE(deploy_mode,'managed') AS deploy_mode, COALESCE(app_kind,'web') AS app_kind, COALESCE(network_mode,'bridge') AS network_mode, COALESCE(external_url,'') AS external_url, COALESCE(import_source,'') AS import_source, COALESCE(import_ref,'') AS import_ref, imported_at, created_at, updated_at`
}
```

> `Create` 的 INSERT 不列 `network_mode` → 走 DB `DEFAULT 'bridge'`，无需改 Create。

- [ ] **Step 6: 实现 — store.go 加 UpdateNetworkMode**（追加到 `store.go` 末尾，范式照 `UpdateAppStatus:295`）

```go
// UpdateNetworkMode 更新应用网络模式（bridge/host）。set-time 角色门禁由 handler 校验。
func (s *Store) UpdateNetworkMode(ctx context.Context, appID, mode string) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE appdeploy_application SET network_mode=$1, updated_at=CURRENT_TIMESTAMP WHERE id=$2`,
		mode, appID)
	return err
}
```

- [ ] **Step 7: 跑测试验证通过**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run TestStore_UpdateNetworkMode -v`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add internal/db/migrations/pg/000034_app_network_mode.up.sql internal/db/migrations/pg/000034_app_network_mode.down.sql internal/appdeploy/model.go internal/appdeploy/store.go internal/appdeploy/handler_http_test.go
git commit -m "feat(appdeploy): network_mode 列 + Application 字段 + UpdateNetworkMode (host 网络门禁 T1)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: deployer — host 网络分支

`Deploy` 把 host flag 提到分支前（与 app_kind 正交），web/service 在 host 模式下跳 `-p`+端口分配、`ins.HostPort=internalPort`、仍 `ensurePortEnv`。headless 分支不变（但顶部加的 host flag 对它也生效）。

**Files:**

- Modify: `internal/appdeploy/deployer.go:148-196`（`Deploy`）
- Test: `internal/appdeploy/deployer_test.go`（+`TestDeploy_Host_NoPortMap_NetworkHost`）

**Interfaces:**

- Consumes: Task 1 `Application.NetworkMode`
- Produces: `Deploy` 在 `a.NetworkMode=="host"` 时输出 `--network host` + 无 `-p` + `ins.HostPort=a.InternalPort` + `ins.URL=http://<urlHost>:<internalPort>`

> 注：`dockerRun` 是包级 `var dockerRun = runDockerOn`（`deployer.go:316`），测试用赋值 + `defer` 还原注入（**非 struct 桩**），范式见既有 `TestDeploy_Headless_NoPortNoURL`。

- [ ] **Step 1: 写失败测试**（追加到 `deployer_test.go`）

```go
// TestDeploy_Host_NoPortMap_NetworkHost host 网络：args 含 --network host、无 -p、HostPort=internalPort、URL 含 internalPort、注入 PORT=。
func TestDeploy_Host_NoPortMap_NetworkHost(t *testing.T) {
	var got []string
	orig := dockerRun
	dockerRun = func(_ context.Context, _ string, args ...string) (string, error) {
		got = args
		return "cid", nil
	}
	defer func() { dockerRun = orig }()

	d := NewDeployer("10.10.0.28")
	ins := &AppInstance{Env: EnvTest, Version: 1}
	a := &Application{Name: "hostapp", AppKind: AppKindWeb, InternalPort: 18080, NetworkMode: "host"}
	if err := d.Deploy(context.Background(), a, ins, nil, ""); err != nil {
		t.Fatal(err)
	}
	hasNet, hasP, hasPort := false, false, false
	for i, arg := range got {
		if arg == "--network" && i+1 < len(got) && got[i+1] == "host" {
			hasNet = true
		}
		if arg == "-p" {
			hasP = true
		}
		if arg == "PORT=18080" {
			hasPort = true
		}
	}
	if !hasNet {
		t.Fatalf("host 部署须含 --network host，得 %v", got)
	}
	if hasP {
		t.Fatalf("host 部署不得 -p 映射端口，得 %v", got)
	}
	if !hasPort {
		t.Fatalf("host 部署须注入 PORT=18080，得 %v", got)
	}
	if ins.HostPort != 18080 {
		t.Fatalf("HostPort 应 = internalPort 18080，得 %d", ins.HostPort)
	}
	if ins.URL == "" || !strings.HasSuffix(ins.URL, ":18080") {
		t.Fatalf("URL 应以 :18080 结尾，得 %q", ins.URL)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run TestDeploy_Host_NoPortMap_NetworkHost -v`
Expected: FAIL（当前 Deploy 不识别 network_mode，会走 bridge 分支 → 有 `-p`、无 `--network host`、HostPort=9100）

- [ ] **Step 3: 实现 — 重构 Deploy**（替换 `deployer.go:148-196` 整个 `Deploy`）

把现有 `Deploy`：

```go
func (d *Deployer) Deploy(ctx context.Context, a *Application, ins *AppInstance, env []string, dockerHost string) error {
	name := fmt.Sprintf("appdeploy-%s-%s-v%d", dockerSlug(a.Name), ins.Env, ins.Version)
	args := []string{"run", "-d", "--name", name, "--restart", "unless-stopped"}

	if a.AppKind == AppKindHeadless {
		for _, e := range env {
			args = append(args, "-e", e)
		}
		args = append(args, ins.Image)
		out, err := dockerRun(ctx, dockerHost, args...)
		if err != nil {
			return fmt.Errorf("docker run 失败: %w: %s", err, out)
		}
		ins.ContainerName = name
		return nil
	}

	min, max := d.envPortRange(ins.Env)
	used := d.usedPortsOn(ctx, dockerHost)
	port := ins.HostPort
	if _, occupied := used[port]; port < min || port > max || occupied {
		port = AllocFreePort(used, min, max)
	}
	if port == 0 {
		return fmt.Errorf("无可用宿主端口（%s 环境 %d-%d 已满）", ins.Env, min, max)
	}
	env = ensurePortEnv(env, a.InternalPort)
	for _, e := range env {
		args = append(args, "-e", e)
	}
	args = append(args, "-p", fmt.Sprintf("%d:%d", port, a.InternalPort), ins.Image)
	out, err := dockerRun(ctx, dockerHost, args...)
	if err != nil {
		return fmt.Errorf("docker run 失败: %w: %s", err, out)
	}
	ins.ContainerName = name
	ins.HostPort = port
	urlHost := d.host
	if dockerHost != "" {
		parts := strings.Split(strings.TrimPrefix(strings.TrimPrefix(dockerHost, "tcp://"), "http://"), ":")
		if len(parts) > 0 && parts[0] != "" {
			urlHost = parts[0]
		}
	}
	ins.URL = fmt.Sprintf("http://%s:%d", urlHost, port)
	return nil
}
```

替换为（host flag 提前 + web/service host/bridge 合并，`urlHost` 抽公共）：

```go
// Deploy 运行容器。headless:无端口/无 URL；web/service:bridge 分配宿主端口 + -p，host 直接绑宿主 internalPort（无 -p）。
// network_mode 与 app_kind 正交：host flag 对所有 app_kind 生效（headless+host 也共享宿主网络）。
// dockerHost 非空时远程部署（host = 该远程节点的 host 网络）。
func (d *Deployer) Deploy(ctx context.Context, a *Application, ins *AppInstance, env []string, dockerHost string) error {
	name := fmt.Sprintf("appdeploy-%s-%s-v%d", dockerSlug(a.Name), ins.Env, ins.Version)
	args := []string{"run", "-d", "--name", name, "--restart", "unless-stopped"}
	isHost := a.NetworkMode == "host"
	if isHost {
		args = append(args, "--network", "host")
	}

	if a.AppKind == AppKindHeadless {
		// headless(bot/worker)：无端口、无 URL。不分配宿主端口、不注入 PORT、不 -p（host flag 已在 args 顶部）。
		for _, e := range env {
			args = append(args, "-e", e)
		}
		args = append(args, ins.Image)
		out, err := dockerRun(ctx, dockerHost, args...)
		if err != nil {
			return fmt.Errorf("docker run 失败: %w: %s", err, out)
		}
		ins.ContainerName = name
		return nil // ins.HostPort/URL 保持零值
	}

	// web/service：host 模式无 -p（绑宿主 internalPort）；bridge 模式分配宿主端口 + -p。
	var port int
	if isHost {
		port = a.InternalPort
	} else {
		min, max := d.envPortRange(ins.Env)
		used := d.usedPortsOn(ctx, dockerHost)
		port = ins.HostPort
		if _, occupied := used[port]; port < min || port > max || occupied {
			port = AllocFreePort(used, min, max)
		}
		if port == 0 {
			return fmt.Errorf("无可用宿主端口（%s 环境 %d-%d 已满）", ins.Env, min, max)
		}
	}
	env = ensurePortEnv(env, a.InternalPort)
	for _, e := range env {
		args = append(args, "-e", e)
	}
	if !isHost {
		args = append(args, "-p", fmt.Sprintf("%d:%d", port, a.InternalPort))
	}
	args = append(args, ins.Image)
	out, err := dockerRun(ctx, dockerHost, args...)
	if err != nil {
		return fmt.Errorf("docker run 失败: %w: %s", err, out)
	}
	ins.ContainerName = name
	ins.HostPort = port // host 模式 = internalPort（host 命名空间可达端口）→ appgw UpsertRoute 零改动
	urlHost := d.host
	if dockerHost != "" {
		parts := strings.Split(strings.TrimPrefix(strings.TrimPrefix(dockerHost, "tcp://"), "http://"), ":")
		if len(parts) > 0 && parts[0] != "" {
			urlHost = parts[0]
		}
	}
	ins.URL = fmt.Sprintf("http://%s:%d", urlHost, port)
	return nil
}
```

- [ ] **Step 4: 跑新测试 + 既有 deployer 回归**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run 'TestDeploy_Host_NoPortMap_NetworkHost|TestDeploy_Headless_NoPortNoURL|TestDeploy_Web_StillMapsPort' -v`
Expected: 全 PASS（host 新测过；headless/web 既有零回归——web 仍 `-p 9100:3000`、headless 仍无 `-p`/无 URL）

- [ ] **Step 5: 提交**

```bash
git add internal/appdeploy/deployer.go internal/appdeploy/deployer_test.go
git commit -m "feat(appdeploy): deployer host 网络分支(--network host, 跳 -p, HostPort=internalPort) (T2)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: RBAC op + PutNetworkMode handler + set-time 门禁

新 op `app.net.host`（gatekeeper/admin）+ `PUT /network-mode` handler（set-time gate）+ 路由 + 测试 helper `newRouterWithRoles`。

**Files:**

- Modify: `internal/auth/permission.go:13-33`（`OpRoles`）
- Modify: `internal/appdeploy/handler.go`（+`PutNetworkMode`；`Register:147` 后 +1 路由）
- Modify: `internal/appdeploy/handler_http_test.go`（+`newRouterWithRoles`；+4 PutNetworkMode 测试）

**Interfaces:**

- Consumes: Task 1 `store.UpdateNetworkMode`；`auth.Allowed`、`rolesFromCtx`（既有）
- Produces: op `app.net.host`；HTTP `PUT /project-spaces/:id/apps/:aid/network-mode`

> 范式：handler 照 `PutDeps:897`（`store.Get(ctx,psID,aid)` 3 参 + `a==nil||a.ID==""` 判 404 + `httpx.Err/OK`）；`httpx.Err(c, <status>, <bizcode>, msg)`（40301=无权限 / 40001=参数非法 / 40420=应用不存在 / 50020=内部错）。

- [ ] **Step 1: 写失败测试**（追加到 `handler_http_test.go`）

先加 403 测试用的 helper（挨着 `newRouterWith:41`，**新建**——既有 `newRouterWith`/`newRouterWithUser` 都写死 admin，无法测 403）：

```go
// newRouterWithRoles 同 newRouterWith 但注入指定角色（测 RBAC 403 用；既有两个 helper 写死 admin）。
func newRouterWithRoles(h *Handler, roles []string) *gin.Engine {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(func(c *gin.Context) { c.Set("roles", roles); c.Next() })
	h.Register(r.Group("/api/v1"))
	return r
}
```

测试：

```go
// TestHandler_PutNetworkMode_admin_ok admin → 200 + 写 host。
func TestHandler_PutNetworkMode_admin_ok(t *testing.T) {
	h, _ := newHTTPHandler(t)
	r := newRouterWith(h) // admin
	a := seedApp(t, h, "ps_1", "nm1", "/tmp/nm1")
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/network-mode",
		map[string]string{"mode": "host"})
	if code != 200 {
		t.Fatalf("admin 应 200，得 %d", code)
	}
	got, _ := h.store.Get(context.Background(), "ps_1", a.ID)
	if got.NetworkMode != "host" {
		t.Fatalf("应写 host，得 %q", got.NetworkMode)
	}
}

// TestHandler_PutNetworkMode_dev_forbidden dev → 403（无 app.net.host）。
func TestHandler_PutNetworkMode_dev_forbidden(t *testing.T) {
	h, _ := newHTTPHandler(t)
	r := newRouterWithRoles(h, []string{"dev"})
	a := seedApp(t, h, "ps_1", "nm2", "/tmp/nm2")
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/network-mode",
		map[string]string{"mode": "host"})
	if code != 403 {
		t.Fatalf("dev 改 host 应 403，得 %d", code)
	}
}

// TestHandler_PutNetworkMode_badMode 非法 mode → 400（admin 绕过角色专测 mode 校验）。
func TestHandler_PutNetworkMode_badMode(t *testing.T) {
	h, _ := newHTTPHandler(t)
	r := newRouterWith(h)
	a := seedApp(t, h, "ps_1", "nm3", "/tmp/nm3")
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/network-mode",
		map[string]string{"mode": "macvlan"})
	if code != 400 {
		t.Fatalf("非法 mode 应 400，得 %d", code)
	}
}

// TestHandler_PutNetworkMode_notFound 应用不存在 → 404。
func TestHandler_PutNetworkMode_notFound(t *testing.T) {
	h, _ := newHTTPHandler(t)
	r := newRouterWith(h)
	code, _ := doReq(t, r, http.MethodPut, "/api/v1/project-spaces/ps_1/apps/app_none/network-mode",
		map[string]string{"mode": "host"})
	if code != 404 {
		t.Fatalf("应用不存在应 404，得 %d", code)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run TestHandler_PutNetworkMode -v`
Expected: FAIL（404：路由未注册）

- [ ] **Step 3: 加 op**（`permission.go` OpRoles，`app.delete` 行后）

```go
	"app.delete":             {RoleAdmin},
	// host 网络门禁（spec 2026-08-03）：host 削弱隔离，仅 gatekeeper/admin 可开启 + 部署
	"app.net.host":           {RoleGatekeeper, RoleAdmin},
```

- [ ] **Step 4: 加 handler**（追加到 `handler.go`，紧邻 `PutDeps` 之后）

```go
// PutNetworkMode 设置应用网络模式（bridge/host）。host 削弱隔离 → 需 gatekeeper/admin（op app.net.host）。
// network_mode 为 app 级配置（test/prod 共用）；下次部署生效（deploy 时 deployer 按 mode 拼 --network）。
func (h *Handler) PutNetworkMode(c *gin.Context) {
	psID, aid := c.Param("id"), c.Param("aid")
	a, _ := h.store.Get(c.Request.Context(), psID, aid)
	if a == nil || a.ID == "" {
		httpx.Err(c, 404, 40420, "应用不存在")
		return
	}
	if !auth.Allowed("app.net.host", rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "无权限修改网络模式（仅 gatekeeper/admin）")
		return
	}
	var in struct {
		Mode string `json:"mode"`
	}
	if err := c.ShouldBindJSON(&in); err != nil {
		httpx.Err(c, 400, 40001, "invalid body: "+err.Error())
		return
	}
	if in.Mode != "bridge" && in.Mode != "host" {
		httpx.Err(c, 400, 40001, "mode 非法: "+in.Mode)
		return
	}
	if err := h.store.UpdateNetworkMode(c.Request.Context(), aid, in.Mode); err != nil {
		httpx.Err(c, 500, 50020, err.Error())
		return
	}
	httpx.OK(c, gin.H{"ok": true, "network_mode": in.Mode})
}
```

- [ ] **Step 5: 注册路由**（`Register` 内，`:147` `r.PUT(.../deps", h.PutDeps)` 后加一行）

```go
	r.PUT("/project-spaces/:id/apps/:aid/deps", h.PutDeps)                  // P6：整体替换依赖声明
	r.PUT("/project-spaces/:id/apps/:aid/network-mode", h.PutNetworkMode)  // host 网络门禁：设网络模式（需 gatekeeper/admin）
```

- [ ] **Step 6: 跑测试验证通过**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run TestHandler_PutNetworkMode -v`
Expected: 4 个全 PASS

- [ ] **Step 7: 提交**

```bash
git add internal/auth/permission.go internal/appdeploy/handler.go internal/appdeploy/handler_http_test.go
git commit -m "feat(appdeploy): app.net.host op + PutNetworkMode(set-time 门禁) (T3)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: deploy-time 门禁（Deploy / Promote / DeployCommit）

三个部署入口加 host gate：`app.NetworkMode=="host" && !auth.Allowed("app.net.host", roles)` → 403。防 dev 部署他人开启的 host 应用。

**Files:**

- Modify: `internal/appdeploy/handler.go`（`Deploy:1479` 后 / `Promote:1557` 后 / `DeployCommit:1612-1617` 改）
- Test: `internal/appdeploy/handler_http_test.go`（+2 deploy-time 403 测试）

**Interfaces:**

- Consumes: Task 1 `a.NetworkMode`；Task 3 op `app.net.host`；`rolesFromCtx`（既有）

> **重要**：roles 只在 HTTP 层可得，gate 必须同步检查（**不进 `buildAndDeploy` goroutine**）。`Deploy`/`Promote` 已有 `a`；`DeployCommit` 当前 `if _, err := h.store.Get(...)` 丢弃 app，须改为捕获 `a`。
> **Promote 的 host-403 在当前角色矩阵下不可达**（gatekeeper 同持 `app.deploy.prod`+`app.net.host`），仍加作 defense-in-depth（备 `app.net.host` 收窄或新增只持 prod 权限的角色）；可测的 host-403 是 Deploy + DeployCommit（dev 有 `app.deploy.test`/`app.deploy-commit.test` 但无 `app.net.host`）。

- [ ] **Step 1: 写失败测试**（追加到 `handler_http_test.go`）

```go
// TestHandler_Deploy_HostApp_Dev_Forbidden dev 部署 host 应用 → 403（deploy-time host 门禁）。
// dev 有 app.deploy.test（过 env 门禁）但无 app.net.host → 命中 host 门禁。
func TestHandler_Deploy_HostApp_Dev_Forbidden(t *testing.T) {
	h, _ := newHTTPHandler(t)
	a := seedApp(t, h, "ps_1", "hdep", "/tmp/hdep")
	if err := h.store.UpdateNetworkMode(context.Background(), a.ID, "host"); err != nil {
		t.Fatalf("UpdateNetworkMode: %v", err)
	}
	r := newRouterWithRoles(h, []string{"dev"})
	code, _ := doReq(t, r, http.MethodPost, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/deploy",
		map[string]string{"env": "test"})
	if code != 403 {
		t.Fatalf("dev 部署 host 应用应 403，得 %d", code)
	}
}

// TestHandler_DeployCommit_HostApp_Dev_Forbidden dev 版本化部署 host 应用 → 403。
func TestHandler_DeployCommit_HostApp_Dev_Forbidden(t *testing.T) {
	h, _ := newHTTPHandler(t)
	a := seedApp(t, h, "ps_1", "hdc", "/tmp/hdc")
	_ = h.store.UpdateNetworkMode(context.Background(), a.ID, "host")
	r := newRouterWithRoles(h, []string{"dev"})
	code, _ := doReq(t, r, http.MethodPost, "/api/v1/project-spaces/ps_1/apps/"+a.ID+"/deploy-commit",
		map[string]string{"env": "test", "sha": "deadbeef"})
	if code != 403 {
		t.Fatalf("dev deploy-commit host 应用应 403，得 %d", code)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -run 'TestHandler_Deploy_HostApp_Dev_Forbidden|TestHandler_DeployCommit_HostApp_Dev_Forbidden' -v`
Expected: FAIL（当前无 host 门禁，dev 部署 host 应用会过门禁进入 buildAndDeploy，返回非 403）

- [ ] **Step 3: Deploy 加 host 门禁**（`handler.go`，env 敏感检查块后）

在 `Deploy` 现有：

```go
	// 部署权限分离（spec 2026-07-26）：按 env 鉴权
	if !auth.Allowed("app.deploy."+env, rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "无权限部署到 "+env+" 环境")
		return
	}
```

之后插入：

```go
	// host 网络门禁：host 模式应用须 gatekeeper/admin 部署（防 dev 部署他人开启的 host 应用）
	if a.NetworkMode == "host" && !auth.Allowed("app.net.host", rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "host 网络应用需 gatekeeper/admin 部署")
		return
	}
```

- [ ] **Step 4: Promote 加 host 门禁**（`handler.go`，prod 检查块后）

在 `Promote` 现有：

```go
	// 部署权限分离：上线 prod 需 gatekeeper/admin
	if !auth.Allowed("app.deploy.prod", rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "无权限上线 prod（仅 gatekeeper/admin）")
		return
	}
```

之后插入（defense-in-depth，注释说明不可达）：

```go
	// host 网络门禁（defense-in-depth：当前角色矩阵下 gatekeeper 同持 app.deploy.prod+app.net.host，此 403 不可达；
	// 保留以备 app.net.host 收窄或新增只持 prod 权限的角色）
	if a.NetworkMode == "host" && !auth.Allowed("app.net.host", rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "host 网络应用需 gatekeeper/admin 部署")
		return
	}
```

- [ ] **Step 5: DeployCommit 改捕获 a + 加 host 门禁**（`handler.go:1612-1617`）

把现有：

```go
	if !auth.Allowed("app.deploy-commit."+env, rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "无权限部署到 "+env+" 环境")
		return
	}
	if _, err := h.store.Get(c.Request.Context(), psID, aid); err != nil {
		httpx.Err(c, 404, 40420, "应用不存在")
		return
	}
```

替换为（捕获 `a` + host 门禁）：

```go
	if !auth.Allowed("app.deploy-commit."+env, rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "无权限部署到 "+env+" 环境")
		return
	}
	a, err := h.store.Get(c.Request.Context(), psID, aid)
	if err != nil {
		httpx.Err(c, 404, 40420, "应用不存在")
		return
	}
	// host 网络门禁：host 模式应用须 gatekeeper/admin 部署
	if a.NetworkMode == "host" && !auth.Allowed("app.net.host", rolesFromCtx(c)) {
		httpx.Err(c, 403, 40301, "host 网络应用需 gatekeeper/admin 部署")
		return
	}
```

- [ ] **Step 6: 跑新测试 + 全包零回归**

Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/ -v`
Expected: 全 PASS（含 2 个新 deploy-time 403；既有 bridge 应用部署/上线测试零回归——bridge 应用 host 门禁条件 `a.NetworkMode=="host"` 不成立，直接跳过）

- [ ] **Step 7: 提交**

```bash
git add internal/appdeploy/handler.go internal/appdeploy/handler_http_test.go
git commit -m "feat(appdeploy): Deploy/Promote/DeployCommit host 网络 deploy-time 门禁 (T4)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 5: 前端 — NetworkModeSection 选择器

应用详情面板加「网络模式」选择器（bridge/host），改选即 PUT；403 时 toast 提示需 gatekeeper/admin（服务端是安全真相；applications 页无 roles 上下文，**不做前置置灰**——对 spec §8 的 YAGNI 偏离，已在计划注明）。external 应用不渲染（同 deps-ui 门控）。

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx`（`App` 类型 +`network_mode`；+`NetworkModeSection` 组件；渲染处）

**Interfaces:**

- Consumes: Task 3 `PUT /project-spaces/:id/apps/:aid/network-mode`；既有 `API_BASE_URL`、`toast`、`Envelope`、详情面板选中 app（含 `network_mode`）

- [ ] **Step 1: App 类型加 network_mode**（`page.tsx:31-52` 的 `type App`）

在 `app_kind: string;` 行后加：

```ts
app_kind: string; // 应用类型 web/desktop/mobile/cli/service/headless
network_mode: string; // bridge(默认) / host(需 gatekeeper/admin)
```

- [ ] **Step 2: 加 NetworkModeSection 组件**（挨着 `DepsSection` 组件定义之后，自包含，范式照 `DepsSection:266`）

```tsx
// 网络模式区：bridge(默认)/host 选择器，改选即 PUT。host 削弱隔离需 gatekeeper/admin；
// applications 页无 roles 上下文 → 不前置置灰，靠 403 toast 提示（服务端是安全真相）。
function NetworkModeSection({ psID, appID, mode }: { psID: string; appID: string; mode: string }) {
  const [m, setM] = useState(mode || "bridge");
  const [saving, setSaving] = useState(false);
  useEffect(() => setM(mode || "bridge"), [mode]);

  async function save(next: string) {
    setSaving(true);
    const res = await fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appID}/network-mode`, {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ mode: next }),
    });
    const j = (await res.json()) as Envelope<unknown>;
    setSaving(false);
    if (res.ok && j.code === 0) {
      toast.success("网络模式已改为 " + next + "，下次部署生效");
      setM(next);
    } else {
      toast.error(j.message || "保存失败（host 需 gatekeeper/admin）");
    }
  }

  return (
    <div className="mt-3 rounded border border-border p-3">
      <div className="mb-1 text-sm font-semibold">🌐 网络模式</div>
      <p className="mb-2 text-xs text-text-muted">
        host 模式容器共享宿主网络（直接占宿主端口、直达 host-LAN），削弱隔离。默认 bridge
        更安全；host 需 gatekeeper/admin。
      </p>
      <div className="flex items-center gap-2 text-xs">
        <select
          value={m}
          onChange={(e) => save(e.target.value)}
          disabled={saving}
          className="rounded border border-border px-1 py-0.5"
        >
          <option value="bridge">bridge（默认）</option>
          <option value="host">host</option>
        </select>
        {saving && <span className="text-text-muted">保存中…</span>}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 渲染 NetworkModeSection**（详情面板，`<DepsSection .../>` 渲染处旁边）

找到详情面板里渲染 `<DepsSection psID={...} appID={...} />` 的位置（external 应用门控：仅 `deploy_mode !== "external"` 时渲染 deps/network-mode），在其后加：

```tsx
        <DepsSection psID={psID} appID={app.id} />
        <NetworkModeSection psID={psID} appID={app.id} mode={app.network_mode} />
```

> 实现者：读 `page.tsx` 找到 `<DepsSection` 实际渲染行（含其 props 的确切变量名 `psID`/`app` 等），紧随其后加 `<NetworkModeSection .../>`，props 变量名与 DepsSection 对齐。若该处对 external 应用有 `{deploy_mode !== "external" && <DepsSection/>}` 门控，NetworkModeSection 套同一门控。

- [ ] **Step 4: 类型检查**

Run: `cd platform/frontend && pnpm exec tsc --noEmit`
Expected: 无类型错（若报 Next.js 相关错，查 `node_modules/next/dist/docs/`——本改动仅 React 组件 + fetch）

- [ ] **Step 5: 提交**

```bash
git add platform/frontend/app/applications/page.tsx
git commit -m "feat(frontend): 应用管理页「网络模式」选择器 (host 网络门禁 T5)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 6: .28 端到端 + 文档/记忆闭环

真前端/接口驱动后端验证全链路；更新 spec §14 e2e 结论；写实现记录 + 记忆闭环 + 下个 session 开场白。

**Files:**

- 部署：scp 源码 + .28 重建（memory `deploy-prod-10.10.0.28`）
- 改：`docs/superpowers/specs/2026-08-03-host网络门禁-design.md`（+§14 e2e 结论）
- 建：`docs/2026-08-03-host网络门禁-实现记录.md`

- [ ] **Step 1: 部署到 .28**

```bash
# 源码（排除 data/.env.prod），scp + 重建 backend+frontend（用 docker-compose v1，-f prod.yml）
tar --force-local -czf /tmp/anp.tar.gz --exclude=node_modules --exclude=.next --exclude=.git --exclude=data --exclude='deploy/.env.prod' -C "D:/Projects/智源-ANP平台" .
scp -i ~/.ssh/miscode /tmp/anp.tar.gz root@10.10.0.28:/root/
ssh -F /dev/null -i ~/.ssh/miscode -o IdentitiesOnly=yes -o StrictHostKeyChecking=no root@10.10.0.28 "cd /opt/anp && tar -xzf /root/anp.tar.gz && docker-compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up -d --build backend frontend"
```

验证三项（memory `deploy-prod-10.10.0.28` push-prod 假成功教训）：

- 源码真更新：`ssh ... "grep network_mode /opt/anp/platform/backend/internal/appdeploy/model.go"`（命中）
- 容器新创建：`ssh ... "docker ps --format '{{.Names}} {{.CreatedAt}}' | grep -E 'backend|frontend'"`（重建时间）
- 迁移上 000034：`ssh ... "docker exec -e PGPASSWORD=anp deploy_postgres_1 psql -U anp -d anp -tAc \"SELECT version FROM schema_migrations ORDER BY version DESC LIMIT 1\""`（含 000034）

- [ ] **Step 2: e2e — 正路径（gatekeeper 部署 host web app）**

登 `admin/admin123`（admin 角色，持 `app.net.host`）→ 应用管理页选一 python web app（internal_port 设高位如 18080，别撞 .28 他人服务）→ 「网络模式」选 host → 工作台 deploy(test) → 验：

- `docker inspect <容器名>` → `"NetworkMode": "host"` + 无 `PortBindings`（无 `-p`）
- app 监听宿主 18080：`curl -s -o /dev/null -w "%{http_code}" http://10.10.0.28:18080/`（或 app 内日志）
- appgw 路由：`SELECT host_port FROM appdeploy_instance WHERE app_id='<aid>' AND env='test'` = 18080（=internalPort）

- [ ] **Step 3: e2e — set-time 负路径（dev → 403）**

用 user-switcher 切到 dev 角色 → 「网络模式」选 host → 保存 → 验前端 toast 报错；直接 curl `PUT /network-mode {mode:host}` → 403。

- [ ] **Step 4: e2e — deploy-time 负路径（dev 部署 host app → 403）**

dev 角色对 Step 2 那个 host app 点部署 → 403（前端 toast）。

- [ ] **Step 5: e2e — 改回 bridge 恢复**

admin 改回 bridge → redeploy → 验 `docker inspect` `NetworkMode=bridge` + 恢复 `-p 9xxx:internalPort`（端口段 9100-9199）。

- [ ] **Step 6: 更新 spec §14 + 写实现记录**

spec 末尾加 §14 e2e 结论（照 deps-ui spec §13 风格：部署三项核查 + 正/负路径结果 + 零回归）。建 `docs/2026-08-03-host网络门禁-实现记录.md` 记变更/迁移 000034/e2e 结论/教训（如「Promote host-403 当前角色矩阵不可达」）。

- [ ] **Step 7: 提交 + 更新记忆 + 下个 session 开场白**

```bash
git add docs/superpowers/specs/2026-08-03-host网络门禁-design.md docs/2026-08-03-host网络门禁-实现记录.md
git commit -m "docs(appdeploy): host 网络门禁 .28 e2e 结论(spec §14) + 实现记录

Co-Authored-By: Claude <noreply@anthropic.com>"
```

更新记忆：新建 `host-net-gate-closed.md`（type:project）+ MEMORY.md 索引一行。给「下个 session 开场白」（memory `handoff-prompt-for-next-session`）——标注 PRD §7 中期-运行态里程碑已全部关闭，候选下一步（PRD §7 中期-产物态 ⑤⑥ / app-db-api-architecture 阶段2 appgw / milvus 鉴权① / P6 minors⑤）。
