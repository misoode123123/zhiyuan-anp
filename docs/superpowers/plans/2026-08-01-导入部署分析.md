# 导入部署分析（Deploy Analyzer）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 导入项目后扫描仓库产出结构化 `DeployAnalysis`（语言/构建/依赖/网络/形态/与 ANP 差异），落库供编码适配与后续依赖注入/headless 用。

**Architecture:** 纯函数 `Analyze(repoDir) → *DeployAnalysis`（规则扫描，不联网不写文件），分 detect 辅助函数；新表 `appdeploy_deploy_analysis` 落库；导入末尾自动触发 + 应用详情"重新分析"端点。

**Tech Stack:** Go（`platform/backend/internal/appdeploy`）+ PostgreSQL（禁 SQLite）+ golang-migrate。

**Spec:** `docs/superpowers/specs/2026-08-01-导入部署分析-design.md`

## Global Constraints

- 禁 SQLite，开发/生产只用 PG（连 .28 dev PG）；store 测试用 `testutil.TestDB`（PG）。
- 本机不跑功能测试，改完 scp 到 .28 重建 backend 验证（.28 是测试库）。
- `go test ./internal/appdeploy/` 单测可本机跑（Analyzer 纯函数用 `t.TempDir()`，不依赖 DB）。
- 全量回归 `go test -p 1 ./...`（并发污染 anp_test 库）。
- commit message 用 `feat(appdeploy): 中文` 风格，末尾 `Co-Authored-By: Claude <noreply@anthropic.com>`。
- GOPATH 本机被污染，go 命令前缀 `GOPATH=C:/Users/yxt/go`。

## File Structure

| 文件                                                             | 责任                                                                         | 动作 |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---- |
| `internal/appdeploy/analyzer.go`                                 | `DeployAnalysis` 类型 + `Analyze` 编排（纯函数）                             | 新建 |
| `internal/appdeploy/analyzer_detect.go`                          | detect 辅助：language/build/ports/deps/network/appkind/mismatches + 配置扫描 | 新建 |
| `internal/appdeploy/analyzer_test.go`                            | `Analyze` 端到端单测（含客服机器人黄金用例）                                 | 新建 |
| `internal/appdeploy/analyzer_store.go`                           | `SaveAnalysis`/`GetAnalysis`（读写 `appdeploy_deploy_analysis`）             | 新建 |
| `internal/appdeploy/analyzer_store_test.go`                      | store 单测（PG）                                                             | 新建 |
| `internal/db/migrations/pg/000028_deploy_analysis.{up,down}.sql` | 新表                                                                         | 新建 |
| `internal/appdeploy/handler.go`                                  | 导入完成 hook + `Reanalyze` handler + 路由                                   | 改   |

---

### Task 1: 类型 + Analyze 编排 + 语言/构建/端口检测

**Files:**

- Create: `platform/backend/internal/appdeploy/analyzer.go`
- Create: `platform/backend/internal/appdeploy/analyzer_detect.go`
- Test: `platform/backend/internal/appdeploy/analyzer_test.go`

**Interfaces:**

- Produces: `DeployAnalysis` 结构（字段见下）、`Analyze(repoDir string) (*DeployAnalysis, error)`、`detectLanguage/detectBuild/detectPorts`（detect 文件内）。

- [ ] **Step 1: 写失败测试（简单 Go 仓：语言/Dockerfile/EXPOSE）**

```go
// analyzer_test.go
package appdeploy

import (
	"os"
	"path/filepath"
	"sort"
	"testing"
)

func writeFile(t *testing.T, dir, rel, content string) {
	t.Helper()
	p := filepath.Join(dir, rel)
	if err := os.MkdirAll(filepath.Dir(p), 0o755); err != nil {
		t.Fatal(err)
	}
	if err := os.WriteFile(p, []byte(content), 0o644); err != nil {
		t.Fatal(err)
	}
}

func TestAnalyze_LanguageBuildPorts(t *testing.T) {
	dir := t.TempDir()
	writeFile(t, dir, "go.mod", "module demo\n\ngo 1.24\n")
	writeFile(t, dir, "Dockerfile", "FROM golang:alpine\nEXPOSE 8080\nCMD [\"/app\"]\n")
	a, err := Analyze(dir)
	if err != nil {
		t.Fatalf("Analyze: %v", err)
	}
	if a.Language != "go" {
		t.Errorf("Language=%q want go", a.Language)
	}
	if !a.Build.Dockerfile {
		t.Errorf("Build.Dockerfile=false want true")
	}
	want := []int{8080}
	got := append([]int(nil), a.Ports.Expose...)
	sort.Ints(got)
	if len(got) != 1 || got[0] != 8080 {
		t.Errorf("Ports.Expose=%v want %v", got, want)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestAnalyze_LanguageBuildPorts -v`
Expected: FAIL（`undefined: Analyze`/`DeployAnalysis`）

- [ ] **Step 3: 实现 analyzer.go（类型 + Analyze 编排）**

```go
package appdeploy

import (
	"fmt"
	"os"
	"path/filepath"
)

// DeployAnalysis 导入部署分析的结构化结果（规则扫描产出）。
type DeployAnalysis struct {
	Language     string          `json:"language"`
	Framework    string          `json:"framework"`
	Build        BuildAnalysis   `json:"build"`
	AppKindGuess string          `json:"app_kind_guess"`
	Deps         []DeployDep     `json:"deps"`
	Network      NetworkAnalysis `json:"network"`
	Ports        PortsAnalysis   `json:"ports"`
	Mismatches   []string        `json:"mismatches"`
	AdaptHints   []string        `json:"adapt_hints"`
}

type BuildAnalysis struct {
	Dockerfile      bool     `json:"dockerfile"`
	Compose         bool     `json:"compose"`
	ComposeServices []string `json:"compose_services"`
}

type DeployDep struct {
	Kind     string `json:"kind"`
	Addr     string `json:"addr"`
	Database string `json:"database,omitempty"`
}

type NetworkAnalysis struct {
	HostModeRequired bool   `json:"host_mode_required"`
	Reason           string `json:"reason,omitempty"`
}

type PortsAnalysis struct {
	Expose []int `json:"expose"`
}

// Analyze 扫描 repoDir 产出 DeployAnalysis（纯函数：只读、不联网、不写文件）。
// 扫描排除 .git/node_modules/vendor 等依赖与构建产物目录。
func Analyze(repoDir string) (*DeployAnalysis, error) {
	root := filepath.Clean(repoDir)
	if info, err := os.Stat(root); err != nil || !info.IsDir() {
		return nil, fmt.Errorf("仓库目录无效: %s", repoDir)
	}
	a := &DeployAnalysis{}
	a.Language, a.Framework = detectLanguage(root)
	a.Build = detectBuild(root)
	a.Ports = detectPorts(root)
	// deps/network/appkind/mismatches 由后续 Task 补全
	return a, nil
}
```

- [ ] **Step 4: 实现 analyzer_detect.go（language/build/ports 部分）**

```go
package appdeploy

import (
	"bufio"
	"os"
	"path/filepath"
	"regexp"
	"sort"
	"strconv"
	"strings"
)

var analyzerSkipDirs = map[string]bool{
	".git": true, "node_modules": true, "vendor": true, "dist": true,
	"build": true, "target": true, "__pycache__": true, ".next": true,
	".worktrees": true, "docker-data": true,
}

func fileExists(p string) bool { _, err := os.Stat(p); return err == nil }

func detectLanguage(root string) (lang, framework string) {
	has := func(names ...string) bool {
		for _, n := range names {
			if fileExists(filepath.Join(root, n)) {
				return true
			}
		}
		return false
	}
	switch {
	case has("go.mod", "main.go"):
		return "go", ""
	case has("package.json"):
		return "node", ""
	case has("requirements.txt", "pyproject.toml"):
		return "python", ""
	case has("pom.xml", "build.gradle"):
		return "java", ""
	}
	return "", ""
}

func detectBuild(root string) BuildAnalysis {
	b := BuildAnalysis{}
	if fileExists(filepath.Join(root, "Dockerfile")) {
		b.Dockerfile = true
	}
	for _, n := range []string{"docker-compose.yml", "docker-compose.yaml"} {
		p := filepath.Join(root, n)
		if fileExists(p) {
			b.Compose = true
			b.ComposeServices = parseComposeServices(p)
			break
		}
	}
	return b
}

var serviceKeyRe = regexp.MustCompile(`^([a-zA-Z0-9_.-]+):`)

// parseComposeServices 解析 compose services: 块下的一级缩进键（服务名）。
func parseComposeServices(p string) []string {
	f, err := os.Open(p)
	if err != nil {
		return nil
	}
	defer f.Close()
	var services []string
	inServices := false
	serviceIndent := -1
	sc := bufio.NewScanner(f)
	for sc.Scan() {
		line := strings.TrimRight(sc.Text(), "\r")
		trimmed := strings.TrimSpace(line)
		if trimmed == "" || strings.HasPrefix(trimmed, "#") {
			continue
		}
		indent := len(line) - len(strings.TrimLeft(line, " "))
		if indent == 0 {
			inServices = trimmed == "services:" || strings.HasPrefix(trimmed, "services:")
			serviceIndent = -1
			continue
		}
		if !inServices {
			continue
		}
		if serviceIndent == -1 {
			serviceIndent = indent
		}
		if indent == serviceIndent {
			if m := serviceKeyRe.FindStringSubmatch(trimmed); m != nil {
				services = append(services, m[1])
			}
		}
	}
	return services
}

var exposePortsRe = regexp.MustCompile(`(?m)^\s*EXPOSE\s+(.*)`)

func detectPorts(root string) PortsAnalysis {
	pa := PortsAnalysis{}
	data, err := os.ReadFile(filepath.Join(root, "Dockerfile"))
	if err != nil {
		return pa
	}
	seen := map[int]bool{}
	for _, m := range exposePortsRe.FindAllStringSubmatch(string(data), -1) {
		for _, tok := range strings.Fields(m[1]) {
			tok = strings.Split(tok, "/")[0]
			if port, e := strconv.Atoi(tok); e == nil && !seen[port] {
				seen[port] = true
				pa.Expose = append(pa.Expose, port)
			}
		}
	}
	sort.Ints(pa.Expose)
	return pa
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestAnalyze_LanguageBuildPorts -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add platform/backend/internal/appdeploy/analyzer.go platform/backend/internal/appdeploy/analyzer_detect.go platform/backend/internal/appdeploy/analyzer_test.go
git commit -m "feat(appdeploy): 导入分析Analyze+语言/构建/端口检测" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 依赖检测 + 网络检测

**Files:**

- Modify: `platform/backend/internal/appdeploy/analyzer_detect.go`（加 `detectDeps`/`detectNetwork` + 扫描辅助）
- Modify: `platform/backend/internal/appdeploy/analyzer.go`（Analyze 内补两行调用）
- Test: `platform/backend/internal/appdeploy/analyzer_test.go`（加用例）

**Interfaces:**

- Produces: `detectDeps(root string) []DeployDep`、`detectNetwork(root string) NetworkAnalysis`。
- Consumes: Task 1 的 `DeployDep`/`NetworkAnalysis` 类型。

- [ ] **Step 1: 写失败测试（redis 127.0.0.1:6379 + compose network_mode host）**

追加到 `analyzer_test.go`：

```go
func TestAnalyze_DepsAndNetwork(t *testing.T) {
	dir := t.TempDir()
	writeFile(t, dir, "go.mod", "module x\n")
	writeFile(t, dir, "config.yaml", "redis:\n  addr: \"127.0.0.1:6379\"\n")
	writeFile(t, dir, "docker-compose.yml", "services:\n  bot:\n    image: bot\n  redis:\n    image: redis\nnetworks:\n  default:\nnetwork_mode: host\n")
	a, err := Analyze(dir)
	if err != nil {
		t.Fatalf("Analyze: %v", err)
	}
	foundRedis := false
	for _, d := range a.Deps {
		if d.Kind == "redis" {
			foundRedis = true
			if d.Addr != "127.0.0.1:6379" {
				t.Errorf("redis addr=%q want 127.0.0.1:6379", d.Addr)
			}
		}
	}
	if !foundRedis {
		t.Errorf("Deps 缺 redis: %+v", a.Deps)
	}
	if !a.Network.HostModeRequired {
		t.Errorf("Network.HostModeRequired=false want true")
	}
	if !contains(a.Build.ComposeServices, "redis") {
		t.Errorf("ComposeServices 缺 redis: %+v", a.Build.ComposeServices)
	}
}

func contains(s []string, v string) bool {
	for _, x := range s {
		if x == v {
			return true
		}
	}
	return false
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestAnalyze_DepsAndNetwork -v`
Expected: FAIL（redis 未检出 / HostModeRequired=false）

- [ ] **Step 3: 实现 detectDeps + detectNetwork + 配置扫描辅助**

追加到 `analyzer_detect.go`：

```go
var analyzerConfigExts = map[string]bool{
	".yaml": true, ".yml": true, ".env": true, ".ini": true,
	".conf": true, ".toml": true, ".properties": true,
}

// 已知中间件默认端口 → kind。
var portKindMap = map[int]string{
	6379: "redis", 19530: "milvus", 5432: "postgres", 3306: "mysql",
	27017: "mongo", 9092: "kafka", 9200: "elasticsearch", 9000: "minio",
	5672: "rabbitmq",
}

var (
	depKeywordRe = regexp.MustCompile(`(?i)\b(redis|milvus|postgres(?:ql)?|mysql|mongo(?:db)?|kafka|rabbitmq|elasticsearch)\b`)
	// 127.0.0.1:6379 / localhost:5432
	localAddrRe = regexp.MustCompile(`(?:127\.0\.0\.1|localhost):(\d{1,5})`)
)

// scanConfigFiles 遍历 repoDir（跳过 skip 目录），返回所有配置文本文件内容拼接 + 各文件路径。
func scanConfigFiles(root string) []string {
	var contents []string
	_ = filepath.Walk(root, func(path string, info os.FileInfo, err error) error {
		if err != nil || info == nil {
			return nil
		}
		if info.IsDir() {
			if analyzerSkipDirs[info.Name()] {
				return filepath.SkipDir
			}
			return nil
		}
		if !analyzerConfigExts[filepath.Ext(info.Name())] {
			return nil
		}
		if info.Size() > 1<<20 { // 单文件 >1MB 跳过（防爆扫描）
			return nil
		}
		b, e := os.ReadFile(path)
		if e != nil {
			return nil
		}
		contents = append(contents, string(b))
		return nil
	})
	return contents
}

func detectDeps(root string) []DeployDep {
	blobs := scanConfigFiles(root)
	byKind := map[string]*DeployDep{}
	ensure := func(kind string) *DeployDep {
		if d, ok := byKind[kind]; ok {
			return d
		}
		d := &DeployDep{Kind: kind}
		byKind[kind] = d
		return d
	}
	for _, s := range blobs {
		// 1) localhost:PORT → 按 portKindMap 定 kind + addr
		for _, m := range localAddrRe.FindAllStringSubmatch(s, -1) {
			if port, err := strconv.Atoi(m[1]); err == nil {
				if kind, ok := portKindMap[port]; ok {
					ensure(kind).Addr = m[0]
				}
			}
		}
		// 2) 关键词兜底（如 pgsql 段 host/port 分行，无 localhost:PORT）
		for _, m := range depKeywordRe.FindAllString(s, -1) {
			kind := strings.ToLower(strings.TrimSuffix(strings.TrimSuffix(m, "ql"), "db"))
			if kind == "elasticsearch" {
				kind = "es"
			}
			ensure(kind)
		}
	}
	deps := make([]DeployDep, 0, len(byKind))
	for _, d := range byKind {
		deps = append(deps, *d)
	}
	sort.Slice(deps, func(i, j int) bool { return deps[i].Kind < deps[j].Kind })
	return deps
}

var composeHostNetRe = regexp.MustCompile(`(?m)^\s*network_mode:\s*host\b`)

func detectNetwork(root string) NetworkAnalysis {
	n := NetworkAnalysis{}
	for _, name := range []string{"docker-compose.yml", "docker-compose.yaml"} {
		p := filepath.Join(root, name)
		if !fileExists(p) {
			continue
		}
		data, err := os.ReadFile(p)
		if err != nil {
			continue
		}
		if composeHostNetRe.Match(data) {
			n.HostModeRequired = true
			n.Reason = "compose network_mode:host"
		}
	}
	return n
}
```

- [ ] **Step 4: Analyze 内补调用**

修改 `analyzer.go` 的 `Analyze`，在 `a.Ports = detectPorts(root)` 后追加：

```go
	a.Deps = detectDeps(root)
	a.Network = detectNetwork(root)
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestAnalyze_DepsAndNetwork -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add platform/backend/internal/appdeploy/analyzer.go platform/backend/internal/appdeploy/analyzer_detect.go platform/backend/internal/appdeploy/analyzer_test.go
git commit -m "feat(appdeploy): 导入分析依赖+网络检测" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 形态猜测 + 差异/建议 + 黄金用例

**Files:**

- Modify: `platform/backend/internal/appdeploy/analyzer_detect.go`（加 `guessAppKind`/`computeMismatches`）
- Modify: `platform/backend/internal/appdeploy/analyzer.go`（Analyze 补调用）
- Test: `platform/backend/internal/appdeploy/analyzer_test.go`（加客服机器人黄金用例）

**Interfaces:**

- Produces: `guessAppKind(root string, a *DeployAnalysis) string`、`computeMismatches(a *DeployAnalysis) (mismatches, hints []string)`。

- [ ] **Step 1: 写失败测试（客服机器人黄金用例）**

追加到 `analyzer_test.go`：

```go
func TestAnalyze_Golden_KeFuBot(t *testing.T) {
	dir := t.TempDir()
	writeFile(t, dir, "go.mod", "module yxt_eino_v2\n\ngo 1.24\n")
	writeFile(t, dir, "Dockerfile", "FROM golang:alpine AS build\nWORKDIR /src\nCOPY . .\nRUN go build -o /out/bot ./cmd/bot/\nFROM alpine:3.20\nCOPY --from=build /out/bot /app/bot\nEXPOSE 9090 9091\nENTRYPOINT [\"/app/bot\"]\n")
	writeFile(t, dir, "docker-compose.yml", "services:\n  bot:\n    image: yxt-bot:latest\n    network_mode: host\n  redis:\n    image: redis:7-alpine\n  milvus:\n    image: milvusdb/milvus\n  postgres:\n    image: postgres:16-alpine\n")
	writeFile(t, dir, "config.yaml", "# yxt_eino_v2\nwecom:\n  bot_id: x\nredis:\n  addr: \"127.0.0.1:6379\"\nkb:\n  milvus:\n    address: \"127.0.0.1:19530\"\npgsql:\n  host: \"127.0.0.1\"\n  port: 5432\n  database: \"yxt_wecom_bot\"\n")
	a, err := Analyze(dir)
	if err != nil {
		t.Fatalf("Analyze: %v", err)
	}
	if a.Language != "go" {
		t.Errorf("Language=%q want go", a.Language)
	}
	if a.AppKindGuess != "headless" {
		t.Errorf("AppKindGuess=%q want headless", a.AppKindGuess)
	}
	if !a.Network.HostModeRequired {
		t.Errorf("应检出 host 网络需求")
	}
	kinds := map[string]bool{}
	for _, d := range a.Deps {
		kinds[d.Kind] = true
	}
	for _, want := range []string{"redis", "milvus", "postgres"} {
		if !kinds[want] {
			t.Errorf("Deps 缺 %s: %+v", want, a.Deps)
		}
	}
	if !containsInt(a.Ports.Expose, 9090) || !containsInt(a.Ports.Expose, 9091) {
		t.Errorf("Ports.Expose=%v want 含 9090/9091", a.Ports.Expose)
	}
	if !containsStr(a.Mismatches, "host") {
		t.Errorf("Mismatches 应含 host 相关: %+v", a.Mismatches)
	}
}

func containsInt(s []int, v int) bool {
	for _, x := range s {
		if x == v {
			return true
		}
	}
	return false
}
func containsStr(s []string, sub string) bool {
	for _, x := range s {
		if strings.Contains(x, sub) {
			return true
		}
	}
	return false
}
```

> 注：`TestAnalyze_DepsAndNetwork` 里已定义 `contains`；本测试另用 `containsStr`/`containsInt`。文件头 import 需补 `"strings"`。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestAnalyze_Golden_KeFuBot -v`
Expected: FAIL（AppKindGuess 空 / Mismatches 空 / postgres 可能漏）

- [ ] **Step 3: 实现 guessAppKind + computeMismatches**

追加到 `analyzer_detect.go`：

```go
var (
	httpFwRe = regexp.MustCompile(`(?i)\b(gin|echo|fiber|chi|mux|net/http|express|fastapi|flask|django|spring)\b`)
	wecomBotRe = regexp.MustCompile(`(?i)\b(wecom|企业微信|bot|worker|cron|daemon)\b`)
)

// guessAppKind 据 HTTP 框架 / bot 关键词 / 组件猜测形态。
func guessAppKind(root string, a *DeployAnalysis) string {
	blobs := strings.Join(scanConfigFiles(root), "\n")
	// 读 cmd/ 与 main 入口源码片段（仅扫配置文本不够，补扫 *.go 的 import 行 via 配置扫描不覆盖 .go；
	// 这里用 Dockerfile/compose 文本与 config 文本一起判）。
	probe := blobs
	if a.Build.Dockerfile {
		if b, err := os.ReadFile(filepath.Join(root, "Dockerfile")); err == nil {
			probe += "\n" + string(b)
		}
	}
	http := httpFwRe.MatchString(probe)
	bot := wecomBotRe.MatchString(probe)
	switch {
	case bot && !http:
		return "headless"
	case http:
		return "web"
	}
	return ""
}

// computeMismatches 比对分析与 ANP 默认模型（单 bridge 容器 + 仅 PG）的差异。
func computeMismatches(a *DeployAnalysis) (mismatches, hints []string) {
	if a.Network.HostModeRequired {
		mismatches = append(mismatches, "需 host 网络但 ANP 默认 bridge")
		hints = append(hints, "改 config 走 env 连部署机可达地址，或按应用开 host 网络(需审批)")
	}
	var nonPg []string
	for _, d := range a.Deps {
		if d.Kind != "postgres" && d.Kind != "es" { // es 视同中间件
			nonPg = append(nonPg, d.Kind)
		}
	}
	if len(nonPg) > 0 {
		mismatches = append(mismatches, "依赖 "+strings.Join(nonPg, "/")+" 但 ANP 默认仅供给 PG")
		hints = append(hints, "供给/绑定缺失的中间件，并把连接信息注入应用 env")
	}
	if a.Language == "go" && !a.Build.Dockerfile {
		mismatches = append(mismatches, "无 Dockerfile，buildpack 可能误判 static")
		hints = append(hints, "补 Go 多阶段 Dockerfile，或让编码适配生成")
	}
	return mismatches, hints
}
```

- [ ] **Step 4: Analyze 内补调用 + 修正 import**

修改 `analyzer.go` 的 `Analyze`，在 `a.Network = detectNetwork(root)` 后追加：

```go
	a.AppKindGuess = guessAppKind(root, a)
	a.Mismatches, a.AdaptHints = computeMismatches(a)
```

`analyzer_detect.go` 顶部 import 已含 `regexp`/`strings`/`os`/`path/filepath`（Task1/2 加过）；若 `guessAppKind` 用到的新变量未引入，确认 `strings` 在 import 块。

- [ ] **Step 5: 跑测试确认通过（含黄金用例）**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestAnalyze -v`
Expected: 三个用例全 PASS

- [ ] **Step 6: Commit**

```bash
git add platform/backend/internal/appdeploy/analyzer.go platform/backend/internal/appdeploy/analyzer_detect.go platform/backend/internal/appdeploy/analyzer_test.go
git commit -m "feat(appdeploy): 导入分析形态猜测+差异/建议+客服机器人黄金用例" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 迁移 000028 + Store（落库）

**Files:**

- Create: `platform/backend/internal/db/migrations/pg/000028_deploy_analysis.up.sql`
- Create: `platform/backend/internal/db/migrations/pg/000028_deploy_analysis.down.sql`
- Create: `platform/backend/internal/appdeploy/analyzer_store.go`
- Test: `platform/backend/internal/appdeploy/analyzer_store_test.go`

**Interfaces:**

- Produces: `(s *Store) SaveAnalysis(ctx, appID string, a *DeployAnalysis) error`、`(s *Store) GetAnalysis(ctx, appID string) (*DeployAnalysis, error)`。
- Consumes: `*sqlx.DB`（Store 既有）。

- [ ] **Step 1: 写迁移 up**

```sql
-- 000028_deploy_analysis.up.sql
CREATE TABLE IF NOT EXISTS appdeploy_deploy_analysis (
    app_id      TEXT PRIMARY KEY,
    analysis    JSONB NOT NULL,
    analyzed_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

- [ ] **Step 2: 写迁移 down**

```sql
-- 000028_deploy_analysis.down.sql
DROP TABLE IF EXISTS appdeploy_deploy_analysis;
```

- [ ] **Step 3: 写 store 失败测试（PG）**

```go
// analyzer_store_test.go
package appdeploy

import (
	"context"
	"testing"

	"zhiyuan-anp/platform/backend/internal/testutil"
)

func TestStore_SaveGetAnalysis(t *testing.T) {
	db := testutil.TestDB(t) // 建表走迁移（含 000028）
	st := NewStore(db)
	ctx := context.Background()
	a := &DeployAnalysis{Language: "go", AppKindGuess: "headless", Deps: []DeployDep{{Kind: "redis", Addr: "127.0.0.1:6379"}}}
	if err := st.SaveAnalysis(ctx, "app_1", a); err != nil {
		t.Fatalf("Save: %v", err)
	}
	got, err := st.GetAnalysis(ctx, "app_1")
	if err != nil {
		t.Fatalf("Get: %v", err)
	}
	if got.Language != "go" || got.AppKindGuess != "headless" || len(got.Deps) != 1 || got.Deps[0].Kind != "redis" {
		t.Errorf("回读不一致: %+v", got)
	}
}
```

> 若 `testutil.TestDB`/`NewStore` 签名与现有 `store_test.go` 用法不同，按现有 `store_test.go` 范式对齐（沿用其建表/截断方式）。

- [ ] **Step 4: 跑测试确认失败**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestStore_SaveGetAnalysis -v`
Expected: FAIL（`undefined: SaveAnalysis`/表不存在）

- [ ] **Step 5: 实现 analyzer_store.go**

```go
package appdeploy

import (
	"context"
	"encoding/json"

	"github.com/jmoiron/sqlx"
)

// SaveAnalysis 写入/覆盖某应用的部署分析（按 app_id 主键 upsert）。
func (s *Store) SaveAnalysis(ctx context.Context, appID string, a *DeployAnalysis) error {
	if a == nil {
		a = &DeployAnalysis{}
	}
	b, err := json.Marshal(a)
	if err != nil {
		return err
	}
	_, err = s.db.ExecContext(ctx, `
		INSERT INTO appdeploy_deploy_analysis (app_id, analysis, analyzed_at)
		VALUES ($1, $2, CURRENT_TIMESTAMP)
		ON CONFLICT (app_id) DO UPDATE SET analysis = EXCLUDED.analysis, analyzed_at = CURRENT_TIMESTAMP`,
		appID, b)
	return err
}

// GetAnalysis 读某应用最新部署分析；无记录返回 (nil, nil)。
func (s *Store) GetAnalysis(ctx context.Context, appID string) (*DeployAnalysis, error) {
	var raw []byte
	if err := s.db.GetContext(ctx, &raw, `SELECT analysis FROM appdeploy_deploy_analysis WHERE app_id=$1`, appID); err != nil {
		return nil, niln(err)
	}
	var a DeployAnalysis
	if err := json.Unmarshal(raw, &a); err != nil {
		return nil, err
	}
	return &a, nil
}
```

> 修正：`niln` 是笔误，应为 `return nil, nil`（无记录不报错）。最终代码用：`if err := s.db.GetContext(...); err != nil { return nil, nil }`。
> `Store` 结构需有 `db *sqlx.DB` 字段（既有）；若字段名不同（如 `*sqlx.DB` 命名为 `DB`），按现有 store.go 对齐。

- [ ] **Step 6: 跑测试确认通过**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestStore_SaveGetAnalysis -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add platform/backend/internal/db/migrations/pg/000028_deploy_analysis.up.sql platform/backend/internal/db/migrations/pg/000028_deploy_analysis.down.sql platform/backend/internal/appdeploy/analyzer_store.go platform/backend/internal/appdeploy/analyzer_store_test.go
git commit -m "feat(appdeploy): 部署分析落库(迁移000028+SaveAnalysis/GetAnalysis)" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 导入 hook + 重新分析端点

**Files:**

- Modify: `platform/backend/internal/appdeploy/handler.go`（导入完成处调 `Analyze+SaveAnalysis`；加 `Reanalyze` handler + 路由）
- Test: `platform/backend/internal/appdeploy/handler_http_test.go`（加 reanalyze 用例）

**Interfaces:**

- Consumes: Task 3 的 `Analyze`、Task 4 的 `SaveAnalysis/GetAnalysis`。
- Produces: `POST /project-spaces/:id/apps/:aid/analyze`（重新分析，返回最新 `DeployAnalysis`）。

- [ ] **Step 1: 写失败测试（reanalyze 端点）**

参照 `handler_http_test.go` 既有 `doReq` 范式追加：

```go
func TestHandler_Reanalyze(t *testing.T) {
	// 建一个带 go.mod 的托管仓库 + Application，POST /analyze 返回 DeployAnalysis
	// 沿用 newSubmitDB/h.application 装配范式（见既有 handler_http_test.go）
	// 断言：HTTP 200，body 含 "language":"go"
}
```

> 实现者按 `handler_http_test.go` 里既有"建 app + 仓库 + doReq"范式补全（参考 `TestHandler_Import_Git` 等）。断言响应 `language == "go"`。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestHandler_Reanalyze -v`
Expected: FAIL（路由未注册）

- [ ] **Step 3: 实现 handler——导入完成处分析落库**

在 `handler.go` 的导入完成处（`UpdateImportDone` 之后，`Import`/`ImportUpload` 两处）追加 best-effort 分析：

```go
	if a, err := Analyze(repoDir); err == nil && a != nil {
		_ = h.store.SaveAnalysis(c.Request.Context(), appID, a) // best-effort，失败不影响导入
	}
```

- [ ] **Step 4: 实现 Reanalyze handler + 路由**

在 `handler.go` 加：

```go
// Reanalyze 重新扫描应用仓库的部署分析并返回。
func (h *Handler) Reanalyze(c *gin.Context) {
	a, _ := h.store.Get(c.Request.Context(), c.Param("id"), c.Param("aid"))
	if a == nil {
		httpx.Err(c, 404, 40401, "应用不存在")
		return
	}
	res, err := Analyze(a.RepoDir)
	if err != nil {
		httpx.Err(c, 500, 50020, "分析失败: "+err.Error())
		return
	}
	_ = h.store.SaveAnalysis(c.Request.Context(), a.ID, res)
	httpx.OK(c, gin.H{"analysis": res})
}
```

路由（`Register` 内，按既有 apps 路由风格登记）：

```go
r.POST("/project-spaces/:id/apps/:aid/analyze", h.Reanalyze)
```

> `httpx`/`gin` import 与既有 handler 一致；若该路由需过 RBAC（`AutoRequire`），按既有 apps 路由的 op 登记方式补占位 op（参考部署权限分离计划）。

- [ ] **Step 5: 跑测试确认通过 + go build**

Run: `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/appdeploy/ -run TestHandler_Reanalyze -v && GOPATH=C:/Users/yxt/go go build ./cmd/server`
Expected: PASS + build exit 0

- [ ] **Step 6: Commit**

```bash
git add platform/backend/internal/appdeploy/handler.go platform/backend/internal/appdeploy/handler_http_test.go
git commit -m "feat(appdeploy): 导入自动分析+重新分析端点(/apps/:aid/analyze)" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: .28 部署 + 端到端验证

**Files:** 无（部署 + 验证）

- [ ] **Step 1: 打包改动 scp 到 .28**

```bash
cd <repo>
tar -czf /tmp/anp_analyzer.tar.gz \
  platform/backend/internal/appdeploy/analyzer.go \
  platform/backend/internal/appdeploy/analyzer_detect.go \
  platform/backend/internal/appdeploy/analyzer_test.go \
  platform/backend/internal/appdeploy/analyzer_store.go \
  platform/backend/internal/appdeploy/analyzer_store_test.go \
  platform/backend/internal/appdeploy/handler.go \
  platform/backend/internal/appdeploy/handler_http_test.go \
  platform/backend/internal/db/migrations/pg/000028_deploy_analysis.up.sql \
  platform/backend/internal/db/migrations/pg/000028_deploy_analysis.down.sql
scp -i ~/.ssh/miscode /tmp/anp_analyzer.tar.gz root@10.10.0.28:/root/
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp && tar -xzf /root/anp_analyzer.tar.gz && rm /root/anp_analyzer.tar.gz"
```

- [ ] **Step 2: 重建 backend（迁移自动执行 000028）**

```bash
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp/deploy && docker-compose -f docker-compose.prod.yml up --build -d backend"
```

- [ ] **Step 3: 端到端验证**

```bash
# 重新分析客服机器人(已导入)，看是否产出 Go+headless+redis/milvus/postgres+host+mismatches
# 取 token 后：
curl -s -H "Authorization: Bearer $TOKEN" http://10.10.0.28:8088/api/v1/project-spaces/<psid>/apps/<aid>/analyze | jq .data.analysis
```

Expected: `language=go`、`app_kind_guess=headless`、`deps` 含 redis/milvus/postgres、`network.host_mode_required=true`、`mismatches` 非空。

- [ ] **Step 4: push**

```bash
git push origin main
```

---

## Self-Review

**1. Spec 覆盖：** spec §3 信号（语言/构建/依赖/网络/端口/形态/差异）→ Task1-3 全覆盖；§4 schema → Task1 类型；§5 数据模型(新表) → Task4；§6 集成(导入hook+重新分析) → Task5；§7 测试(黄金用例) → Task3。✓

**2. 占位扫描：** Task5 Step1/Step3 的 handler 测试与路由 RBAC 标注了"按既有范式补全"——这是对既有代码模式的引用（不是 TBD），但实现者需查 `handler_http_test.go`/部署权限分离计划对齐。其余步骤代码完整。✓（注：Task4 Step5 的 `niln` 已在注释里修正为 `return nil,nil`。）

**3. 类型一致：** `DeployAnalysis`/`DeployDep`/`BuildAnalysis`/`NetworkAnalysis`/`PortsAnalysis` 全程一致；`Analyze(repoDir)→*DeployAnalysis`；`SaveAnalysis(ctx,appID,*DeployAnalysis)`/`GetAnalysis(ctx,appID)→*DeployAnalysis`。✓

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-01-导入部署分析.md`. 两种执行方式：

1. **Subagent-Driven（推荐）** — 每个 Task 派新 subagent，task 间 review，迭代快。
2. **Inline Execution** — 本 session 内用 executing-plans 批量执行 + checkpoint。

选哪种？
