# 编码会话隔离与 token 治理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把编码工作台（opencode）的会话从「一个开发者在一个应用上永远复用同一个、历史无限累积」改为「按需求隔离 + 同需求内轮数双限」，从根上刹住 token 用量；同时下调 opencode context limit 让 autocompact 提前生效，并清理 .28 上已膨胀的旧会话。

**Architecture:** 三层叠加治理——①后端 `codews/manager.go` 的 `ensureSession` 增加 `forceNew`，换需求时强制新建 opencode 会话，堵住「按 workDir 复用最近会话」把旧需求会话捞回的漏洞；②`SendPrompt` 在同需求会话达轮次上限时轮转到新会话，并通过 `InjectRequirement` 响应回传新 `deep_url` 让前端 iframe 跳转；③下调 DB `compute_model` 的 context/output limit，让 opencode autocompact（默认开启）在 ~64K 触发而非 ~200K。前端两处联动：进页面拉起带 `requirement_id`、inject 响应处理 `deep_url`。

**Tech Stack:** Go 1.x（`backend/internal/codews`、`appdeploy`、`compute`）/ React + Next.js + TypeScript（`frontend/app/workspace`）/ opencode 1.18.13 / PostgreSQL（`compute_model` 表）/ Go `testing` + `net/http/httptest`（后端测试）/ vitest（前端，本次前端改动用类型检查+手动验证）。

## Global Constraints

- **分支**：在 `feat/coding-session-token-control` feature 分支开发，PR 合入 main，**不直接推 main**（项目 git 分支工作流）。
- **commit**：conventional commits，中文 body 可，**每行 ≤ 100 字符**；husky/commitlint 会校验。
- **opencode.json 不可直改**：它由 `compute.WriteOpenCodeConfig` 从 DB `compute_provider/compute_model` 生成（`opencode_gen.go`），改 limit 必须改 DB + 重新生成 + kill opencode serve，否则被覆盖。
- **.28 部署**：只动 `deploy_` 容器；`ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa deploy@10.10.0.28`；流程见 `deploy/README.md`。
- **后端本地跑**：用 `scripts/dev.sh` 或 `backend/.env`，强制 PG（裸 `go run` 会 panic）；Go 用户级 GOPATH = `C:\Users\yxt\go`，报 module cache 错先查此。
- **关键常量**：`maxTurnsPerReq = 20`（同需求会话 user 提问轮次上限，可调）；opencode `limit.context` 目标值 `65536`、`limit.output` 目标值 `16384`。

## File Structure

- **Modify** `backend/internal/codews/manager.go` — `ensureSession` 加 `forceNew`；新增 `shouldForceNewForRequirement`、`countUserTurns`、`maxTurnsPerReq`；`Ensure` 计算 forceNew；`SendPrompt` 轮转。
- **Modify** `backend/internal/codews/coverage_test.go` — 更新现有 `ensureSession` 测试调用签名；新增 forceNew / 轮数轮转测试。
- **Modify** `backend/internal/appdeploy/handler.go` — `InjectRequirement` 调 `SendPrompt` 后回传当前 `deep_url`。
- **Modify** `frontend/app/workspace/workspace-frame.tsx` — 进页面拉起 body 带 `requirement_id`；`dispatchReq` 响应处理 `deep_url` 跳转。
- **DB** `compute_model` 表 — `glm-5.1` 的 `ContextWindow`/`MaxOutput` 下调。
- **无新文件**。

---

### Task 1: ensureSession 增加 forceNew 参数

**Files:**

- Modify: `backend/internal/codews/manager.go:239-282`（`ensureSession` 签名与函数体）
- Modify: `backend/internal/codews/coverage_test.go:255,270,285`（现有 3 处 `ensureSession` 调用加 `, false`）
- Test: `backend/internal/codews/coverage_test.go`（新增 forceNew 用例）

**Interfaces:**

- Produces: `func ensureSession(port int, repoDir string, forceNew bool) string` —— `forceNew=true` 时跳过 `/api/session` 列表复用，直接 `initSession(port)` 新建。

- [ ] **Step 1: 写失败测试（forceNew 跳过复用直接新建）**

追加到 `coverage_test.go`（紧跟 `TestEnsureSession_EmptyList` 之后）：

```go
// TestEnsureSession_ForceNewBypassesReuse forceNew=true 时即使 /api/session 有匹配的最近会话,
// 也应跳过复用直接 initSession 新建（换需求时不把旧需求会话捞回）。
func TestEnsureSession_ForceNewBypassesReuse(t *testing.T) {
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if r.URL.Path == "/api/session" {
			fmt.Fprint(w, `{"data":[{"id":"reuse_me","time":{"updated":9000},"location":{"directory":"/r"}}]}`)
			return
		}
		// /session (initSession)
		fmt.Fprint(w, `{"id":"fresh"}`)
	}))
	defer srv.Close()
	if got := ensureSession(portOf(t, srv.URL), "/r", true); got != "fresh" {
		t.Errorf("forceNew=true 应跳过 reuse_me 走 initSession 返回 fresh, got %q", got)
	}
}

// TestEnsureSession_ForceNewFalseStillReuses forceNew=false 时保留原"取 updated 最近匹配"行为。
func TestEnsureSession_ForceNewFalseStillReuses(t *testing.T) {
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprint(w, `{"data":[{"id":"new","time":{"updated":5000},"location":{"directory":"/r"}}]}`)
	}))
	defer srv.Close()
	if got := ensureSession(portOf(t, srv.URL), "/r", false); got != "new" {
		t.Errorf("forceNew=false 应复用 new, got %q", got)
	}
}
```

- [ ] **Step 2: 更新现有 3 处调用（编译需要）**

把 `coverage_test.go` 中三处 `ensureSession(portOf(t, srv.URL), "/r")` 改为 `ensureSession(portOf(t, srv.URL), "/r", false)`（`TestEnsureSession_PicksMatchingNewest`、`TestEnsureSession_NoMatchCallsInit`、`TestEnsureSession_EmptyList`）。

- [ ] **Step 3: 运行测试确认失败**

Run: `cd platform/backend && go test ./internal/codews/ -run TestEnsureSession -v`
Expected: 编译失败 `too many arguments in call to ensureSession`（签名还没改）。

- [ ] **Step 4: 改 ensureSession 签名与函数体**

`manager.go:239` 起替换为：

```go
// ensureSession 复用 opencode 已有会话(按 repo 目录匹配,取 updated 最近的一个);无则新建。
// forceNew=true(换需求)时跳过复用直接新建——避免按 workDir 把上一个需求的会话捞回,真正按需求隔离。
// opencode 会话持久化在磁盘(/root/.local/share/opencode),进程或后端重启后仍可据此
// 恢复开发者上次的编码上下文,而非每次打开都新建空会话。
func ensureSession(port int, repoDir string, forceNew bool) string {
	if forceNew {
		log.Printf("[codews] forceNew 新建 opencode 会话 (repo=%s)", repoDir)
		return initSession(port)
	}
	// opencode serve 刚 listen 时 HTTP handler 可能尚未就绪（请求挂起直至起来），
	// 用短超时 + 重试等就绪；就绪后 /api/session 仅几毫秒。
	for i := 0; i < 6; i++ {
		resp, err := sessionListClient.Get(fmt.Sprintf("http://127.0.0.1:%d/api/session", port))
		if err != nil {
			time.Sleep(500 * time.Millisecond)
			continue
		}
		var r struct {
			Data []struct {
				ID   string `json:"id"`
				Time struct {
					Updated int64 `json:"updated"`
				} `json:"time"`
				Location struct {
					Directory string `json:"directory"`
				} `json:"location"`
			} `json:"data"`
		}
		_ = json.NewDecoder(resp.Body).Decode(&r)
		resp.Body.Close()
		var bestID string
		var bestT int64
		match := 0
		for _, s := range r.Data {
			if s.Location.Directory == repoDir {
				match++
				if s.Time.Updated > bestT {
					bestID = s.ID
					bestT = s.Time.Updated
				}
			}
		}
		if bestID != "" {
			log.Printf("[codews] 复用 opencode 会话 %s (repo=%s, 匹配 %d/%d)", bestID, repoDir, match, len(r.Data))
			return bestID
		}
		log.Printf("[codews] 新建 opencode 会话 (repo=%s, 现有 %d 个均不匹配 directory)", repoDir, len(r.Data))
		return initSession(port)
	}
	log.Printf("[codews] 查询 opencode 会话重试6次仍失败将新建")
	return initSession(port)
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd platform/backend && go test ./internal/codews/ -run TestEnsureSession -v`
Expected: PASS（含两个新用例 + 三个旧用例）。

- [ ] **Step 6: 暂不 commit**（`Ensure` 调用处还未传 `forceNew`，编译会断；留到 Task 2 一起 commit）。仅确认 `go build ./internal/codews/` 对 `ensureSession` 本身通过——注意此步 `manager.go:183` 仍调用旧签名会编译失败，所以本 Task 先不单独 build 整包，Task 2 Step 5 一并验证。

---

### Task 2: Ensure 换需求时传 forceNew + 前端进页面带 requirement_id

**Files:**

- Modify: `backend/internal/codews/manager.go:113-199`（`Ensure`）+ 新增 `shouldForceNewForRequirement`
- Modify: `backend/internal/codews/manager.go:183`（`ensureSession` 调用传 forceNew）
- Modify: `frontend/app/workspace/workspace-frame.tsx:137-165`（进页面拉起 body + 依赖）
- Test: `backend/internal/codews/coverage_test.go`（新增 `shouldForceNewForRequirement` 用例）

**Interfaces:**

- Consumes: Task 1 的 `ensureSession(port, repoDir, forceNew)`。
- Produces: `func shouldForceNewForRequirement(old *Session, reqID string) bool`。

- [ ] **Step 1: 写失败测试（换需求判定纯函数）**

追加到 `coverage_test.go`：

```go
// TestShouldForceNewForRequirement 换需求(或旧会话无需求绑定而现在选了需求)→true;
// 首次(nil)/没选需求(reqID空)/同需求 →false。
func TestShouldForceNewForRequirement(t *testing.T) {
	cases := []struct {
		name string
		old  *Session
		req  string
		want bool
	}{
		{"首次无旧会话", nil, "r1", false},
		{"没选需求", &Session{RequirementID: "r1"}, "", false},
		{"同需求", &Session{RequirementID: "r1"}, "r1", false},
		{"换需求", &Session{RequirementID: "r1"}, "r2", true},
		{"旧会话无需求现在选了", &Session{RequirementID: ""}, "r1", true},
	}
	for _, c := range cases {
		if got := shouldForceNewForRequirement(c.old, c.req); got != c.want {
			t.Errorf("%s: got %v want %v", c.name, got, c.want)
		}
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd platform/backend && go test ./internal/codews/ -run TestShouldForceNewForRequirement -v`
Expected: FAIL `undefined: shouldForceNewForRequirement`。

- [ ] **Step 3: 新增 shouldForceNewForRequirement + 改 Ensure**

在 `manager.go` `ensureSession` 函数上方新增：

```go
// shouldForceNewForRequirement 判定是否因切换需求需强制新建 opencode 会话。
// 换需求(旧会话绑了不同 RequirementID，或旧会话无绑定而现在选了需求)→ true，杜绝跨需求历史串台/累积。
// 首次(无旧会话) / 没选需求(reqID 空) / 同需求 → false。
func shouldForceNewForRequirement(old *Session, reqID string) bool {
	return old != nil && reqID != "" && old.RequirementID != reqID
}
```

改 `Ensure`（`manager.go:122-144` 区块）。当前开头：

```go
	key := appID + ":" + userID
	m.mu.Lock()
	tool, ok := m.tools[toolName]
	if !ok {
		m.mu.Unlock()
		return nil, fmt.Errorf("未知编码工具: %s（已注册: %v）", toolName, m.Tools())
	}
	// 同开发者同工具 且 需求未变 → 复用（reqID 空=沿用现有，刷新不破坏已绑定会话）
	if s, exists := m.sessions[key]; exists && s.alive() && s.Tool == toolName && (reqID == "" || s.RequirementID == reqID) {
		m.mu.Unlock()
		return s, nil
	}
```

改为（在取 tool 后、复用判断前，取 old 并算 forceNew）：

```go
	key := appID + ":" + userID
	m.mu.Lock()
	tool, ok := m.tools[toolName]
	if !ok {
		m.mu.Unlock()
		return nil, fmt.Errorf("未知编码工具: %s（已注册: %v）", toolName, m.Tools())
	}
	old := m.sessions[key] // 旧会话（可能 nil）；用于判定换需求是否强制新建
	forceNew := shouldForceNewForRequirement(old, reqID)
	// 同开发者同工具 且 需求未变 → 复用（reqID 空=沿用现有，刷新不破坏已绑定会话）
	if s, exists := m.sessions[key]; exists && s.alive() && s.Tool == toolName && (reqID == "" || s.RequirementID == reqID) {
		m.mu.Unlock()
		return s, nil
	}
```

（后续 kill / allocPort / Unlock 逻辑不变。）

把 `manager.go:183` 的 `s.SessionID = ensureSession(port, workDir)` 改为：

```go
		s.SessionID = ensureSession(port, workDir, forceNew)
```

- [ ] **Step 4: 运行测试确认通过 + 整包编译**

Run: `cd platform/backend && go test ./internal/codews/ -v && go build ./...`
Expected: 全部 PASS，整包编译通过（Task 1 的签名断裂在此修复）。

- [ ] **Step 5: 前端进页面拉起带 requirement_id**

`workspace-frame.tsx:137-141` 当前：

```tsx
fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appID}/workspace`, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ tool }),
});
```

改为：

```tsx
fetch(`${API_BASE_URL}/project-spaces/${psID}/apps/${appID}/workspace`, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ tool, ...(selectedReq ? { requirement_id: selectedReq } : {}) }),
});
```

并把该 `useEffect` 依赖数组（`workspace-frame.tsx:165`）由 `[appID, psID, tool, reloadKey, missingParams]` 改为 `[appID, psID, tool, reloadKey, missingParams, selectedReq]`，使切换需求时重新拉起、触发后端换需求新建。

> 说明：认领需求处（`:507-514`）已带 `requirement_id` 且单独 setUrl，与本 useEffect 都打 `/workspace`；`Ensure` 对同 reqID 幂等（复用），重复调用安全。

- [ ] **Step 6: 前端类型检查**

Run: `cd platform/frontend && pnpm typecheck`（若无该 script 用 `pnpm exec tsc --noEmit`）
Expected: 无类型错误。

- [ ] **Step 7: Commit**

```bash
cd platform
git add backend/internal/codews/manager.go backend/internal/codews/coverage_test.go frontend/app/workspace/workspace-frame.tsx
git commit -m "feat(codews): 按需求隔离 opencode 会话

ensureSession 加 forceNew；Ensure 检测换需求时强制新建会话，
堵住按 workDir 复用把旧需求会话捞回的漏洞。前端进页面拉起
带 requirement_id，使切换需求触发后端新建。同需求内仍复用，
保留恢复上下文体验。"
```

---

### Task 3: 同需求轮数双限 + iframe 跳转联动

**Files:**

- Modify: `backend/internal/codews/manager.go`（新增 `maxTurnsPerReq`、`countUserTurns`；改 `SendPrompt`）
- Modify: `backend/internal/appdeploy/handler.go:497-501`（`InjectRequirement` 回传 `deep_url`）
- Modify: `frontend/app/workspace/workspace-frame.tsx:292-302`（`dispatchReq` 响应处理 `deep_url`）
- Test: `backend/internal/codews/coverage_test.go`（新增轮数计数 + 轮转用例）

**Interfaces:**

- Consumes: `LiveTranscript(port, sessionID)`（`live.go:16`）、`initSession(port)`（`manager.go:286`）、`sessionDeepURL`（`manager.go:224`）。
- Produces: `func countUserTurns(port int, sessionID string) int`；`SendPrompt` 行为变更（达上限轮转）。

- [ ] **Step 1: 写失败测试（轮数计数）**

追加到 `coverage_test.go`：

```go
// TestCountUserTurns 拉 /api/session/<id>/message 后数 role=user 的条数（工具/assistant 不计）。
func TestCountUserTurns(t *testing.T) {
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprint(w, `{"data":[
            {"type":"user","parts":[{"type":"text","text":"q1"}]},
            {"type":"assistant","parts":[{"type":"text","text":"a1"}]},
            {"type":"tool","parts":[{"type":"text","text":"..."}]},
            {"type":"user","parts":[{"type":"text","text":"q2"}]},
            {"type":"user","parts":[{"type":"text","text":"   "}]}
        ]}`)
	}))
	defer srv.Close()
	port := portOf(t, srv.URL)
	// 3 条 user（含 1 条纯空白 part 仍算一条 user 消息——LiveTranscript 会因无文本跳过整条，
	// 故实际计数 2；此处断言与 LiveTranscript 行为一致：纯空白 user 不计入）
	if got := countUserTurns(port, "ses_1"); got != 2 {
		t.Errorf("countUserTurns = %d, want 2（纯空白 user 被 LiveTranscript 过滤）", got)
	}
}

// TestCountUserTurns_FetchFails 不可达 → 0（非致命，不阻断 prompt）。
func TestCountUserTurns_FetchFails(t *testing.T) {
	if got := countUserTurns(1, "ses_1"); got != 0 {
		t.Errorf("不可达应返回 0, got %d", got)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd platform/backend && go test ./internal/codews/ -run TestCountUserTurns -v`
Expected: FAIL `undefined: countUserTurns`。

- [ ] **Step 3: 新增 maxTurnsPerReq + countUserTurns，改 SendPrompt**

在 `manager.go` 顶部 `const` 块（`:28-32`）追加：

```go
	// maxTurnsPerReq 同一需求会话内 user 提问轮次上限：超过则轮转到新会话，
	// 防止单需求历史无限累积烧 token（与 opencode autocompact 互补）。可按需调。
	maxTurnsPerReq = 20
```

在 `SendPrompt` 上方新增：

```go
// countUserTurns 数 opencode 会话中 role=user 的消息条数（≈用户提问轮次）。
// 读失败/无会话返回 0（非致命，不阻断发 prompt）。
func countUserTurns(port int, sessionID string) int {
	msgs, err := LiveTranscript(port, sessionID)
	if err != nil || len(msgs) == 0 {
		return 0
	}
	n := 0
	for _, m := range msgs {
		if m.Role == "user" {
			n++
		}
	}
	return n
}
```

把 `SendPrompt`（`manager.go:337-351`）整体替换为：

```go
// SendPrompt 向某开发者当前 opencode 会话发送一条 prompt(注入需求/指令),
// opencode AI 在工作台实时响应(流式),开发者可看编码过程并随时介入。
// 同需求会话累计 user 轮次达 maxTurnsPerReq 时，先轮转到新会话再发，避免历史滚雪球。
func (m *Manager) SendPrompt(appID, userID, text string) error {
	s := m.Get(appID, userID)
	if s == nil || s.SessionID == "" {
		return fmt.Errorf("无活跃编码会话(请先打开工作台)")
	}
	// 轮次双限：同需求会话达上限 → 新建会话轮转（DeepURL 一并由 handler 回传前端跳转）
	if countUserTurns(s.Port, s.SessionID) >= maxTurnsPerReq {
		if newID := initSession(s.Port); newID != "" {
			s.SessionID = newID
			s.DeepURL = sessionDeepURL(s.URL, s.RepoDir, newID)
			log.Printf("[codews] 需求会话达 %d 轮, 轮转到新会话 %s", maxTurnsPerReq, newID)
		}
	}
	body, _ := json.Marshal(map[string]interface{}{
		"prompt": map[string]string{"text": text},
	})
	resp, err := wsHTTPClient.Post(fmt.Sprintf("http://127.0.0.1:%d/api/session/%s/prompt", s.Port, s.SessionID), "application/json", bytes.NewReader(body))
	if err != nil {
		return err
	}
	defer resp.Body.Close()
	return nil
}
```

- [ ] **Step 4: 运行测试确认通过（含原有 SendPrompt 用例）**

Run: `cd platform/backend && go test ./internal/codews/ -run "TestCountUserTurns|TestSendPrompt" -v`
Expected: PASS。

- [ ] **Step 5: InjectRequirement 回传 deep_url**

`handler.go:497-501` 当前：

```go
	if err := h.codeWS.SendPrompt(aid, user, in.Prompt); err != nil {
		httpx.Err(c, 500, 50021, err.Error())
		return
	}
	httpx.OK(c, gin.H{"injected": true, "note": "需求已发给 opencode,在工作台看 AI 实时编码"})
```

改为（轮转后 Get 拿最新 DeepURL 回传，前端据此跳 iframe）：

```go
	if err := h.codeWS.SendPrompt(aid, user, in.Prompt); err != nil {
		httpx.Err(c, 500, 50021, err.Error())
		return
	}
	deepURL := ""
	if s := h.codeWS.Get(aid, user); s != nil {
		deepURL = s.DeepURL
	}
	httpx.OK(c, gin.H{"injected": true, "deep_url": deepURL, "note": "需求已发给 opencode,在工作台看 AI 实时编码"})
```

- [ ] **Step 6: 前端 dispatchReq 处理 deep_url 跳转**

`workspace-frame.tsx:292-302` 当前在 `r.code !== 0` 判断之后直接 `setTaskMsg(...)`。在 `setTaskMsg` 之前插入跳转：

```tsx
if (r.code !== 0) {
  setTaskMsg(r.message || "失败");
  setDispatching(false);
  return;
}
// 轮数轮转后后端回传新 deep_url → 跳 iframe 到新会话
if (r.data?.deep_url && r.data.deep_url !== url) {
  setUrl(r.data.deep_url);
}
setTaskMsg(
  next
    ? `✅ 已发送子任务: ${next.text}\n做完后在左侧 checklist 打勾,再点「🤖AI编码」做下一个`
    : "✅ 需求已发给 opencode → 在右侧工作台看 AI 实时编码,可随时介入/纠偏"
);
```

- [ ] **Step 7: 前端类型检查 + 后端构建**

Run: `cd platform/frontend && pnpm exec tsc --noEmit` 然后 `cd platform/backend && go build ./...`
Expected: 均无错误。

- [ ] **Step 8: Commit**

```bash
cd platform
git add backend/internal/codews/manager.go backend/internal/codews/coverage_test.go backend/internal/appdeploy/handler.go frontend/app/workspace/workspace-frame.tsx
git commit -m "feat(codews): 同需求会话轮数双限 + 轮转联动

SendPrompt 在同需求会话达 maxTurnsPerReq(20) 轮时轮转到新会话,
InjectRequirement 回传 deep_url,前端 iframe 据此跳转新会话。
防止单需求长对话历史无限累积。"
```

---

### Task 4: 下调 opencode context/output limit（经 DB）

**Files:**

- DB: `compute_model` 表（`glm-5.1` 所在行）
- 重生成: `opencode.json`（由 `compute.WriteOpenCodeConfig` 产出，**不手改文件**）

**Interfaces:**

- Consumes: `compute_model.ContextWindow`/`MaxOutput` → `opencode_gen.go:81-92` → `opencode.json` `limit.context`/`limit.output`。

> 背景依据：opencode 1.18 autocompact 默认开启（`auto=true`），只在「接近 limit」才触发。当前 `limit.context=204800` → 压缩点在 ~200K，历史一路累积才压，这是 token 爆的机制。下调到 65536 → 压缩点提前到 ~64K。

- [ ] **Step 1: 备份当前值并确认目标行**

连 .28（或本地 PG，依实际部署）查 glm-5.1 当前行：

```bash
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa deploy@10.10.0.28 'docker exec deploy_postgres psql -U <user> -d <db> -c "SELECT id, name, context_window, max_output FROM compute_model WHERE name LIKE '\''%glm-5%'\'' OR display_name LIKE '\''%GLM-5%'\'';"'
```

（`<user>/<db>` 见 `backend/.env` 或 `deploy/README.md`；记下旧值用于回滚。）

Expected: 看到 `glm-5.1` 行，`context_window≈204800`、`max_output≈131072`。

- [ ] **Step 2: 下调 DB 值**

```bash
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa deploy@10.10.0.28 'docker exec deploy_postgres psql -U <user> -d <db> -c "UPDATE compute_model SET context_window=65536, max_output=16384 WHERE name='\''glm-5.1'\'';"'
```

> 若 .28 上 glm-5.1 的 `name` 字段实际带 provider 前缀，按 Step 1 查到的真实 `name` 调整 WHERE。

- [ ] **Step 3: 重新生成 opencode.json + kill opencode serve 让新 limit 生效**

按项目运维惯例（改 provider/model 后须重新生成 + kill serve）。在 .28 的 deploy 后端容器内触发重生成（具体入口依 `cmd/server/main.go:107-125` 的启动复制逻辑——重启后端容器即重生成并覆盖 `$HOME/.config/opencode/opencode.json`），然后 kill 已在跑的 opencode serve 进程，使其下次 `Ensure` 用新配置重启：

```bash
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa deploy@10.10.0.28 'docker restart deploy_backend && docker exec deploy_backend sh -c "pkill -f \"opencode serve\" || true"'
```

- [ ] **Step 4: 校验生成的 opencode.json 已是 65536/16384**

```bash
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa deploy@10.10.0.28 'docker exec deploy_backend sh -c "cat \$HOME/.config/opencode/opencode.json | grep -A3 glm-5.1"'
```

Expected: `"limit": { "context": 65536, "output": 16384 }`。

- [ ] **Step 5: 无代码 commit（DB/运维变更，记录到 commit 说明）**

```bash
cd platform
git commit --allow-empty -m "chore(compute): glm-5.1 context 20万→6万5 output 13万→1万6

经 DB compute_model 下调 + 重生成 opencode.json + kill serve。
让 opencode autocompact 在 ~64K 触发而非 ~200K。"
```

---

### Task 5: .28 部署后端 + 清理已膨胀旧会话 + 验证

**Files:**

- 部署: `deploy_backend` 容器（只动 deploy_ 容器）
- 清理: opencode 磁盘会话 `/root/.local/share/opencode`

**Interfaces:**

- Consumes: Task 1-3 的后端改动（合入 main 后部署）；Task 4 的 limit 调整。

- [ ] **Step 1: PR 合入 main**

把 `feat/coding-session-token-control` 推远端、开 PR、过 review/CI 后合 main（遵循项目分支工作流，不直推 main）。

- [ ] **Step 2: 部署后端到 .28（只动 deploy\_ 容器）**

按 `deploy/README.md` 流程构建并更新 `deploy_backend`（前端本次无后端依赖变更可不重发，若有 typecheck/build 产物变更一并部署）。确认 `deploy_backend` 重启后健康。

- [ ] **Step 3: 清理 .28 上已膨胀的旧 opencode 会话**

代码改动只对新建会话生效；.28 上现存的超大会话需手动清。优先用 opencode CLI：

```bash
ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa deploy@10.10.0.28 'docker exec deploy_backend sh -c "opencode session list 2>/dev/null | head -40"'
```

找到体积大/updated 最旧的本应用会话后删除（命令以 1.18 实际子命令为准，`opencode session --help` 确认；若无 delete 子命令，直接清 storage）：

```bash
# 方式A：CLI（首选）
docker exec deploy_backend opencode session delete <sessionID>
# 方式B：清 storage（兜底，会清掉该工具实例全部会话——谨慎，确认无需要保留的上下文）
docker exec deploy_backend sh -c "rm -rf /root/.local/share/opencode/storage/*"
```

> 清理后开发者下次打开工作台，`ensureSession` 因无匹配会话自然新建——配合 Task 2/3 后续按需求+轮数隔离。

- [ ] **Step 4: 端到端验证（一次完整编码任务前后对比）**

在 .28 工作台选一个需求，连续派发若干子任务编码，观察：

1. 切换需求时日志出现 `forceNew 新建 opencode 会话`（Task 2 生效）。
2. 同需求满 20 轮后日志出现 `需求会话达 20 轮, 轮转到新会话` 且 iframe 自动跳新会话（Task 3 生效）。
3. 智谱后台/账单：单次编码任务 input_tokens 较改动前显著下降（对比 Task 4 Step 1 记录的基线）。

- [ ] **Step 5: 回滚预案（如效果异常）**

- 轮数过激/隔离问题：`maxTurnsPerReq` 调大或临时把 `shouldForceNewForRequirement` 恒返回 false 重新部署。
- limit 过低致长任务截断：DB 把 `context_window` 调回（如 131072）重生成 + kill serve。
- 旧值回滚：Task 4 Step 1 记录的原 `context_window/max_output` UPDATE 回去。

---

## Self-Review

**1. Spec coverage：**

- 按需求隔离 → Task 1（forceNew 基础）+ Task 2（Ensure 信号 + 前端 reqID）✅
- 同需求轮数双限 → Task 3（SendPrompt 轮转 + handler 回传 deep_url + 前端跳转）✅
- 下调 limit 让 autocompact 提前 → Task 4 ✅
- .28 已膨胀会话清理 + 部署 + 验证 + 回滚 → Task 5 ✅
- 前端两处联动（进页面 reqID / inject deep_url）→ Task 2 Step 5 / Task 3 Step 6 ✅

**2. Placeholder scan：** 无 TBD/TODO；DB 的 `<user>/<db>` 与容器名 `deploy_postgres`/`deploy_backend` 依 `.env` 与 `deploy/README.md` 实际值填入——这是运行时参数非占位（已在 Step 注明来源）。`opencode session delete` 子命令已注明以 `--help` 实际为准并给兜底方式。

**3. Type consistency：** `ensureSession(port, repoDir, forceNew bool)` 在 Task 1 定义、Task 2 Step 3 调用一致；`shouldForceNewForRequirement(old *Session, reqID string) bool` 定义与测试一致；`countUserTurns(port, sessionID) int` 定义与测试一致；`maxTurnsPerReq` 常量名全程一致；前端 `r.data.deep_url` 与 handler 回传的 `gin.H{"deep_url": ...}` 键名一致。
