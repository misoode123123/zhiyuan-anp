# 依赖供给统一 P1：KindSpec 注册表基础 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 mwsupply 引入 KindSpec 注册表，把 redis/milvus 供给从 per-kind switch 重构为 registry 驱动，**零行为变化**（既有 P1–P5 测试当安全网），为后续 pg/未来 kind 零改主逻辑铺路。

**Architecture:** 新增 `KindSpec` 类型（每 kind 声明 AddrEnv / token 语义 / dedicated 三件套 / shared-token 分配 / env 派生）+ `kindRegistry`。redis/milvus 的现有 docker/naming/supply 逻辑收敛成两个 spec，由 `BuildSpecs(deps)` 在 `NewReconciler` 时注入 store/flusher/ready/docker 构造并注册。`supplyOne`/`supplyShared`/`supplyDedicated`/`Cleanup`/`ReleaseDep` 改为查 spec 分派，消除 kind switch。

**Tech Stack:** Go 1.24（`internal/mwsupply`）；PG（anp_test，禁 SQLite）；docker CLI（dedicated）；表测试 + 既有 P1–P5 回归。

**Spec:** `docs/superpowers/specs/2026-08-04-依赖供给统一三策略-design.md`

## Global Constraints

- 禁 SQLite，只用 PG；纯函数单测本机跑，PG 测试连 .28 anp_test（`GOPATH=C:/Users/yxt/go`）。
- go 命令前缀 `GOPATH=C:/Users/yxt/go`；全量回归 `go test -p 1 ./internal/mwsupply/...`。
- commit `refactor(mwsupply): 中文` + 末尾 `Co-Authored-By: Claude <noreply@anthropic.com>`。
- **零行为变化**：重构后既有 mwsupply 测试（P1–P5）必须全绿——这是 P1 的硬约束和安全网。
- 改完 scp 到 .28 重建 backend 验证（Task 3）。
- **自编译**：每个 task 结束必须 `go build ./cmd/server` 通过（任务切法已保证：supply.go 的 helper 搬迁与调用方改造在同 task 内同步完成）。

## File Structure

| 文件                                                           | 责任                                                                                                                                                                                                                                                                                                                        | 动作           |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| `internal/mwsupply/kindspec.go`                                | `KindSpec`/`TokenSemantics`/`EnvKV` + `kindRegistry` + `RegisterKind/LookupKind`                                                                                                                                                                                                                                            | 新建（Task 1） |
| `internal/mwsupply/kindspec_test.go`                           | registry 行为测                                                                                                                                                                                                                                                                                                             | 新建（Task 1） |
| `internal/mwsupply/spec_redis.go`                              | redis KindSpec（含搬入的 shared-token 分配 + docker dedicated 三件套 + env 派生）                                                                                                                                                                                                                                           | 新建（Task 2） |
| `internal/mwsupply/spec_milvus.go`                             | milvus KindSpec                                                                                                                                                                                                                                                                                                             | 新建（Task 2） |
| `internal/mwsupply/spec_redis_test.go` / `spec_milvus_test.go` | spec 纯字段/env 测                                                                                                                                                                                                                                                                                                          | 新建（Task 2） |
| `internal/mwsupply/specs.go`                                   | `BuildSpecs(store,flusher,ready,docker)` 注册 redis+milvus                                                                                                                                                                                                                                                                  | 新建（Task 2） |
| `internal/mwsupply/supply.go`                                  | `supplyShared`/`supplyDedicated`/`supplyOne`(bind_existing)/`Cleanup`/`ReleaseDep` 改 spec 分派；删除 `writeSharedEnv`/`writeDedicatedEnv`/`launchDedicated`/`waitDedicatedReady`/`rmDedicated`/`allocAndClaimShared`/`allocRedisDB`/`allocMilvusPrefix`/`claimWithRetry`（逻辑搬入 spec）；`NewReconciler` 调 `BuildSpecs` | 改（Task 2）   |

---

### Task 1: KindSpec 类型 + registry

**Files:**

- Create: `internal/mwsupply/kindspec.go`
- Test: `internal/mwsupply/kindspec_test.go`

**Interfaces:**

- Produces: `KindSpec` struct；`EnvKV`；`TokenSemantics` 常量；`RegisterKind(KindSpec)`；`LookupKind(kind string) (KindSpec, bool)`；`kindRegistry`（包内 map）。Task 2 的 spec 构造与 supply.go 重构消费这些。

- [ ] **Step 1: 写失败测试** — `kindspec_test.go`

```go
package mwsupply

import "testing"

func TestRegisterAndLookupKind(t *testing.T) {
	resetRegistry(t)
	k := "testkind-x"
	RegisterKind(KindSpec{Kind: k, AddrEnv: "TESTKIND_ADDR", DisplayName: "Test"})

	got, ok := LookupKind(k)
	if !ok {
		t.Fatalf("LookupKind(%q) ok=false，应已注册", k)
	}
	if got.AddrEnv != "TESTKIND_ADDR" {
		t.Fatalf("AddrEnv=%q want TESTKIND_ADDR", got.AddrEnv)
	}
	if _, ok := LookupKind("not-registered"); ok {
		t.Fatal("未注册 kind 应 ok=false")
	}
}

// resetRegistry 清空 registry（测试隔离用）。
func resetRegistry(t *testing.T) {
	t.Helper()
	kindRegistry = map[string]KindSpec{}
}
```

- [ ] **Step 2: 跑确认失败** — `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/mwsupply/ -run TestRegisterAndLookupKind -v` → FAIL（`KindSpec`/`RegisterKind` 未定义）。

- [ ] **Step 3: 实现** — `kindspec.go`

```go
package mwsupply

import "context"

// TokenSemantics 描述 shared 策略下"实例内隔离 token"的语义（文档/诊断用；实际分配由 AllocSharedToken）。
type TokenSemantics string

const (
	TokenDBNumber         TokenSemantics = "db-number"         // redis：db 号池
	TokenCollectionPrefix TokenSemantics = "collection-prefix" // milvus：collection 前缀
	// 未来：TokenDatabaseName(pg) / TokenTopicPrefix(kafka) ...
)

// EnvKV 单条注入 env（source=platform）。
type EnvKV struct {
	Key      string
	Value    string
	IsSecret bool
}

// KindSpec 每 kind 的供给规格。registry 驱动：供给主逻辑查 spec 分派，无 kind switch。
// 依赖（store/flusher/ready/docker）由 BuildSpecs 在构造闭包时捕获。
type KindSpec struct {
	Kind        string
	DisplayName string
	AddrEnv     string // 主连接 env 键：REDIS_ADDR / MILVUS_ADDR
	Token       TokenSemantics

	// dedicated 三件套（起 / 探 / 收）
	PortRange        func() (int, int)                                                          // 端口池上下界
	ContainerName    func(short string) string                                                 // dedicated 容器名
	LaunchDedicated  func(ctx context.Context, name string, port int) (authRef string, err error)
	ReadyDedicated   func(ctx context.Context, name string, port int, authRef string) error
	CleanupDedicated func(ctx context.Context, name string) error

	// shared：为某实例分配一个隔离 token（per-kind 策略；redis=db 号池 / milvus=随机前缀）。
	// 实现内部用 store.ClaimSharedToken 写库；返回 token + nil。
	AllocSharedToken func(ctx context.Context, appID, psID, instID string, inst *ServiceInstance) (string, error)

	// env 派生（主连接 env 由调用方写 spec.AddrEnv，这里只给 token/authRef 派生项）
	SharedEnv    func(token string, inst *ServiceInstance) []EnvKV // redis→[REDIS_DB(,+REDIS_PASSWORD)]；milvus→[MILVUS_COLLECTION_PREFIX]
	DedicatedEnv func(authRef string) []EnvKV                       // redis→[REDIS_PASSWORD]；milvus→[]
}

var kindRegistry = map[string]KindSpec{}

func RegisterKind(s KindSpec) { kindRegistry[s.Kind] = s }

func LookupKind(kind string) (KindSpec, bool) {
	s, ok := kindRegistry[kind]
	return s, ok
}
```

- [ ] **Step 4: 跑确认通过** — 同上 → PASS。

- [ ] **Step 5: Commit** — `refactor(mwsupply): 加 KindSpec 类型与 registry`

---

### Task 2: 完整迁移——redis/milvus spec + supply.go 改 registry 驱动

> 这是 P1 的核心重构。一个内聚 task：新建 redis/milvus spec（搬入现有 alloc/docker/env 逻辑）+ 重构 supply.go 所有 kind switch 为 spec 分派 + BuildSpecs 装配。**测试周期 = 既有 P1–P5 测试保持全绿 + 新增 spec 纯字段测**。helper 搬迁与调用方改造同 task 内同步，保证中间可编译。

**Files:**

- Create: `internal/mwsupply/spec_redis.go`, `internal/mwsupply/spec_milvus.go`, `internal/mwsupply/specs.go`
- Create: `internal/mwsupply/spec_redis_test.go`, `internal/mwsupply/spec_milvus_test.go`
- Modify: `internal/mwsupply/supply.go`

**Interfaces:**

- Consumes: Task 1 的 `KindSpec`/`RegisterKind`/`LookupKind`/`EnvKV`；现有 `Store`（store.go）、`MWDockerRunner`（docker.go）、`DBFlusher`/`ReadyChecker`（supply.go 接口）、`ServiceInstance`、`ConnStr`、`ParseDBRange`/`pickLowestFree`/`isUniqueViolation`/`genMilvusPrefix`/`genPassword`/`genShortID`/`allocPort`/端口与命名常量（naming.go）。
- Produces: `redisSpec(st *Store, flusher DBFlusher, ready ReadyChecker, docker MWDockerRunner) KindSpec`；`milvusSpec(st *Store, docker MWDockerRunner) KindSpec`；`BuildSpecs(store, flusher, ready, docker)`。

- [ ] **Step 1: 写 spec 纯字段/env 失败测试** — `spec_redis_test.go`

```go
package mwsupply

import "testing"

func TestRedisSpec_FieldsAndEnv(t *testing.T) {
	s := redisSpec(nil, nil, nil, nil) // 纯字段测：deps 传 nil
	if s.Kind != "redis" || s.AddrEnv != "REDIS_ADDR" || s.Token != TokenDBNumber {
		t.Fatalf("redis 基本字段错: %+v", s)
	}
	if lo, hi := s.PortRange(); lo != mwPortMin || hi != mwPortMax {
		t.Fatalf("PortRange=%d-%d want %d-%d", lo, hi, mwPortMin, mwPortMax)
	}
	if got := s.ContainerName("1a2b3c"); got != "mwredis-1a2b3c" {
		t.Fatalf("ContainerName=%q want mwredis-1a2b3c", got)
	}
	envs := s.SharedEnv("3", &ServiceInstance{Kind: "redis"})
	if len(envs) != 1 || envs[0].Key != "REDIS_DB" || envs[0].Value != "3" {
		t.Fatalf("SharedEnv(db=3)=%+v want [REDIS_DB=3]", envs)
	}
	envs = s.SharedEnv("3", &ServiceInstance{Kind: "redis", AuthRef: "pw"})
	if len(envs) != 2 || envs[1].Key != "REDIS_PASSWORD" || !envs[1].IsSecret || envs[1].Value != "pw" {
		t.Fatalf("SharedEnv(+auth)=%+v want 含 REDIS_PASSWORD=pw(secret)", envs)
	}
	if d := s.DedicatedEnv("pw"); len(d) != 1 || d[0].Key != "REDIS_PASSWORD" || d[0].Value != "pw" {
		t.Fatalf("DedicatedEnv=%+v want [REDIS_PASSWORD=pw]", d)
	}
}
```

`spec_milvus_test.go`：

```go
package mwsupply

import "testing"

func TestMilvusSpec_FieldsAndEnv(t *testing.T) {
	s := milvusSpec(nil, nil)
	if s.Kind != "milvus" || s.AddrEnv != "MILVUS_ADDR" || s.Token != TokenCollectionPrefix {
		t.Fatalf("milvus 基本字段错: %+v", s)
	}
	if lo, hi := s.PortRange(); lo != milvusPortMin || hi != milvusPortMax {
		t.Fatalf("PortRange=%d-%d want %d-%d", lo, hi, milvusPortMin, milvusPortMax)
	}
	if got := s.ContainerName("1a2b3c"); got != "mwmilvus-1a2b3c" {
		t.Fatalf("ContainerName=%q want mwmilvus-1a2b3c", got)
	}
	if envs := s.SharedEnv("app1a2b_", &ServiceInstance{Kind: "milvus"}); len(envs) != 1 || envs[0].Key != "MILVUS_COLLECTION_PREFIX" || envs[0].Value != "app1a2b_" {
		t.Fatalf("SharedEnv=%+v want [MILVUS_COLLECTION_PREFIX=app1a2b_]", envs)
	}
	if len(s.DedicatedEnv("")) != 0 {
		t.Fatalf("milvus DedicatedEnv 应空，得 %+v", s.DedicatedEnv(""))
	}
}
```

- [ ] **Step 2: 跑确认失败** — `go test ./internal/mwsupply/ -run 'TestRedisSpec_FieldsAndEnv|TestMilvusSpec_FieldsAndEnv' -v` → FAIL（`redisSpec`/`milvusSpec` 未定义）。

- [ ] **Step 3: 实现 redis spec** — `spec_redis.go`。把 supply.go 的 `allocRedisDB`+`claimWithRetry` 逻辑**逐字搬入** `AllocSharedToken` 闭包（捕获 st/flusher）；dedicated 三件套接 `docker`/`ready`：

```go
package mwsupply

import (
	"context"
	"fmt"
	"strconv"
)

// redisSpec 构造 redis 的 KindSpec。AllocSharedToken 搬入 supply.go 原 allocRedisDB+claimWithRetry
// （db 号池 + flush + 撞号重试），dedicated 三件套接 docker/ready。
func redisSpec(st *Store, flusher DBFlusher, ready ReadyChecker, docker MWDockerRunner) KindSpec {
	return KindSpec{
		Kind: "redis", DisplayName: "Redis", AddrEnv: "REDIS_ADDR", Token: TokenDBNumber,
		PortRange:     func() (int, int) { return mwPortMin, mwPortMax },
		ContainerName: func(short string) string { return "mwredis-" + short },
		LaunchDedicated: func(ctx context.Context, name string, port int) (string, error) {
			pwd := genPassword()
			if err := docker.RunRedisContainer(ctx, name, pwd, port); err != nil {
				return "", err
			}
			return pwd, nil
		},
		ReadyDedicated: func(ctx context.Context, _ string, port int, authRef string) error {
			// 复用既有 readyPingTimeout（naming.go）。host 由调用方环境（r.host）—此处仅签名对齐，
			// 实际拨号需 r.host；supplyDedicated 改造时改为传 host（见 Step 6 说明）。
			readyCtx, cancel := context.WithTimeout(ctx, readyPingTimeout)
			defer cancel()
			return ready.Ping(readyCtx, "", port, authRef) // host 占位：见 Step 6 处理
		},
		CleanupDedicated: func(ctx context.Context, name string) error { return docker.RmForce(ctx, name) },
		SharedEnv: func(token string, inst *ServiceInstance) []EnvKV {
			out := []EnvKV{{Key: "REDIS_DB", Value: token}}
			if inst != nil && inst.AuthRef != "" {
				out = append(out, EnvKV{Key: "REDIS_PASSWORD", Value: inst.AuthRef, IsSecret: true})
			}
			return out
		},
		DedicatedEnv: func(authRef string) []EnvKV {
			return []EnvKV{{Key: "REDIS_PASSWORD", Value: authRef, IsSecret: true}}
		},
		// AllocSharedToken：逐字搬 supply.go:allocRedisDB + claimWithRetry（保留 flush+重试语义）。
		AllocSharedToken: func(ctx context.Context, appID, psID, instID string, inst *ServiceInstance) (string, error) {
			lo, hi, ok := ParseDBRange(inst.Isolation)
			if !ok {
				return "", fmt.Errorf("shared 实例 isolation 缺 db_range")
			}
			allocated, _ := st.AllocatedTokens(ctx, instID)
			first, found := pickLowestFree(lo, hi, allocated)
			if !found {
				return "", fmt.Errorf("shared redis db 号耗尽（池 %d-%d）", lo, hi)
			}
			return redisClaimWithRetry(ctx, st, flusher, appID, psID, inst, lo, hi, first, allocated)
		},
	}
}

// redisClaimWithRetry 逐字搬自 supply.go 原 claimWithRetry（flush + ClaimSharedToken + 撞号换号重试）。
// 签名加 st/flusher 入参（原为 Reconciler 方法，现闭包外提）。函数体与原 claimWithRetry 一致。
func redisClaimWithRetry(ctx context.Context, st *Store, flusher DBFlusher,
	appID, psID string, inst *ServiceInstance, lo, hi int, first string, allocated []string) (string, error) {
	token := first
	seen := append([]string{}, allocated...)
	for attempts := 0; attempts <= (hi - lo + 1); attempts++ {
		dbNum, _ := strconv.Atoi(token)
		if ferr := flusher.FlushDB(ctx, inst.Host, inst.Port, inst.AuthRef, dbNum); ferr != nil {
			// flush best-effort：非首次分配正确性所需；记日志由调用方（原 r.log）做——此处静默继续。
		}
		err := st.ClaimSharedToken(ctx, appID, psID, "redis", inst.ID, token, "REDIS_ADDR")
		if err == nil {
			return token, nil
		}
		if !isUniqueViolation(err) {
			return "", err
		}
		seen = append(seen, token)
		next, found := pickLowestFree(lo, hi, seen)
		if !found {
			return "", fmt.Errorf("shared redis db 号耗尽（并发重试）")
		}
		token = next
	}
	return "", fmt.Errorf("claim 重试用尽")
}
```

> **flush 日志**：原 `claimWithRetry` 用 `r.log.Warn` 记 flush 失败。搬迁后无 r.log，先静默（best-effort，不影正确性）。若需保留日志，supplyDedicated/supplyShared 改造时把 `*zap.Logger` 透传给 spec 构造（可选，本 task 可不带）。

- [ ] **Step 4: 实现 milvus spec** — `spec_milvus.go`。搬入 supply.go 原 `allocMilvusPrefix`：

```go
package mwsupply

import (
	"context"
	"fmt"
)

func milvusSpec(st *Store, docker MWDockerRunner) KindSpec {
	return KindSpec{
		Kind: "milvus", DisplayName: "Milvus", AddrEnv: "MILVUS_ADDR", Token: TokenCollectionPrefix,
		PortRange:     func() (int, int) { return milvusPortMin, milvusPortMax },
		ContainerName: func(short string) string { return "mwmilvus-" + short },
		LaunchDedicated:  func(ctx context.Context, base string, port int) (string, error) { return "", docker.RunMilvusStack(ctx, base, port) },
		ReadyDedicated:   func(ctx context.Context, base string, _ int, _ string) error { return docker.MilvusReady(ctx, base, milvusReadyTimeout) },
		CleanupDedicated: func(ctx context.Context, base string) error { return docker.RmMilvusStack(ctx, base) },
		SharedEnv: func(token string, _ *ServiceInstance) []EnvKV {
			return []EnvKV{{Key: "MILVUS_COLLECTION_PREFIX", Value: token}}
		},
		DedicatedEnv: func(_ string) []EnvKV { return nil },
		// AllocSharedToken：逐字搬 supply.go:allocMilvusPrefix（随机前缀 + ClaimSharedToken + 撞号重生）。
		AllocSharedToken: func(ctx context.Context, appID, psID, instID string, inst *ServiceInstance) (string, error) {
			allocated, _ := st.AllocatedTokens(ctx, instID)
			taken := make(map[string]bool, len(allocated))
			for _, t := range allocated {
				taken[t] = true
			}
			for attempts := 0; attempts < 4; attempts++ {
				token := genMilvusPrefix()
				if taken[token] {
					continue
				}
				err := st.ClaimSharedToken(ctx, appID, psID, "milvus", instID, token, "MILVUS_ADDR")
				if err == nil {
					return token, nil
				}
				if !isUniqueViolation(err) {
					return "", err
				}
				taken[token] = true
			}
			return "", fmt.Errorf("milvus 前缀分配重试用尽（并发撞号）")
		},
	}
}
```

- [ ] **Step 5: 实现 BuildSpecs** — `specs.go`

```go
package mwsupply

// BuildSpecs 构造并注册 redis/milvus 的 KindSpec（捕获 store/flusher/ready/docker）。
// NewReconciler 调一次。加新 kind（pg）= 这里加一行 + 一个 spec 构造函数。
func BuildSpecs(store *Store, flusher DBFlusher, ready ReadyChecker, docker MWDockerRunner) {
	RegisterKind(redisSpec(store, flusher, ready, docker))
	RegisterKind(milvusSpec(store, docker))
}
```

- [ ] **Step 6: 重构 supply.go** —— 改造供给主逻辑为 spec 分派，删除被搬走的 helper：

  1. `NewReconciler` 末尾调 `BuildSpecs(store, flusher, ready, docker)`。
  2. `supplyOne` 的 bind_existing 段：`spec, ok := LookupKind(dep.Kind)`（未注册→mkBind failed）；`envKey := EnvKeyFor(dep.Kind)` 删除，改用 `spec.AddrEnv`。
  3. `supplyShared`：改为 spec 驱动——复用判定不变；新分配调 `spec.AllocSharedToken(ctx, appID, psID, inst.ID, inst)`；写 env 用新 helper `writeSpecEnv(ctx, appID, spec, inst, token)`（写 `spec.AddrEnv`=ConnStr + 遍历 `spec.SharedEnv(token, inst)`）。
  4. `supplyDedicated`：spec 驱动——`spec.PortRange()`/`spec.ContainerName(short)`/`spec.LaunchDedicated(ctx,base,port)`/`spec.ReadyDedicated(...)`/`spec.CleanupDedicated(ctx,base)`；写 env 用 `writeDedicatedEnvSpec(ctx, appID, spec, inst)`（`spec.AddrEnv`=ConnStr + `spec.DedicatedEnv(inst.AuthRef)`）。
     - ready 拨号 host：`ReadyDedicated` 闭包内需 `r.host`——把 `ReadyDedicated` 签名改为接 host，或在 `supplyDedicated` 内直接调 `r.ready.Ping`/`r.docker.MilvusReady`（host 由 r.host 给）。**推荐**：`KindSpec.ReadyDedicated` 闭包捕获 host（spec 构造加 host 参数），保持 supplyDedicated 纯 spec 调用。故 `redisSpec`/`milvusSpec`/`BuildSpecs`/`NewReconciler` 透传 `host string`。Step 3/4/5 的构造签名相应加 `host`（redis 的 ready 闭包用 `ready.Ping(ctx, host, port, authRef)`）。
  5. `Cleanup`/`ReleaseDep`：kind switch 改 `spec, ok := LookupKind(b.ServiceKind)` → `spec.CleanupDedicated(ctx, inst.ContainerName)`（redis/milvus 统一走 spec.CleanupDedicated，无需 switch）。
  6. 删除：`writeSharedEnv`/`writeDedicatedEnv`/`launchDedicated`/`waitDedicatedReady`/`rmDedicated`/`allocAndClaimShared`/`allocRedisDB`/`allocMilvusPrefix`/`claimWithRetry`（逻辑已搬入 spec）。
  7. `Reconciler` 若不再用 `flusher`/`ready` 字段（全搬入 spec 闭包），可保留字段不删（避免大改）；或删除并调整构造。**推荐保留字段**（最小改动，spec 闭包与字段并存不冲突）。

  新增 helper（supply.go 内）：

```go
func (r *Reconciler) writeSpecEnv(ctx context.Context, appID string, spec KindSpec, inst *ServiceInstance, token string) {
	_ = r.env.UpsertEnv(ctx, appID, spec.AddrEnv, ConnStr(inst), false, "platform")
	for _, e := range spec.SharedEnv(token, inst) {
		_ = r.env.UpsertEnv(ctx, appID, e.Key, e.Value, e.IsSecret, "platform")
	}
}
func (r *Reconciler) writeDedicatedEnvSpec(ctx context.Context, appID string, spec KindSpec, inst *ServiceInstance) {
	_ = r.env.UpsertEnv(ctx, appID, spec.AddrEnv, ConnStr(inst), false, "platform")
	for _, e := range spec.DedicatedEnv(inst.AuthRef) {
		_ = r.env.UpsertEnv(ctx, appID, e.Key, e.Value, e.IsSecret, "platform")
	}
}
```

- [ ] **Step 7: 跑全包 + 编译** — `cd platform/backend && GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/... && go build ./cmd/server` → 全绿 + 编译通过。**既有 P1–P5 测试全绿 = 零行为变化的硬证据**。

- [ ] **Step 8: Commit** — `refactor(mwsupply): redis/milvus 供给改 KindSpec registry 驱动(零行为变化)`

---

### Task 3: 全量回归 + .28 e2e（零行为变化验证）

**Files:** 无（验证 task）

- [ ] **Step 1: 全量单测** — `cd platform/backend && GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/... ./internal/appdeploy/...` → 全绿。

- [ ] **Step 2: scp + 重建 backend** — 改动 scp 到 .28：`ssh -i ~/.ssh/miscode -o PubkeyAcceptedAlgorithms=+ssh-rsa root@10.10.0.28 "cd /opt/anp/deploy && docker-compose -f docker-compose.prod.yml up --build -d backend"`。

- [ ] **Step 3: e2e 零行为变化** — 用一个测试 app 声明 redis/milvus 的 bind_existing / shared / dedicated 各部署一次，确认 binding 表（status/instance/token/env_key）与重构前一致、容器起停正常。复用 P1–P5 验证步骤（`docs/superpowers/plans/2026-08-0[1-3]-中间件依赖注入-P*.md`）。

- [ ] **Step 4: push** — `git push -u origin feat/unified-dep-supply`。

---

## Self-Review

**1. Spec 覆盖：** §4.1(KindSpec)→Task1+2；§4.2(strategy-generic 主逻辑)→Task2 Step6；§4.3(registry)→Task1+2(BuildSpecs)；§10(错误处理 best-effort)→保留；§11(测试分层)→Task1 registry 测 + Task2 spec 纯字段测 + 既有 P1-P5 回归。P1 范围=redis/milvus 迁移；pg/迁移/配额/UI 留 Plan 2-5。✓

**2. 占位扫描：** Task2 的 alloc 搬迁是"逐字搬 supply.go 原函数体"（源函数已存在于 supply.go，非 TODO）；spec 构造签名在 Step 3-6 一致（redisSpec/milvusSpec/BuildSpecs 均含 host 透传）。无遗留 TODO。✓

**3. 类型一致：** `KindSpec` 字段名（`AddrEnv`/`PortRange`/`ContainerName`/`LaunchDedicated`/`ReadyDedicated`/`CleanupDedicated`/`AllocSharedToken`/`SharedEnv`/`DedicatedEnv`）Task1 定义，Task2 引用一致。`redisSpec`/`milvusSpec`/`BuildSpecs` 签名（含 host）Step3-6 一致。`writeSpecEnv`/`writeDedicatedEnvSpec` Task2 Step6 定义即用。✓

**4. 自编译保证：** Task2 把 alloc/helper 搬入 spec 与 supply.go 调用方改造放同一 task（Step3-6），中间不切 task → 每 task 末 `go build` 通过。Task1 独立可编译。✓

**5. 风险：** (a) `ReadyDedicated` 的 host 透传——Step6.4 已说明把 host 加入 spec 构造签名，实现者须同步改 redisSpec/milvusSpec/BuildSpecs/NewReconciler；(b) `Reconciler.flusher/ready` 字段去留——推荐保留（最小改动）。两者漏改会被编译器/既有 dedicated 测挡住。
