# 依赖供给统一 P1：KindSpec 注册表基础 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 mwsupply 引入 KindSpec 注册表，把 redis/milvus 供给从 per-kind switch 重构为 registry 驱动，**零行为变化**（既有 P1–P5 测试当安全网），为后续 pg/未来 kind 零改主逻辑铺路。

**Architecture:** 新增 `KindSpec` 类型（每 kind 声明 AddrEnv / token 语义 / dedicated 三件套 / shared-token 分配 / env 派生）+ `kindRegistry`。redis/milvus 的现有 docker/naming/supply 逻辑收敛成两个 spec，由 `BuildSpecs(deps)` 在 `NewReconciler` 时注入 store/flusher/ready/docker 构造并注册。`supplyOne`/`supplyShared`/`supplyDedicated` 改为查 spec 分派，消除 kind switch。

**Tech Stack:** Go 1.24（`internal/mwsupply`）；PG（anp_test，禁 SQLite）；docker CLI（dedicated）；表测试 + 既有 P1–P5 回归。

**Spec:** `docs/superpowers/specs/2026-08-04-依赖供给统一三策略-design.md`

## Global Constraints

- 禁 SQLite，只用 PG；纯函数单测本机跑，PG 测试连 .28 anp_test（`GOPATH=C:/Users/yxt/go`）。
- go 命令前缀 `GOPATH=C:/Users/yxt/go`；全量回归 `go test -p 1 ./internal/mwsupply/...`。
- commit `refactor(mwsupply): 中文` + 末尾 `Co-Authored-By: Claude <noreply@anthropic.com>`。
- **零行为变化**：每个重构 task 后，既有 mwsupply 测试必须全绿（这是 P1 的硬约束）。
- 改完 scp 到 .28 重建 backend 验证（P1 末尾 Task 7）。

## File Structure

| 文件                                           | 责任                                                                                                                                                                                                        | 动作 |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `internal/mwsupply/kindspec.go`                | `KindSpec` 类型 + `TokenSemantics` + `EnvKV` + `kindRegistry` + `RegisterKind/LookupKind`                                                                                                                   | 新建 |
| `internal/mwsupply/spec_redis.go`              | redis KindSpec 构造（AddrEnv/token/dedicated 三件套/SharedEnv/DedicatedEnv）                                                                                                                                | 新建 |
| `internal/mwsupply/spec_milvus.go`             | milvus KindSpec 构造                                                                                                                                                                                        | 新建 |
| `internal/mwsupply/specs.go`                   | `BuildSpecs(store, flusher, ready, docker)` 装配 redis+milvus 并 Register                                                                                                                                   | 新建 |
| `internal/mwsupply/kindspec_test.go`           | registry 行为测（Register/Lookup/unknown）                                                                                                                                                                  | 新建 |
| `internal/mwsupply/spec_redis_test.go`         | redis spec 字段纯函数测                                                                                                                                                                                     | 新建 |
| `internal/mwsupply/spec_milvus_test.go`        | milvus spec 字段纯函数测                                                                                                                                                                                    | 新建 |
| `internal/mwsupply/supply.go`                  | `supplyOne`/`supplyShared`/`supplyDedicated` 改 spec 分派；移除 `launchDedicated`/`waitDedicatedReady`/`rmDedicated`/`writeSharedEnv`/`writeDedicatedEnv`/`allocRedisDB`/`allocMilvusPrefix` 的 kind switch | 改   |
| `internal/mwsupply/supply.go`（NewReconciler） | 调 `BuildSpecs(...)` 注册                                                                                                                                                                                   | 改   |

---

### Task 1: KindSpec 类型 + registry

**Files:**

- Create: `internal/mwsupply/kindspec.go`
- Test: `internal/mwsupply/kindspec_test.go`

**Interfaces:**

- Produces: `KindSpec` struct；`EnvKV`；`TokenSemantics` 常量；`RegisterKind(KindSpec)`；`LookupKind(kind string) (KindSpec, bool)`；`kindRegistry`（包内 map）。

- [ ] **Step 1: 写失败测试** — `kindspec_test.go`

```go
package mwsupply

import "testing"

func TestRegisterAndLookupKind(t *testing.T) {
	// 用临时 kind 名，避免与 redis/milvus 冲突
	resetRegistry(t)
	k := "testkind-" + "x"
	RegisterKind(KindSpec{Kind: k, AddrEnv: "TESTKIND_ADDR", DisplayName: "Test"})

	got, ok := LookupKind(k)
	if !ok {
		t.Fatalf("LookupKind(%q) ok=false，应已注册", k)
	}
	if got.AddrEnv != "TESTKIND_ADDR" {
		t.Fatalf("AddrEnv=%q want TESTKIND_ADDR", got.AddrEnv)
	}

	if _, ok := LookupKind("not-registered"); ok {
		t.Fatal("未注册 kind 应 ok=false")
	}
}
```

> `resetRegistry(t)` 清空 `kindRegistry`，避免测试间污染（Task 1 Step 3 一并实现）。后续 redis/milvus spec 注册后，该测试用独立 kind 名不冲突。

- [ ] **Step 2: 跑确认失败** — `cd platform/backend && GOPATH=C:/Users/yxt/go go test ./internal/mwsupply/ -run TestRegisterAndLookupKind -v` → FAIL（`KindSpec`/`RegisterKind` 未定义）。

- [ ] **Step 3: 实现** — `kindspec.go`

```go
package mwsupply

import "context"

// TokenSemantics 描述 shared 策略下"实例内隔离 token"的语义（文档/诊断用；实际分配由 AllocSharedToken）。
type TokenSemantics string

const (
	TokenDBNumber         TokenSemantics = "db-number"         // redis：db 号池
	TokenCollectionPrefix TokenSemantics = "collection-prefix" // milvus：collection 前缀
	// 未来：TokenDatabaseName(pg) / TokenTopicPrefix(kafka) / TokenIndexPrefix(es) ...
)

// EnvKV 单条注入 env（source=platform）。
type EnvKV struct {
	Key      string
	Value    string
	IsSecret bool
}

// KindSpec 每 kind 的供给规格。registry 驱动：supplyOne/supplyShared/supplyDedicated 查 spec 分派，无 kind switch。
// 依赖（store/flusher/ready/docker）由 BuildSpecs 在构造闭包时捕获（spec 不是包级常量）。
type KindSpec struct {
	Kind        string
	DisplayName string
	AddrEnv     string // 主连接 env 键：REDIS_ADDR / MILVUS_ADDR
	Token       TokenSemantics

	// dedicated 三件套（起 / 探 / 收）
	PortRange         func() (int, int)                                  // 端口池上下界
	ContainerName     func(short string) string                         // dedicated 容器名
	LaunchDedicated   func(ctx context.Context, name string, port int) (authRef string, err error)
	ReadyDedicated    func(ctx context.Context, name string, port int, authRef string) error
	CleanupDedicated  func(ctx context.Context, name string) error

	// shared：为某实例分配一个隔离 token（per-kind 策略；redis=db 号池 / milvus=随机前缀）。
	// claim（写库）由实现内部用 store.ClaimSharedToken 完成；返回 token + nil。
	AllocSharedToken func(ctx context.Context, appID, psID, instID string, inst *ServiceInstance) (string, error)

	// env 派生
	SharedEnv    func(token string, inst *ServiceInstance) []EnvKV // redis→[REDIS_DB(,+REDIS_PASSWORD)]；milvus→[MILVUS_COLLECTION_PREFIX]
	DedicatedEnv func(authRef string) []EnvKV                       // redis→[REDIS_PASSWORD]；milvus→[]
}

// kindRegistry 已注册 kind 规格。启动时由 BuildSpecs 填充。
var kindRegistry = map[string]KindSpec{}

// RegisterKind 注册/覆盖一个 kind 规格。
func RegisterKind(s KindSpec) { kindRegistry[s.Kind] = s }

// LookupKind 取规格；未注册 ok=false。
func LookupKind(kind string) (KindSpec, bool) {
	s, ok := kindRegistry[kind]
	return s, ok
}
```

`kindspec_test.go` 末尾加 reset 辅助（同文件）：

```go
// resetRegistry 清空 registry（测试隔离用）。
func resetRegistry(t *testing.T) {
	t.Helper()
	kindRegistry = map[string]KindSpec{}
}
```

- [ ] **Step 4: 跑确认通过** — `go test ./internal/mwsupply/ -run TestRegisterAndLookupKind -v` → PASS。

- [ ] **Step 5: Commit** — `refactor(mwsupply): 加 KindSpec 类型与 registry`

---

### Task 2: BuildSpecs 装配器 + redis spec

**Files:**

- Create: `internal/mwsupply/spec_redis.go`
- Create: `internal/mwsupply/specs.go`
- Test: `internal/mwsupply/spec_redis_test.go`

**Interfaces:**

- Consumes: Task 1 的 `KindSpec`/`RegisterKind`；现有 `MWDockerRunner`（docker.go）、`DBFlusher`/`ReadyChecker`（supply.go 的接口）、`Store`（store.go）、`ServiceInstance`。
- Produces: `BuildSpecs(store *Store, flusher DBFlusher, ready ReadyChecker, docker MWDockerRunner) map[string]KindSpec`；redis spec 的 `SharedEnv`/`DedicatedEnv`/`AllocSharedToken`/dedicated 三件套（提取自 supply.go 现有 redis 分支）。

- [ ] **Step 1: 写失败测试** — `spec_redis_test.go`

```go
package mwsupply

import (
	"context"
	"testing"
)

// fakeStore 仅满足 ClaimSharedToken 路径（AllocSharedToken 用）。
type fakeStore struct{ claimed string }
func (f *fakeStore) ClaimSharedToken(_ context.Context, _, _, _, _, token, _ string) error {
	f.claimed = token
	return nil
}

func TestRedisSpec_FieldsAndEnv(t *testing.T) {
	s := redisSpec(nil, nil, nil) // store/flusher/ready 用 nil（本测只验纯字段）
	if s.Kind != "redis" || s.AddrEnv != "REDIS_ADDR" {
		t.Fatalf("redis spec Kind/AddrEnv = %q/%q", s.Kind, s.AddrEnv)
	}
	if lo, hi := s.PortRange(); lo != mwPortMin || hi != mwPortMax {
		t.Fatalf("redis PortRange=%d-%d want %d-%d", lo, hi, mwPortMin, mwPortMax)
	}
	if got := s.ContainerName("1a2b3c"); got != "mwredis-1a2b3c" {
		t.Fatalf("ContainerName=%q want mwredis-1a2b3c", got)
	}
	// SharedEnv：redis → REDIS_DB（无 auth）
	envs := s.SharedEnv("3", &ServiceInstance{Kind: "redis"})
	if len(envs) != 1 || envs[0].Key != "REDIS_DB" || envs[0].Value != "3" {
		t.Fatalf("SharedEnv(db=3)=%+v want [REDIS_DB=3]", envs)
	}
	// SharedEnv：有 auth_ref → 追加 REDIS_PASSWORD（isSecret）
	envs = s.SharedEnv("3", &ServiceInstance{Kind: "redis", AuthRef: "pw"})
	if len(envs) != 2 || envs[1].Key != "REDIS_PASSWORD" || !envs[1].IsSecret || envs[1].Value != "pw" {
		t.Fatalf("SharedEnv(+auth)=%+v want 含 REDIS_PASSWORD=pw(secret)", envs)
	}
	// DedicatedEnv：redis → REDIS_PASSWORD
	denv := s.DedicatedEnv("pw")
	if len(denv) != 1 || denv[0].Key != "REDIS_PASSWORD" || denv[0].Value != "pw" {
		t.Fatalf("DedicatedEnv=%+v want [REDIS_PASSWORD=pw]", denv)
	}
}
```

- [ ] **Step 2: 跑确认失败** — `go test ./internal/mwsupply/ -run TestRedisSpec -v` → FAIL（`redisSpec` 未定义）。

- [ ] **Step 3: 实现 redis spec** — `spec_redis.go`。把 supply.go 里 redis 的 `writeSharedEnv`/`writeDedicatedEnv`/`allocRedisDB` 与 docker.go 的 `RunRedisContainer`/`RmForce`、naming.go 的 `mwPortMin/Max`/`dedicatedContainerName` redis 分支收敛成闭包：

```go
package mwsupply

import (
	"context"
	"strconv"
	"strings"
)

// redisSpec 构造 redis 的 KindSpec。store/flusher/ready 可为 nil（纯字段测试用）；
// AllocSharedToken 内部用 store + flusher（P2 flush+重试逻辑逐字搬入）。
func redisSpec(st *Store, flusher DBFlusher, ready ReadyChecker) KindSpec {
	return KindSpec{
		Kind: "redis", DisplayName: "Redis", AddrEnv: "REDIS_ADDR", Token: TokenDBNumber,
		PortRange:     func() (int, int) { return mwPortMin, mwPortMax },
		ContainerName: func(short string) string { return "mwredis-" + short },
		// dedicated：复用 docker.go 的 redis 配方（LaunchDedicated/ReadyDedicated/CleanupDedicated 在 BuildSpecs 接 docker）
		// SharedEnv：REDIS_ADDR 由调用方写主键，此处只写 token 派生项
		SharedEnv: func(token string, inst *ServiceInstance) []EnvKV {
			out := []EnvKV{{Key: "REDIS_DB", Value: token}}
			if inst != nil && inst.AuthRef != "" {
				out = append(out, EnvKV{Key: "REDIS_PASSWORD", Value: inst.AuthRef, IsSecret: true})
			}
			return out
		},
		DedicatedEnv: func(authRef string) []EnvKV {
			return []EnvKV{{Key: "REDIS_PASSWORD", Value: authRef, IsSecret: true}}
		},
		// AllocSharedToken：搬 allocRedisDB 的池选号 + claimWithRetry（flush+重试）。
		AllocSharedToken: func(ctx context.Context, appID, psID, instID string, inst *ServiceInstance) (string, error) {
			lo, hi, ok := ParseDBRange(inst.Isolation)
			if !ok {
				return "", errSharedNoDBRange // 见下
			}
			allocated, _ := st.AllocatedTokens(ctx, instID)
			first, found := pickLowestFree(lo, hi, allocated)
			if !found {
				return "", errRedisDBPoolExhausted(lo, hi)
			}
			return redisClaimWithRetry(ctx, st, flusher, appID, psID, "redis", inst, lo, hi, first, allocated)
		},
	}
}

// errSharedNoDBRange / errRedisDBPoolExhausted / redisClaimWithRetry 在 Task 4 从 supply.go 的
// allocRedisDB/claimWithRetry 逐字搬入 spec_redis.go（保留原 flush+重试语义）。
// 占位：Task 4 Step 3 完成搬迁前，这两个 helper 暂用 supply.go 现有名（allocRedisDB 的内部函数）。
var errSharedNoDBRange = strsErr("shared 实例 isolation 缺 db_range")

func strsErr(s string) error { return &strErr{s} }
type strErr struct{ s string }
func (e *strErr) Error() string { return e.s }

// 避免未用 import
var _ = strings.ToUpper
var _ = strconv.Itoa
```

> 说明：`AllocSharedToken` 内部的 `redisClaimWithRetry`（原 `claimWithRetry`）在 Task 4 从 supply.go 搬入；Task 2 先实现 spec 的**纯字段 + SharedEnv/DedicatedEnv**（测试只验这些），`AllocSharedToken` 的搬迁留 Task 4 与 supply.go 重构同步完成。为让 Task 2 编译通过，`AllocSharedToken` 先调一个 stub（Task 4 替换为真实搬迁逻辑）。

- [ ] **Step 4: 跑确认通过** — `go test ./internal/mwsupply/ -run TestRedisSpec_FieldsAndEnv -v` → PASS（纯字段 + env 派生）。

- [ ] **Step 5: Commit** — `refactor(mwsupply): redis KindSpec(字段+env派生)`

---

### Task 3: milvus spec

**Files:**

- Create: `internal/mwsupply/spec_milvus.go`
- Test: `internal/mwsupply/spec_milvus_test.go`

**Interfaces:**

- Produces: `milvusSpec(st *Store) KindSpec`；milvus 的 AddrEnv/token/dedicated 三件套/SharedEnv（`MILVUS_COLLECTION_PREFIX`）/DedicatedEnv（空）。

- [ ] **Step 1: 写失败测试** — `spec_milvus_test.go`

```go
package mwsupply

import "testing"

func TestMilvusSpec_FieldsAndEnv(t *testing.T) {
	s := milvusSpec(nil)
	if s.Kind != "milvus" || s.AddrEnv != "MILVUS_ADDR" || s.Token != TokenCollectionPrefix {
		t.Fatalf("milvus spec 基本字段错: %+v", s)
	}
	if lo, hi := s.PortRange(); lo != milvusPortMin || hi != milvusPortMax {
		t.Fatalf("milvus PortRange=%d-%d want %d-%d", lo, hi, milvusPortMin, milvusPortMax)
	}
	if got := s.ContainerName("1a2b3c"); got != "mwmilvus-1a2b3c" {
		t.Fatalf("ContainerName=%q want mwmilvus-1a2b3c", got)
	}
	envs := s.SharedEnv("app1a2b_", &ServiceInstance{Kind: "milvus"})
	if len(envs) != 1 || envs[0].Key != "MILVUS_COLLECTION_PREFIX" || envs[0].Value != "app1a2b_" {
		t.Fatalf("SharedEnv=%+v want [MILVUS_COLLECTION_PREFIX=app1a2b_]", envs)
	}
	if len(s.DedicatedEnv("")) != 0 {
		t.Fatalf("milvus DedicatedEnv 应为空，得 %+v", s.DedicatedEnv(""))
	}
}
```

- [ ] **Step 2: 跑确认失败** — `go test ./internal/mwsupply/ -run TestMilvusSpec -v` → FAIL（`milvusSpec` 未定义）。

- [ ] **Step 3: 实现** — `spec_milvus.go`

```go
package mwsupply

import "context"

// milvusSpec 构造 milvus 的 KindSpec。AllocSharedToken 搬 allocMilvusPrefix（随机前缀+claim 重试）。
func milvusSpec(st *Store) KindSpec {
	return KindSpec{
		Kind: "milvus", DisplayName: "Milvus", AddrEnv: "MILVUS_ADDR", Token: TokenCollectionPrefix,
		PortRange:     func() (int, int) { return milvusPortMin, milvusPortMax },
		ContainerName: func(short string) string { return "mwmilvus-" + short },
		SharedEnv: func(token string, _ *ServiceInstance) []EnvKV {
			return []EnvKV{{Key: "MILVUS_COLLECTION_PREFIX", Value: token}}
		},
		DedicatedEnv: func(_ string) []EnvKV { return nil },
		// AllocSharedToken：搬 allocMilvusPrefix（Task 4 与 supply.go 重构同步搬入；先 stub）。
		AllocSharedToken: func(ctx context.Context, appID, psID, instID string, inst *ServiceInstance) (string, error) {
			return milvusAllocPrefix(ctx, st, appID, psID, instID)
		},
	}
}

// milvusAllocPrefix 在 Task 4 从 supply.go:allocMilvusPrefix 逐字搬入。
```

- [ ] **Step 4: 跑确认通过** — `go test ./internal/mwsupply/ -run TestMilvusSpec -v` → PASS。

- [ ] **Step 5: Commit** — `refactor(mwsupply): milvus KindSpec(字段+env派生)`

---

### Task 4: 搬迁 shared-token 分配 + 重构 supplyShared 用 spec

**Files:**

- Modify: `internal/mwsupply/supply.go`（`supplyShared`/`writeSharedEnv`/`allocAndClaimShared`/`allocRedisDB`/`claimWithRetry`/`allocMilvusPrefix`）
- Modify: `internal/mwsupply/spec_redis.go`（补 `redisClaimWithRetry`）
- Modify: `internal/mwsupply/spec_milvus.go`（补 `milvusAllocPrefix`）

**Interfaces:**

- Consumes: Task 2/3 的 redis/milvus spec（`AllocSharedToken`/`SharedEnv`/`AddrEnv`）。
- Produces: `supplyShared` 改为 `spec := mustLookup(dep.Kind)` → 复用判定 → `spec.AllocSharedToken(...)` → 写 `spec.AddrEnv` + `spec.SharedEnv(token, inst)`。

- [ ] **Step 1: 搬迁分配逻辑** — 把 `supply.go` 的 `claimWithRetry`/`allocMilvusPrefix` 逐字移入 spec_redis.go（`redisClaimWithRetry`）/ spec_milvus.go（`milvusAllocPrefix`），保留 flush + 重试语义不变；删除 supply.go 的 `allocAndClaimShared`/`allocRedisDB`/`allocMilvusPrefix`/`claimWithRetry`。同时把 Task 2/3 的 stub `AllocSharedToken` 替换为真实搬迁逻辑。

- [ ] **Step 2: 写回归测试（若 supply_test.go 已有 shared 测则跳过新增，确认仍绿）** — 检查 `supply_test.go` 是否覆盖 shared redis/milvus；若无，加一个断言：shared 供给后 binding 的 `env_key == spec.AddrEnv`、`isolation_token` 非空。优先复用既有测。

- [ ] **Step 3: 重构 supplyShared** — `supply.go`：

```go
// supplyShared shared 供给（spec 驱动）：复用判定 / 新分配（spec.AllocSharedToken → claim）→ 写 env（spec.AddrEnv + SharedEnv）。
func (r *Reconciler) supplyShared(ctx context.Context, appID, psID string, dep DepService,
	mkBind func(status, instID, token, lastErr string)) {
	spec, ok := LookupKind(dep.Kind)
	if !ok {
		mkBind(StatusFailed, "", "", "未注册 kind: "+dep.Kind)
		return
	}
	inst, err := r.store.LookupShared(ctx, dep.Kind)
	if err != nil || inst == nil {
		mkBind(StatusFailed, "", "", "无 shared "+dep.Kind+" 实例")
		return
	}
	// 复用：同 app 已 bound 同实例同 token → 不换 token（保数据），重写 env。
	if existing, e := r.store.GetBinding(ctx, appID, dep.Kind); e == nil && existing != nil &&
		existing.Status == StatusBound && existing.IsolationToken != "" && existing.ServiceInstanceID == inst.ID {
		r.writeSpecEnv(ctx, appID, spec, inst, existing.IsolationToken)
		mkBind(StatusBound, inst.ID, existing.IsolationToken, "")
		return
	}
	token, err := spec.AllocSharedToken(ctx, appID, psID, inst.ID, inst)
	if err != nil {
		mkBind(StatusFailed, inst.ID, "", err.Error())
		return
	}
	r.writeSpecEnv(ctx, appID, spec, inst, token)
	mkBind(StatusBound, inst.ID, token, "")
}

// writeSpecEnv 写主连接 env（AddrEnv）+ spec 派生 env，均 source=platform。
func (r *Reconciler) writeSpecEnv(ctx context.Context, appID string, spec KindSpec, inst *ServiceInstance, token string) {
	_ = r.env.UpsertEnv(ctx, appID, spec.AddrEnv, ConnStr(inst), false, "platform")
	for _, e := range spec.SharedEnv(token, inst) {
		_ = r.env.UpsertEnv(ctx, appID, e.Key, e.Value, e.IsSecret, "platform")
	}
}
```

删除旧的 `writeSharedEnv`。

- [ ] **Step 4: 跑 mwsupply 全包** — `cd platform/backend && GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/...` → 全绿（既有 shared 测是回归安全网）。

- [ ] **Step 5: Commit** — `refactor(mwsupply): supplyShared 改 spec 驱动(搬 token 分配)`

---

### Task 5: 重构 supplyDedicated 用 spec

**Files:**

- Modify: `internal/mwsupply/supply.go`（`supplyDedicated`/`launchDedicated`/`waitDedicatedReady`/`rmDedicated`/`writeDedicatedEnv`）
- Modify: `internal/mwsupply/spec_redis.go`/`spec_milvus.go`（dedicated 三件套接 docker，由 BuildSpecs 注入）

**Interfaces:**

- Consumes: spec 的 `PortRange`/`ContainerName`/`LaunchDedicated`/`ReadyDedicated`/`CleanupDedicated`/`DedicatedEnv`。
- Produces: `supplyDedicated` 改 spec 分派，无 kind switch。

- [ ] **Step 1: 在 BuildSpecs 里给 redis/milvus spec 注入 dedicated 三件套**（见 Task 6 的 `BuildSpecs`；此处先确保 spec 的 Launch/Ready/Cleanup 字段在 BuildSpecs 接 `docker`）。`spec_redis.go`/`spec_milvus.go` 的构造函数加 `docker MWDockerRunner` 参数，把 `docker.RunRedisContainer`/`RmForce`、`docker.RunMilvusStack`/`MilvusReady`/`RmMilvusStack` 包成 spec 字段。

- [ ] **Step 2: 重构 supplyDedicated** — `supply.go`：

```go
// supplyDedicated dedicated 供给（spec 驱动）：复用判定 / 新供给（端口→Launch→Ready→登记→env）。
func (r *Reconciler) supplyDedicated(ctx context.Context, appID, psID string, dep DepService,
	mkBind func(status, instID, token, lastErr string)) {
	spec, ok := LookupKind(dep.Kind)
	if !ok {
		mkBind(StatusFailed, "", "", "未注册 kind: "+dep.Kind)
		return
	}
	// 复用：同 app 已 bound dedicated 实例 → 不重启、保数据、重写 env。
	if b, e := r.store.GetBinding(ctx, appID, dep.Kind); e == nil && b != nil &&
		b.Status == StatusBound && b.ServiceInstanceID != "" {
		if inst, ie := r.store.GetInstance(ctx, b.ServiceInstanceID); ie == nil && inst != nil && inst.Status == "active" {
			r.writeDedicatedEnvSpec(ctx, appID, spec, inst)
			mkBind(StatusBound, inst.ID, "", "")
			return
		}
	}
	lo, hi := spec.PortRange()
	port := allocPort(r.docker.UsedPorts(ctx), lo, hi)
	if port == 0 {
		mkBind(StatusFailed, "", "", fmt.Sprintf("%s 端口池 %d-%d 已满", dep.Kind, lo, hi))
		return
	}
	short := genShortID()
	base := spec.ContainerName(short)
	authRef, launchErr := spec.LaunchDedicated(ctx, base, port)
	if launchErr != nil {
		mkBind(StatusFailed, "", "", "起 "+dep.Kind+" 容器: "+launchErr.Error())
		return
	}
	if err := spec.ReadyDedicated(ctx, base, port, authRef); err != nil && r.log != nil {
		r.log.Warn("dedicated 就绪检测失败 (best-effort, proceed to bound)",
			zap.String("app", appID), zap.String("kind", dep.Kind), zap.Int("port", port), zap.Error(err))
	}
	inst := &ServiceInstance{
		ID: "svinst-" + dep.Kind + "-ded-" + short, ProjectSpaceID: nil,
		Kind: dep.Kind, Name: base, SupplyMode: ModeDedicated,
		Host: r.host, Port: port, AuthRef: authRef, ContainerName: base, Status: "active",
	}
	if err := r.store.CreateInstance(ctx, inst); err != nil {
		spec.CleanupDedicated(ctx, base)
		mkBind(StatusFailed, "", "", "登记实例: "+err.Error())
		return
	}
	r.writeDedicatedEnvSpec(ctx, appID, spec, inst)
	mkBind(StatusBound, inst.ID, "", "")
}

// writeDedicatedEnvSpec 写主连接 env + spec.DedicatedEnv。
func (r *Reconciler) writeDedicatedEnvSpec(ctx context.Context, appID string, spec KindSpec, inst *ServiceInstance) {
	_ = r.env.UpsertEnv(ctx, appID, spec.AddrEnv, ConnStr(inst), false, "platform")
	for _, e := range spec.DedicatedEnv(inst.AuthRef) {
		_ = r.env.UpsertEnv(ctx, appID, e.Key, e.Value, e.IsSecret, "platform")
	}
}
```

删除旧的 `launchDedicated`/`waitDedicatedReady`/`rmDedicated`/`writeDedicatedEnv`。`Cleanup`/`ReleaseDep` 里的 kind switch 改用 `LookupKind` → `spec.CleanupDedicated`（同法）。

- [ ] **Step 3: 跑全包** — `go test -p 1 ./internal/mwsupply/...` → 全绿（dedicated 测回归）。

- [ ] **Step 4: Commit** — `refactor(mwsupply): supplyDedicated 改 spec 驱动`

---

### Task 6: bind_existing 用 spec + NewReconciler 装配 BuildSpecs

**Files:**

- Modify: `internal/mwsupply/supply.go`（`supplyOne` bind_existing 段 + `NewReconciler`）
- Create: `internal/mwsupply/specs.go`

**Interfaces:**

- Produces: `BuildSpecs(store, flusher, ready, docker)` 注册 redis+milvus；`supplyOne` bind_existing 段用 `spec.AddrEnv`（替代 `EnvKeyFor` 在该路径的使用）；`NewReconciler` 末尾调 `BuildSpecs`。

- [ ] **Step 1: 实现 BuildSpecs** — `specs.go`

```go
package mwsupply

// BuildSpecs 构造并注册 redis/milvus 的 KindSpec（捕获 store/flusher/ready/docker 依赖）。
// 在 NewReconciler 调一次。新增 kind（如 pg）= 这里加一行构造 + 一个 spec 构造函数。
func BuildSpecs(store *Store, flusher DBFlusher, ready ReadyChecker, docker MWDockerRunner) {
	RegisterKind(redisSpec(store, flusher, ready, docker))
	RegisterKind(milvusSpec(store, docker))
}
```

并改 `spec_redis.go`/`spec_milvus.go` 构造签名接 `docker`，填充 dedicated 三件套（Task 5 Step 1 已铺垫）。

- [ ] **Step 2: NewReconciler 调 BuildSpecs** — `supply.go`

```go
func NewReconciler(store *Store, env EnvWriter, flusher DBFlusher, ready ReadyChecker, docker MWDockerRunner, host string) *Reconciler {
	BuildSpecs(store, flusher, ready, docker) // 注册 redis/milvus spec（幂等：覆盖式）
	return &Reconciler{store: store, env: env, flusher: flusher, ready: ready, docker: docker, host: host}
}
```

- [ ] **Step 3: supplyOne bind_existing 用 spec.AddrEnv** — `supply.go`（bind_existing 段）：

```go
	// —— bind_existing（spec 驱动）——
	spec, ok := LookupKind(dep.Kind)
	if !ok {
		mkBind(StatusFailed, "", "", "未注册 kind: "+dep.Kind)
		return
	}
	inst, err := r.store.LookupBindExisting(ctx, psID, dep.Kind)
	if err != nil || inst == nil {
		mkBind(StatusFailed, "", "", "无可绑定的 "+dep.Kind+" 实例")
		return
	}
	connStr := ConnStr(inst)
	isSecret := inst.AuthRef != ""
	if err := r.env.UpsertEnv(ctx, appID, spec.AddrEnv, connStr, isSecret, "platform"); err != nil {
		mkBind(StatusFailed, inst.ID, "", err.Error())
		return
	}
	mkBind(StatusBound, inst.ID, "", "")
```

`supplyOne` 顶部的 `envKey := EnvKeyFor(dep.Kind)` 删除（改用 spec.AddrEnv）。未注册 kind 早退兜底保留。

- [ ] **Step 4: 跑全包 + 编译 server** — `go test -p 1 ./internal/mwsupply/... && go build ./cmd/server` → 全绿 + 编译通过。

- [ ] **Step 5: Commit** — `refactor(mwsupply): bind_existing+NewReconciler 接 BuildSpecs registry`

---

### Task 7: 全量回归 + .28 e2e（零行为变化验证）

**Files:** 无（验证 task）

- [ ] **Step 1: 全量单测** — `cd platform/backend && GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/... ./internal/appdeploy/...` → 全绿。

- [ ] **Step 2: scp + 重建 backend** — 改动 scp 到 .28，`ssh ... "cd /opt/anp/deploy && docker-compose -f docker-compose.prod.yml up --build -d backend"`。

- [ ] **Step 3: e2e 零行为变化** — 用一个测试 app 声明 redis(milvus) 的 bind_existing / shared / dedicated 三策略各部署一次，确认：binding 表状态/instance/env_key/token 与重构前一致；容器起停正常。复用 P1–P5 的验证步骤（`docs/superpowers/plans/2026-08-0[1-3]-中间件依赖注入-P*.md`）。

- [ ] **Step 4: push** — `git push -u origin feat/unified-dep-supply`。

---

## Self-Review

**1. Spec 覆盖：** spec §4.1(KindSpec)→Task1-3；§4.2(strategy-generic 主逻辑)→Task4-6；§4.3(registry)→Task1+6；§10(错误处理 best-effort)→保留（重构不改）；§11(测试分层 generic 测一次)→Task1 registry 测 + 既有 P1-P5 回归。P1 范围=redis/milvus 迁移（spec §5 pg 收编、§8 迁移、§9 配额、§7 UI 留 Plan 2-5）。✓

**2. 占位扫描：** Task2/3 的 `AllocSharedToken` stub 在 Task4 Step1 明确替换为真实搬迁逻辑（非遗留 TODO）。其余步骤代码完整。✓

**3. 类型一致：** `KindSpec` 字段名（`AddrEnv`/`PortRange`/`ContainerName`/`LaunchDedicated`/`ReadyDedicated`/`CleanupDedicated`/`AllocSharedToken`/`SharedEnv`/`DedicatedEnv`）在 Task1 定义，Task2-6 引用一致。`BuildSpecs` 签名 Task6 定义与 Task2/3 构造函数接 `docker` 一致。✓

**4. 风险：** Task4 搬 `claimWithRetry`/`allocMilvusPrefix` 须逐字搬（保留 flush+重试+撞号重生）；Task5 `Cleanup`/`ReleaseDep` 的 kind switch 也要同步改 spec.CleanupDedicated（Step 2 已注明）。两者漏改会被既有 dedicated 测挡住。
