# 日志体系 M1（排障优先）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让所有业务报错分级落库可查、应用构建失败原因前端直接可见（解决"只看到 failed / exit status 1"类痛点）。

**Architecture:** 复用已有 zap + `platform_log` 表 + `DualLogger` 地基，做 5 处接入：① logger 全局化（`zap.ReplaceGlobals`）② `httpx.Err` 一处改全局自动打日志 ③ `DualLogger` 扩到 4xx 入库 ④ 前端展开 `build_log` ⑤ appdeploy 关键吞错清理。不新建表。

**Tech Stack:** Go 1.25（zap、gin、sqlx、PG）、Next.js 16 + React 19、vitest。

## Global Constraints

- **GOPATH 必须先修**（用户环境错配为 `C:\Program Files\Go\bin\go.exe`）：执行前先跑一次 `go env -w GOPATH=C:\Users\yxt\go`；之后所有 go 命令正常。若不修，临时前缀 `GOPATH='C:\Users\yxt\go'`。
- 后端测试连真 PG（`testutil.TestDB` 用 `anp_test` 库），需能访问 .28 dev PG 或本地 PG。
- 后端测试命令模板：`cd platform/backend && go test ./internal/<pkg>/ -run <Test> -v`（GOPATH 已修前提下）。
- 前端测试：`cd platform/frontend && pnpm test`（vitest）。
- **TDD**：每个后端改动先写失败测试，跑红，再实现，跑绿。
- **提交**：工作区有其他 WIP 改动，每个 task 的 commit 只 `git add` 本 task 涉及的文件（见各 task 的 commit 步骤），勿 `git add -A`。
- 改动遵循现有代码风格（中文注释、`_ =` 容错、`httpx.OK/Err` 约定）。

## File Structure

| 文件                                         | 职责              | 本计划改动                          |
| -------------------------------------------- | ----------------- | ----------------------------------- |
| `backend/internal/log/log.go`                | zap logger 构造   | Task 1：`New` 末尾 `ReplaceGlobals` |
| `backend/internal/log/log_test.go`           | logger 测试       | Task 1：新建                        |
| `backend/internal/httpx/response.go`         | 统一响应封装      | Task 2：`Err` 内自动打日志          |
| `backend/internal/httpx/response_test.go`    | response 测试     | Task 2：新建                        |
| `backend/internal/logsvc/middleware.go`      | DualLogger 中间件 | Task 3：4xx 入库                    |
| `backend/internal/logsvc/dual.go`            | 双写日志器        | Task 3：入库门槛放宽到 WARN+        |
| `backend/internal/logsvc/middleware_test.go` | 中间件测试        | Task 3：补 4xx/5xx 测试             |
| `backend/internal/logsvc/dual_test.go`       | DualLogger 测试   | Task 3：补 WARN 入库测试            |
| `backend/internal/appdeploy/handler.go`      | 应用部署 handler  | Task 5：关键吞错补 WARN             |
| `frontend/app/applications/page.tsx`         | 应用部署页        | Task 4：失败横幅展开 build_log      |

---

### Task 1: logger 全局化

**Files:**

- Modify: `platform/backend/internal/log/log.go`
- Test: `platform/backend/internal/log/log_test.go` (Create)

**Interfaces:**

- Produces: `New()` 调用后全局 `zap.L()` 可用（后续 task 依赖此）

- [ ] **Step 1: Write the failing test**

Create `platform/backend/internal/log/log_test.go`:

```go
package log

import (
	"testing"

	"go.uber.org/zap"
)

// TestNew_ReplacesGlobal New 后全局 zap.L() 被替换为可用 logger（非 nil、可写不 panic）。
// 后续 httpx.Err / appdeploy 等业务代码直接用 zap.L()，依赖此全局已就绪。
func TestNew_ReplacesGlobal(t *testing.T) {
	zap.ReplaceGlobals(zap.NewNop()) // 先污染全局为 nop
	_ = New("warn")
	gl := zap.L()
	if gl == nil {
		t.Fatal("New 后全局 logger 不应为 nil")
	}
	gl.Warn("smoke") // 不 panic 即全局可用
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd platform/backend && go test ./internal/log/ -run TestNew_ReplacesGlobal -v`
Expected: FAIL — 当前 `New` 未调 `ReplaceGlobals`，全局仍是 nop（测试其实会过，因为 nop 也非 nil 且 Warn 不 panic）。

> 注：此改动属 zap 原生行为，测试是 smoke 性质。若 Step 2 直接 PASS，说明全局已被别处设置；仍按 Step 3 落实 `ReplaceGlobals` 以保证业务 handler 用 `zap.L()` 一定拿到真实 logger。

- [ ] **Step 3: Write minimal implementation**

Modify `platform/backend/internal/log/log.go`，`New` 函数改为：

```go
// New 按级别构造 zap.Logger 并设为全局（zap.L() 可用）；失败时返回 no-op logger。
func New(level string) *zap.Logger {
	cfg := zap.NewDevelopmentConfig()
	cfg.EncoderConfig.EncodeLevel = zapcore.CapitalLevelEncoder
	switch level {
	case "debug":
		cfg.Level = zap.NewAtomicLevelAt(zapcore.DebugLevel)
	case "info":
		cfg.Level = zap.NewAtomicLevelAt(zapcore.InfoLevel)
	case "warn":
		cfg.Level = zap.NewAtomicLevelAt(zapcore.WarnLevel)
	case "error":
		cfg.Level = zap.NewAtomicLevelAt(zapcore.ErrorLevel)
	}
	l, err := cfg.Build()
	if err != nil {
		nop := zap.NewNop()
		zap.ReplaceGlobals(nop) // 失败也设全局，保证 zap.L() 永不 nil
		return nop
	}
	zap.ReplaceGlobals(l) // 业务代码用 zap.L() 即可
	return l
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd platform/backend && go test ./internal/log/ -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd "/d/Projects/智源-ANP平台"
git add platform/backend/internal/log/log.go platform/backend/internal/log/log_test.go
git commit -m "feat(log): New 设全局 zap.L()，业务 handler 可直接用"
```

---

### Task 2: `httpx.Err` 自动打日志（一处改全局生效）

**Files:**

- Modify: `platform/backend/internal/httpx/response.go`
- Test: `platform/backend/internal/httpx/response_test.go` (Create)

**Interfaces:**

- Consumes: Task 1 的全局 `zap.L()`
- Produces: 所有 `httpx.Err(...)`（273 处）自动产生结构化日志（trace_id/user_id/path/method/biz_code/http_status），4xx→WARN、5xx→ERROR

- [ ] **Step 1: Write the failing test**

Create `platform/backend/internal/httpx/response_test.go`:

```go
package httpx

import (
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"
	"go.uber.org/zap"
	"go.uber.org/zap/zapcore"
	"go.uber.org/zap/zaptest/observer"
)

// newObsContext 建一个带 trace_id 的 gin test context + 全局 logger observer。
// 返回 context 和已记录日志的 observer handle。
func newObsContext(t *testing.T, method, path string) (*gin.Context, *observer.ObservedLogs) {
	t.Helper()
	gin.SetMode(gin.TestMode)
	c, _ := gin.CreateTestContext(httptest.NewRecorder())
	c.Request = httptest.NewRequest(method, path, nil)
	c.Set("trace_id", "tr_test_1")
	c.Set("user_id", "u_test")
	core, recorded := observer.New(zapcore.DebugLevel)
	zap.ReplaceGlobals(zap.New(core))
	t.Cleanup(func() { zap.ReplaceGlobals(zap.NewNop()) })
	return c, recorded
}

// TestErr_LogsWarnFor4xx 4xx 应记一条 WARN，带 trace_id/path。
func TestErr_LogsWarnFor4xx(t *testing.T) {
	c, recorded := newObsContext(t, http.MethodGet, "/api/v1/x")

	Err(c, 404, 40420, "应用不存在")

	if recorded.FilterMessage("应用不存在").Len() != 1 {
		t.Fatalf("应记 1 条 WARN，得到 %d", recorded.Len())
	}
	if recorded.FilterLevelExact(zapcore.WarnLevel).Len() != 1 {
		t.Fatalf("4xx 应为 WARN 级")
	}
	ctx := recorded.All()[0].ContextMap()
	if ctx["trace_id"] != "tr_test_1" || ctx["path"] != "/api/v1/x" {
		t.Fatalf("日志字段缺 trace_id/path：%v", ctx)
	}
}

// TestErr_LogsErrorFor5xx 5xx 应记 ERROR。
func TestErr_LogsErrorFor5xx(t *testing.T) {
	c, recorded := newObsContext(t, http.MethodPost, "/api/v1/y")

	Err(c, 500, 50012, "内部错误")

	if recorded.FilterLevelExact(zapcore.ErrorLevel).Len() != 1 {
		t.Fatalf("5xx 应为 ERROR 级，得到 %v", recorded.All())
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd platform/backend && go test ./internal/httpx/ -run TestErr -v`
Expected: FAIL — 当前 `Err` 不打日志，`recorded` 为空，`FilterMessage(...).Len() != 1` 失败。

- [ ] **Step 3: Write minimal implementation**

Modify `platform/backend/internal/httpx/response.go` 完整内容：

```go
// Package httpx 提供统一的 HTTP 响应封装与错误码约定。
package httpx

import (
	"github.com/gin-gonic/gin"
	"go.uber.org/zap"
)

// Response 是统一响应体。
type Response struct {
	Code    int         `json:"code"` // 0 表示成功，其余为业务错误码
	Message string      `json:"message"`
	Data    interface{} `json:"data,omitempty"`
	TraceID string      `json:"trace_id,omitempty"`
}

// OK 返回 200 + 成功数据。
func OK(c *gin.Context, data interface{}) {
	c.JSON(200, Response{Code: 0, Message: "ok", Data: data, TraceID: c.GetString("trace_id")})
}

// Created 返回 201 + 创建结果。
func Created(c *gin.Context, data interface{}) {
	c.JSON(201, Response{Code: 0, Message: "created", Data: data, TraceID: c.GetString("trace_id")})
}

// Err 返回错误状态码 + 业务错误信息，并自动打一条结构化日志（4xx→WARN，5xx→ERROR）。
// 一处改造，所有 handler 的 httpx.Err(...) 调用自动有日志，带 trace_id/user_id/path。
func Err(c *gin.Context, status int, code int, msg string) {
	logErr(c, status, code, msg)
	c.JSON(status, Response{Code: code, Message: msg, TraceID: c.GetString("trace_id")})
}

// logErr 按 http 状态码分级打日志。4xx 业务失败→WARN，5xx→ERROR。
func logErr(c *gin.Context, status int, code int, msg string) {
	fields := []zap.Field{
		zap.String("trace_id", c.GetString("trace_id")),
		zap.String("user_id", c.GetString("user_id")),
		zap.String("method", c.Request.Method),
		zap.String("path", c.Request.URL.Path),
		zap.Int("biz_code", code),
		zap.Int("http_status", status),
	}
	if status >= 500 {
		zap.L().Error(msg, fields...)
		return
	}
	zap.L().Warn(msg, fields...)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd platform/backend && go test ./internal/httpx/ -v`
Expected: PASS（两个 TestErr 用例都过）

- [ ] **Step 5: Run appdeploy tests to confirm no regression**

Run: `cd platform/backend && go test ./internal/appdeploy/ -count=1`
Expected: PASS（确认改 response.go 没破坏依赖 httpx 的现有测试）

- [ ] **Step 6: Commit**

```bash
cd "/d/Projects/智源-ANP平台"
git add platform/backend/internal/httpx/response.go platform/backend/internal/httpx/response_test.go
git commit -m "feat(httpx): Err 自动打结构化日志，273 处调用全局生效"
```

---

### Task 3: `DualLogger` 扩到 4xx 入库

**Files:**

- Modify: `platform/backend/internal/logsvc/middleware.go`
- Modify: `platform/backend/internal/logsvc/dual.go`
- Test: `platform/backend/internal/logsvc/middleware_test.go` (Augment), `platform/backend/internal/logsvc/dual_test.go` (Augment)

**Interfaces:**

- Consumes: 无新接口
- Produces: 4xx 响应自动产生一条 WARN 记录入 `platform_log`（之前仅 5xx）

- [ ] **Step 1: Write the failing test（dual: WARN 入库）**

Append to `platform/backend/internal/logsvc/dual_test.go`（若文件不存在则新建 `package logsvc`）：

```go
package logsvc

import (
	"context"
	"testing"

	"go.uber.org/zap"
)

// TestLog_WarnEntersDB WARN 级应入库（修复前仅 ERROR/FATAL 入）。
func TestLog_WarnEntersDB(t *testing.T) {
	db := testutil.TestDB(t)
	testutil.Truncate(t, db, "platform_log")
	dl := NewDualLogger(zap.NewExample(), NewStore(db))

	dl.Log(LogEntryInput{Level: "WARN", Module: "test", Msg: "warn case", TraceID: "t1"})

	var n int
	_ = db.GetContext(context.Background(), &n,
		`SELECT COUNT(*) FROM platform_log WHERE level='WARN' AND trace_id='t1'`)
	if n != 1 {
		t.Fatalf("WARN 应入库 1 条，得到 %d", n)
	}
}

// TestLog_InfoNotInDB INFO 不入库（只 zap）。
func TestLog_InfoNotInDB(t *testing.T) {
	db := testutil.TestDB(t)
	testutil.Truncate(t, db, "platform_log")
	dl := NewDualLogger(zap.NewExample(), NewStore(db))

	dl.Log(LogEntryInput{Level: "INFO", Module: "test", Msg: "info case"})

	var n int
	_ = db.GetContext(context.Background(), &n, `SELECT COUNT(*) FROM platform_log WHERE level='INFO'`)
	if n != 0 {
		t.Fatalf("INFO 不应入库，得到 %d", n)
	}
}
```

> 若 `dual_test.go` 已存在，把上面两个函数追加进去（注意 import `testutil` 与现有 import 合并，勿重复）。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd platform/backend && go test ./internal/logsvc/ -run 'TestLog_Warn|TestLog_Info' -v`
Expected: FAIL — 当前 `Log`（dual.go:58）只 ERROR/FATAL 入库，WARN 不入，`n != 1` 失败。

- [ ] **Step 3: Implement — dual.go 放宽入库门槛**

Modify `platform/backend/internal/logsvc/dual.go`，把 `Log` 末尾的入库判断（原 `if in.Level != "ERROR" && in.Level != "FATAL"`）改为：

```go
	// DB 记 WARN 及以上（查询用）；INFO/DEBUG 只 zap，省 DB
	if in.Level != "WARN" && in.Level != "ERROR" && in.Level != "FATAL" {
		return
	}
	if dl.Store == nil {
		return
	}
	ctx := context.Background()
	_ = dl.Store.CreateFromJSON(ctx, in.Level, "backend", in.Msg, mergeFields(in))
```

- [ ] **Step 4: Run dual test to verify it passes**

Run: `cd platform/backend && go test ./internal/logsvc/ -run 'TestLog_Warn|TestLog_Info' -v`
Expected: PASS

- [ ] **Step 5: Write the failing test（middleware: 4xx 入库）**

Append to `platform/backend/internal/logsvc/middleware_test.go`（若不存在则新建 `package logsvc`）：

```go
package logsvc

import (
	"context"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"
	"go.uber.org/zap"
)

// dispatchWith 构造一个挂 DualLoggerMiddleware 的 gin engine，handler 返回指定 status。
func dispatchWith(t *testing.T, dl *DualLogger, status int) {
	t.Helper()
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(func(c *gin.Context) { c.Set("trace_id", "t_mid"); c.Next() })
	r.Use(DualLoggerMiddleware(dl))
	r.GET("/x", func(c *gin.Context) { c.Status(status) })
	req := httptest.NewRequest(http.MethodGet, "/x", nil)
	r.ServeHTTP(httptest.NewRecorder(), req)
}

// TestDualLoggerMiddleware_4xxEntersWarn 4xx 响应应入一条 WARN。
func TestDualLoggerMiddleware_4xxEntersWarn(t *testing.T) {
	db := testutil.TestDB(t)
	testutil.Truncate(t, db, "platform_log")
	dl := NewDualLogger(zap.NewExample(), NewStore(db))

	dispatchWith(t, dl, 404)

	var n int
	_ = db.GetContext(context.Background(), &n,
		`SELECT COUNT(*) FROM platform_log WHERE level='WARN' AND trace_id='t_mid'`)
	if n != 1 {
		t.Fatalf("4xx 应入 1 条 WARN，得到 %d", n)
	}
}

// TestDualLoggerMiddleware_2xxNoEntry 2xx 响应不入库。
func TestDualLoggerMiddleware_2xxNoEntry(t *testing.T) {
	db := testutil.TestDB(t)
	testutil.Truncate(t, db, "platform_log")
	dl := NewDualLogger(zap.NewExample(), NewStore(db))

	dispatchWith(t, dl, 200)

	var n int
	_ = db.GetContext(context.Background(), &n, `SELECT COUNT(*) FROM platform_log WHERE trace_id='t_mid'`)
	if n != 0 {
		t.Fatalf("2xx 不应入库，得到 %d", n)
	}
}
```

- [ ] **Step 6: Run middleware test to verify it fails**

Run: `cd platform/backend && go test ./internal/logsvc/ -run TestDualLoggerMiddleware_4xx -v`
Expected: FAIL — 当前 middleware 只 5xx 入库，4xx 不入。

- [ ] **Step 7: Implement — middleware.go 处理 4xx**

Modify `platform/backend/internal/logsvc/middleware.go`，把 `c.Next()` 后的 `if status >= 500` 块改为：

```go
		// 5xx → ERROR；4xx → WARN（业务失败也可查可统计）
		status := c.Writer.Status()
		switch {
		case status >= 500:
			dl.Log(LogEntryInput{
				Level:   "ERROR",
				Module:  "http",
				TraceID: c.GetString("trace_id"),
				UserID:  c.GetString("user_id"),
				Msg:     c.Request.Method + " " + c.Request.URL.Path + " → " + strconv.Itoa(status),
				Fields: map[string]interface{}{
					"method":     c.Request.Method,
					"path":       c.Request.URL.Path,
					"status":     status,
					"latency_ms": time.Since(start).Milliseconds(),
				},
			})
		case status >= 400:
			dl.Log(LogEntryInput{
				Level:   "WARN",
				Module:  "http",
				TraceID: c.GetString("trace_id"),
				UserID:  c.GetString("user_id"),
				Msg:     c.Request.Method + " " + c.Request.URL.Path + " → " + strconv.Itoa(status),
				Fields: map[string]interface{}{
					"method":     c.Request.Method,
					"path":       c.Request.URL.Path,
					"status":     status,
					"latency_ms": time.Since(start).Milliseconds(),
				},
			})
		}
```

- [ ] **Step 8: Run all logsvc tests**

Run: `cd platform/backend && go test ./internal/logsvc/ -v`
Expected: PASS（含新增 + 现有 store_test.go）

- [ ] **Step 9: Commit**

```bash
cd "/d/Projects/智源-ANP平台"
git add platform/backend/internal/logsvc/middleware.go platform/backend/internal/logsvc/dual.go platform/backend/internal/logsvc/middleware_test.go platform/backend/internal/logsvc/dual_test.go
git commit -m "feat(logsvc): DualLogger 扩到 4xx→WARN 入库"
```

---

### Task 4: 构建失败前端可见（展开 build_log）

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx`

**Interfaces:**

- Consumes: 后端列表已返回 `build_log`（`App` 类型已有该字段，`page.tsx:41`）
- Produces: 失败横幅可展开看完整构建日志

- [ ] **Step 1: 改失败横幅，加 build_log 展开**

Modify `platform/frontend/app/applications/page.tsx`，把 `{a.status === "failed" && (...)}` 块（约 `page.tsx:540-544`）：

```tsx
{
  a.status === "failed" && (
    <div className="mb-2 rounded bg-red-50 px-3 py-1.5 text-sm text-red-700">
      ❌ 构建失败：{a.last_error?.slice(0, 100)}
    </div>
  );
}
```

改为：

```tsx
{
  a.status === "failed" && (
    <div className="mb-2 rounded bg-red-50 px-3 py-1.5 text-sm text-red-700">
      <div>❌ 构建失败：{a.last_error?.slice(0, 100) || "(无错误摘要)"}</div>
      {a.build_log && (
        <details className="mt-1">
          <summary className="cursor-pointer text-xs text-red-500">查看构建日志详情</summary>
          <pre className="mt-1 max-h-64 overflow-auto rounded bg-neutral-900 p-2 text-xs text-green-300 whitespace-pre-wrap">
            {a.build_log}
          </pre>
        </details>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 验证（人工，无自动化测试）**

> 此改动是纯展示层（`<details>` 展开），且 `page.tsx` 是 996 行单页组件、无现成组件测试设施，TDD 成本高于价值。验证方式：
>
> 1. 前端编译通过：`cd platform/frontend && pnpm lint`（无 TS 报错）。
> 2. 推送到 .28 或本地起前端，对一个失败应用（如 hello-service）点「构建部署(test)」，失败横幅出现后点「查看构建日志详情」，应看到完整 build_log（含 `redeclared` 编译错误）。

Run: `cd platform/frontend && pnpm lint`
Expected: 无错误。

- [ ] **Step 3: Commit**

```bash
cd "/d/Projects/智源-ANP平台"
git add platform/frontend/app/applications/page.tsx
git commit -m "feat(applications): 失败横幅可展开 build_log，构建错误前端直接可见"
```

> ⚠️ 注意：`page.tsx` 在工作区已有 WIP 改动（`git status` 显示 M）。此 commit 会把 WIP + 本改动一起提交。**若要分离**，用 `git add -p platform/frontend/app/applications/page.tsx` 只选本 task 的 hunk。请执行者按当前 WIP 情况判断。

---

### Task 5: appdeploy 关键吞错清理（补 WARN）

**Files:**

- Modify: `platform/backend/internal/appdeploy/handler.go`

**Interfaces:**

- Consumes: Task 1 全局 `zap.L()`
- Produces: 部署链路关键失败（清理容器、写路由）不再静默

> **测试策略**：这两处吞错涉及真实 docker（`RemoveByPrefix`）/appgw（`UpsertRoute`），单元测试需 mock 整个 deployer，成本高。本 task 不写新单测，靠：① 改动极小且明确（失败分支加一行日志）；② 现有 `appdeploy` 测试全绿（不回归）；③ 部署到 .28 后人工/日志确认 WARN 出现。

- [ ] **Step 1: `RemoveByPrefix` 失败补 WARN**

Modify `platform/backend/internal/appdeploy/handler.go` 的 `buildAndDeploy`，把清理历史容器那行（约 `handler.go:1011`）：

```go
	// 清理该 app+env 所有历史容器（DB 记录的 + 孤儿残留），彻底释放端口避免漂移/Conflict
	_, _ = h.deployer.RemoveByPrefix(ctx, "appdeploy-"+a.Name+"-"+env+"-")
```

改为：

```go
	// 清理该 app+env 所有历史容器（DB 记录的 + 孤儿残留），彻底释放端口避免漂移/Conflict
	if _, err := h.deployer.RemoveByPrefix(ctx, "appdeploy-"+a.Name+"-"+env+"-"); err != nil {
		zap.L().Warn("清理历史容器失败（不阻塞部署）",
			zap.String("app", a.Name), zap.String("env", env), zap.Error(err))
	}
```

- [ ] **Step 2: `UpsertRoute` 失败补 WARN**

同文件，把路由写入失败块（约 `handler.go:1062-1067`）：

```go
		if h.routeWriter != nil {
			if err := h.routeWriter.UpsertRoute(ctx, a.ID, a.ProjectSpaceID, env, h.deployer.host, ins.HostPort); err != nil {
				// 路由表写入失败仅记录到 instance.LastError，不影响部署成功态
				ins.LastError = "部署成功但 appgw 路由表写入失败: " + err.Error()
				_ = h.store.UpdateInstance(ctx, ins)
			}
		}
```

改为：

```go
		if h.routeWriter != nil {
			if err := h.routeWriter.UpsertRoute(ctx, a.ID, a.ProjectSpaceID, env, h.deployer.host, ins.HostPort); err != nil {
				// 路由表写入失败仅记录到 instance.LastError，不影响部署成功态；另记 WARN 便于排查
				ins.LastError = "部署成功但 appgw 路由表写入失败: " + err.Error()
				_ = h.store.UpdateInstance(ctx, ins)
				zap.L().Warn("appgw 路由表写入失败（部署仍成功）",
					zap.String("app_id", a.ID), zap.String("env", env), zap.Error(err))
			}
		}
```

- [ ] **Step 3: 加 zap import（若未有）**

确认 `handler.go` 顶部 import 块含 `"go.uber.org/zap"`。若无，在 import 块加入。现有 import 在 `handler.go:3-27`。

- [ ] **Step 4: Run appdeploy tests（回归）**

Run: `cd platform/backend && go test ./internal/appdeploy/ -count=1`
Expected: PASS（含 Task 8 修的 deploy_status_test.go + 现有全部）

- [ ] **Step 5: Commit**

```bash
cd "/d/Projects/智源-ANP平台"
# handler.go 在工作区有 WIP（含前面 bug 修复 + 本改动）。若 bug 修复已单独提交，此处 add handler.go；
# 否则用 git add -p 只选本 task 的两个 hunk。
git add platform/backend/internal/appdeploy/handler.go
git commit -m "feat(appdeploy): 部署链路关键失败补 WARN 日志，不再静默"
```

---

## 部署到 .28 验证（M1 整体验收）

所有 task 完成后：

```bash
# 推送改动的后端文件 + 前端文件
scp -i ~/.ssh/miscode platform/backend/internal/log/log.go \
    platform/backend/internal/httpx/response.go \
    platform/backend/internal/logsvc/middleware.go \
    platform/backend/internal/logsvc/dual.go \
    root@10.10.0.28:/tmp/  # 中转，再逐个 cp 到 /opt/anp 对应路径
# 实际用 README 方式 A：逐文件 scp 到 /opt/anp/platform/backend/...
# 前端文件 scp 到 /opt/anp/platform/frontend/...
# 重建 backend + frontend
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp && docker-compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up --build -d backend frontend"
```

验收点（对照 spec §13）：

- [ ] 任意 handler 返回 4xx → `platform_log` 有 WARN（trace_id/path）；5xx → ERROR
- [ ] hello-service 构建失败 → 前端失败横幅点「查看构建日志详情」能看到 `redeclared` 编译错误
- [ ] `GET /api/v1/logs?level=WARN` 能查到 4xx 记录
- [ ] 部署时清理容器/写路由失败 → 后端日志有 WARN

---

## Self-Review（计划自检）

**1. Spec 覆盖**：

- §5.2.1 logger 全局化 → Task 1 ✓
- §5.2.2 httpx.Err 自动日志 → Task 2 ✓
- §5.2.3 4xx 入库（middleware + dual）→ Task 3 ✓
- §5.2.4 构建失败前端可见 → Task 4 ✓
- §5.2.5 关键吞错清理 → Task 5 ✓
- §5.3 数据结构（复用 platform_log）→ 无新表，各 task 体现 ✓

**2. Placeholder 扫描**：无 TBD/TODO；每步含完整代码；测试命令含期望输出 ✓

**3. 类型一致性**：

- Task 2 用 `zap.L()` 依赖 Task 1 `ReplaceGlobals` ✓
- Task 3 `LogEntryInput{Level:"WARN"}` 与 dual.go 现有结构一致 ✓
- Task 4 `a.build_log` 已在 `App` 类型（`page.tsx:41`）✓
- Task 5 `zap.L()` + `zap.String/zap.Error` 标准 ✓

**4. 测试难点诚实标注**：Task 1（zap 原生，smoke）、Task 4（前端无组件测试设施，人工）、Task 5（docker/appgo 依赖，回归+人工）均已说明，未假装 TDD。
