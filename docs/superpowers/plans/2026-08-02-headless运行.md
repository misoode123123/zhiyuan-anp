# headless 应用运行态 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让平台支持 headless(bot/worker)应用——无端口部署 + 进程存活健康监控(后台 reconcile + crash-loop 增量判定 + ops 告警联动)。

**Architecture:** 新增 `headless` AppKind;`Deployer.Deploy` 加无端口分支;新增 `HealthReconciler` 后台 goroutine(仿 `ServerMonitor.Start`)周期 `docker inspect` headless 实例,用「单周期新增重启数」增量判 crash-loop(非粘),status 翻转时写库 + 经 `OpsHealthAlerter` 联动 ops `Alert` 表;本期只巡 headless 实例,web 链路零改动。

**Tech Stack:** Go(gin + sqlx + PG `$N` 占位符)、PG 迁移(migrate,000032)、Next.js 前端(本仓魔改版,见 Global Constraints)、docker socket 探活。

## Global Constraints

- **PG only,禁 sqlite**(memory `no-sqlite-pg-only`);store 单测用 `newTestStore(t)` 连 anp_test PG(`store_test.go:13`,testutil 跑迁移建全表)。纯函数测试无需 DB。
- **全量 go test 串行**:`go test ./... -p 1`(memory `go-test-serial-p1`,并发污染 anp_test 库)。单包测试 `cd platform/backend && go test ./internal/appdeploy/ -run <Test> -p 1`。
- **本机只 `go build` 验证编译**,功能/端到端验证全在 .28(memory `deploy-28-no-local-test`)。
- **迁移编号 000032**,纯 SQL,**不写 BEGIN/COMMIT**(migrate runner 每迁移包一事务,与既有迁移一致)。up/down 成对。
- **commit message**:`feat(appdeploy): 中文摘要`,末尾 `Co-Authored-By: Claude <noreply@anthropic.com>`。每 task 末 commit。
- **前端是魔改 Next.js**(memory `stale-next-build-trap` + `platform/frontend/AGENTS.md`):写前端代码前**先读 `platform/frontend/node_modules/next/dist/docs/` 相关指南**;改完 `cd platform/frontend && npx tsc --noEmit` 验类型,必要时 `rm -rf .next && npm run build`。
- **.28 部署**:`ssh -i ~/.ssh/miscode root@10.10.0.28`,源码 `/opt/anp`,scp 改动 + `docker-compose` rebuild 对应服务,入口 8088(memory `deploy-prod-10.10.0.28`)。
- **前后端交叉验证**:端到端必须真开浏览器看渲染,不只 curl(memory `verify-cross-frontend-backend`)。
- **$N 占位符**:PG 用 `$1,$2...`,不用 `?`(ops/store.go 里动态 WHERE 用 `sqlx.Rebind(sqlx.DOLLAR,...)`)。

**Spec:** `docs/superpowers/specs/2026-08-02-headless运行-design.md`(已 commit 1114bae)。

---

## File Structure

**新增:**

- `platform/backend/internal/db/migrations/pg/000032_appdeploy_headless_health.up.sql` / `.down.sql` — `appdeploy_instance` 加 `restart_count` 列。
- `platform/backend/internal/appdeploy/health_reconcile.go` — `HealthReconciler` + `aggregateHealth` 纯函数 + 接口(`InstanceHealthStore`/`HealthInspector`/`HealthAlerter`)+ `ContainerHealth`。
- `platform/backend/internal/appdeploy/health_reconcile_test.go` — reconcile + aggregateHealth 测试。
- `platform/backend/internal/appdeploy/health_alerter.go` — `OpsHealthAlerter`(HealthAlerter 的 ops 实现)。

**修改:**

- `platform/backend/internal/appdeploy/model.go` — 加 `AppKindHeadless` 常量 + `AppInstance.RestartCount` 字段。
- `platform/backend/internal/appdeploy/store.go` — `insCols` 加 `restart_count`;加 `ListHeadlessActiveInstances`/`UpdateInstanceHealth`/`UpdateRestartCount`;导出 `headlessInstance` 行类型。
- `platform/backend/internal/appdeploy/deployer.go` — `Deploy` 加 headless 无端口分支;加 `InspectHealth`/`parseInspectHealth`。
- `platform/backend/internal/appdeploy/handler.go` — `validAppKind` 加 headless;docker 部署成功后跳过 routeWriter(headless);部署响应 `probeHealth` 守卫(headless 用 status)。
- `platform/backend/internal/appdeploy/deployer_test.go` — headless 部署分支 + InspectHealth 测试。
- `platform/backend/internal/ops/store.go` — 加 `ResolveByFingerprint`;导出 `Fingerprint`。
- `platform/backend/cmd/server/main.go` — 装配 `HealthReconciler` + `Start(backupCtx)`。
- `platform/frontend/app/applications/page.tsx` — headless 健康徽标(替 URL)+ ArtifactsSection 跳过 headless + 建表单 app_kind 选项。

---

## Task 1: 数据模型基础(AppKindHeadless + restart_count 列 + store 读列)

**Files:**

- Create: `platform/backend/internal/db/migrations/pg/000032_appdeploy_headless_health.up.sql`
- Create: `platform/backend/internal/db/migrations/pg/000032_appdeploy_headless_health.down.sql`
- Modify: `platform/backend/internal/appdeploy/model.go`(AppKind 常量块 + AppInstance struct)
- Modify: `platform/backend/internal/appdeploy/store.go`(`insCols`)

**Interfaces:**

- Produces: `AppKindHeadless = "headless"` 常量;`AppInstance.RestartCount int` 字段(db:`restart_count`);`insCols` 含 `restart_count`(后续 task 读用)。

- [ ] **Step 1: 写迁移 up**

`platform/backend/internal/db/migrations/pg/000032_appdeploy_headless_health.up.sql`:

```sql
-- 000032: headless 运行态健康监控——appdeploy_instance 加 restart_count 列。
-- 存 HealthReconciler 上次观测的 docker RestartCount,做 crash-loop 增量判定(非粘)。
-- 不写显式 BEGIN/COMMIT:migrate runner 每迁移包一事务(与既有迁移一致)。
ALTER TABLE appdeploy_instance ADD COLUMN IF NOT EXISTS restart_count INT NOT NULL DEFAULT 0;
```

- [ ] **Step 2: 写迁移 down**

`platform/backend/internal/db/migrations/pg/000032_appdeploy_headless_health.down.sql`:

```sql
-- 000032 回滚:删 restart_count 列。历史 reconcile 观测值丢失(可由下一轮 reconcile 重建)。
ALTER TABLE appdeploy_instance DROP COLUMN IF EXISTS restart_count;
```

- [ ] **Step 3: model.go 加 AppKindHeadless 常量**

`platform/backend/internal/appdeploy/model.go` 的 AppKind 常量块(当前 `AppKindWeb`..`AppKindService`),在 `AppKindService` 后加一行:

```go
const (
	AppKindWeb      = "web"     // 现有链路（docker build→容器→URL）
	AppKindDesktop  = "desktop" // 桌面安装包（exe/dmg/AppImage）
	AppKindMobile   = "mobile"  // 移动安装包（apk/ipa）
	AppKindCLI      = "cli"     // 命令行二进制
	AppKindService  = "service" // 后端服务（可能非 HTTP，本期等同 web）
	AppKindHeadless = "headless" // 无端口长驻进程(bot/worker),进程存活健康监控
)
```

- [ ] **Step 4: model.go AppInstance 加 RestartCount 字段**

`AppInstance` struct 的 `BuildLog` 字段后加:

```go
	BuildLog      string       `json:"build_log,omitempty" db:"build_log"`
	RestartCount  int          `json:"restart_count" db:"restart_count"` // 上次观测的 docker RestartCount(reconcile 增量判定基准)
	CreatedAt     time.Time    `json:"created_at" db:"created_at"`
```

- [ ] **Step 5: store.go insCols 加 restart_count**

`platform/backend/internal/appdeploy/store.go` 的 `insCols` 常量,在 `build_log` 后、`created_at` 前插 `restart_count`:

```go
const insCols = `id, app_id, env, COALESCE(image,'') AS image, COALESCE(container_name,'') AS container_name, host_port, COALESCE(url,'') AS url, version, status, COALESCE(last_error,'') AS last_error, COALESCE(build_log,'') AS build_log, restart_count, created_at, updated_at`
```

- [ ] **Step 6: 编译验证**

Run: `cd platform/backend && go build ./...`
Expected: 编译通过(新字段有默认零值,存量 SELECT/INSERT 不受影响)。

- [ ] **Step 7: 迁移落地验证(anp_test)**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestStore_CreateManagedDefault -p 1 -v`
Expected: PASS(newTestStore 跑迁移含 000032,实例查询带 restart_count 列不报错)。

- [ ] **Step 8: Commit**

```bash
git add platform/backend/internal/db/migrations/pg/000032_*.sql platform/backend/internal/appdeploy/model.go platform/backend/internal/appdeploy/store.go
git commit -m "$(cat <<'EOF'
feat(appdeploy): headless 运行态基础(AppKindHeadless + restart_count 列)

- 迁移 000032:appdeploy_instance 加 restart_count(INT NOT NULL DEFAULT 0)
- model:AppKindHeadless 常量 + AppInstance.RestartCount 字段
- store:insCols 读 restart_count

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Deployer headless 无端口部署分支 + InspectHealth

**Files:**

- Modify: `platform/backend/internal/appdeploy/deployer.go`(`Deploy` 重构 + `InspectHealth`/`parseInspectHealth`/`ContainerHealth`)
- Test: `platform/backend/internal/appdeploy/deployer_test.go`

**Interfaces:**

- Consumes: `AppKindHeadless`(Task 1)。
- Produces: `Deployer.Deploy` 对 headless 跳过 `-p`/URL/PORT;`Deployer.InspectHealth(ctx, container) (ContainerHealth, error)`;`parseInspectHealth(out) (ContainerHealth, error)` 纯函数;`ContainerHealth{Running, RestartCount, ExitCode, OOMKilled}`。

- [ ] **Step 1: 写 parseInspectHealth 失败测试**

`platform/backend/internal/appdeploy/deployer_test.go` 末尾加:

```go
func TestParseInspectHealth(t *testing.T) {
	cases := []struct{ in string; want ContainerHealth }{
		{"running|3|0|false", ContainerHealth{Running: true, RestartCount: 3, ExitCode: 0, OOMKilled: false}},
		{"exited|5|137|true", ContainerHealth{Running: false, RestartCount: 5, ExitCode: 137, OOMKilled: true}},
		{"restarting|2|0|false", ContainerHealth{Running: false, RestartCount: 2, ExitCode: 0, OOMKilled: false}},
	}
	for _, c := range cases {
		got, err := parseInspectHealth(c.in)
		if err != nil {
			t.Fatalf("parseInspectHealth(%q) err: %v", c.in, err)
		}
		if got != c.want {
			t.Fatalf("parseInspectHealth(%q) = %+v, want %+v", c.in, got, c.want)
		}
	}
	if _, err := parseInspectHealth("bad|format"); err == nil {
		t.Fatal("parseInspectHealth 期望 4 字段,错误格式应报错")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestParseInspectHealth -p 1 -v`
Expected: FAIL(`parseInspectHealth` 未定义)。

- [ ] **Step 3: 写 ContainerHealth + parseInspectHealth + InspectHealth**

`platform/backend/internal/appdeploy/deployer.go`,在文件末尾(`dockerRun` 变量前)加:

```go
// ContainerHealth docker inspect 解析出的容器健康观测。
type ContainerHealth struct {
	Running      bool
	RestartCount int
	ExitCode     int
	OOMKilled    bool
}

// InspectHealth 经 docker inspect 取容器健康观测。走 docker socket,不受应用网络拓扑影响。
func (d *Deployer) InspectHealth(ctx context.Context, container string) (ContainerHealth, error) {
	out, err := runDocker(ctx, "inspect", "--format",
		"{{.State.Status}}|{{.RestartCount}}|{{.State.ExitCode}}|{{.State.OOMKilled}}", container)
	if err != nil {
		return ContainerHealth{}, fmt.Errorf("docker inspect %s: %w: %s", container, err, out)
	}
	return parseInspectHealth(strings.TrimSpace(out))
}

// parseInspectHealth 解析 `status|restartCount|exitCode|oomKilled` 输出。纯函数,可单测。
func parseInspectHealth(out string) (ContainerHealth, error) {
	parts := strings.Split(out, "|")
	if len(parts) != 4 {
		return ContainerHealth{}, fmt.Errorf("parseInspectHealth: expect 4 fields, got %d in %q", len(parts), out)
	}
	rc, _ := strconv.Atoi(parts[1])
	ec, _ := strconv.Atoi(parts[2])
	return ContainerHealth{
		Running:      parts[0] == "running",
		RestartCount: rc,
		ExitCode:     ec,
		OOMKilled:    parts[3] == "true",
	}, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestParseInspectHealth -p 1 -v`
Expected: PASS。

- [ ] **Step 5: 写 headless 部署分支失败测试**

`deployer_test.go` 加:

```go
// TestDeploy_Headless_NoPortNoURL headless 部署:无 -p、无 PORT=、无 URL、无 HostPort。
func TestDeploy_Headless_NoPortNoURL(t *testing.T) {
	var got []string
	orig := dockerRun
	dockerRun = func(_ context.Context, _ string, args ...string) (string, error) {
		got = args
		return "cid", nil
	}
	defer func() { dockerRun = orig }()

	d := NewDeployer("10.10.0.28")
	ins := &AppInstance{Env: EnvTest, Version: 1}
	a := &Application{Name: "bot", AppKind: AppKindHeadless, InternalPort: 0}
	if err := d.Deploy(context.Background(), a, ins, []string{"FOO=bar"}, ""); err != nil {
		t.Fatal(err)
	}
	for _, arg := range got {
		if arg == "-p" {
			t.Fatal("headless 部署不得映射端口(-p)")
		}
		if strings.HasPrefix(arg, "PORT=") {
			t.Fatal("headless 部署不得注入 PORT=")
		}
	}
	if ins.URL != "" {
		t.Fatalf("headless 不得设 URL,得到 %q", ins.URL)
	}
	if ins.HostPort != 0 {
		t.Fatalf("headless 不得设 HostPort,得到 %d", ins.HostPort)
	}
	if ins.ContainerName == "" {
		t.Fatal("container name 未设")
	}
}

// TestDeploy_Web_StillMapsPort 回归:web 仍 -p + 设 URL。
func TestDeploy_Web_StillMapsPort(t *testing.T) {
	var got []string
	orig := dockerRun
	dockerRun = func(_ context.Context, _ string, args ...string) (string, error) {
		got = args
		return "cid", nil
	}
	defer func() { dockerRun = orig }()
	// usedPortsOn 走 dockerRun,返回空 → AllocFreePort 取 min(9100)
	d := NewDeployer("10.10.0.28")
	ins := &AppInstance{Env: EnvTest, Version: 1}
	a := &Application{Name: "webapp", AppKind: AppKindWeb, InternalPort: 3000}
	if err := d.Deploy(context.Background(), a, ins, nil, ""); err != nil {
		t.Fatal(err)
	}
	hasP := false
	for i, arg := range got {
		if arg == "-p" {
			hasP = true
			if got[i+1] != "9100:3000" {
				t.Fatalf("web -p 映射 = %s,期望 9100:3000", got[i+1])
			}
		}
	}
	if !hasP {
		t.Fatal("web 部署必须有 -p")
	}
	if ins.URL == "" {
		t.Fatal("web 部署必须设 URL")
	}
}
```

注:`deployer_test.go` 顶部若未 import `context`/`strings`,补上。

- [ ] **Step 6: 运行测试确认失败**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run 'TestDeploy_Headless|TestDeploy_Web' -p 1 -v`
Expected: FAIL(headless 走旧逻辑,会分配端口设 URL)。

- [ ] **Step 7: 重构 Deploy 加 headless 分支**

`platform/backend/internal/appdeploy/deployer.go` 把现有 `Deploy` 方法(`func (d *Deployer) Deploy(...)` 到 `return nil` 整段)替换为:

```go
// Deploy 运行容器。headless:无端口/无 URL;其余:按环境端口映射 + URL。dockerHost 非空时远程部署。
func (d *Deployer) Deploy(ctx context.Context, a *Application, ins *AppInstance, env []string, dockerHost string) error {
	name := fmt.Sprintf("appdeploy-%s-%s-v%d", dockerSlug(a.Name), ins.Env, ins.Version)
	args := []string{"run", "-d", "--name", name, "--restart", "unless-stopped"}

	if a.AppKind == AppKindHeadless {
		// headless(bot/worker):无端口、无 URL。不分配宿主端口、不注入 PORT、不 -p。
		for _, e := range env {
			args = append(args, "-e", e)
		}
		args = append(args, ins.Image)
		out, err := runDockerOn(ctx, dockerHost, args...)
		if err != nil {
			return fmt.Errorf("docker run 失败: %w: %s", err, out)
		}
		ins.ContainerName = name
		return nil // ins.HostPort/URL 保持零值
	}

	// web/service 等:端口映射 + URL(原有逻辑)
	min, max := d.envPortRange(ins.Env)
	used := d.usedPortsOn(ctx, dockerHost)
	port := ins.HostPort
	if _, occupied := used[port]; port < min || port > max || occupied {
		port = AllocFreePort(used, min, max)
	}
	if port == 0 {
		return fmt.Errorf("无可用宿主端口（%s 环境 %d-%d 已满）", ins.Env, min, max)
	}
	env = ensurePORTEnv(env, a.InternalPort)
	for _, e := range env {
		args = append(args, "-e", e)
	}
	args = append(args, "-p", fmt.Sprintf("%d:%d", port, a.InternalPort), ins.Image)
	out, err := runDockerOn(ctx, dockerHost, args...)
	if err != nil {
		return fmt.Errorf("docker run 失败: %w: %s", err, out)
	}
	ins.ContainerName = name
	ins.HostPort = port
	urlHost := d.host
	if dockerHost != "" {
		parts := strings.Split(strings.TrimPrefix(strings.TrimPrefix(dockerHost, "tcp://"), "http://"), ":")
		if len(parts) > 0 && parts[0] != "" {
			urlHost = parts[0]
		}
	}
	ins.URL = fmt.Sprintf("http://%s:%d", urlHost, port)
	return nil
}
```

- [ ] **Step 8: 运行测试确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run 'TestDeploy_Headless|TestDeploy_Web' -p 1 -v`
Expected: PASS(两条)。

- [ ] **Step 9: 全包回归**

Run: `cd platform/backend && go test ./internal/appdeploy/ -p 1 -count=1` 2>&1 | tail -20
Expected: PASS(无回归;Deploy 改动对 web 行为不变)。

- [ ] **Step 10: Commit**

```bash
git add platform/backend/internal/appdeploy/deployer.go platform/backend/internal/appdeploy/deployer_test.go
git commit -m "$(cat <<'EOF'
feat(appdeploy): Deploy headless 无端口分支 + InspectHealth

- Deploy:app_kind=headless 时跳过 -p/URL/PORT,仅 docker run -d --restart
- InspectHealth/parseInspectHealth:docker inspect 解析 running/restart/exit/oom
- 测试:headless 无端口无 URL;web 回归仍 -p+URL

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: handler 校验 + docker 部署编排 headless 守卫

**Files:**

- Modify: `platform/backend/internal/appdeploy/handler.go`(`validAppKind` ~861;docket 部署方法 routeWriter 块 ~1651;部署响应 probeHealth ~1972)
- Test: `platform/backend/internal/appdeploy/handler_test.go`

**Interfaces:**

- Consumes: `AppKindHeadless`(Task 1)、`Deploy` headless 分支(Task 2)。
- Produces:`validAppKind("headless")==true`;docker 部署成功后 headless 跳过 routeWriter;部署响应 headless 用 status 而非 probeHealth(URL)。

- [ ] **Step 1: 写 validAppKind 测试**

`platform/backend/internal/appdeploy/handler_test.go` 加:

```go
func TestValidAppKind_IncludesHeadless(t *testing.T) {
	if !validAppKind(AppKindHeadless) {
		t.Fatal("validAppKind 应接受 headless")
	}
	if !validAppKind("") { // 空默认 web,合法
		t.Fatal("validAppKind 应接受空(默认 web)")
	}
	if validAppKind("bogus") {
		t.Fatal("validAppKind 应拒绝非法 kind")
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestValidAppKind_IncludesHeadless -p 1 -v`
Expected: FAIL(headless 不在 case)。

- [ ] **Step 3: validAppKind 加 headless**

`handler.go` 的 `validAppKind`(约 861 行)替换为:

```go
// validAppKind 应用形态合法性校验(web/desktop/mobile/cli/service/headless)。
func validAppKind(k string) bool {
	switch k {
	case "", AppKindWeb, AppKindDesktop, AppKindMobile, AppKindCLI, AppKindService, AppKindHeadless:
		return true
	}
	return false
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestValidAppKind_IncludesHeadless -p 1 -v`
Expected: PASS。

- [ ] **Step 5: docker 部署成功后跳过 routeWriter(headless)**

`handler.go` docker 部署方法里(约 1651-1659 行,`Deploy` 成功后写 appgw 路由块),把整个 `if h.routeWriter != nil { ... }` 块外层再加 headless 守卫:

```go
	// 写 appgw 路由表:部署成功即时把 /apps/<app_id>/ 映射到本环境容器。
	// headless 无端口/无 URL,不写 HTTP 路由。失败不阻塞部署;routeWriter nil = 未启用 appgw。
	if h.routeWriter != nil && a.AppKind != AppKindHeadless {
		if err := h.routeWriter.UpsertRoute(ctx, a.ID, a.ProjectSpaceID, env, h.deployer.host, ins.HostPort); err != nil {
			ins.LastError = "部署成功但 appgw 路由表写入失败: " + err.Error()
			_ = h.store.UpdateInstance(ctx, ins)
			zap.L().Warn("appgw 路由表写入失败（部署仍成功）",
				zap.String("app_id", a.ID), zap.String("env", env), zap.Error(err))
		}
	}
```

- [ ] **Step 6: 部署响应 probeHealth 守卫(headless)**

`handler.go` 部署成功响应构建处(约 1971-1972 行,`"url": ins.URL, ... "health": probeHealth(ins.URL)`),改为按 kind 取 health:

```go
		"env": env,
		"url": ins.URL,
		"deployed": true,
		"stats": stats,
		"health": appDeployHealth(a, ins),
```

并在 `handler.go` 合适位置(该响应函数附近)加小助手:

```go
// appDeployHealth 部署响应的健康字段:headless 用实例 status(无 URL 可探);其余按 URL 探活。
func appDeployHealth(a *Application, ins *AppInstance) string {
	if a.AppKind == AppKindHeadless {
		return ins.Status // running/degraded/failed(由 HealthReconciler 维护;刚部署=running)
	}
	return probeHealth(ins.URL)
}
```

(若 `a`/`ins` 在该响应作用域内变量名不同,按实际名调整;`probeHealth` 为已有函数。)

- [ ] **Step 7: 编译验证**

Run: `cd platform/backend && go build ./...`
Expected: 编译通过。

- [ ] **Step 8: 回归**

Run: `cd platform/backend && go test ./internal/appdeploy/ -p 1 -count=1` 2>&1 | tail -20
Expected: PASS。

- [ ] **Step 9: Commit**

```bash
git add platform/backend/internal/appdeploy/handler.go platform/backend/internal/appdeploy/handler_test.go
git commit -m "$(cat <<'EOF'
feat(appdeploy): handler 接受 headless + docker 部署守卫

- validAppKind 加 headless
- docker 部署成功后 headless 跳过 appgw routeWriter(无端口无路由)
- 部署响应 health:headless 用实例 status,其余 probeHealth(URL)

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Store headless 健康查询方法

**Files:**

- Modify: `platform/backend/internal/appdeploy/store.go`(加 `headlessInstance` 类型 + 3 方法)
- Test: `platform/backend/internal/appdeploy/store_test.go`

**Interfaces:**

- Consumes: `insCols` 含 restart_count(Task 1)。
- Produces: `headlessInstance` 行类型;`Store.ListHeadlessActiveInstances()` / `UpdateInstanceHealth(appID,env,status,lastErr,restartCount)` / `UpdateRestartCount(appID,env,count)`。`*Store` 满足 `InstanceHealthStore` 接口(Task 5 定义)。

- [ ] **Step 1: 写查询测试**

`platform/backend/internal/appdeploy/store_test.go` 加:

```go
// TestStore_ListHeadlessActiveInstances 只返回 headless 且 running/degraded 的实例,带 restart_count + 项目空间。
func TestStore_ListHeadlessActiveInstances(t *testing.T) {
	s := newTestStore(t)
	ps := "ps_test_headless"
	// headless running 实例(应命中)
	ah := &Application{ProjectSpaceID: ps, Name: "bot1", AppKind: AppKindHeadless, InternalPort: 0}
	if err := s.Create(context.Background(), ah); err != nil {
		t.Fatal(err)
	}
	ih := &AppInstance{ID: "ins_h1", AppID: ah.ID, Env: EnvTest, Status: "running"}
	if _, err := s.db.ExecContext(context.Background(),
		`INSERT INTO appdeploy_instance (id, app_id, env, status, restart_count) VALUES ($1,$2,$3,$4,2)`,
		ih.ID, ih.AppID, ih.Env, ih.Status, 2); err != nil {
		t.Fatal(err)
	}
	// web running 实例(不应命中)
	aw := &Application{ProjectSpaceID: ps, Name: "web1", AppKind: AppKindWeb, InternalPort: 3000}
	s.Create(context.Background(), aw)
	s.db.ExecContext(context.Background(),
		`INSERT INTO appdeploy_instance (id, app_id, env, status) VALUES ($1,$2,$3,'running')`,
		"ins_w1", aw.ID, EnvTest)
	// headless stopped 实例(不应命中)
	ih2 := &AppInstance{ID: "ins_h2", AppID: ah.ID, Env: EnvProd, Status: "stopped"}
	s.db.ExecContext(context.Background(),
		`INSERT INTO appdeploy_instance (id, app_id, env, status) VALUES ($1,$2,$3,'stopped')`,
		ih2.ID, ih2.AppID, ih2.Env)

	got, err := s.ListHeadlessActiveInstances(context.Background())
	if err != nil {
		t.Fatal(err)
	}
	if len(got) != 1 {
		t.Fatalf("期望 1 条 headless 活跃实例,得到 %d", len(got))
	}
	if got[0].AppID != ah.ID || got[0].RestartCount != 2 || got[0].Name != "bot1" || got[0].ProjectSpaceID != ps {
		t.Fatalf("返回字段不对: %+v", got[0])
	}
}

// TestStore_UpdateInstanceHealth 写 status+last_error+restart_count。
func TestStore_UpdateInstanceHealth(t *testing.T) {
	s := newTestStore(t)
	ps := "ps_test_uh"
	a := &Application{ProjectSpaceID: ps, Name: "bot2", AppKind: AppKindHeadless, InternalPort: 0}
	s.Create(context.Background(), a)
	s.db.ExecContext(context.Background(),
		`INSERT INTO appdeploy_instance (id, app_id, env, status) VALUES ('ins_uh',$1,$2,'running')`, a.ID, EnvTest)
	if err := s.UpdateInstanceHealth(context.Background(), a.ID, EnvTest, "degraded", "crash-loop", 7); err != nil {
		t.Fatal(err)
	}
	ins, _ := s.GetInstance(context.Background(), a.ID, EnvTest)
	if ins.Status != "degraded" || ins.LastError != "crash-loop" || ins.RestartCount != 7 {
		t.Fatalf("UpdateInstanceHealth 未生效: %+v", ins)
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run 'TestStore_ListHeadlessActiveInstances|TestStore_UpdateInstanceHealth' -p 1 -v`
Expected: FAIL(方法未定义)。

- [ ] **Step 3: 实现 headlessInstance 类型 + 3 方法**

`platform/backend/internal/appdeploy/store.go`,在 `ListInstancesByApp` 后加:

```go
// headlessInstance HealthReconciler 巡检目标行(appdeploy_instance JOIN appdeploy_application)。
type headlessInstance struct {
	AppID          string `db:"app_id"`
	Env            string `db:"env"`
	ContainerName  string `db:"container_name"`
	Status         string `db:"status"`
	RestartCount   int    `db:"restart_count"`
	ProjectSpaceID string `db:"project_space_id"`
	Name           string `db:"name"`
}

// ListHeadlessActiveInstances 列出需健康巡检的实例:headless 应用 且 status∈{running,degraded}。
func (s *Store) ListHeadlessActiveInstances(ctx context.Context) ([]headlessInstance, error) {
	var list []headlessInstance
	err := s.db.SelectContext(ctx, &list,
		`SELECT i.app_id, i.env, COALESCE(i.container_name,'') AS container_name,
		        i.status, i.restart_count, a.project_space_id, a.name
		 FROM appdeploy_instance i
		 JOIN appdeploy_application a ON a.id = i.app_id
		 WHERE a.app_kind='headless' AND i.status IN ('running','degraded')`)
	return list, err
}

// UpdateInstanceHealth 更新实例 status + last_error + restart_count(reconcile 翻转时用)。
func (s *Store) UpdateInstanceHealth(ctx context.Context, appID, env, status, lastErr string, restartCount int) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE appdeploy_instance SET status=$1, last_error=$2, restart_count=$3, updated_at=CURRENT_TIMESTAMP
		 WHERE app_id=$4 AND env=$5`, status, lastErr, restartCount, appID, env)
	return err
}

// UpdateRestartCount 仅更新 restart_count 基线(status 未翻转时用)。
func (s *Store) UpdateRestartCount(ctx context.Context, appID, env string, restartCount int) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE appdeploy_instance SET restart_count=$1 WHERE app_id=$2 AND env=$3`, restartCount, appID, env)
	return err
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run 'TestStore_ListHeadlessActiveInstances|TestStore_UpdateInstanceHealth' -p 1 -v`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add platform/backend/internal/appdeploy/store.go platform/backend/internal/appdeploy/store_test.go
git commit -m "$(cat <<'EOF'
feat(appdeploy): store headless 健康查询方法

- ListHeadlessActiveInstances:JOIN app,筛 app_kind=headless 且 status∈{running,degraded}
- UpdateInstanceHealth / UpdateRestartCount:reconcile 写 status/last_error/restart_count

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: HealthReconciler + aggregateHealth

**Files:**

- Create: `platform/backend/internal/appdeploy/health_reconcile.go`
- Test: `platform/backend/internal/appdeploy/health_reconcile_test.go`

**Interfaces:**

- Consumes: `ContainerHealth`/`InspectHealth`(Task 2)、store 方法(Task 4)。
- Produces: `HealthReconciler`、`aggregateHealth`、`NewHealthReconciler(store, deployer, alerter)`、接口 `InstanceHealthStore`/`HealthInspector`/`HealthAlerter`。`*Store` 满足 `InstanceHealthStore`,`*Deployer` 满足 `HealthInspector`。

- [ ] **Step 1: 写 aggregateHealth 失败测试**

`platform/backend/internal/appdeploy/health_reconcile_test.go`:

```go
package appdeploy

import (
	"context"
	"errors"
	"testing"
	"time"
)

func TestAggregateHealth(t *testing.T) {
	cases := []struct {
		name        string
		h           ContainerHealth
		stored      int
		wantStatus  string
		wantCount   int
	}{
		{"exited→failed", ContainerHealth{Running: false, RestartCount: 5}, 5, "failed", 5},
		{"running 稳定→running", ContainerHealth{Running: true, RestartCount: 2}, 2, "running", 2},
		{"冷启动基线(有历史重启)→running 不告警", ContainerHealth{Running: true, RestartCount: 9}, 0, "running", 9},
		{"crash-loop(本周期新增≥3)→degraded", ContainerHealth{Running: true, RestartCount: 8}, 5, "degraded", 8},
		{"曾 degraded,无新增→running(非粘恢复)", ContainerHealth{Running: true, RestartCount: 8}, 8, "running", 8},
	}
	for _, c := range cases {
		got, cnt := aggregateHealth(c.h, c.stored, 3)
		if got != c.wantStatus || cnt != c.wantCount {
			t.Fatalf("%s: got (%s,%d) want (%s,%d)", c.name, got, cnt, c.wantStatus, c.wantCount)
		}
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestAggregateHealth -p 1 -v`
Expected: FAIL(`aggregateHealth` 未定义)。

- [ ] **Step 3: 写 health_reconcile.go(接口 + aggregateHealth + HealthReconciler)**

`platform/backend/internal/appdeploy/health_reconcile.go`:

```go
package appdeploy

import (
	"context"
	"fmt"
	"time"
)

// InstanceHealthStore HealthReconciler 需要的 store 子集(*Store 满足)。
type InstanceHealthStore interface {
	ListHeadlessActiveInstances(ctx context.Context) ([]headlessInstance, error)
	UpdateInstanceHealth(ctx context.Context, appID, env, status, lastErr string, restartCount int) error
	UpdateRestartCount(ctx context.Context, appID, env string, restartCount int) error
}

// HealthInspector 容器健康探查(*Deployer 满足)。
type HealthInspector interface {
	InspectHealth(ctx context.Context, container string) (ContainerHealth, error)
}

// HealthAlerter 不健康/恢复告警(ops 实现见 health_alerter.go)。
type HealthAlerter interface {
	OnUnhealthy(ctx context.Context, projectSpaceID, appID, appName, env, severity, reason string) error
	OnRecovered(ctx context.Context, projectSpaceID, appID, appName, env string) error
}

// HealthReconciler 周期巡检 headless 实例进程存活,翻 status + 联动告警。仿 ServerMonitor.Start。
type HealthReconciler struct {
	store    InstanceHealthStore
	deployer HealthInspector
	alerter  HealthAlerter
	interval time.Duration
	burst    int // 单周期新增重启阈值,判 crash-loop
}

func NewHealthReconciler(store InstanceHealthStore, deployer HealthInspector, alerter HealthAlerter) *HealthReconciler {
	return &HealthReconciler{store: store, deployer: deployer, alerter: alerter, interval: 30 * time.Second, burst: 3}
}

// Start 后台 ticker 循环,直到 ctx 取消。内部起 goroutine,非阻塞。
func (r *HealthReconciler) Start(ctx context.Context) {
	ticker := time.NewTicker(r.interval)
	go func() {
		defer ticker.Stop()
		for {
			select {
			case <-ctx.Done():
				return
			case <-ticker.C:
				r.reconcileOnce(ctx)
			}
		}
	}()
}

func (r *HealthReconciler) reconcileOnce(ctx context.Context) {
	defer func() {
		if rec := recover(); rec != nil {
			fmt.Printf("health reconcile panic: %v\n", rec)
		}
	}()
	targets, err := r.store.ListHeadlessActiveInstances(ctx)
	if err != nil {
		return
	}
	for _, t := range targets {
		r.checkOne(ctx, t)
	}
}

func (r *HealthReconciler) checkOne(ctx context.Context, t headlessInstance) {
	defer func() {
		if rec := recover(); rec != nil {
			fmt.Printf("health check panic app=%s env=%s: %v\n", t.AppID, t.Env, rec)
		}
	}()
	h, err := r.deployer.InspectHealth(ctx, t.ContainerName)
	if err != nil {
		// inspect 失败(docker 不可达/容器查不到):记 last_error,保留原 status,不误判崩溃
		_ = r.store.UpdateInstanceHealth(ctx, t.AppID, t.Env, t.Status, "inspect 失败: "+err.Error(), t.RestartCount)
		return
	}
	newStatus, newCount := aggregateHealth(h, t.RestartCount, r.burst)
	if newStatus != t.Status {
		reason := describeHealth(h, t.RestartCount)
		_ = r.store.UpdateInstanceHealth(ctx, t.AppID, t.Env, newStatus, reason, newCount)
		switch newStatus {
		case "degraded", "failed":
			_ = r.alerter.OnUnhealthy(ctx, t.ProjectSpaceID, t.AppID, t.Name, t.Env, severityFor(newStatus), reason)
		case "running":
			_ = r.alerter.OnRecovered(ctx, t.ProjectSpaceID, t.AppID, t.Name, t.Env)
		}
	} else if newCount != t.RestartCount {
		_ = r.store.UpdateRestartCount(ctx, t.AppID, t.Env, newCount) // 仅更新基线
	}
}

// aggregateHealth 由 docker 观测 + 上次存储 restart_count 推导新 status 与新基线。纯函数,增量判 crash-loop(非粘)。
func aggregateHealth(h ContainerHealth, storedCount, burst int) (string, int) {
	newCount := h.RestartCount
	if !h.Running {
		return "failed", newCount
	}
	if storedCount == 0 && h.RestartCount > 0 {
		return "running", newCount // 冷启动基线:首次观测到历史重启,只记不告警
	}
	if newCount-storedCount >= burst {
		return "degraded", newCount // 本周期新增 ≥burst = 活跃 crash-loop
	}
	return "running", newCount
}

func describeHealth(h ContainerHealth, stored int) string {
	if !h.Running {
		if h.OOMKilled {
			return fmt.Sprintf("容器退出(OOMKilled exit=%d)", h.ExitCode)
		}
		return fmt.Sprintf("容器退出(exit=%d)", h.ExitCode)
	}
	return fmt.Sprintf("crash-loop(本周期新增重启 %d 次)", h.RestartCount-stored)
}

func severityFor(status string) string {
	if status == "failed" {
		return "critical"
	}
	return "warning"
}
```

- [ ] **Step 4: 运行 aggregateHealth 测试确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestAggregateHealth -p 1 -v`
Expected: PASS。

- [ ] **Step 5: 写 reconcile 翻转测试(fakes)**

`health_reconcile_test.go` 加:

```go
type fakeInspector struct{ h ContainerHealth; err error }

func (f *fakeInspector) InspectHealth(_ context.Context, _ string) (ContainerHealth, error) {
	return f.h, f.err
}

type fakeAlerter struct {
	unhealthy []string // 记 appID+env
	recovered []string
}

func (a *fakeAlerter) OnUnhealthy(_ context.Context, _, appID, _, env, _, _ string) error {
	a.unhealthy = append(a.unhealthy, appID+":"+env)
	return nil
}
func (a *fakeAlerter) OnRecovered(_ context.Context, _, appID, _, env string) error {
	a.recovered = append(a.recovered, appID+":"+env)
	return nil
}

// 直接测 checkOne(绕过 ticker):崩溃翻 failed+告警;恢复翻 running+resolved;restart_count 回写。
func TestCheckOne_CrashAndRecover(t *testing.T) {
	store := newTestStore(t)
	ps := "ps_rec"
	a := &Application{ProjectSpaceID: ps, Name: "bot", AppKind: AppKindHeadless, InternalPort: 0}
	store.Create(context.Background(), a)
	store.db.ExecContext(context.Background(),
		`INSERT INTO appdeploy_instance (id, app_id, env, status, container_name, restart_count)
		 VALUES ('ins1',$1,$2,'running','bot-ctr',0)`, a.ID, EnvTest)

	al := &fakeAlerter{}
	r := &HealthReconciler{store: store, deployer: &fakeInspector{h: ContainerHealth{Running: false, ExitCode: 137}}, alerter: al, interval: time.Second, burst: 3}

	// 崩溃 → failed + 告警
	r.checkOne(context.Background(), headlessInstance{AppID: a.ID, Env: EnvTest, ContainerName: "bot-ctr", Status: "running", RestartCount: 0, ProjectSpaceID: ps, Name: "bot"})
	ins, _ := store.GetInstance(context.Background(), a.ID, EnvTest)
	if ins.Status != "failed" || len(al.unhealthy) != 1 {
		t.Fatalf("崩溃后应 failed+告警,得 status=%s alerts=%v", ins.Status, al.unhealthy)
	}

	// 恢复(running) → running + recovered
	r.deployer = &fakeInspector{h: ContainerHealth{Running: true, RestartCount: 0}}
	r.checkOne(context.Background(), headlessInstance{AppID: a.ID, Env: EnvTest, ContainerName: "bot-ctr", Status: "failed", RestartCount: 0, ProjectSpaceID: ps, Name: "bot"})
	ins, _ = store.GetInstance(context.Background(), a.ID, EnvTest)
	if ins.Status != "running" || len(al.recovered) != 1 {
		t.Fatalf("恢复后应 running+resolved,得 status=%s recovered=%v", ins.Status, al.recovered)
	}

	// crash-loop → degraded + 告警 + restart_count 回写
	r.deployer = &fakeInspector{h: ContainerHealth{Running: true, RestartCount: 5}}
	r.checkOne(context.Background(), headlessInstance{AppID: a.ID, Env: EnvTest, ContainerName: "bot-ctr", Status: "running", RestartCount: 1, ProjectSpaceID: ps, Name: "bot"})
	ins, _ = store.GetInstance(context.Background(), a.ID, EnvTest)
	if ins.Status != "degraded" || ins.RestartCount != 5 {
		t.Fatalf("crash-loop 应 degraded 且 restart_count=5,得 %+v", ins)
	}

	// 稳定(无新增) → 恢复 running(非粘)
	r.deployer = &fakeInspector{h: ContainerHealth{Running: true, RestartCount: 5}}
	r.checkOne(context.Background(), headlessInstance{AppID: a.ID, Env: EnvTest, ContainerName: "bot-ctr", Status: "degraded", RestartCount: 5, ProjectSpaceID: ps, Name: "bot"})
	ins, _ = store.GetInstance(context.Background(), a.ID, EnvTest)
	if ins.Status != "running" {
		t.Fatalf("crash-loop 停止后应非粘恢复 running,得 %s", ins.Status)
	}
}

// inspect 失败:只记 last_error,不改 status,不告警。
func TestCheckOne_InspectFail_NoFlip(t *testing.T) {
	store := newTestStore(t)
	ps := "ps_if"
	a := &Application{ProjectSpaceID: ps, Name: "bot3", AppKind: AppKindHeadless, InternalPort: 0}
	store.Create(context.Background(), a)
	store.db.ExecContext(context.Background(),
		`INSERT INTO appdeploy_instance (id, app_id, env, status) VALUES ('ins_if',$1,$2,'running')`, a.ID, EnvTest)
	al := &fakeAlerter{}
	r := &HealthReconciler{store: store, deployer: &fakeInspector{err: errors.New("docker unreachable")}, alerter: al, burst: 3}
	r.checkOne(context.Background(), headlessInstance{AppID: a.ID, Env: EnvTest, ContainerName: "x", Status: "running", RestartCount: 0, ProjectSpaceID: ps, Name: "bot3"})
	ins, _ := store.GetInstance(context.Background(), a.ID, EnvTest)
	if ins.Status != "running" || len(al.unhealthy) != 0 {
		t.Fatalf("inspect 失败应保留 running 不告警,得 status=%s alerts=%d", ins.Status, len(al.unhealthy))
	}
}
```

- [ ] **Step 6: 运行确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run 'TestCheckOne' -p 1 -v`
Expected: PASS(4 场景)。

- [ ] **Step 7: 全包回归**

Run: `cd platform/backend && go test ./internal/appdeploy/ -p 1 -count=1` 2>&1 | tail -20
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add platform/backend/internal/appdeploy/health_reconcile.go platform/backend/internal/appdeploy/health_reconcile_test.go
git commit -m "$(cat <<'EOF'
feat(appdeploy): HealthReconciler 进程存活巡检(crash-loop 增量判定)

- 后台 ticker(30s)巡 headless 活跃实例:docker inspect → aggregateHealth
- crash-loop 用「单周期新增重启≥burst」判 degraded,非粘(无新增即恢复 running)
- 翻转才写库+告警;inspect 失败只记 last_error 不误判
- 接口 InstanceHealthStore/HealthInspector/HealthAlerter 解耦

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: ops ResolveByFingerprint + OpsHealthAlerter

**Files:**

- Modify: `platform/backend/internal/ops/store.go`(导出 `Fingerprint` + 加 `ResolveByFingerprint`)
- Create: `platform/backend/internal/appdeploy/health_alerter.go`
- Test: `platform/backend/internal/ops/store_test.go` + `platform/backend/internal/appdeploy/health_reconcile_test.go`(加 OpsHealthAlerter 测试)

**Interfaces:**

- Consumes: `HealthAlerter` 接口(Task 5)、ops `CreateAlert`/`HasFiringFingerprint`(已存在)。
- Produces: `ops.Fingerprint`/`ops.ResolveByFingerprint`;`appdeploy.NewOpsHealthAlerter(*ops.Store)` 满足 `HealthAlerter`。

- [ ] **Step 1: ops 导出 Fingerprint + 加 ResolveByFingerprint**

`platform/backend/internal/ops/store.go`:把 `func fingerprint(source, title string) string` 重命名为 `func Fingerprint(...)`(导出),并更新 `CreateAlert` 内 `a.Fingerprint = fingerprint(...)` → `Fingerprint(...)`。然后在 `ResolveAlert` 后加:

```go
// ResolveByFingerprint 按 fingerprint 把 firing 告警置 resolved(HealthReconciler 恢复时用;无命中 no-op)。
func (s *Store) ResolveByFingerprint(ctx context.Context, fp string) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE ops_alert SET status='resolved', resolved_at=CURRENT_TIMESTAMP WHERE fingerprint=$1 AND status='firing'`, fp)
	return err
}
```

- [ ] **Step 2: 写 OpsHealthAlerter**

`platform/backend/internal/appdeploy/health_alerter.go`:

```go
package appdeploy

import (
	"context"

	"zhiyuan-anp/platform/backend/internal/ops"
)

// OpsHealthAlerter HealthAlerter 的 ops 实现:写 ops_alert 表(fingerprint 去重 + 恢复 resolve)。
type OpsHealthAlerter struct{ store *ops.Store }

func NewOpsHealthAlerter(store *ops.Store) *OpsHealthAlerter { return &OpsHealthAlerter{store: store} }

func alertTitle(appName, env string) string { return "应用 " + appName + " " + env + " 异常" }

func (a *OpsHealthAlerter) OnUnhealthy(ctx context.Context, psID, appID, appName, env, severity, reason string) error {
	title := alertTitle(appName, env)
	fp := ops.Fingerprint("apphealth", title)
	if firing, _ := a.store.HasFiringFingerprint(ctx, fp); firing {
		return nil // 已有 firing 告警,去重
	}
	return a.store.CreateAlert(ctx, &ops.Alert{
		ProjectSpaceID: psID, Source: "apphealth", Severity: severity, Status: "firing",
		Title: title, Description: reason,
	})
}

func (a *OpsHealthAlerter) OnRecovered(ctx context.Context, psID, appID, appName, env string) error {
	return a.store.ResolveByFingerprint(ctx, ops.Fingerprint("apphealth", alertTitle(appName, env)))
}
```

- [ ] **Step 3: 写 ResolveByFingerprint 测试**

`platform/backend/internal/ops/store_test.go` 加(沿用其既有 newTestStore 桩):

```go
// TestStore_ResolveByFingerprint 按 fingerprint resolve firing 告警;无命中 no-op。
func TestStore_ResolveByFingerprint(t *testing.T) {
	s := newTestStore(t) // ops 既有 PG 测试桩;若名字不同按实际改
	ps := "ps_rfp"
	fp := Fingerprint("apphealth", "应用 bot test 异常")
	s.CreateAlert(context.Background(), &Alert{ProjectSpaceID: ps, Source: "apphealth", Severity: "critical", Title: "应用 bot test 异常"})
	if firing, _ := s.HasFiringFingerprint(context.Background(), fp); !firing {
		t.Fatal("应有 firing 告警")
	}
	if err := s.ResolveByFingerprint(context.Background(), fp); err != nil {
		t.Fatal(err)
	}
	if firing, _ := s.HasFiringFingerprint(context.Background(), fp); firing {
		t.Fatal("resolve 后不应再有 firing")
	}
}
```

(若 `ops/store_test.go` 的测试桩函数名不是 `newTestStore`,改为实际名;`Alert` 在 `ops` 包内直接用。)

- [ ] **Step 4: 运行 ops 测试**

Run: `cd platform/backend && go test ./internal/ops/ -run TestStore_ResolveByFingerprint -p 1 -v`
Expected: PASS。

- [ ] **Step 5: 写 OpsHealthAlerter 去重测试(appdeploy 包)**

`health_reconcile_test.go` 加:

```go
import "zhiyuan-anp/platform/backend/internal/ops" // 补到 import 块

// TestOpsHealthAlerter_Dedup 已有 firing 时 OnUnhealthy 跳过(去重)。
func TestOpsHealthAlerter_Dedup(t *testing.T) {
	os := newTestStore(t) // appdeploy 包的 newTestStore 连 anp_test;ops.NewStore 同库可用
	alerter := NewOpsAlerterForTest(os.DB()) // 见 Step 6 测试助手
	ps := "ps_dedup"
	_ = alerter.OnUnhealthy(context.Background(), ps, "app1", "bot", EnvTest, "critical", "exit 1")
	_ = alerter.OnUnhealthy(context.Background(), ps, "app1", "bot", EnvTest, "critical", "exit 1")
	list, _ := ops.NewStore(os.DB()).ListAlerts(context.Background(), ps, "", "firing")
	if len(list) != 1 {
		t.Fatalf("去重后期望 1 条 firing,得到 %d", len(list))
	}
	_ = alerter.OnRecovered(context.Background(), ps, "app1", "bot", EnvTest)
	list, _ = ops.NewStore(os.DB()).ListAlerts(context.Background(), ps, "", "firing")
	if len(list) != 0 {
		t.Fatalf("recover 后期望 0 条 firing,得到 %d", len(list))
	}
}
```

注:`newTestStore` 返回 `*appdeploy.Store`,需暴露底层 `*sqlx.DB`。若 `Store` 无公开 DB 访问器,改用 `appdeploy` 包测试里直接拿 `testutil` 建库(见 Step 6 说明)。

- [ ] **Step 6: 测试助手(暴露 DB 给 ops.NewStore)**

若 `appdeploy.Store` 无 `DB() *sqlx.DB` 访问器,在 `store.go` 加:

```go
// DB 暴露底层连接(跨模块测试/装配用,如 HealthReconciler 装配处复用)。
func (s *Store) DB() *sqlx.DB { return s.db }
```

并把 Step 5 测试里的 `os.DB()` 对应改对。`NewOpsAlerterForTest` 即 `NewOpsHealthAlerter(ops.NewStore(db))`,测试里直接写 `NewOpsHealthAlerter(ops.NewStore(os.DB()))`,删掉 `NewOpsAlerterForTest` 别名。最终 Step 5 的 `alerter :=` 行改为:

```go
odb := ops.NewStore(os.DB())
alerter := NewOpsHealthAlerter(odb)
```

并把后续 `ops.NewStore(os.DB())` 复用 `odb`。

- [ ] **Step 7: 运行确认通过**

Run: `cd platform/backend && go test ./internal/appdeploy/ -run TestOpsHealthAlerter_Dedup -p 1 -v`
Expected: PASS。

- [ ] **Step 8: 编译 + 回归**

Run: `cd platform/backend && go build ./... && go test ./internal/appdeploy/ ./internal/ops/ -p 1 -count=1` 2>&1 | tail -20
Expected: PASS。

- [ ] **Step 9: Commit**

```bash
git add platform/backend/internal/ops/store.go platform/backend/internal/ops/store_test.go platform/backend/internal/appdeploy/health_alerter.go platform/backend/internal/appdeploy/health_reconcile_test.go platform/backend/internal/appdeploy/store.go
git commit -m "$(cat <<'EOF'
feat(appdeploy): OpsHealthAlerter 联动 ops 告警(fingerprint 去重/resolve)

- ops:导出 Fingerprint + ResolveByFingerprint
- appdeploy:OpsHealthAlerter 实现 HealthAlerter,写 ops_alert(去重+恢复 resolve)
- store:DB() 暴露连接(装配/测试复用)

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: main.go 装配 HealthReconciler

**Files:**

- Modify: `platform/backend/cmd/server/main.go`(`monitor.Start(backupCtx)` 附近 ~335)

**Interfaces:**

- Consumes: `NewHealthReconciler`/`NewOpsHealthAlerter`(Task 5/6)、`appDeployStore`(~161)、`opsStore`(~201)、`cfg.AppDeployHost`、`backupCtx`(~322)。

- [ ] **Step 1: 装配并启动 HealthReconciler**

`main.go` 在 `monitor.Start(backupCtx)`(约 335 行)后加:

```go
	// ---- headless 应用健康巡检:HealthReconciler 周期 docker inspect headless 实例 → 翻 status + ops 告警 ----
	healthInspector := appdeploy.NewDeployer(cfg.AppDeployHost)          // 无状态,满足 HealthInspector
	healthAlerter := appdeploy.NewOpsHealthAlerter(opsStore)             // 写 ops_alert
	healthRec := appdeploy.NewHealthReconciler(appDeployStore, healthInspector, healthAlerter)
	healthRec.Start(backupCtx)
```

- [ ] **Step 2: 编译验证**

Run: `cd platform/backend && go build ./...`
Expected: 编译通过(`*Store` 满足 `InstanceHealthStore`、`*Deployer` 满足 `HealthInspector`、`*OpsHealthAlerter` 满足 `HealthAlerter`)。

- [ ] **Step 3: vet**

Run: `cd platform/backend && go vet ./cmd/server/`
Expected: 无错误。

- [ ] **Step 4: Commit**

```bash
git add platform/backend/cmd/server/main.go
git commit -m "$(cat <<'EOF'
feat(main): 装配 HealthReconciler 巡检 headless 实例

与 ServerMonitor 同 backupCtx 后台启动:周期 docker inspect headless 实例,
翻 status(running/degraded/failed)+ ops 告警联动。

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 前端 headless 健康徽标 + 建表单

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx`(Instance 类型 ~13、App 类型注释 ~46、实例渲染 ~121-135、ArtifactsSection 守卫 ~162、建表单 app_kind 选项 ~234)

**Global Constraint(前端):** 写代码前**先读** `platform/frontend/node_modules/next/dist/docs/` 相关指南(本仓魔改 Next.js,`platform/frontend/AGENTS.md` 要求)。改完 `npx tsc --noEmit`;若状态丢失类异常先 `rm -rf .next && npm run build`(memory `stale-next-build-trap`)。

- [ ] **Step 1: 读 Next.js 指南**

Run: `ls platform/frontend/node_modules/next/dist/docs/` 浏览,确认本版本客户端组件/状态用法无 breaking。
Expected: 了解后再动手。

- [ ] **Step 2: Instance 类型加 health 注释、App 类型注释加 headless**

`applications/page.tsx`:

- App 类型(约 46 行)`app_kind: string` 注释改为 `// 应用类型 web/desktop/mobile/cli/service/headless`。
- (Instance 的 `status` 字段已存在,headless 复用它承载 running/degraded/failed,无需加 health 字段。)

- [ ] **Step 3: 实例渲染——headless 显健康徽标替「:端口 状态」**

`applications/page.tsx` 约 121-135 行,把 `testIns`/`prodIns` 两个 span 改为按 `app.app_kind` 分支。在文件顶部组件区(或 `devStep` 同文件的工具函数区)加小助手:

```tsx
// healthBadge headless 实例的健康徽标(进程存活:running/degraded/failed)。
// 配色用本仓既有自定义类:text-success / text-danger(已 grep 确认 app/ 下存在)。
function healthBadge(status: string): { text: string; cls: string } {
  switch (status) {
    case "running":
      return { text: "运行中", cls: "text-success" };
    case "degraded":
      return { text: "不稳定(crash-loop)", cls: "text-danger" };
    case "failed":
      return { text: "已停止", cls: "text-danger" };
    default:
      return { text: status, cls: "" };
  }
}
```

把 121-135 的 test/prod 块替换为:

```tsx
{
  testIns && (
    <span>
      test{" "}
      {app.app_kind === "headless" ? (
        <span className={healthBadge(testIns.status).cls}>{healthBadge(testIns.status).text}</span>
      ) : (
        <span className={testIns.status === "running" ? "text-success" : ""}>
          :{testIns.host_port} {testIns.status}
        </span>
      )}
    </span>
  );
}
{
  prodIns && (
    <span>
      prod{" "}
      {app.app_kind === "headless" ? (
        <span className={healthBadge(prodIns.status).cls}>{healthBadge(prodIns.status).text}</span>
      ) : (
        <span className={prodIns.status === "running" ? "text-success" : ""}>
          :{prodIns.host_port} {prodIns.status}
        </span>
      )}
    </span>
  );
}
```

(确认 `app` 在该渲染作用域可见——121-137 行属渲染 `app` 的组件块,`app.app_kind` 可直接读。)

- [ ] **Step 4: ArtifactsSection 跳过 headless**

`applications/page.tsx` 约 162 行:

```tsx
if (appKind === "web" || appKind === "service") return null;
```

改为:

```tsx
// headless 是运行态应用(无产物),不显产物区。
if (appKind === "web" || appKind === "service" || appKind === "headless") return null;
```

- [ ] **Step 5: 建表单 app_kind 加 headless 选项**

`applications/page.tsx` 约 234 行 `const [appKind, setAppKind] = useState<string>("web");` 附近,找 app_kind 下拉/选择控件(grep `appKind` 在 JSX 里的 `<select>`/`<option>`),给选项列表加 `headless`(label「headless(无端口进程)」)。若该表单无显式下拉而是文本输入,跳过此步。

- [ ] **Step 6: 类型检查**

Run: `cd platform/frontend && npx tsc --noEmit`
Expected: 无类型错误。

- [ ] **Step 7: (如 tsc 后行为异常)清 build 产物重 build**

Run: `cd platform/frontend && rm -rf .next && npm run build`
Expected: build 成功(memory `stale-next-build-trap`)。

- [ ] **Step 8: Commit**

```bash
git add platform/frontend/app/applications/page.tsx
git commit -m "$(cat <<'EOF'
feat(frontend): headless 应用健康徽标(替 URL)+ 跳过产物区

- 实例行 headless 显 running/degraded/failed 徽标,不显 :端口
- ArtifactsSection 跳过 headless(运行态应用,无产物)
- 建表单 app_kind 加 headless 选项

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: .28 端到端验证

**Files:** 无代码改动;部署 + 手动验证(spec §11/§14)。

**Global Constraint:** 本机不跑功能测试;.28(anp_dev 库 + docker)端到端;前后端交叉验证必须真开浏览器看渲染。

- [ ] **Step 1: push**

Run: `cd /d/Projects/智源-ANP平台 && git push origin main`
Expected: push 成功。

- [ ] **Step 2: scp 改动到 .28**

Run(按 memory `deploy-prod-10.10.0.28` 的实际路径调整):

```bash
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp && git pull"
```

Expected: .28 源码更新到含本特性的 HEAD。

- [ ] **Step 3: rebuild backend + 跑迁移**

Run:

```bash
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp && docker-compose up -d --build backend"
```

Expected: backend 重建启动;迁移 000032 自动跑(restart_count 列加成功)。`curl http://10.10.0.28:8088/healthz` → healthy。

- [ ] **Step 4: rebuild frontend(若 Task 8 有前端改动)**

Run:

```bash
ssh -i ~/.ssh/miscode root@10.10.0.28 "cd /opt/anp && docker-compose up -d --build frontend"
```

Expected: frontend 重建。

- [ ] **Step 5: 建 headless 应用 + 部署 test**

浏览器开 `http://10.10.0.28:8088`,建应用 app_kind=headless(或经 API `POST /api/v1/project-spaces/:id/apps` 带 `app_kind=headless`),带一个会前台运行的 Dockerfile(如 `FROM alpine:latest; CMD ["sh","-c","while true; do echo hi; sleep 5; done"]`)。点「构建并部署到 test」。
Expected:

- `.28` 上 `docker ps` 见该容器 running、**无端口映射**(PORTS 列空)。
- 应用实例 URL 为空;详情页实例行显「运行中」徽标,不显链接。

- [ ] **Step 6: 模拟崩溃 → failed + 告警**

Run:`ssh -i ~/.ssh/miscode root@10.10.0.28 "docker stop <容器名>"`(用 docker ps 查到名)。等 ≤30s。
Expected:

- 实例 status 翻 `failed`,徽标变「已停止」。
- ops 告警表出 critical:`SELECT * FROM ops_alert WHERE source='apphealth' ORDER BY fired_at DESC LIMIT 5;`(经 anp_dev 库)。

- [ ] **Step 7: 恢复 → running + resolved**

Run:`docker start <容器名>`。等 ≤30s。
Expected:status 翻 `running`,徽标「运行中」;该告警 `status='resolved'`。

- [ ] **Step 8: crash-loop → degraded(非粘恢复)**

把 Dockerfile CMD 改成立即退出(如 `CMD ["false"]`)重新部署,等 docker 反复重启累计。1-2 个巡检周期后:
Expected:status 翻 `degraded` + warning 告警;徽标「不稳定」。改回正常 CMD 重部署 → 下周期翻 `running`(非粘),告警 resolved。

- [ ] **Step 9: web 回归**

建/部署一个 web 应用:test/prod 实例行显「:端口 running」+ URL;停止/重启 web 应用,确认其 status **不被** HealthReconciler 改动(本期只巡 headless)。
Expected:web 全链路行为不变。

- [ ] **Step 10: inspect 失败不误判**

临时停 docker daemon(`systemctl stop docker` 几秒再起)或观察一次 backend docker 抽风时段:
Expected:headless 实例 status 保持 running,仅 last_error 记「inspect 失败」,不出告警。

- [ ] **Step 11: 记录 e2e 结论**

在 `docs/superpowers/specs/2026-08-02-headless运行-design.md` 末尾或新开 `docs/2026-08-02-headless运行-e2e记录.md`,记上述各步 PASS/FAIL + 实际现象(含 .28 实际数据)。commit。
Expected:闭环文档落库(memory `dev-docs-rule`)。

---

## Self-Review(spec 覆盖核对)

- §4.1 AppKind headless → Task 1。✅
- §4.2 状态语义(running/degraded/failed + 增量)→ Task 5 aggregateHealth + Task 4 store。✅
- §4.4 restart_count 迁移 → Task 1。✅
- §5.1 Deploy 无端口分支 → Task 2。✅
- §5.2 InspectHealth → Task 2。✅
- §5.3 Build 不变 → 无需任务(确认)。✅
- §6 HealthReconciler(循环/翻转/告警/restart_count 回写)→ Task 5 + Task 7 装配。✅
- §7 告警(ops Fingerprint/ResolveByFingerprint/OpsHealthAlerter)→ Task 6。✅
- §8 错误处理(inspect 失败不误判/翻转才告警/panic recover)→ Task 5 checkOne。✅
- §9 前端(健康徽标替 URL)→ Task 8。✅
- §10 测试 → 各 Task 内 TDD 步骤。✅
- §11/§14 .28 e2e → Task 9。✅
- §2 handler 校验/routeWriter 守卫 → Task 3。✅(spec §5 未显式提 routeWriter,但 handler 部署编排必须守卫——属实现细节,Task 3 覆盖)
- 类型一致性:`aggregateHealth` 返回 `(string,int)` 在 Task 5 定义、Task 5 测试用一致;`headlessInstance` 字段在 Task 4 定义、Task 5 checkOne 用一致;`HealthAlerter.OnUnhealthy` 6 参在 Task 5 定义、Task 6 OpsHealthAlerter 实现一致。✅
