# 依赖供给统一 P3：PG 声明化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 把 PG 切纯声明驱动（删 handler 三处自动供给），并修 P2a/P2b 三个遗留（Provision 复用 lag、pg dedicated host 耦合、bind_existing 空 AuthRef），.28 e2e 验收 pg 三策略。

**Architecture:** handler 不再无条件 `pgsupply.Provision`（PG 仅声明才有）；`pgsupply.Provision` 复用只认 `Ready`，`Provisioning`/`Failed` 落续供路径推进到 Ready；pg dedicated 的 `service_instance.Host` 改由 `ProvisionDedicated` 返回的实际 host 单源填充；bind_existing 加通用空连接值守卫。

**Tech Stack:** Go 1.24（appdeploy + mwsupply + pgsupply）；PG（.28 anp_test）；TDD + 回归。

**Spec:** `docs/superpowers/specs/2026-08-05-依赖供给统一P3-pg声明化-design.md`

## Global Constraints

- go 命令前缀 `GOPATH=C:/Users/yxt/go`；禁 SQLite；PG 测试连 .28 anp_test。
- commit `feat(<pkg>): 中文` + body 每行 ≤100 字符 + `Co-Authored-By: Claude <noreply@anthropic.com>`。
- **redis/milvus 零行为变化**：每 task 末 `go test -p 1` 全绿 + `go build ./cmd/server` 通过。
- 自编译：每 task 末 build 通过。
- 不做存量平迁、不做生产回滚（.28 开发测试库，项目后期重导）。

## File Structure

| 文件                                      | 责任                                                    | 动作     |
| ----------------------------------------- | ------------------------------------------------------- | -------- |
| `internal/appdeploy/handler.go`           | 加 `pgAutoProvisioner` 接口字段；删 3 处自动 Provision  | 改（T1） |
| `internal/appdeploy/handler_http_test.go` | spy 注入 + 红绿测（自动供给不再触发）                   | 改（T1） |
| `internal/pgsupply/provisioner.go`        | 复用只认 Ready；加续供路径；加 `isDuplicateDB`          | 改（T2） |
| `internal/pgsupply/provisioner_test.go`   | 续供 4 场景（Ready/Provisioning/Failed/dup-吞）         | 改（T2） |
| `internal/pgsupply/instance.go`           | `ProvisionDedicated` 返回 `host`                        | 改（T3） |
| `internal/mwsupply/spec_pg.go`            | `PgDedicatedRunner` 接口加 host；`SupplyDedicated` 用它 | 改（T3） |
| `internal/mwsupply/spec_pg_ded_test.go`   | fake 加 host + 单源断言                                 | 改（T3） |
| `internal/mwsupply/supply.go`             | bind_existing 加空 `connVal` 守卫                       | 改（T4） |
| `internal/mwsupply/spec_pg_test.go`       | 空 AuthRef → failed 测                                  | 改（T4） |

---

### Task 1: 移除 handler 自动 PG 供给（PG 切纯声明驱动）

**Files:**

- Modify: `internal/appdeploy/handler.go`（`Handler` 结构字段 + `Create` 1233-1237 / `runImportGit-Dir` 1408-1412 / `runImportZip` 1526-1530）
- Test: `internal/appdeploy/handler_http_test.go`

**Interfaces:**

- Produces: `pgAutoProvisioner` 接口（仅 `Provision` 方法）；`Handler.provisioner` 字段类型由 `*pgsupply.Provisioner` 改为此接口。`NewHandler`/`Register` 的入参类型**不动**（仍 `*pgsupply.Provisioner`，赋值给接口字段），故 main.go 与所有测试调用处零改动。

> 背景：现有 handler 测试 `NewHandler(...)` 第 7 参全传 `nil`，`if h.provisioner != nil` 守卫使自动供给在测试里从不触发——纯删调用对测试不可见。把字段改成接口后可注入 spy，写真正的红绿测。字段保留（spec §4.1：pg shared KindSpec 仍用 `*pgsupply.Provisioner` 类型，handler 字段作为负向测试缝保留）。

- [ ] **Step 1: 加接口 + 改字段类型** — `handler.go` 在 `Handler` 结构定义上方加：

```go
// pgAutoProvisioner 是 handler 自动 PG 供给的抽象。
// P3 移除自动供给后 handler 不再调它——保留为接口字段仅为测试注入 spy 验证「不被调」
// （生产仍传 *pgsupply.Provisioner，满足此接口；pg shared 供给走 mwsupply.Reconcile，不经此字段）。
type pgAutoProvisioner interface {
	Provision(ctx context.Context, psID, appID string) (*pgsupply.AppDatabase, error)
}
```

把 `Handler` 结构里的 `provisioner *pgsupply.Provisioner` 改为 `provisioner pgAutoProvisioner`。`NewHandler` 函数体里 `provisioner: provisioner` 赋值不变（concrete→interface 自动适配）。Build 确认通过。

- [ ] **Step 2: 写失败测试（spy 注入）** — `handler_http_test.go` 末尾加：

```go
// countingProv 计数 Provision 调用（spy）。
type countingProv struct{ calls int }

func (c *countingProv) Provision(context.Context, string, string) (*pgsupply.AppDatabase, error) {
	c.calls++
	return nil, nil
}

// TestHandler_Create_NoAutoPGProvision 验证 P3 后建应用不再无条件触发 PG 供给。
// 注入 spy 到 h.provisioner（接口字段），POST 建一 managed 应用 → spy.calls 应 0。
func TestHandler_Create_NoAutoPGProvision(t *testing.T) {
	h, _ := newHTTPHandler(t)
	prov := &countingProv{}
	h.provisioner = prov // 注入 spy（接口字段；newHTTPHandler 默认 nil）
	r := newRouterWith(h)

	repo := filepath.Join(t.TempDir(), "repo")
	code, _ := doReq(t, r, http.MethodPost, "/api/v1/project-spaces/ps_1/apps",
		map[string]any{"name": "nopgapp", "app_kind": "service", "internal_port": 8080, "repo_dir": repo})
	if code != 201 && code != 200 {
		t.Fatalf("建应用应成功，状态码 %d", code)
	}
	if prov.calls != 0 {
		t.Fatalf("P3 后建应用不应触发自动 PG 供给，spy 被调 %d 次", prov.calls)
	}
}
```

> 需在 `handler_http_test.go` import 加 `"zhiyuan-anp/platform/backend/internal/pgsupply"`（若未导入）。

- [ ] **Step 3: 跑确认失败** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 -run TestHandler_Create_NoAutoPGProvision ./internal/appdeploy/...`
      Expected: FAIL（`spy 被调 1 次`——因 `Create` 仍无条件调 `h.provisioner.Provision`）。

- [ ] **Step 4: 删三处自动供给** — `handler.go`：

  a. `Create`（约 1230-1237）删整块：

  ```go
  // 供给独立库 + 注入 DATABASE_URL（失败不阻塞应用创建，仅记录；DATABASE_URL 缺失时应用自处理）
  // 配额超限（库数/库大小）→ Provision 在最前拦（不建任何库记录）；
  // 此处把配额错误写到 application.last_error，前端应用列表能显示「库供给失败：配额超限」。
  if h.provisioner != nil {
      if _, perr := h.provisioner.Provision(c.Request.Context(), a.ProjectSpaceID, a.ID); perr != nil {
          _ = h.store.SetStatus(c.Request.Context(), a.ProjectSpaceID, a.ID, a.Status, perr.Error(), "")
      }
  }
  ```

  b. `runImportGit/Dir`（约 1407-1412）删整块：

  ```go
  // 供给独立库（失败不阻塞导入，仅记 last_error）
  if h.provisioner != nil {
      if _, pe := h.provisioner.Provision(ctx, psID, appID); pe != nil {
          _ = h.store.SetStatus(ctx, psID, appID, "registered", pe.Error(), "")
      }
  }
  ```

  c. `runImportZip`（约 1526-1530）删整块：

  ```go
  if h.provisioner != nil {
      if _, pe := h.provisioner.Provision(ctx, psID, appID); pe != nil {
          _ = h.store.SetStatus(ctx, psID, appID, "registered", pe.Error(), "")
      }
  }
  ```

- [ ] **Step 5: 跑确认通过 + 回归** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/appdeploy/... ./internal/mwsupply/... ./internal/pgsupply/...` + `GOPATH=C:/Users/yxt/go go build ./cmd/server`
      Expected: 全绿（spy.calls==0；redis/milvus 无影响）。

- [ ] **Step 6: Commit** — `feat(appdeploy): 移除 handler 三处无条件PG自动供给,PG切纯声明驱动`

---

### Task 2: pgsupply Provision 复用收紧（只复用 Ready）+ 续供路径

**Files:**

- Modify: `internal/pgsupply/provisioner.go`（复用条件 54-57 + Provision 主体 + 新增 `isDuplicateDB`）
- Test: `internal/pgsupply/provisioner_test.go`

**Interfaces:**

- Produces: `isDuplicateDB(err error) bool`（续供时判 CreateDatabase 的"库已存在"）。
- 行为：`Ready` 行原样复用；`Provisioning`/`Failed` 行复用既有行推进到 Ready（库已存在则吞错；role 用 Drop+Create 以新密码重建）；全新 app 走原建库流程（fresh 路径多一步无害的 `DropRole` no-op）。

- [ ] **Step 1: 写失败测试（续供 Provisioning 行）** — `provisioner_test.go` 加（import 补 `"errors"`）：

```go
// dupDBAdmin: CreateDatabase 返"already exists"（模拟续供时库已建），其余成功；计 createRole。
type dupDBAdmin struct{ createRole int }

func (f *dupDBAdmin) CreateDatabase(context.Context, string, string) error {
	return errors.New(`create database app_x: ERROR: database "app_x" already exists`)
}
func (f *dupDBAdmin) CreateRole(context.Context, string, string, string) error { f.createRole++; return nil }
func (f *dupDBAdmin) GrantAll(context.Context, string, string, string) error   { return nil }
func (f *dupDBAdmin) DropDatabase(context.Context, string, string) error       { return nil }
func (f *dupDBAdmin) DropRole(context.Context, string, string) error           { return nil }
func (f *dupDBAdmin) Ping(context.Context, string) error                       { return nil }
func (f *dupDBAdmin) DatabaseSizes(context.Context, string, []string) (map[string]int64, error) {
	return nil, nil
}

// TestProvision_ResumesProvisioningRow 验证 Provisioning 半成品行被续供推进到 Ready：
// 库已建(CreateDatabase 返 already-exists 被吞) + role 用新密码重建 + DATABASE_URL 重写 + 翻 Ready。
// 修 P2a #2：Provisioning 不再被当"已供给"早返回(DATABASE_URL-lag)，而是续供补齐 env。
func TestProvision_ResumesProvisioningRow(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()
	ins := mkInstance("ps_1")
	_ = s.CreateInstance(ctx, ins)
	dbName := DBName("app_1")
	stuck := &AppDatabase{
		ID: "apdb_stuck", AppID: "app_1", ProjectSpaceID: "ps_1",
		DBName: dbName, DBRole: RoleName(dbName), PGInstanceID: ins.ID,
		DBHost: ins.Host, DBPort: ins.Port, Status: StatusProvisioning, BackupEnabled: true,
	}
	if err := s.CreateAppDB(ctx, stuck); err != nil {
		t.Fatalf("seed stuck row: %v", err)
	}
	adm := &dupDBAdmin{}
	env := &fakeEnvWriter{}
	p := NewProvisioner(recordingInstance{ins: ins}, s, adm, env, nil)

	ad, err := p.Provision(ctx, "ps_1", "app_1")
	if err != nil {
		t.Fatalf("续供: %v", err)
	}
	if ad.Status != StatusReady {
		t.Fatalf("续供后应 Ready，得 %q", ad.Status)
	}
	if adm.createRole != 1 {
		t.Fatalf("续供应重建 role(新密码)，createRole=%d", adm.createRole)
	}
	if len(env.writes) != 1 || env.writes[0].key != "DATABASE_URL" {
		t.Fatalf("续供应重写 DATABASE_URL，得 %+v", env.writes)
	}
	if !contains(env.writes[0].value, dbName) {
		t.Fatalf("DSN 应含库名 %s，得 %s", dbName, env.writes[0].value)
	}
	got, _ := s.GetAppDBByApp(ctx, "app_1")
	if got == nil || got.Status != StatusReady || got.ID != stuck.ID {
		t.Fatalf("应复用既有行翻 Ready，得 %+v", got)
	}
}

// TestProvision_ResumesFailedRow 验证 Failed 行同样落续供推进到 Ready。
func TestProvision_ResumesFailedRow(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()
	ins := mkInstance("ps_1")
	_ = s.CreateInstance(ctx, ins)
	dbName := DBName("app_2")
	bad := &AppDatabase{
		ID: "apdb_bad", AppID: "app_2", ProjectSpaceID: "ps_1",
		DBName: dbName, DBRole: RoleName(dbName), PGInstanceID: ins.ID,
		DBHost: ins.Host, DBPort: ins.Port, Status: StatusFailed, BackupEnabled: true,
	}
	if err := s.CreateAppDB(ctx, bad); err != nil {
		t.Fatalf("seed failed row: %v", err)
	}
	p := NewProvisioner(recordingInstance{ins: ins}, s, &dupDBAdmin{}, &fakeEnvWriter{}, nil)
	ad, err := p.Provision(ctx, "ps_1", "app_2")
	if err != nil {
		t.Fatalf("续供 Failed: %v", err)
	}
	if ad.Status != StatusReady {
		t.Fatalf("Failed 续供后应 Ready，得 %q", ad.Status)
	}
}
```

- [ ] **Step 2: 跑确认失败** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 -run 'TestProvision_Resumes' ./internal/pgsupply/...`
      Expected: FAIL（现实现 `Provisioning` 早 `return existing` → env 不写、createRole=0、status 仍 Provisioning）。

- [ ] **Step 3: 实现** — `provisioner.go`：

  3a. import 块改为：

  ```go
  import (
      "context"
      "errors"
      "fmt"
      "strings"

      "github.com/jackc/pgx/v5/pgconn"
  )
  ```

  3b. 把 `Provision` 方法体（从配额检查后到 `return ad, nil`）整体替换为：

  ```go
  func (p *Provisioner) Provision(ctx context.Context, psID, appID string) (*AppDatabase, error) {
      // 配额强制：建任何记录/容器前先查（超限不污染数据，便于前端精准提示）
      if p.quota != nil {
          if err := p.quota.CheckDatabases(ctx, psID); err != nil {
              return nil, err
          }
          if err := p.quota.CheckDBSize(ctx, psID); err != nil {
              return nil, err
          }
      }
      // 幂等：同 app 已有 Ready 库 → 复用，不重建。
      // 只复用 Ready（保证 DATABASE_URL env 已写）；Provisioning/Failed 不早返回，落续供路径推进到 Ready
      // （修 P2a #2：Provisioning 行 env 未写，早返回会造成 binding bound 但无 DATABASE_URL 的 lag）。
      if existing, err := p.store.GetAppDBByApp(ctx, appID); err == nil && existing != nil && existing.Status == StatusReady {
          return existing, nil
      }
      ins, err := p.instances.GetOrCreate(ctx, psID)
      if err != nil {
          return nil, fmt.Errorf("取/建项目 PG 实例: %w", err)
      }
      dbName := DBName(appID)
      role := RoleName(dbName)
      pwd := genPassword()

      var ad *AppDatabase
      if existing, err := p.store.GetAppDBByApp(ctx, appID); err == nil && existing != nil {
          // 续供：复用既有 Provisioning/Failed 行（不新建，避免 UNIQUE 冲突），推进到 Ready。
          ad = existing
          _ = p.store.SetAppDBStatus(ctx, ad.ID, StatusProvisioning, "")
          ad.Status = StatusProvisioning
      } else {
          ad = &AppDatabase{
              ID: "apdb_" + genShortID(), AppID: appID, ProjectSpaceID: psID,
              DBName: dbName, DBRole: role, PGInstanceID: ins.ID,
              DBHost: ins.Host, DBPort: ins.Port, Status: StatusProvisioning, BackupEnabled: true,
          }
          if err := p.store.CreateAppDB(ctx, ad); err != nil {
              return nil, fmt.Errorf("登记库记录: %w", err)
          }
      }

      // 建库（续供时库可能已存在 → 吞 already-exists；其余失败回滚）。
      if err := p.admin.CreateDatabase(ctx, ins.AdminURLRef, dbName); err != nil {
          if !isDuplicateDB(err) {
              p.markFailed(ctx, ad, err)
              return nil, err
          }
      }
      // role：Drop(IF EXISTS) + Create。续供时 role 可能半建 → 重建以新密码；全新则 Drop 为 no-op。
      _ = p.admin.DropRole(ctx, ins.AdminURLRef, role)
      if err := p.admin.CreateRole(ctx, ins.AdminURLRef, role, pwd); err != nil {
          p.markFailed(ctx, ad, err)
          return nil, err
      }
      if err := p.admin.GrantAll(ctx, ins.AdminURLRef, dbName, role); err != nil {
          _ = p.admin.DropRole(ctx, ins.AdminURLRef, role)
          p.markFailed(ctx, ad, err)
          return nil, err
      }

      dsn := DSN(ins.Host, ins.Port, role, pwd, dbName)
      if err := p.env.UpsertEnv(ctx, appID, "DATABASE_URL", dsn, true, "platform"); err != nil {
          _ = p.admin.DropRole(ctx, ins.AdminURLRef, role)
          p.markFailed(ctx, ad, err)
          return nil, fmt.Errorf("写 DATABASE_URL env: %w", err)
      }

      _ = p.store.SetAppDBStatus(ctx, ad.ID, StatusReady, "")
      ad.Status = StatusReady
      return ad, nil
  }
  ```

  3c. 文件末尾加 `isDuplicateDB`：

  ```go
  // isDuplicateDB 判 CreateDatabase 错误是否"库已存在"（续供容忍）。
  // PG duplicate_database 错误码 42P04；pgx 字符串化含 "already exists"。
  func isDuplicateDB(err error) bool {
      if err == nil {
          return false
      }
      var pgErr *pgconn.PgError
      if errors.As(err, &pgErr) && pgErr.Code == "42P04" {
          return true
      }
      return strings.Contains(err.Error(), "already exists")
  }
  ```

- [ ] **Step 4: 跑确认通过 + 回归** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/pgsupply/... ./internal/mwsupply/... ./internal/appdeploy/...` + `go build ./cmd/server`
      Expected: 全绿（含既有 `TestProvisioner_Provision`/`TestProvision_IdempotentReuse`：fresh 路径多的 `DropRole` 对 fake/counting admin 是 no-op，计数不变；Ready 复用仍早返回）。

- [ ] **Step 5: Commit** — `fix(pgsupply): Provision复用只认Ready,Provisioning/Failed落续供补env(修DATABASE_URL-lag)`

---

### Task 3: 修 M-2 pg dedicated host 耦合（ProvisionDedicated 返回 host 单源）

**Files:**

- Modify: `internal/pgsupply/instance.go`（`ProvisionDedicated` 签名 + 返回）
- Modify: `internal/mwsupply/spec_pg.go`（`PgDedicatedRunner` 接口 + `SupplyDedicated` 闭包）
- Test: `internal/mwsupply/spec_pg_ded_test.go`（fake 加 host + 单源断言）

**Interfaces:**

- Changes: `PgDedicatedRunner.ProvisionDedicated` 返回 tuple 加 `host string`（实际用的 `m.host`）。`*pgsupply.InstanceManager` 实现随之改。
- `spec_pg.go` `SupplyDedicated` 闭包用**返回的 host** 填 `service_instance.Host`，忽略传入的 `host` 参数（泛型签名 `(ctx, appID, psID, host)` 不动）。

- [ ] **Step 1: 写失败测试（host 单源）** — `spec_pg_ded_test.go`：给 `fakePgDedicated` 加 `host` 字段并在 `ProvisionDedicated` 返回它。先把 fake 改为：

```go
type fakePgDedicated struct {
	container  string
	dbName     string
	dsn        string
	adminURL   string
	host       string // 新增：ProvisionDedicated 返回的 host（模拟 InstanceManager.host）
	port       int
	err        error
	provCalls  int
	cleanCalls []string
}

func (f *fakePgDedicated) ProvisionDedicated(_ context.Context, _, _ string) (container, dbName, dsn, adminURL, host string, port int, err error) {
	f.provCalls++
	return f.container, f.dbName, f.dsn, f.adminURL, f.host, f.port, f.err
}
```

再在 `TestPgDedicated` 里：构造 fake 时设 `host: "ded-real-host"`（≠ r.host `testdeploy`），并在断言段加（取登记的 service_instance 校验 Host）：

```go
	// M-2：service_instance.Host 应 = ProvisionDedicated 返回的 host（单源），而非 r.host(testdeploy)。
	var instHost string
	if err := // 见下方完整查询
```

完整新增断言（紧接该用例已有断言之后，`db` 需从 `newReconcilerTestWithPgDed` 取——把签名第 3 返回值接出）：

```go
	r, appStore, db, _, _ := newReconcilerTestWithPgDed(t, ded) // 原为 `_, _, _`；改接 db
	...
	// 末尾加：
	var instHost string
	_ = db.Get(&instHost, `SELECT host FROM appdeploy_service_instance WHERE kind='pg' AND supply_mode='dedicated' AND name=$1`, ded.container)
	if instHost != "ded-real-host" {
		t.Fatalf("service_instance.Host 应为 ProvisionDedicated 返回的 ded-real-host，得 %q", instHost)
	}
```

（fake 构造里加 `host: "ded-real-host"`。）

- [ ] **Step 2: 跑确认失败** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 -run TestPgDedicated ./internal/mwsupply/...`
      Expected: 编译失败（`PgDedicatedRunner.ProvisionDedicated` 返回值数不匹配）→ 先做 Step 3 接口/实现改动使其编译，再跑：FAIL（`service_instance.Host` = `testdeploy` 而非 `ded-real-host`，因闭包仍用传入 host）。

- [ ] **Step 3: 改接口 + 实现返回 host** —

  3a. `spec_pg.go` `PgDedicatedRunner` 接口：

  ```go
  type PgDedicatedRunner interface {
      // ProvisionDedicated 起 per-app 独立 PG 容器 + 建库/role，返回 container/dbName/dsn/adminURL/host/port。
      // host = 实际起容器用的宿主地址（InstanceManager.host），供 mwsupply 登记到 service_instance.Host（单源）。
      ProvisionDedicated(ctx context.Context, appID, psID string) (container, dbName, dsn, adminURL, host string, port int, err error)
      CleanupDedicated(ctx context.Context, container string) error
  }
  ```

  3b. `spec_pg.go` `SupplyDedicated` 闭包：把

  ```go
  container, dbName, dsn, adminURL, port, err := ded.ProvisionDedicated(ctx, appID, psID)
  ```

  改为

  ```go
  container, dbName, dsn, adminURL, dedHost, port, err := ded.ProvisionDedicated(ctx, appID, psID)
  ```

  并把登记 `inst` 的 `Host: host`（传入参数）改为 `Host: dedHost`（ProvisionDedicated 返回，单源）。删/改原注释「host 由 supplyDedicated 闭包传入（=r.host）…二者同值」为「host 取 ProvisionDedicated 返回值（实际起容器用的 InstanceManager.host），不再依赖 r.host，消除双 host 耦合」。

  3c. `instance.go` `ProvisionDedicated` 签名 + 返回值加 `host`：

  ```go
  func (m *InstanceManager) ProvisionDedicated(ctx context.Context, appID, psID string) (container, dbName, dsn, adminURL, host string, port int, err error) {
  ```

  方法体内 5 处错误 `return "", "", "", "", 0, ...` 全改为 `return "", "", "", "", "", 0, ...`（多一个空串对应 host）；末尾成功返回前加 `host = m.host`，最终：

  ```go
  	dsn = DSN(m.host, port, role, pwd, dbName)
  	host = m.host
  	return container, dbName, dsn, adminURL, host, port, nil
  }
  ```

- [ ] **Step 4: 跑确认通过 + 回归** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/... ./internal/pgsupply/... ./internal/appdeploy/...` + `go build ./cmd/server`
      Expected: 全绿（`service_instance.Host` == `ded-real-host`；其余 pg dedicated 用例不受影响，fake 返回 host 即可）。

- [ ] **Step 5: Commit** — `fix(mwsupply): pg dedicated service_instance.Host 取 ProvisionDedicated 返回host单源(修M-2)`

---

### Task 4: 修 M-3 bind_existing 空 AuthRef 守卫（通用空 connVal 拦截）

**Files:**

- Modify: `internal/mwsupply/supply.go`（bind_existing 段 134-144）
- Test: `internal/mwsupply/spec_pg_test.go`

**Interfaces:** 无新接口。bind_existing 算出 `connVal` 后，若为空串 → `mkBind(StatusFailed, ...)` 不写 env。

- [ ] **Step 1: 写失败测试** — `spec_pg_test.go` 末尾加：

```go
// TestSupplyOne_pgBindExisting_EmptyAuthRefFails 验证登记的 pg bind_existing 实例 AuthRef(DSN) 为空时
// 不静默写空 DATABASE_URL，而是 binding failed（M-3 守卫）。通用：任何 kind connVal 空 → failed。
func TestSupplyOne_pgBindExisting_EmptyAuthRefFails(t *testing.T) {
	r, appStore, db, _, _ := newReconcilerTest(t)
	ctx := context.Background()

	inst := &ServiceInstance{Kind: "pg", Name: "pg-noauth", Host: "10.10.0.28", Port: 5432, AuthRef: ""}
	if err := r.store.RegisterBindExisting(ctx, inst); err != nil {
		t.Fatalf("register: %v", err)
	}
	got, lookErr := r.store.LookupBindExisting(ctx, "ps_1", "pg")
	if lookErr != nil || got == nil {
		t.Fatalf("登记后 LookupBindExisting 取不到 pg 实例: %v", lookErr)
	}
	instID := got.ID
	t.Cleanup(func() { _, _ = db.Exec(`DELETE FROM appdeploy_service_instance WHERE id=$1`, instID) })

	a := &appdeploy.Application{ProjectSpaceID: "ps_1", Name: "pgemptyapp", RepoDir: "/x", InternalPort: 8080}
	if err := appStore.Create(ctx, a); err != nil {
		t.Fatalf("create app: %v", err)
	}

	r.supplyOne(ctx, a.ID, "ps_1", DepService{Kind: "pg", Strategy: ModeBindExisting})

	b, _ := NewStore(db).GetBinding(ctx, a.ID, "pg")
	if b == nil || b.Status != StatusFailed {
		t.Fatalf("空 AuthRef 应 binding failed，得 %+v", b)
	}
	if v, _ := appStore.GetEnvValue(ctx, a.ID, "DATABASE_URL"); v != "" {
		t.Fatalf("空 AuthRef 不应写 DATABASE_URL，得 %q", v)
	}
}
```

- [ ] **Step 2: 跑确认失败** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 -run TestSupplyOne_pgBindExisting_EmptyAuthRefFails ./internal/mwsupply/...`
      Expected: FAIL（现实现写空 `DATABASE_URL=""` 且 binding bound）。

- [ ] **Step 3: 加守卫** — `supply.go` bind_existing 段，把：

  ```go
  connVal := ConnStr(inst)
  if spec.ConnValue != nil {
      connVal = spec.ConnValue(inst)
  }
  isSecret := inst.AuthRef != ""
  ```

  改为（在 `isSecret` 前插入空值守卫）：

  ```go
  connVal := ConnStr(inst)
  if spec.ConnValue != nil {
      connVal = spec.ConnValue(inst)
  }
  // 通用空连接值守卫（M-3）：ConnStr/ConnValue 派生值为空（如 pg 登记实例 AuthRef 空）→
  // 不写空 env、binding failed，避免静默注入空 DATABASE_URL/地址。
  if connVal == "" {
      mkBind(StatusFailed, inst.ID, "", "无可绑定的 "+dep.Kind+" 实例连接信息（AuthRef 空）")
      return
  }
  isSecret := inst.AuthRef != ""
  ```

- [ ] **Step 4: 跑确认通过 + 回归** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/mwsupply/... ./internal/pgsupply/... ./internal/appdeploy/...` + `go build ./cmd/server`
      Expected: 全绿（空 AuthRef → failed；既有 bind_existing 用例 ConnStr/ConnValue 非空不受影响）。

- [ ] **Step 5: Commit** — `fix(mwsupply): bind_existing加空connVal守卫,空AuthRef不再静默写空DATABASE_URL(修M-3)`

---

### Task 5: .28 e2e（scp 重建 backend + 真 pg 三策略）

**Files:** 无代码改动（验收 runbook）。deploy 流程见 `deploy/README.md`；ssh 需 `-o PubkeyAcceptedAlgorithms=+ssh-rsa`，只动 `deploy_` 容器。

- [ ] **Step 1: 本地全量绿** → Run: `GOPATH=C:/Users/yxt/go go test -p 1 ./internal/...` + `go build ./cmd/server`
      Expected: 全绿。

- [ ] **Step 2: 推送** → `git push` 续攒 PR #3（5 个 P3 commit）。

- [ ] **Step 3: scp 重建 .28 backend** → 按 `deploy/README.md` 把 backend 镜像/二进制部署到 .28 `deploy_` 容器并重启。

- [ ] **Step 4: e2e pg shared（声明驱动）** —
  - 建应用 + `PUT /apps/:aid/deps`（或仓库 `.anp/deps.yaml` `{services:[{kind:pg,strategy:shared}]}`）声明 `pg=shared`。
  - 部署 → 确认 `appdeploy_database` 新增一行（库 `app_<id>`）+ 应用 env 注入正确 `DATABASE_URL`。
  - 验复用：再次部署 → 库不重建（同一 `db_name`）、`DATABASE_URL` 不变。
  - 验续供：把该行 status 手动改 `provisioning` → 部署 → 续供补 env、翻 `ready`（修 P2a #2）。

- [ ] **Step 5: e2e pg bind_existing** —
  - 运维登记一个已有 PG（`POST /mw-instances` kind=pg + 完整 DSN auth_ref）。
  - 声明 `pg=bind_existing` → 部署 → `DATABASE_URL` = 登记的 DSN，不建库。
  - 验 M-3：登记 auth_ref 空 → binding failed、不写 env。

- [ ] **Step 6: e2e pg dedicated** —
  - 声明 `pg=dedicated` → 部署 → 起 per-app pgvector 容器 + 建库/role + `DATABASE_URL` 注入。
  - 验 M-2：`service_instance.host` = 容器实际可达 host（与 InstanceManager.host 一致）。
  - 释放（删 app / 移除声明）→ 容器被 `docker rm`（`CleanupDedicated`）。

- [ ] **Step 7: 回归** — redis/milvus 既有 e2e 抽验一个（无行为变化）。

- [ ] **Step 8: 记录** — 把 e2e 结果（每策略 pass/fail + 关键观测）写进 `.superpowers/sdd/progress.md` 的 P3 段。

---

## Self-Review

**1. Spec 覆盖：** §4.1 删三处自动供给→T1；§4.2 复用收紧+续供→T2；§4.3 M-2 host 单源→T3；§4.4 M-3 空 connVal 守卫→T4；§5 .28 e2e→T5。✓
**2. 占位：** 每步含完整代码/命令；T5 为 runbook（验收性质，非 TDD，步骤明确）。✓
**3. 类型一致：** `pgAutoProvisioner.Provision` 返回 `(*pgsupply.AppDatabase, error)` 与 `*pgsupply.Provisioner.Provision` 一致（T1 接口适配）；`PgDedicatedRunner.ProvisionDedicated` 返回 tuple 加 host，T3 接口/实现/fake/调用四处一致（container,dbName,dsn,adminURL,host,port,err）；`isDuplicateDB` T2 定义并被 `Provision` 引用。✓
**4. 自编译：** T1（接口字段+删调用，redis/milvus 无影响）独立；T2（pgsupply 内部）独立；T3（接口签名跨 spec_pg.go/instance.go/fake，同 task 一起改）独立；T4（supply.go 守卫）独立；T5 验收。✓
**5. 风险：** (a) T1 字段改接口后 production-dead（仅 spy 测试读）——go vet/build 不报，spec §4.1 明确保留，可接受；(b) T2 fresh 路径新增 `DropRole` 对 fake/counting admin 为 no-op，既有测不变，真 PG 上 `DROP ROLE IF EXISTS` 对新 role 无害；(c) T3 接口签名变更须同 task 改全四处否则编译断；(d) T4 守卫是通用空串判定，对 redis/milvus 的 ConnStr(host:port) 非空无影响。
