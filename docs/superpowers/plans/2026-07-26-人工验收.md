# 测试中心 · 人工验收录入 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给测试中心 `manual` 用例加"人工验收"录入(结论+备注),翻 status 天然进发布门禁,已验用例不被自动运行冲掉。

**Architecture:** `test_case` 表加 3 列(`manual_note/verifier_id/verified_at`),后端新增 `POST .../test-cases/:tcid/manual-verdict` 端点(Service 内做 verdict 校验 + 范围守卫,Handler 做 HTTP 映射),`RunHTTPRequest` 对已验用例短路;前端 `manual` 卡片换成"通过/失败+备注"录入,已验的显示摘要+改判。验收人显示名走 SQL `LEFT JOIN "user"`。

**Tech Stack:** Go / gin / sqlx / PostgreSQL(禁 SQLite)/ golang-migrate(embed)// Next.js app router("use client")/ fetch

## Global Constraints

- 后端测试连 anp_test PG:`testutil.TestDB(t)` 跑全量迁移建表;**禁用 sqlite**(`memory: no-sqlite-pg-only`),警惕 sqlite 单测漏 PG 类型陷阱(`memory: sqlite-test-pg-type-trap`)。单测在 `platform/backend` 下 `go test ./internal/qa/ -run <Test> -v` 跑。
- 迁移文件 `internal/db/migrations/pg/NNNNNN_name.up.sql` + `.down.sql`,首行注释 `-- NNNNNN_name.up.sql`;由 `//go:embed migrations/pg/*.sql` 打包,`migrateUp` 按版本号事务执行——**加文件即自动生效**,无需改 loader。
- 统一响应:`httpx.OK(c, data)` / `httpx.Created(c, data)` / `httpx.Err(c, httpStatus, bizCode, msg)`。
- 鉴权上下文:`c.GetString(auth.CtxUserDBID)`,常量值 `"user_db_id"`,存 `usr_xxx`。
- 前端 Next.js:`platform/frontend/AGENTS.md` 明确"这不是你熟悉的 Next.js"——写前端代码前**先读 `platform/frontend/node_modules/next/dist/docs/`** 相关指南(强制)。
- 纪律:开发完 scp 部署 .28 真驱动前端打后端看渲染,本机不跑功能测试(`memory: deploy-28-no-local-test`)。
- 提交信息走仓库惯例:`feat(db)/feat(qa)/feat(frontend)/chore(release): 中文描述`。

---

## File Structure

| 文件                                                                 | 责任                                                   | 动作 |
| -------------------------------------------------------------------- | ------------------------------------------------------ | ---- |
| `internal/db/migrations/pg/000019_test_case_manual_verdict.up.sql`   | 加 3 列                                                | 新建 |
| `internal/db/migrations/pg/000019_test_case_manual_verdict.down.sql` | 回滚 3 列                                              | 新建 |
| `internal/qa/model.go`                                               | `TestCase` 加 4 字段                                   | 改   |
| `internal/qa/store.go`                                               | `tcCols`+3 查询 join user;`UpdateManualVerdict`        | 改   |
| `internal/qa/service.go`                                             | 哨兵错误 + `RecordManualVerdict`;`RunHTTPRequest` 短路 | 改   |
| `internal/qa/handler.go`                                             | `ManualVerdict` + 路由;import errors/auth              | 改   |
| `internal/qa/store_test.go`                                          | 列读取 + join + UpdateManualVerdict 测试               | 改   |
| `internal/qa/service_test.go`                                        | RecordManualVerdict + 防冲掉测试                       | 改   |
| `internal/qa/handler_test.go`                                        | HTTP 200/404/400/409 测试                              | 新建 |
| `platform/frontend/app/testing/page.tsx`                             | manual 卡片人工验收 UI                                 | 改   |
| `internal/release/handler.go`                                        | 门禁提示文案补人工验收                                 | 改   |

---

## Task 1: Migration 000019 + TestCase 模型字段 + store 读取(含 verifier_name join)

**Files:**

- Create: `platform/backend/internal/db/migrations/pg/000019_test_case_manual_verdict.up.sql`
- Create: `platform/backend/internal/db/migrations/pg/000019_test_case_manual_verdict.down.sql`
- Modify: `platform/backend/internal/qa/model.go`
- Modify: `platform/backend/internal/qa/store.go`(`tcCols` 常量 + `Get`/`ListByProjectSpace`/`ListByRequirement` 三处查询)
- Test: `platform/backend/internal/qa/store_test.go`

**Interfaces:**

- Produces:`TestCase.ManualNote/VerifierID(string)`、`TestCase.VerifiedAt(*time.Time)`、`TestCase.VerifierName(string,join 列,非表列)`;`Store` 查询返回的 `TestCase` 已带这 4 字段。

- [ ] **Step 1: 写失败测试**(store_test.go 末尾追加)

```go
// TestManualVerdictFields_DefaultEmpty 新列默认空:新建用例读回 verifier_id/manual_note 为空串、verified_at 为 nil。
func TestManualVerdictFields_DefaultEmpty(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "defaults")
	if err := s.Create(ctx, tc); err != nil {
		t.Fatalf("create: %v", err)
	}
	got, err := s.Get(ctx, tc.ID)
	if err != nil {
		t.Fatalf("get: %v", err)
	}
	if got.ManualNote != "" || got.VerifierID != "" {
		t.Fatalf("新列默认应空:note=%q verifier=%q", got.ManualNote, got.VerifierID)
	}
	if got.VerifiedAt != nil {
		t.Fatalf("VerifiedAt 默认应为 nil,得到 %v", *got.VerifiedAt)
	}
	if got.VerifierName != "" {
		t.Fatalf("无 verifier 时 VerifierName 应空,得到 %q", got.VerifierName)
	}
}

// TestList_VerifierNameJoin verifier_id 指向的用户名经 LEFT JOIN 回填到 VerifierName。
func TestList_VerifierNameJoin(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()
	// 白盒插入一个用户 + 把用例 verifier_id 指过去(Create 不写 verifier_id,用 raw UPDATE)。
	const uid = "usr_verifier_join"
	if _, err := s.db.ExecContext(ctx,
		`INSERT INTO "user" (id, name, email, status) VALUES ($1, $2, $3, 'active')`,
		uid, "张三", "z3@example.com"); err != nil {
		t.Fatalf("insert user: %v", err)
	}
	tc := mkTC("ps_1", "req_1", "joined")
	if err := s.Create(ctx, tc); err != nil {
		t.Fatalf("create: %v", err)
	}
	if _, err := s.db.ExecContext(ctx,
		`UPDATE test_case SET verifier_id=$1 WHERE id=$2`, uid, tc.ID); err != nil {
		t.Fatalf("set verifier_id: %v", err)
	}
	got, err := s.Get(ctx, tc.ID)
	if err != nil {
		t.Fatalf("get: %v", err)
	}
	if got.VerifierID != uid {
		t.Fatalf("VerifierID 应为 %s,得到 %s", uid, got.VerifierID)
	}
	if got.VerifierName != "张三" {
		t.Fatalf("VerifierName 应为 张三,得到 %q", got.VerifierName)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd platform/backend && go test ./internal/qa/ -run 'TestManualVerdictFields_DefaultEmpty|TestList_VerifierNameJoin' -v`
Expected: 编译失败(`tc.ManualNote undefined` / `got.VerifierName undefined`)或列不存在错误。

- [ ] **Step 3: 建迁移文件**

`000019_test_case_manual_verdict.up.sql`:

```sql
-- 000019_test_case_manual_verdict.up.sql
-- 测试中心人工验收:test_case 加备注/验收人/验收时间三列。

ALTER TABLE test_case ADD COLUMN IF NOT EXISTS manual_note TEXT DEFAULT '';
ALTER TABLE test_case ADD COLUMN IF NOT EXISTS verifier_id  TEXT DEFAULT '';
ALTER TABLE test_case ADD COLUMN IF NOT EXISTS verified_at  TIMESTAMPTZ;
```

`000019_test_case_manual_verdict.down.sql`:

```sql
-- 000019_test_case_manual_verdict.down.sql
ALTER TABLE test_case DROP COLUMN IF EXISTS verified_at;
ALTER TABLE test_case DROP COLUMN IF EXISTS verifier_id;
ALTER TABLE test_case DROP COLUMN IF EXISTS manual_note;
```

- [ ] **Step 4: model.go 加字段**

在 `TestCase` 结构体 `ActualBody/RunAt/CreatedAt` 之后追加:

```go
	// 人工验收(AC:manual 用例人录结论+备注)。
	ManualNote   string     `json:"manual_note,omitempty" db:"manual_note"`
	VerifierID   string     `json:"verifier_id,omitempty" db:"verifier_id"`
	VerifiedAt   *time.Time `json:"verified_at,omitempty" db:"verified_at"`
	VerifierName string     `json:"verifier_name,omitempty" db:"verifier_name"` // LEFT JOIN "user".name,非表列
```

- [ ] **Step 5: store.go 改 tcCols + 三处查询 join**

替换 `tcCols` 常量为(各列加 `t.` 前缀,末尾加 3 新列 + join 名):

```go
const tcCols = `t.id, t.project_space_id, COALESCE(t.requirement_id,'') AS requirement_id, t.title, COALESCE(t.steps,'') AS steps, COALESCE(t.expected,'') AS expected, t.status, COALESCE(t.method,'') AS method, COALESCE(t.path,'') AS path, COALESCE(t.expected_status,0) AS expected_status, COALESCE(t.expected_body,'') AS expected_body, COALESCE(t.actual_status,0) AS actual_status, COALESCE(t.actual_body,'') AS actual_body, t.run_at, t.created_at, COALESCE(t.manual_note,'') AS manual_note, COALESCE(t.verifier_id,'') AS verifier_id, t.verified_at, COALESCE(ver.name,'') AS verifier_name`
```

`Get`:

```go
func (s *Store) Get(ctx context.Context, id string) (*TestCase, error) {
	var tc TestCase
	err := s.db.GetContext(ctx, &tc,
		`SELECT `+tcCols+` FROM test_case t LEFT JOIN "user" ver ON ver.id = t.verifier_id WHERE t.id = $1`, id)
	return &tc, err
}
```

`ListByProjectSpace`:

```go
func (s *Store) ListByProjectSpace(ctx context.Context, projectSpaceID string) ([]TestCase, error) {
	var list []TestCase
	err := s.db.SelectContext(ctx, &list,
		`SELECT `+tcCols+` FROM test_case t LEFT JOIN "user" ver ON ver.id = t.verifier_id WHERE t.project_space_id = $1 ORDER BY t.created_at DESC`, projectSpaceID)
	return list, err
}
```

`ListByRequirement`:

```go
func (s *Store) ListByRequirement(ctx context.Context, requirementID string) ([]TestCase, error) {
	var list []TestCase
	err := s.db.SelectContext(ctx, &list,
		`SELECT `+tcCols+` FROM test_case t LEFT JOIN "user" ver ON ver.id = t.verifier_id WHERE t.requirement_id = $1 ORDER BY t.created_at DESC`, requirementID)
	return list, err
}
```

- [ ] **Step 6: 跑测试验证通过**

Run: `cd platform/backend && go test ./internal/qa/ -run 'TestManualVerdictFields_DefaultEmpty|TestList_VerifierNameJoin' -v`
Expected: PASS。同时跑全量 qa 测试确认 join 改动无回归:`go test ./internal/qa/ -v`,全绿。

- [ ] **Step 7: 提交**

```bash
git add platform/backend/internal/db/migrations/pg/000019_test_case_manual_verdict.up.sql platform/backend/internal/db/migrations/pg/000019_test_case_manual_verdict.down.sql platform/backend/internal/qa/model.go platform/backend/internal/qa/store.go platform/backend/internal/qa/store_test.go
git commit -m "feat(db): 000019 test_case 加人工验收字段 + qa 读取 join user 取验收人名"
```

---

## Task 2: Store.UpdateManualVerdict

**Files:**

- Modify: `platform/backend/internal/qa/store.go`(新增方法)
- Test: `platform/backend/internal/qa/store_test.go`

**Interfaces:**

- Produces:`func (s *Store) UpdateManualVerdict(ctx context.Context, tc *TestCase) error`——写 `status/manual_note/verifier_id/verified_at` 4 字段(Task 3 的 Service 依赖)。

- [ ] **Step 1: 写失败测试**(store_test.go 追加)

```go
// TestUpdateManualVerdict 回写人工验收:status/manual_note/verifier_id/verified_at 四字段落库。
func TestUpdateManualVerdict(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "mv")
	tc.Status = "manual"
	if err := s.Create(ctx, tc); err != nil {
		t.Fatalf("create: %v", err)
	}
	now := time.Now().UTC()
	tc.Status = "passed"
	tc.ManualNote = "界面正常,手动确认"
	tc.VerifierID = "usr_a"
	tc.VerifiedAt = &now
	if err := s.UpdateManualVerdict(ctx, tc); err != nil {
		t.Fatalf("UpdateManualVerdict: %v", err)
	}
	got, _ := s.Get(ctx, tc.ID)
	if got.Status != "passed" || got.ManualNote != "界面正常,手动确认" || got.VerifierID != "usr_a" {
		t.Fatalf("回写不符:status=%s note=%q verifier=%q", got.Status, got.ManualNote, got.VerifierID)
	}
	if got.VerifiedAt == nil || !got.VerifiedAt.Equal(now) {
		t.Fatalf("VerifiedAt 应等于 %v,得到 %v", now, got.VerifiedAt)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd platform/backend && go test ./internal/qa/ -run TestUpdateManualVerdict -v`
Expected: 编译失败(`s.UpdateManualVerdict undefined`)。

- [ ] **Step 3: 实现**(store.go,`UpdateRun` 之后追加)

```go
// UpdateManualVerdict 回写人工验收结论(状态 + 备注 + 验收人 + 时间)。
// 与 UpdateRun 职责不同:后者写 HTTP 自动运行结果,不写验收三列。
func (s *Store) UpdateManualVerdict(ctx context.Context, tc *TestCase) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE test_case SET status=$1, manual_note=$2, verifier_id=$3, verified_at=$4 WHERE id=$5`,
		tc.Status, tc.ManualNote, tc.VerifierID, tc.VerifiedAt, tc.ID)
	return err
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd platform/backend && go test ./internal/qa/ -run TestUpdateManualVerdict -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add platform/backend/internal/qa/store.go platform/backend/internal/qa/store_test.go
git commit -m "feat(qa): Store.UpdateManualVerdict 回写人工验收四字段"
```

---

## Task 3: Service.RecordManualVerdict + 哨兵错误 + 已验用例防自动覆盖

**Files:**

- Modify: `platform/backend/internal/qa/service.go`(import `errors`;哨兵;`RecordManualVerdict`;`RunHTTPRequest` 短路)
- Test: `platform/backend/internal/qa/service_test.go`

**Interfaces:**

- Produces:哨兵 `ErrInvalidVerdict` / `ErrNotManualVerifiable`;`func (s *Service) RecordManualVerdict(ctx, tc *TestCase, verdict, note, verifierID string) error`(Task 4 的 Handler 依赖);`RunHTTPRequest` 对 `tc.VerifierID != ""` 短路。

- [ ] **Step 1: 写失败测试**(service_test.go 追加)

```go
// TestRecordManualVerdict_ManualCase manual 用例:录 passed → 字段齐 + 落库。
func TestRecordManualVerdict_ManualCase(t *testing.T) {
	svc := newSvcWithStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "m1")
	tc.ID = "tc_rm1"
	tc.Status = "manual"
	_ = svc.store.Create(ctx, tc)

	if err := svc.RecordManualVerdict(ctx, tc, "passed", "看着没问题", "usr_x"); err != nil {
		t.Fatalf("RecordManualVerdict: %v", err)
	}
	if tc.Status != "passed" || tc.ManualNote != "看着没问题" || tc.VerifierID != "usr_x" || tc.VerifiedAt == nil {
		t.Fatalf("字段未置:status=%s note=%q verifier=%q at=%v", tc.Status, tc.ManualNote, tc.VerifierID, tc.VerifiedAt)
	}
	got, _ := svc.store.Get(ctx, tc.ID)
	if got.Status != "passed" || got.VerifierID != "usr_x" {
		t.Fatalf("未落库:status=%s verifier=%q", got.Status, got.VerifierID)
	}
}

// TestRecordManualVerdict_Reverify 已验过(verifier_id 非空)允许改判。
func TestRecordManualVerdict_Reverify(t *testing.T) {
	svc := newSvcWithStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "m2")
	tc.ID = "tc_rm2"
	tc.Status = "manual"
	_ = svc.store.Create(ctx, tc)
	_ = svc.RecordManualVerdict(ctx, tc, "passed", "", "usr_x")   // 先验通过
	if err := svc.RecordManualVerdict(ctx, tc, "failed", "复测发现 bug", "usr_y"); err != nil {
		t.Fatalf("改判应允许: %v", err)
	}
	if tc.Status != "failed" || tc.VerifierID != "usr_y" || tc.ManualNote != "复测发现 bug" {
		t.Fatalf("改判字段不符:status=%s verifier=%q note=%q", tc.Status, tc.VerifierID, tc.ManualNote)
	}
}

// TestRecordManualVerdict_AutoOnlyRejected auto-only(passed 且无 verifier)→ ErrNotManualVerifiable。
func TestRecordManualVerdict_AutoOnlyRejected(t *testing.T) {
	svc := newSvcWithStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "auto")
	tc.ID = "tc_auto"
	tc.Status = "passed" // 自动验过的,无 verifier_id
	_ = svc.store.Create(ctx, tc)
	err := svc.RecordManualVerdict(ctx, tc, "failed", "x", "usr_x")
	if !errors.Is(err, ErrNotManualVerifiable) {
		t.Fatalf("auto-only 应拒,得到 err=%v", err)
	}
}

// TestRecordManualVerdict_InvalidVerdict verdict 非法 → ErrInvalidVerdict。
func TestRecordManualVerdict_InvalidVerdict(t *testing.T) {
	svc := newSvcWithStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "iv")
	tc.ID = "tc_iv"
	tc.Status = "manual"
	_ = svc.store.Create(ctx, tc)
	if err := svc.RecordManualVerdict(ctx, tc, "bogus", "", "usr_x"); !errors.Is(err, ErrInvalidVerdict) {
		t.Fatalf("非法 verdict 应 ErrInvalidVerdict,得到 %v", err)
	}
}

// TestRunHTTPRequest_SkipsVerified 已验(verifier_id 非空)用例:RunHTTPRequest 短路,不改写。
func TestRunHTTPRequest_SkipsVerified(t *testing.T) {
	svc := newSvcWithStore(t)
	ctx := context.Background()
	tc := mkTC("ps_1", "req_1", "skip")
	tc.ID = "tc_skip"
	tc.Status = "passed"
	tc.VerifierID = "usr_already" // 已人工验收
	_ = svc.store.Create(ctx, tc)
	// 注意:Create 不写 verifier_id,这里白盒补上。
	_, _ = svc.store.db.ExecContext(ctx, `UPDATE test_case SET verifier_id=$1 WHERE id=$2`, "usr_already", tc.ID)
	tc.VerifiedAt = nil // 清掉内存态,确保不被旧值干扰

	if err := svc.RunHTTPRequest(ctx, tc, "http://example.com"); err != nil {
		t.Fatalf("RunHTTPRequest: %v", err)
	}
	if tc.Status != "passed" {
		t.Fatalf("已验用例不应被改写,得到 status=%s", tc.Status)
	}
	if tc.ActualBody != "" {
		t.Fatalf("已验用例不应写 ActualBody,得到 %q", tc.ActualBody)
	}
}
```

> 测试文件需 import `"errors"`。

- [ ] **Step 2: 跑测试验证失败**

Run: `cd platform/backend && go test ./internal/qa/ -run 'TestRecordManualVerdict|TestRunHTTPRequest_SkipsVerified' -v`
Expected: 编译失败(`ErrInvalidVerdict/ErrNotManualVerifiable/RecordManualVerdict undefined`)。

- [ ] **Step 3: 实现**(service.go)

import 块加 `"errors"`。在 `NewService` 之后加哨兵:

```go
// 人工验收哨兵(Handler 据此映射 400/409)。
var (
	ErrInvalidVerdict      = errors.New("非法的验收结论,仅支持 passed/failed")
	ErrNotManualVerifiable = errors.New("该用例非人工验收范围(仅 manual 用例支持人工验收)")
)
```

`RecordManualVerdict`(放在 `RunHTTPRequest` 之前):

```go
// RecordManualVerdict 人工验收:校验 verdict + 范围(manual 或已验过),置字段并落库。
// verifierID 来自 auth.CtxUserDBID(Handler 注入)。
func (s *Service) RecordManualVerdict(ctx context.Context, tc *TestCase, verdict, note, verifierID string) error {
	if verdict != "passed" && verdict != "failed" {
		return ErrInvalidVerdict
	}
	if tc.Status != "manual" && tc.VerifierID == "" {
		return ErrNotManualVerifiable
	}
	tc.Status = verdict
	tc.ManualNote = note
	tc.VerifierID = verifierID
	tc.VerifiedAt = nowTime()
	return s.store.UpdateManualVerdict(ctx, tc)
}
```

`RunHTTPRequest` 函数体最开头(`tc.RunAt = nowTime()` **之前**)插入短路:

```go
	// 已人工验收:自动运行不覆盖(防批量验收把人工结论冲回 manual)。
	if tc.VerifierID != "" {
		return nil
	}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd platform/backend && go test ./internal/qa/ -run 'TestRecordManualVerdict|TestRunHTTPRequest_SkipsVerified' -v`
Expected: PASS。全量回归:`go test ./internal/qa/ -v`,全绿(尤其原有 `TestRunHTTPRequest_*` 不受短路影响——它们 VerifierID 为空)。

- [ ] **Step 5: 提交**

```bash
git add platform/backend/internal/qa/service.go platform/backend/internal/qa/service_test.go
git commit -m "feat(qa): Service.RecordManualVerdict(哨兵校验) + 已验用例防自动覆盖"
```

---

## Task 4: Handler.ManualVerdict + 路由

**Files:**

- Modify: `platform/backend/internal/qa/handler.go`(import `errors`、`auth`;`ManualVerdict`;Register 加路由)
- Test: `platform/backend/internal/qa/handler_test.go`(新建)

**Interfaces:**

- Consumes:Task 3 的 `Service.RecordManualVerdict` + 哨兵;`auth.CtxUserDBID`。
- Produces:`POST /project-spaces/:id/test-cases/:tcid/manual-verdict` 端点。

- [ ] **Step 1: 写失败测试**(新建 `handler_test.go`)

```go
package qa

import (
	"bytes"
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"

	"zhiyuan-anp/platform/backend/internal/auth"
)

// newVerdictRouter 搭真 store+service+handler,注入测试 CtxUserDBID。
func newVerdictRouter(t *testing.T) (*Store, *gin.Engine) {
	t.Helper()
	svc := newSvcWithStore(t)
	h := NewHandler(svc, nil, nil) // ManualVerdict 不用 reqRepo/apps
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(func(c *gin.Context) { c.Set(auth.CtxUserDBID, "usr_test"); c.Next() })
	r.POST("/project-spaces/:id/test-cases/:tcid/manual-verdict", h.ManualVerdict)
	return svc.store, r
}

func doVerdict(t *testing.T, r *gin.Engine, tcid, body string) *httptest.ResponseRecorder {
	req := httptest.NewRequest("POST", "/project-spaces/ps_1/test-cases/"+tcid+"/manual-verdict", bytes.NewReader([]byte(body)))
	req.Header.Set("Content-Type", "application/json")
	w := httptest.NewRecorder()
	r.ServeHTTP(w, req)
	return w
}

// TestHandler_ManualVerdict_OK manual 用例录通过 → 200,响应 status=passed/verifier_id=usr_test。
func TestHandler_ManualVerdict_OK(t *testing.T) {
	st, r := newVerdictRouter(t)
	tc := mkTC("ps_1", "req_1", "ok")
	tc.ID = "tc_ok"
	tc.Status = "manual"
	_ = st.Create(context.Background(), tc)

	w := doVerdict(t, r, "tc_ok", `{"verdict":"passed","note":"手验通过"}`)
	if w.Code != 200 {
		t.Fatalf("应 200,得到 %d body=%s", w.Code, w.Body.String())
	}
	var env struct {
		Code int `json:"code"`
		Data struct {
			Status     string `json:"status"`
			VerifierID string `json:"verifier_id"`
			ManualNote string `json:"manual_note"`
		} `json:"data"`
	}
	_ = json.Unmarshal(w.Body.Bytes(), &env)
	if env.Code != 0 || env.Data.Status != "passed" || env.Data.VerifierID != "usr_test" || env.Data.ManualNote != "手验通过" {
		t.Fatalf("响应不符:code=%d status=%s verifier=%q note=%q", env.Code, env.Data.Status, env.Data.VerifierID, env.Data.ManualNote)
	}
}

// TestHandler_ManualVerdict_NotFound 用例不存在 → 404。
func TestHandler_ManualVerdict_NotFound(t *testing.T) {
	_, r := newVerdictRouter(t)
	w := doVerdict(t, r, "nope", `{"verdict":"passed"}`)
	if w.Code != 404 {
		t.Fatalf("应 404,得到 %d", w.Code)
	}
}

// TestHandler_ManualVerdict_BadVerdict verdict 非法 → 400。
func TestHandler_ManualVerdict_BadVerdict(t *testing.T) {
	st, r := newVerdictRouter(t)
	tc := mkTC("ps_1", "req_1", "bad")
	tc.ID = "tc_bad"
	tc.Status = "manual"
	_ = st.Create(context.Background(), tc)
	w := doVerdict(t, r, "tc_bad", `{"verdict":"bogus"}`)
	if w.Code != 400 {
		t.Fatalf("应 400,得到 %d", w.Code)
	}
}

// TestHandler_ManualVerdict_AutoOnlyRejected auto-only → 409。
func TestHandler_ManualVerdict_AutoOnlyRejected(t *testing.T) {
	st, r := newVerdictRouter(t)
	tc := mkTC("ps_1", "req_1", "auto")
	tc.ID = "tc_auto_h"
	tc.Status = "passed" // 自动验过,无 verifier_id
	_ = st.Create(context.Background(), tc)
	w := doVerdict(t, r, "tc_auto_h", `{"verdict":"failed"}`)
	if w.Code != 409 {
		t.Fatalf("应 409,得到 %d", w.Code)
	}
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd platform/backend && go test ./internal/qa/ -run TestHandler_ManualVerdict -v`
Expected: 编译失败(`h.ManualVerdict undefined`)。

- [ ] **Step 3: 实现 handler.go**

import 块加 `"errors"` 和 `"zhiyuan-anp/platform/backend/internal/auth"`。

`Register` 路由块加一行(在 `run-tests` 之后):

```go
	r.POST("/project-spaces/:id/test-cases/:tcid/manual-verdict", h.ManualVerdict) // 人工验收:manual 用例录结论
```

新增 `ManualVerdict` handler(放在 `RunForReq` 之后):

```go
// ManualVerdict 人工验收:manual 用例(或已验过改判)录 passed/failed + 备注。
//
// @Summary      人工验收测试用例
// @Tags         qa
// @Accept       json
// @Produce      json
// @Param        id    path  string  true  "项目空间ID"
// @Param        tcid  path  string  true  "测试用例ID"
// @Param        body  body  object  true  "{verdict:string,note:string}"
// @Success      200  {object}  map[string]interface{}  "更新后的用例(含 verifier_name)"
// @Failure      400  {object}  map[string]interface{}  "verdict 非法"
// @Failure      404  {object}  map[string]interface{}  "用例不存在"
// @Failure      409  {object}  map[string]interface{}  "非人工验收范围"
// @Security     BearerAuth
// @Router       /project-spaces/{id}/test-cases/{tcid}/manual-verdict [post]
func (h *Handler) ManualVerdict(c *gin.Context) {
	tc, err := h.svc.GetCase(c.Request.Context(), c.Param("tcid"))
	if err != nil || tc == nil || tc.ID == "" {
		httpx.Err(c, 404, 40402, "用例不存在")
		return
	}
	var body struct {
		Verdict string `json:"verdict"`
		Note    string `json:"note"`
	}
	if err := c.BindJSON(&body); err != nil {
		httpx.Err(c, 400, 40001, "请求体格式错误")
		return
	}
	verifierID := c.GetString(auth.CtxUserDBID)
	if err := h.svc.RecordManualVerdict(c.Request.Context(), tc, body.Verdict, body.Note, verifierID); err != nil {
		switch {
		case errors.Is(err, ErrInvalidVerdict):
			httpx.Err(c, 400, 40001, err.Error())
		case errors.Is(err, ErrNotManualVerifiable):
			httpx.Err(c, 409, 40902, err.Error())
		default:
			httpx.Err(c, 500, 50008, err.Error())
		}
		return
	}
	// re-Get 回填 verifier_name(UpdateManualVerdict 不写 join 列)。
	refreshed, err := h.svc.GetCase(c.Request.Context(), tc.ID)
	if err != nil || refreshed == nil || refreshed.ID == "" {
		httpx.OK(c, tc) // 回退内存态
		return
	}
	httpx.OK(c, refreshed)
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd platform/backend && go test ./internal/qa/ -run TestHandler_ManualVerdict -v`
Expected: PASS。全量 qa 回归:`go test ./internal/qa/ -v` 全绿。

- [ ] **Step 5: 提交**

```bash
git add platform/backend/internal/qa/handler.go platform/backend/internal/qa/handler_test.go
git commit -m "feat(qa): POST manual-verdict 端点(404/400/409 守卫 + re-Get 回填验收人名)"
```

---

## Task 5: 前端 testing/page.tsx — manual 用例人工验收 UI

**Files:**

- Modify: `platform/frontend/app/testing/page.tsx`

**Interfaces:**

- Consumes:Task 4 的 `POST /project-spaces/:id/test-cases/:tcid/manual-verdict`(body `{verdict,note}`)。
- Produces:`manual` 卡片显示"通过/失败+备注"录入;已验卡片显示摘要+改判;auto-only 维持「▶运行」。

> **前置(AGENTS.md 强制):** 写代码前先 `ls platform/frontend/node_modules/next/dist/docs/`,读覆盖 client component('use client')、受控 input、event handler 的指南;遵守其 deprecation 提示。

- [ ] **Step 1: 读 Next.js in-tree 文档**

Run: `ls platform/frontend/node_modules/next/dist/docs/`
阅读其中与"use client"、表单受控组件、`onClick`/`onChange` 相关的篇目后再改。

- [ ] **Step 2: 扩展 TC 类型 + 新增 state**

`TC` 类型(约第 9-23 行)末尾的 `run_at?: string;` 之后追加:

```ts
  manual_note?: string;
  verifier_id?: string;
  verified_at?: string;
  verifier_name?: string;
```

在现有 `useState` 区(约第 39-46 行)`runReqID` 之后追加:

```ts
const [verdictID, setVerdictID] = useState(""); // 正在提交的 tcid
const [editTC, setEditTC] = useState(""); // 展开录入(含改判)的 tcid
const [note, setNote] = useState(""); // 录入框文本
```

- [ ] **Step 3: 加 recordVerdict 提交函数**

在 `runAll` 函数之后新增:

```ts
async function recordVerdict(tcid: string, verdict: string) {
  setVerdictID(tcid);
  setMsg("");
  try {
    const res = await fetch(
      `${API_BASE_URL}/project-spaces/${psID}/test-cases/${tcid}/manual-verdict`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ verdict, note }),
      }
    );
    const r = await res.json();
    if (r.code === 0) {
      const tc: TC = r.data;
      setMsg(`「${tc.title}」→ ${STATUS_LABEL[tc.status] ?? tc.status}(人工验收)`);
      setEditTC("");
      setNote("");
    } else {
      setMsg(`✗ ${r.message}`);
    }
    load(psID);
  } catch (e) {
    setMsg(`✗ ${e}`);
  } finally {
    setVerdictID("");
  }
}
```

- [ ] **Step 4: 替换卡片右侧按钮区为条件渲染**

把原卡片 header 右侧的「▶运行」按钮:

```tsx
<button
  onClick={() => runOne(c.id)}
  disabled={!!runID}
  className="rounded bg-neutral-800 px-2 py-1 text-xs text-white disabled:opacity-50"
>
  {runID === c.id ? "运行中…" : "▶ 运行"}
</button>
```

替换为:

```tsx
{
  c.status === "manual" || c.verifier_id ? (
    editTC === c.id ? (
      <div className="flex items-center gap-1">
        <input
          value={note}
          onChange={(e) => setNote(e.target.value)}
          placeholder="备注(可选)"
          className="w-32 rounded border border-neutral-300 px-2 py-1 text-xs"
        />
        <button
          onClick={() => recordVerdict(c.id, "passed")}
          disabled={!!verdictID}
          className="rounded bg-emerald-600 px-2 py-1 text-xs text-white disabled:opacity-50"
        >
          {verdictID === c.id ? "…" : "✓ 通过"}
        </button>
        <button
          onClick={() => recordVerdict(c.id, "failed")}
          disabled={!!verdictID}
          className="rounded bg-red-600 px-2 py-1 text-xs text-white disabled:opacity-50"
        >
          {verdictID === c.id ? "…" : "✗ 失败"}
        </button>
        <button
          onClick={() => {
            setEditTC("");
            setNote("");
          }}
          className="rounded bg-neutral-200 px-2 py-1 text-xs"
        >
          取消
        </button>
      </div>
    ) : c.verifier_id ? (
      <div className="flex items-center gap-1">
        <span className="text-xs text-neutral-500">
          {c.verifier_name || c.verifier_id} 验收 · {c.verified_at?.slice(0, 16).replace("T", " ")}
        </span>
        <button
          onClick={() => {
            setEditTC(c.id);
            setNote(c.manual_note ?? "");
          }}
          className="rounded bg-neutral-200 px-2 py-1 text-xs"
        >
          改判
        </button>
      </div>
    ) : (
      <button
        onClick={() => {
          setEditTC(c.id);
          setNote("");
        }}
        className="rounded bg-amber-500 px-2 py-1 text-xs text-white"
      >
        ⊙ 人工验收
      </button>
    )
  ) : (
    <button
      onClick={() => runOne(c.id)}
      disabled={!!runID}
      className="rounded bg-neutral-800 px-2 py-1 text-xs text-white disabled:opacity-50"
    >
      {runID === c.id ? "运行中…" : "▶ 运行"}
    </button>
  );
}
```

- [ ] **Step 5: 已验用例显示备注行**

在卡片内 `{c.run_at && (...)}` 块之后追加:

```tsx
{
  c.verifier_id && c.manual_note && (
    <div className="mt-1.5 rounded bg-amber-50 px-2 py-1 text-xs">
      <span className="text-neutral-400">验收备注:</span> {c.manual_note}
    </div>
  );
}
```

- [ ] **Step 6: 类型检查 / 构建**

Run: `cd platform/frontend && npm run build`(或项目既定的 lint/typecheck 命令)
Expected: 无 TS 报错,构建通过。

- [ ] **Step 7: 提交**

```bash
git add platform/frontend/app/testing/page.tsx
git commit -m "feat(frontend): 测试中心 manual 用例人工验收(通过/失败+备注+改判)"
```

---

## Task 6: 发布门禁提示文案补人工验收路径

**Files:**

- Modify: `platform/backend/internal/release/handler.go`(约第 90 行错误文案)

- [ ] **Step 1: 改文案**

把:

```go
			httpx.Err(c, 409, 40901, "发布被测试门禁拦截：来源需求无 passed 测试用例。请先到「测试中心」生成用例并运行至至少 1 条 passed，或在「系统配置」关闭 release_require_passed_test")
```

改为:

```go
			httpx.Err(c, 409, 40901, "发布被测试门禁拦截：来源需求无 passed 测试用例。请先到「测试中心」生成用例并运行至至少 1 条 passed(或对 manual 用例做人工验收)，或在「系统配置」关闭 release_require_passed_test")
```

- [ ] **Step 2: 构建 + 跑 release 测试确认无回归**

Run: `cd platform/backend && go build ./... && go test ./internal/release/ -v`
Expected: 构建通过,release 测试全绿(文案变更不影响测试断言——若 `handler_test.go` 断言了原串则同步改断言)。

- [ ] **Step 3: 提交**

```bash
git add platform/backend/internal/release/handler.go
git commit -m "chore(release): 测试门禁提示文案补人工验收路径"
```

---

## Task 7: .28 端到端验证

**Files:** 无(部署 + 手动验收)

> 部署流程见 `memory: deploy-prod-10.10.0.28`:scp 源码到 .28 `/opt/anp` → docker-compose 重建后端(+ 前端)→ 入口 8088。迁移 000019 由后端启动 `migrateUp` 自动应用。

- [ ] **Step 1: 部署到 .28**

scp 改动到 .28(或推 origin 后 .28 拉取),重建后端容器(前端若单独构建一并重建)。确认启动日志含 000019 迁移应用、无报错。

- [ ] **Step 2: 验 AC1/AC2/AC7(前端渲染)**

浏览器开测试中心:对一条 `manual` 用例 → 卡片显示「⊙ 人工验收」(AC7:无「▶运行」);点开 → 输备注 → 「✓ 通过」→ 卡片变绿、显示验收人+时间+备注(AC1);再点「改判」→ 录失败 → 变红(AC2)。

- [ ] **Step 3: 验 AC3(接口码)**

curl/前端分别触发:不存在 tcid → 404;verdict=bogus → 400;对 auto-only(passed 自动验过)用例 POST → 409;manual 用例 → 200。

- [ ] **Step 4: 验 AC4(防冲掉)**

对已验(已 passed 人工)用例点「⚡ 批量验收」→ 用例状态不被冲回 manual,验收摘要仍在。

- [ ] **Step 5: 验 AC5(进发布门禁)**

`release_require_passed_test=true` 下,某需求原本无 passed 被门禁拦截;对其 manual 用例人工录 passed 后,再次发布该需求 → 门禁放行。

- [ ] **Step 6: 验 AC6(审计)**

查 `operation_log`:每次人工验收 POST 留 actor(`usr_xxx`)+ 时间 + path。

- [ ] **Step 7: 收尾**

全绿后,如需要推 origin:`git push origin main`(对外动作,确认后推)。

---

## Self-Review(写完后自查记录)

- **Spec 覆盖**:AC1→T5;AC2→T5;AC3→T4;AC4→T3(RunHTTPRequest 短路)+ T7 Step4;AC5→T7 Step5(门禁计数 status='passed',T1/T2 保证人工 passed 落 status);AC6→audit 中间件自动 + T7 Step6;AC7→T5。迁移→T1;model→T1;store 读取→T1;UpdateManualVerdict→T2;RecordManualVerdict+哨兵→T3;Handler+路由→T4;前端→T5;门禁文案→T6。无遗漏。
- **占位扫描**:各步均含完整代码/命令,无 TBD/TODO/"类似上文"。
- **类型一致**:`RecordManualVerdict(ctx, *TestCase, verdict, note, verifierID string) error` 在 T3/T4/测试签名一致;`UpdateManualVerdict(ctx, *TestCase) error` 在 T2/T3 一致;哨兵名 `ErrInvalidVerdict/ErrNotManualVerifiable` 在 T3/T4 一致;`manual_note/verifier_id/verified_at/verifier_name` 在 model/store/handler/前端字段名一致。
