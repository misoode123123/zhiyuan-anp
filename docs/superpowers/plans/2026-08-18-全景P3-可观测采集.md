# 全景 P3 可观测采集（一期：部署历史时间线 + AI 部署运营统计）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 每次部署尝试落 `deploy_history` 表（两链同步直写、best-effort），应用详情页展示可折叠部署历史时间线，运维侧新增部署统计页（成功率/耗时/失败 top 原因/每日 trend，按 engine 分组）。

**Architecture:** 方案 A 同步直写。固定链在 `buildAndDeploy` 内版本确定后 INSERT、六处终态（成功/build 失败/run 失败/原生成败/记录缺失失败/panic）UPDATE；AI 链在 `aiDeploy` 版本自增后 INSERT、`aiFail`/`aiFailWithRollback`/成功点 UPDATE。写失败一律 `zap.Warn` 后继续——可观测采集永不反噬部署主链。孤儿行（backend 重启留下的 `result=''` 行）查询侧过滤（超 30min 排除统计与时间线），不清扫。统计 SQL 聚合 + Go 侧简单分词，前端表格式，不引图表库。

**Tech Stack:** Go(gin+sqlx+pgx) + PG 迁移 `000037_deploy_history`；Next.js App Router + 既有 tailwind 自定义类（`text-success`/`text-danger`/`text-warn`/`bg-surface-2` 等）。

**Spec:** `docs/superpowers/specs/2026-08-18-全景P3-可观测采集-design.md`（已批准，2026-08-18）

## Global Constraints

- 迁移文件：`000037_deploy_history.up.sql` / `.down.sql`（下一号确认是 000037，写前 `ls internal/db/migrations/pg | tail -3` 复核；testutil.TestDB 每次跑迁移，加了 up.sql 后所有 PG 测试自动建表）。
- 写 `deploy_history` 任何失败：`zap.Warn` 后继续，**绝不 return / 绝不改变部署结果**。
- `result` 三态：`''`(在途) / `success` / `failed`（AI 链失败回滚成功也记 `failed`，回滚详情进 notes——失败率不被回滚稀释）。
- UPDATE 按 `(app_id, env, version)` 定位且 `AND result=''`（防重复终结覆写；版本计数器失败不回退保证同版本只尝试一次）。
- 孤儿行：查询侧跳过 `result='' AND created_at < NOW()-interval '30 minutes'`；无后台清扫。在途行（<30min）时间线展示为「部署中…」。
- 统计 API `days` 钳制 1–90，默认 30；空表返回零值结构不 500。
- 后端测试跑法（Windows 本机无 docker，PG 在 .28）：`cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/...`（全仓则 `go test -p 1 -count=1 ./...`）。
- commit 规范：conventional commits（中文 body 可），body 每行 ≤100 字符， trailer `Co-Authored-By: Claude <noreply@anthropic.com>`。
- 测试隔离：所有 appdeploy 测试 fixture 的 `testutil.Truncate` 调用须加 `deploy_history` 表（本计划 Task 1 统一处理 8 处）。

## 对 spec 的实现精化（评审按此口径，不改 spec 语义）

1. **固定链 INSERT 锚点**：spec §2 锚「Deploy handler markBuilding 处」。实现放 `buildAndDeploy` 内 `GetOrCreateInstance` 成功后（version=ins.Version+1 预测值）——版本自增实际发生在 `Deployer.Build`（deployer.go:187），handler 同步段拿不到版本号；且 `buildAndDeploy` 有 4 个入口（Deploy/Promote/DeployCommit/DeployByAppID），只锚 Deploy handler 会漏 prod 部署与发布中心触发。AI 链锚 `aiDeploy` 内 `ins.Version++` 后（版本精确已知）。
2. **operator 透传**：`buildAndDeploy`/`aiDeploy` 新增 `operator string` 尾参。Deploy/Promote/DeployCommit 传 `c.GetString(auth.CtxUserID)`；`DeployByAppID`（发布中心触发）传 `"system"`；空值 INSERT 时归一 `"unknown"`。
3. **FinishDeployHistory 签名**：不收 finishedAt/duration 参数（SQL 按 `NOW()` 服务器时钟计算，免传参免时钟漂移）；增收 `image`/`hostPort`（成功行记实态镜像/端口，DDL 列落地）。
4. **固定链成功行断言移 .28 e2e**：本机/CI 无 docker daemon，`Build` 必失败；Go 集成测试覆盖固定链失败路径 + AI 全链（fake 注入），固定链 success 行由 .28 e2e 验证。
5. **panic 兜底用闭包变量 `histVer`**：goroutine 顶部声明，INSERT 前赋值；panic recover 里 `histVer > 0` 才 finish（INSERT 前 panic 无行可终结）。
6. **孤儿行 e2e 用 SQL backdate**：`UPDATE deploy_history SET created_at = NOW() - interval '31 minutes' WHERE result=''` 制造超 30min 行（无法真等 30 分钟）。

---

### Task 1: 迁移 + Store 四方法（deploy_history CRUD）

**Files:**

- Create: `platform/backend/internal/db/migrations/pg/000037_deploy_history.up.sql`
- Create: `platform/backend/internal/db/migrations/pg/000037_deploy_history.down.sql`
- Create: `platform/backend/internal/appdeploy/deploy_history.go`
- Test: `platform/backend/internal/appdeploy/deploy_history_test.go`
- Modify: 8 处测试 fixture 的 `testutil.Truncate` 加 `deploy_history`（handler_http_test.go:33、:714、:964、:1392；detail_test.go:49；quota_integration_test.go:27 与 :81；store_test.go:16）

**Interfaces:**

- Produces（后续任务依赖的确切签名）:
  - `type DeployHistoryItem struct` — 字段含 `db` tag：`ID int64 \`json:"id" db:"id"\``、`AppID string \`json:"app_id" db:"app_id"\``、`Env string \`json:"env" db:"env"\``、`Version int \`json:"version" db:"version"\``、`Engine string \`json:"engine" db:"engine"\``、`Result string \`json:"result" db:"result"\``、`Operator string \`json:"operator" db:"operator"\``、`SHA string \`json:"sha,omitempty" db:"sha"\``、`Image string \`json:"image,omitempty" db:"image"\``、`HostPort int \`json:"host_port,omitempty" db:"host_port"\``、`DurationSec *int \`json:"duration_sec,omitempty" db:"duration_sec"\``、`ErrorSummary string \`json:"error_summary,omitempty" db:"error_summary"\``、`Notes string \`json:"notes,omitempty" db:"notes"\``、`CreatedAt time.Time \`json:"created_at" db:"created_at"\``、`FinishedAt *time.Time \`json:"finished_at,omitempty" db:"finished_at"\``
  - `func (s *Store) InsertDeployHistory(ctx context.Context, appID, env string, version int, engine, operator, sha string) error`
  - `func (s *Store) FinishDeployHistory(ctx context.Context, appID, env string, version int, result, errSummary, notes, image string, hostPort int) error`
  - `func (s *Store) ListDeployHistoryByApp(ctx context.Context, appID string, limit int) ([]DeployHistoryItem, error)`（在途行返回；孤儿行已过滤；`result=''` 行 DurationSec 为 nil）
  - `func (s *Store) DeployStats(ctx context.Context, days int) (*DeployStatsResult, error)`
  - `type DeployStatsResult struct` — `Engines []EngineStats \`json:"engines"\``、`Daily []DailyCount \`json:"daily"\``
  - `type EngineStats struct` — `Engine string \`json:"engine"\``、`Success int \`json:"success"\``、`Failed int \`json:"failed"\``、`AvgSec int \`json:"avg_sec"\``、`MedSec int \`json:"med_sec"\``、`TopErrors []ErrFreq \`json:"top_errors"\``
  - `type ErrFreq struct` — `Fragment string \`json:"fragment"\``、`Count int \`json:"count"\``
  - `type DailyCount struct` — `Day string \`json:"day"\``（`2006-01-02`）、`Engine string \`json:"engine"\``、`Success int \`json:"success"\``、`Failed int \`json:"failed"\``

- [ ] **Step 1: 写迁移 SQL**

`000037_deploy_history.up.sql`（DDL 逐字来自 spec §1）：

```sql
CREATE TABLE deploy_history (
  id            BIGSERIAL PRIMARY KEY,
  app_id        TEXT NOT NULL,
  env           TEXT NOT NULL,           -- test / prod
  version       INT  NOT NULL,           -- 部署目标版本号（计数器值，失败不回退）
  engine        TEXT NOT NULL,           -- fixed / ai
  result        TEXT NOT NULL DEFAULT '', -- ''=在途 / success / failed
  operator      TEXT NOT NULL,           -- 触发用户名（CtxUserID）
  sha           TEXT,                    -- 构建提交（有则记，可空）
  image         TEXT,
  host_port     INT,
  duration_sec  INT,                     -- 开始→终态秒数（在途为 NULL）
  error_summary TEXT,                    -- 失败摘要 ≤200 字（last_error 头部）
  notes         TEXT,                    -- AI 链备注：回滚详情/验证失败步/绕过 shim 等
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  finished_at   TIMESTAMPTZ
);
CREATE INDEX idx_dephist_app ON deploy_history(app_id, created_at DESC);
CREATE INDEX idx_dephist_stat ON deploy_history(engine, result, created_at DESC);
```

`000037_deploy_history.down.sql`：

```sql
DROP TABLE IF EXISTS deploy_history;
```

- [ ] **Step 2: 写失败测试（Store CRUD 往返 + 孤儿过滤）**

`deploy_history_test.go`：

```go
package appdeploy

import (
	"context"
	"testing"
	"time"

	"zhiyuan-anp/platform/backend/internal/testutil"
)

// newHistStore 建干净 deploy_history 环境（truncate 独立，防与其它 fixture 串数据）。
func newHistStore(t *testing.T) *Store {
	t.Helper()
	db := testutil.TestDB(t)
	testutil.Truncate(t, db, "deploy_history")
	return NewStore(db)
}

// TestDeployHistory_InsertFinishRoundtrip 一行=一次尝试：INSERT 在途 → Finish 终态。
func TestDeployHistory_InsertFinishRoundtrip(t *testing.T) {
	s := newHistStore(t)
	ctx := context.Background()

	if err := s.InsertDeployHistory(ctx, "app_x", EnvTest, 3, "fixed", "yxt", ""); err != nil {
		t.Fatalf("insert: %v", err)
	}
	// 在途行：result=''，duration/finished 为 NULL
	items, err := s.ListDeployHistoryByApp(ctx, "app_x", 20)
	if err != nil || len(items) != 1 {
		t.Fatalf("在途应 1 行: %v %v", items, err)
	}
	if items[0].Result != "" || items[0].DurationSec != nil || items[0].FinishedAt != nil {
		t.Fatalf("在途行应为空 result/nil duration: %+v", items[0])
	}
	// 终结 success
	if err := s.FinishDeployHistory(ctx, "app_x", EnvTest, 3, "success", "", "", "appdeploy/x-test:v3", 9100); err != nil {
		t.Fatalf("finish: %v", err)
	}
	items, _ = s.ListDeployHistoryByApp(ctx, "app_x", 20)
	if len(items) != 1 || items[0].Result != "success" || items[0].DurationSec == nil || *items[0].DurationSec < 0 {
		t.Fatalf("终态行: %+v", items[0])
	}
	if items[0].Image != "appdeploy/x-test:v3" || items[0].HostPort != 9100 {
		t.Fatalf("成功行应记实态镜像/端口: %+v", items[0])
	}
	// 重复终结 no-op（result='' 守卫）：再次 finish failed 不覆写
	_ = s.FinishDeployHistory(ctx, "app_x", EnvTest, 3, "failed", "later", "", "", 0)
	items, _ = s.ListDeployHistoryByApp(ctx, "app_x", 20)
	if items[0].Result != "success" {
		t.Fatalf("已终态行不得被覆写: %+v", items[0])
	}
}

// TestDeployHistory_OrphanFiltered 孤儿行（result='' 超 30min）不进 List；在途（<30min）可见。
func TestDeployHistory_OrphanFiltered(t *testing.T) {
	s := newHistStore(t)
	ctx := context.Background()
	_ = s.InsertDeployHistory(ctx, "app_o", EnvTest, 1, "ai", "yxt", "")
	// backdate 成孤儿
	if _, err := s.db.Exec(`UPDATE deploy_history SET created_at = NOW() - interval '31 minutes' WHERE app_id='app_o'`); err != nil {
		t.Fatalf("backdate: %v", err)
	}
	_ = s.InsertDeployHistory(ctx, "app_o", EnvTest, 2, "ai", "yxt", "") // 在途（新）
	_ = s.InsertDeployHistory(ctx, "app_o", EnvTest, 3, "ai", "yxt", "")
	_ = s.FinishDeployHistory(ctx, "app_o", EnvTest, 3, "failed", "boom", "", "", 0)

	items, err := s.ListDeployHistoryByApp(ctx, "app_o", 20)
	if err != nil {
		t.Fatalf("list: %v", err)
	}
	if len(items) != 2 {
		t.Fatalf("孤儿应被过滤（剩在途 v2 + 终态 v3 共 2 行），得到 %d: %+v", len(items), items)
	}
}

// TestDeployHistory_OperatorNormalized 空 operator 归一 unknown。
func TestDeployHistory_OperatorNormalized(t *testing.T) {
	s := newHistStore(t)
	ctx := context.Background()
	if err := s.InsertDeployHistory(ctx, "app_n", EnvTest, 1, "fixed", "", ""); err != nil {
		t.Fatalf("insert: %v", err)
	}
	items, _ := s.ListDeployHistoryByApp(ctx, "app_n", 20)
	if len(items) != 1 || items[0].Operator != "unknown" {
		t.Fatalf("空 operator 应归一 unknown: %+v", items)
	}
}
```

- [ ] **Step 3: 跑测试确认失败**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run TestDeployHistory -v
```

预期：编译失败 `undefined: (*Store).InsertDeployHistory`。

- [ ] **Step 4: 写 deploy_history.go 实现**

```go
package appdeploy

import (
	"context"
	"math"
	"sort"
	"strings"
	"time"
)

// DeployHistoryItem 部署历史行（一行=一次部署尝试）。
type DeployHistoryItem struct {
	ID           int64      `json:"id" db:"id"`
	AppID        string     `json:"app_id" db:"app_id"`
	Env          string     `json:"env" db:"env"`
	Version      int        `json:"version" db:"version"`
	Engine       string     `json:"engine" db:"engine"`
	Result       string     `json:"result" db:"result"`
	Operator     string     `json:"operator" db:"operator"`
	SHA          string     `json:"sha,omitempty" db:"sha"`
	Image        string     `json:"image,omitempty" db:"image"`
	HostPort     int        `json:"host_port,omitempty" db:"host_port"`
	DurationSec  *int       `json:"duration_sec,omitempty" db:"duration_sec"`
	ErrorSummary string     `json:"error_summary,omitempty" db:"error_summary"`
	Notes        string     `json:"notes,omitempty" db:"notes"`
	CreatedAt    time.Time  `json:"created_at" db:"created_at"`
	FinishedAt   *time.Time `json:"finished_at,omitempty" db:"finished_at"`
}

// orphanCond 孤儿行过滤条件：result='' 且创建超 30min = backend 重启杀 goroutine
// 留下的永不终结行。统计与时间线共用；在途（<30min）行保留（时间线展示「部署中…」）。
const orphanCond = `NOT (result = '' AND created_at < NOW() - interval '30 minutes')`

// InsertDeployHistory 部署开始时插入在途行（result=''）。operator 空归一 unknown。
func (s *Store) InsertDeployHistory(ctx context.Context, appID, env string, version int, engine, operator, sha string) error {
	if operator == "" {
		operator = "unknown"
	}
	_, err := s.db.ExecContext(ctx,
		`INSERT INTO deploy_history (app_id, env, version, engine, operator, sha)
		 VALUES ($1, $2, $3, $4, $5, NULLIF($6,''))`,
		appID, env, version, engine, operator, sha)
	return err
}

// FinishDeployHistory 终结本次部署行：按 (app_id,env,version) 定位且仅终结在途行
// （result='' 守卫防重复覆写）。duration/finished_at 按 DB 服务器时钟计算（免传参免漂移）。
// 成功行传 image/hostPort 实态值；失败行传零值（列保持 NULL）。
func (s *Store) FinishDeployHistory(ctx context.Context, appID, env string, version int, result, errSummary, notes, image string, hostPort int) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE deploy_history SET result=$1, error_summary=NULLIF($2,''), notes=NULLIF($3,''),
		        image=COALESCE(NULLIF($4,''), image), host_port=CASE WHEN $5>0 THEN $5 ELSE host_port END,
		        duration_sec=EXTRACT(EPOCH FROM (NOW()-created_at))::INT, finished_at=NOW()
		 WHERE app_id=$6 AND env=$7 AND version=$8 AND result=''`,
		result, errSummary, notes, image, hostPort, appID, env, version)
	return err
}

// ListDeployHistoryByApp 某应用部署历史（新→旧，含在途行，孤儿过滤，默认条数由调用方传）。
func (s *Store) ListDeployHistoryByApp(ctx context.Context, appID string, limit int) ([]DeployHistoryItem, error) {
	var list []DeployHistoryItem
	err := s.db.SelectContext(ctx, &list,
		`SELECT id, app_id, env, version, engine, result, operator,
		        COALESCE(sha,'') AS sha, COALESCE(image,'') AS image,
		        COALESCE(host_port,0) AS host_port, duration_sec,
		        COALESCE(error_summary,'') AS error_summary, COALESCE(notes,'') AS notes,
		        created_at, finished_at
		 FROM deploy_history WHERE app_id=$1 AND `+orphanCond+`
		 ORDER BY created_at DESC LIMIT $2`, appID, limit)
	return list, err
}

// DeployStatsResult 部署统计聚合（按 engine 分组 + 每日计数）。
type DeployStatsResult struct {
	Engines []EngineStats `json:"engines"`
	Daily   []DailyCount  `json:"daily"`
}

// EngineStats 单引擎聚合。MedSec 为中位耗时（偶数个取中间偏小，展示用）。
type EngineStats struct {
	Engine    string    `json:"engine"`
	Success   int       `json:"success"`
	Failed    int       `json:"failed"`
	AvgSec    int       `json:"avg_sec"`
	MedSec    int       `json:"med_sec"`
	TopErrors []ErrFreq `json:"top_errors"`
}

// ErrFreq 失败原因片段词频。
type ErrFreq struct {
	Fragment string `json:"fragment"`
	Count    int    `json:"count"`
}

// DailyCount 每日每引擎计数（day=created_at 日期，按东八区取日界）。
type DailyCount struct {
	Day     string `json:"day"`
	Engine  string `json:"engine"`
	Success int    `json:"success"`
	Failed  int    `json:"failed"`
}

// DeployStats 近 N 天（钳制 1-90）按引擎聚合：计数/耗时/每日 trend。空表返回零值结构。
// 失败 top 原因在 Go 侧分词（SQL 不做文本处理）：按标点切 + 取 ≥4 字（rune 计）片段，词频 top5。
func (s *Store) DeployStats(ctx context.Context, days int) (*DeployStatsResult, error) {
	if days < 1 {
		days = 1
	}
	if days > 90 {
		days = 90
	}
	res := &DeployStatsResult{Engines: []EngineStats{}, Daily: []DailyCount{}}
	since := time.Now().AddDate(0, 0, -days)

	// 每 engine 终态计数
	var agg []struct {
		Engine string `db:"engine"`
		Status string `db:"status"`
		N      int    `db:"n"`
	}
	if err := s.db.SelectContext(ctx, &agg,
		`SELECT engine, result AS status, COUNT(*)::INT AS n
		 FROM deploy_history
		 WHERE result IN ('success','failed') AND created_at >= $1 AND `+orphanCond+`
		 GROUP BY engine, result`, since); err != nil {
		return nil, err
	}
	byEngine := map[string]*EngineStats{}
	for _, r := range agg {
		e := byEngine[r.Engine]
		if e == nil {
			e = &EngineStats{Engine: r.Engine, TopErrors: []ErrFreq{}}
			byEngine[r.Engine] = e
		}
		if r.Status == "success" {
			e.Success = r.N
		} else {
			e.Failed = r.N
		}
	}
	// 耗时集合（success+failed 一起算均值/中位）+ 失败摘要集合
	var rows []struct {
		Engine string `db:"engine"`
		Dur    *int   `db:"duration_sec"`
		Err    string `db:"error_summary"`
	}
	if err := s.db.SelectContext(ctx, &rows,
		`SELECT engine, duration_sec, COALESCE(error_summary,'') AS error_summary
		 FROM deploy_history
		 WHERE result IN ('success','failed') AND created_at >= $1 AND `+orphanCond, since); err != nil {
		return nil, err
	}
	durs := map[string][]int{}
	errs := map[string][]string{}
	for _, r := range rows {
		if r.Dur != nil {
			durs[r.Engine] = append(durs[r.Engine], *r.Dur)
		}
		if r.Err != "" {
			errs[r.Engine] = append(errs[r.Engine], r.Err)
		}
	}
	for eng, e := range byEngine {
		if ds := durs[eng]; len(ds) > 0 {
			sum := 0
			for _, d := range ds {
				sum += d
			}
			e.AvgSec = sum / len(ds)
			sorted := append([]int(nil), ds...)
			sort.Ints(sorted)
			e.MedSec = sorted[len(sorted)/2]
		}
		e.TopErrors = topErrorFragments(errs[eng], 5)
	}
	// 引擎稳定排序（fixed 在前 ai 在后，其余按字典序）
	order := map[string]int{"fixed": 0, "ai": 1}
	engines := make([]string, 0, len(byEngine))
	for eng := range byEngine {
		engines = append(engines, eng)
	}
	sort.Slice(engines, func(i, j int) bool {
		oi, oj := order[engines[i]], order[engines[j]]
		if oi != oj {
			return oi < oj
		}
		return engines[i] < engines[j]
	})
	for _, eng := range engines {
		res.Engines = append(res.Engines, *byEngine[eng])
	}

	// 每日 trend（FILTER 宽行；东八区取日界——.28 部署与用户时区一致）
	var wide []struct {
		Day     string `db:"day"`
		Engine  string `db:"engine"`
		Success int    `db:"success"`
		Failed  int    `db:"failed"`
	}
	if err := s.db.SelectContext(ctx, &wide,
		`SELECT to_char((created_at AT TIME ZONE 'Asia/Shanghai')::DATE, 'YYYY-MM-DD') AS day,
		        engine,
		        COUNT(*) FILTER (WHERE result='success')::INT AS success,
		        COUNT(*) FILTER (WHERE result='failed')::INT AS failed
		 FROM deploy_history
		 WHERE result IN ('success','failed') AND created_at >= $1 AND `+orphanCond+`
		 GROUP BY 1, 2 ORDER BY 1 DESC, 2`, since); err != nil {
		return nil, err
	}
	for _, w := range wide {
		res.Daily = append(res.Daily, DailyCount{Day: w.Day, Engine: w.Engine, Success: w.Success, Failed: w.Failed})
	}
	return res, nil
}

// topErrorFragments 失败原因简单分词词频：按标点/空白切，取 rune 长度 ≥4 的片段，
// 计频取 top n。中文错误（如「docker build 失败: exit 1」）切出「docker」「build」等英文
// 片段与「失败」等短词——≥4 字过滤掉短中文词，保留可读英文词。
func topErrorFragments(errs []string, n int) []ErrFreq {
	freq := map[string]int{}
	for _, e := range errs {
		fields := strings.FieldsFunc(e, func(r rune) bool {
			return !unicode.IsLetter(r) && !unicode.IsDigit(r)
		})
		for _, f := range fields {
			if len([]rune(f)) >= 4 {
				freq[strings.ToLower(f)]++
			}
		}
	}
	out := make([]ErrFreq, 0, len(freq))
	for k, v := range freq {
		out = append(out, ErrFreq{Fragment: k, Count: v})
	}
	sort.Slice(out, func(i, j int) bool {
		if out[i].Count != out[j].Count {
			return out[i].Count > out[j].Count
		}
		return out[i].Fragment < out[j].Fragment
	})
	if len(out) > n {
		out = out[:n]
	}
	return out
}
```

（import 清单：`context` / `sort` / `strings` / `time` / `unicode`——无 math。）

- [ ] **Step 5: 8 处测试 fixture 加 deploy_history truncate**

用 grep 找到全部调用点并逐一加表名（示例 handler_http_test.go:33，其余同形）：

```go
	testutil.Truncate(t, db,
		"release_record", "change_request", "requirement",
		"appdeploy_env", "appdeploy_instance", "appdeploy_application",
		"deploy_history",
	)
```

store_test.go:16 单行式改为：

```go
	testutil.Truncate(t, db, "deploy_history", "appdeploy_env", "appdeploy_instance", "appdeploy_application")
```

quota_integration_test.go:81 单表 `"appdeploy_application"` → `"deploy_history", "appdeploy_application"`。

- [ ] **Step 6: 跑测试确认通过**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run TestDeployHistory -v
```

预期：3 个测试全 PASS。然后全包回归：

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/...
```

预期：全 PASS（fixture 加了 truncate，无跨测试污染）。

- [ ] **Step 7: Commit**

```bash
git add platform/backend/internal/db/migrations/pg/000037_deploy_history.*.sql platform/backend/internal/appdeploy/deploy_history.go platform/backend/internal/appdeploy/deploy_history_test.go platform/backend/internal/appdeploy/*_test.go
git commit -m "feat(appdeploy): deploy_history 表 + Store 四方法（P3 可观测采集）

- 迁移 000037：一行=一次部署尝试，result 三态，两索引
- Insert/Finish/List/Stats；孤儿行（在途超30min）查询侧过滤
- Finish 按 (app_id,env,version) 定位且 result='' 守卫防覆写
- 8 处测试 fixture 补 deploy_history truncate 隔离

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: Stats 聚合 + 分词单测

**Files:**

- Test: `platform/backend/internal/appdeploy/deploy_history_test.go`（追加）

**Interfaces:**

- Consumes: Task 1 的 `DeployStats` / `topErrorFragments` / `InsertDeployHistory` / `FinishDeployHistory`。
- Produces: 无新接口（钉死 Task 1 行为的测试）。

- [ ] **Step 1: 追加统计聚合测试**

```go
// TestDeployStats_Aggregation 统计聚合：按 engine 分组计数/均值中位/每日 trend。
func TestDeployStats_Aggregation(t *testing.T) {
	s := newHistStore(t)
	ctx := context.Background()
	// fixed: 2 成功（60s, 100s）1 失败（30s, err="docker build 失败: exit status 1"）
	_ = s.InsertDeployHistory(ctx, "app_s", EnvTest, 1, "fixed", "yxt", "")
	_ = s.FinishDeployHistory(ctx, "app_s", EnvTest, 1, "success", "", "", "img:v1", 9100)
	_ = s.InsertDeployHistory(ctx, "app_s", EnvTest, 2, "fixed", "yxt", "")
	_ = s.FinishDeployHistory(ctx, "app_s", EnvTest, 2, "success", "", "", "img:v2", 9100)
	_ = s.InsertDeployHistory(ctx, "app_s", EnvTest, 3, "fixed", "yxt", "")
	_ = s.FinishDeployHistory(ctx, "app_s", EnvTest, 3, "failed", "docker build 失败: exit status 1", "", "", 0)
	// ai: 1 成功 1 失败（同 err 片段 "docker"）
	_ = s.InsertDeployHistory(ctx, "app_s", EnvTest, 4, "ai", "ai-op", "")
	_ = s.FinishDeployHistory(ctx, "app_s", EnvTest, 4, "success", "", "", "img:v4", 9100)
	_ = s.InsertDeployHistory(ctx, "app_s", EnvTest, 5, "ai", "ai-op", "")
	_ = s.FinishDeployHistory(ctx, "app_s", EnvTest, 5, "failed", "docker run 失败: port 已占用", "已自动回滚", "", 0)
	// 时长用 SQL 回填：直接 UPDATE duration_sec 造确定值（Finish 按真实时钟，测试不可假设）
	if _, err := s.db.Exec(`UPDATE deploy_history SET duration_sec = CASE version
		WHEN 1 THEN 60 WHEN 2 THEN 100 WHEN 3 THEN 30 WHEN 4 THEN 200 WHEN 5 THEN 40 END
		WHERE app_id='app_s'`); err != nil {
		t.Fatalf("fix durations: %v", err)
	}

	res, err := s.DeployStats(ctx, 30)
	if err != nil {
		t.Fatalf("stats: %v", err)
	}
	if len(res.Engines) != 2 {
		t.Fatalf("应 2 引擎组: %+v", res.Engines)
	}
	var fx, ai *EngineStats
	for i := range res.Engines {
		if res.Engines[i].Engine == "fixed" {
			fx = &res.Engines[i]
		}
		if res.Engines[i].Engine == "ai" {
			ai = &res.Engines[i]
		}
	}
	if fx == nil || ai == nil {
		t.Fatalf("缺引擎组: %+v", res.Engines)
	}
	if fx.Success != 2 || fx.Failed != 1 {
		t.Fatalf("fixed 计数: %+v", fx)
	}
	if fx.AvgSec != (60+100+30)/3 || fx.MedSec != 60 {
		t.Fatalf("fixed 均值/中位: avg=%d med=%d", fx.AvgSec, fx.MedSec)
	}
	if ai.Success != 1 || ai.Failed != 1 {
		t.Fatalf("ai 计数: %+v", ai)
	}
	if len(fx.TopErrors) == 0 || fx.TopErrors[0].Fragment != "docker" {
		t.Fatalf("fixed top error 应含 docker: %+v", fx.TopErrors)
	}
	if len(res.Daily) != 2 {
		t.Fatalf("每日 trend 应 2 行（两引擎同日）: %+v", res.Daily)
	}
}

// TestDeployStats_Empty 空表零值不 500。
func TestDeployStats_Empty(t *testing.T) {
	s := newHistStore(t)
	res, err := s.DeployStats(context.Background(), 30)
	if err != nil {
		t.Fatalf("空表应不报错: %v", err)
	}
	if res == nil || res.Engines == nil || res.Daily == nil {
		t.Fatalf("空表应返回零值结构（非 nil 切片）: %+v", res)
	}
	if len(res.Engines) != 0 || len(res.Daily) != 0 {
		t.Fatalf("空表应 0 组: %+v", res)
	}
}

// TestDeployStats_DaysClamped days 钳制（0→1, 999→90），超窗行不进统计。
func TestDeployStats_DaysClamped(t *testing.T) {
	s := newHistStore(t)
	ctx := context.Background()
	_ = s.InsertDeployHistory(ctx, "app_c", EnvTest, 1, "fixed", "yxt", "")
	_ = s.FinishDeployHistory(ctx, "app_c", EnvTest, 1, "success", "", "", "img", 1)
	// 91 天前的行：days=90 窗外
	_ = s.InsertDeployHistory(ctx, "app_old", EnvTest, 1, "fixed", "yxt", "")
	_ = s.FinishDeployHistory(ctx, "app_old", EnvTest, 1, "success", "", "", "img", 1)
	if _, err := s.db.Exec(`UPDATE deploy_history SET created_at = NOW() - interval '91 days' WHERE app_id='app_old'`); err != nil {
		t.Fatalf("backdate: %v", err)
	}
	res, err := s.DeployStats(ctx, 0) // 钳到 1
	if err != nil {
		t.Fatalf("days=0 应钳 1 不报错: %v", err)
	}
	total := 0
	for _, e := range res.Engines {
		total += e.Success + e.Failed
	}
	if total != 1 {
		t.Fatalf("91 天前行不应计入（期望 1），得 %d", total)
	}
	if _, err := s.DeployStats(ctx, 999); err != nil { // 钳到 90，不报错
		t.Fatalf("days=999 应钳 90 不报错: %v", err)
	}
}

// TestTopErrorFragments 分词词频：标点切 + ≥4 rune 片段 + topN + 大小写归一。
func TestTopErrorFragments(t *testing.T) {
	errs := []string{
		"Docker build 失败: exit status 1",
		"docker pull 超时",
		"DOCKER! build again",
	}
	got := topErrorFragments(errs, 5)
	if len(got) == 0 || got[0].Fragment != "docker" || got[0].Count != 3 {
		t.Fatalf("docker 应 3 次居首: %+v", got)
	}
	// 短中文词（失败/超时=2 字）被 ≥4 过滤
	for _, f := range got {
		if f.Fragment == "失败" || f.Fragment == "超时" {
			t.Fatalf("2 字词不应入选: %+v", got)
		}
	}
	if len(topErrorFragments(nil, 5)) != 0 {
		t.Fatal("空输入应空输出")
	}
}
```

- [ ] **Step 2: 跑测试**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run 'TestDeployStats|TestTopErrorFragments' -v
```

预期：4 个测试 PASS（若 Task 1 死代码未清导致行为偏差，在此暴露修复）。

- [ ] **Step 3: Commit**

```bash
git add platform/backend/internal/appdeploy/deploy_history_test.go
git commit -m "test(appdeploy): deploy_history 统计聚合/分词/钳制单测

- 按 engine 分组计数/均值中位耗时断言
- topErrorFragments 标点切+≥4字+大小写归一
- days 钳制与窗外行排除；空表零值

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 固定链写入（buildAndDeploy 四写点 + panic 兜底）

**Files:**

- Modify: `platform/backend/internal/appdeploy/handler.go` — `buildAndDeploy`（约 1930-2114 行）加 `operator string` 尾参 + INSERT + 4 处 finish；panic recover 兜底
- Modify: `platform/backend/internal/appdeploy/handler.go` — 4 个调用点传 operator（Deploy:1801、Promote:1869-1870、DeployCommit:1914-1915、DeployByAppID:2498-2499）
- Test: `platform/backend/internal/appdeploy/deploy_history_test.go`（追加集成测试）

**Interfaces:**

- Consumes: Task 1 的 `InsertDeployHistory` / `FinishDeployHistory` / `truncateStr`（handler.go:464 现成，UTF-8 安全截断）。
- Produces: `func (h *Handler) buildAndDeploy(psID, aid, sha, env, nodeID, buildDir, operator string)`（新签名，Task 4/5 的 AI 链与 e2e 按此调用）。

- [ ] **Step 1: 写失败集成测试**

追加到 `deploy_history_test.go`：

```go
// TestDeployHistory_FixedChainFailPath 固定链失败路径：buildAndDeploy（无 docker 环境
// Build 必失败）→ deploy_history 落 failed 行（result/errSummary/duration）。
// 成功路径（result=success）本机无 docker 不可测，由 .28 e2e 验证（计划「实现精化」第 4 条）。
func TestDeployHistory_FixedChainFailPath(t *testing.T) {
	h, _ := newHTTPHandler(t)
	ctx := context.Background()
	a := seedApp(t, h, "ps_1", "hist-fail", t.TempDir())
	h.buildAndDeploy("ps_1", a.ID, "", EnvTest, "", "", "yxt")

	items, err := h.store.ListDeployHistoryByApp(ctx, a.ID, 20)
	if err != nil || len(items) != 1 {
		t.Fatalf("应恰 1 行: %v %v", items, err)
	}
	it := items[0]
	if it.Engine != "fixed" || it.Result != "failed" || it.Operator != "yxt" || it.Version != 1 {
		t.Fatalf("失败行字段: %+v", it)
	}
	if it.ErrorSummary == "" {
		t.Fatalf("失败行应记 error_summary: %+v", it)
	}
	if it.DurationSec == nil || *it.DurationSec < 0 {
		t.Fatalf("失败行应有 duration: %+v", it)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run TestDeployHistory_FixedChainFailPath -v
```

预期：编译失败（`buildAndDeploy` 还是 6 参签名）或断言失败 0 行。

- [ ] **Step 3: 实现 buildAndDeploy 写入**

3a. **签名 + panic 兜底 + INSERT**（handler.go:1930 起）。`histVer := 0` 声明放 recover 之后、`GetOrCreateInstance` 之前；INSERT 紧跟 `GetOrCreateInstance` 成功块（约 1963-1971 行）之后、原生部署分流之前——这样 prod/test 全入口覆盖且版本可预测（ins.Version+1）：

```go
func (h *Handler) buildAndDeploy(psID, aid, sha, env, nodeID, buildDir, operator string) {
	ctx := context.Background()
	// P3 部署历史：本行版本号（INSERT 后赋值；panic 兜底用它判断有无行可终结）
	histVer := 0
	defer func() {
		if r := recover(); r != nil {
			zap.L().Error("[appdeploy] buildAndDeploy panic", zap.String("app", aid), zap.Any("panic", r))
			msg := fmt.Sprintf("[panic] 构建部署异常崩溃: %v", r)
			h.markFailed(ctx, psID, aid, msg, "")
			if ins, e := h.store.GetOrCreateInstance(ctx, aid, env); e == nil && ins != nil {
				ins.Status = "failed"
				ins.LastError = msg
				_ = h.store.UpdateInstance(ctx, ins)
			}
			if histVer > 0 {
				if e := h.store.FinishDeployHistory(ctx, aid, env, histVer, "failed", truncateStr(msg, 200), "panic 兜底终结", "", 0); e != nil {
					zap.L().Warn("deploy_history panic 终结失败（不影响部署）", zap.String("app", aid), zap.Error(e))
				}
			}
		}
	}()
	// ……（Get 读取 / GetOrCreateInstance 原有代码不动）……

	// P3 部署历史：开始行（在途）。版本预测=当前计数器+1（Deployer.Build 内自增后即此值；
	// 计数器只升不降，同版本号不会重复尝试）。best-effort：失败仅 Warn，绝不阻塞部署。
	histVer = ins.Version + 1
	if e := h.store.InsertDeployHistory(ctx, a.ID, env, histVer, "fixed", operator, sha); e != nil {
		zap.L().Warn("deploy_history 插入失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
	}
```

3b. **原生成败终点**（约 1979-1985 行 `deployNative` err 分支，`return` 前加）：

```go
				if nerr := h.deployNative(ctx, a, ins, env, node, desc); nerr != nil {
					ins.Status = "failed"
					ins.LastError = nerr.Error()
					_ = h.store.UpdateInstance(ctx, ins)
					h.markFailed(ctx, psID, a.ID, ins.LastError, ins.BuildLog)
					if e := h.store.FinishDeployHistory(ctx, a.ID, env, histVer, "failed", truncateStr(nerr.Error(), 200), "原生部署失败", "", 0); e != nil {
						zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
					}
				}
```

（原生成功 `return nil` 前补 success finish——deployNative 返回 nil 分支，即 `if nerr != nil` 块之后、`return` 之前。注意 deployNative 成功时 ins.HostPort=0/URL=node.Host；image 无值传空串：）

```go
				if e := h.store.FinishDeployHistory(ctx, a.ID, env, histVer, "success", "", "原生部署", ins.Image, ins.HostPort); e != nil {
					zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
				}
				return
```

3c. **build 失败终点**（约 2023-2029 行 `if err != nil` 块 return 前）：

```go
			if e := h.store.FinishDeployHistory(ctx, a.ID, env, histVer, "failed", truncateStr(err.Error(), 200), "", "", 0); e != nil {
				zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
			}
```

3d. **run 失败终点**（约 2072-2077 行 `if dErr != nil` 块 return 前）：

```go
		if e := h.store.FinishDeployHistory(ctx, a.ID, env, histVer, "failed", truncateStr(dErr.Error(), 200), "", "", 0); e != nil {
			zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
		}
```

3e. **成功终点**（函数尾 `syncOverviewIfProd` 之前，约 2110 行；ins.Image/ins.HostPort 此刻已是实态值）：

```go
	// P3 部署历史：成功终态（记实态镜像/端口）。
	if e := h.store.FinishDeployHistory(ctx, a.ID, env, histVer, "success", "", "", ins.Image, ins.HostPort); e != nil {
		zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
	}
```

另有两处早退（应用记录读取失败 :1953 / 实例创建失败 :1969 return）：**不写 history**（无 app 行可关联或 INSERT 锚点未到，属系统级失败，instance 都没有）。

3f. **4 个调用点传 operator**（同文件）：

- Deploy:1801 → `go h.buildAndDeploy(psID, aid, "", env, in.NodeID, buildDir, c.GetString(auth.CtxUserID))`
- Promote:1870 → `go h.buildAndDeploy(psID, aid, "", EnvProd, in.NodeID, "", c.GetString(auth.CtxUserID))`
- DeployCommit:1915 → `go h.buildAndDeploy(psID, aid, in.SHA, env, in.NodeID, "", c.GetString(auth.CtxUserID))`
- DeployByAppID:2499 → `go h.buildAndDeploy(a.ProjectSpaceID, appID, "", EnvTest, "", "", "system")`

- [ ] **Step 4: 跑测试确认通过**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run TestDeployHistory -v
```

预期：Task 1/2/3 全部 PASS。全包回归：

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/...
```

预期：全 PASS（既有 ai_engine_test 直接调 `h.aiDeploy` 未改签名——Task 4 才改——此处先确认无其它 buildAndDeploy 调用点编译错）。

- [ ] **Step 5: Commit**

```bash
git add platform/backend/internal/appdeploy/handler.go platform/backend/internal/appdeploy/deploy_history_test.go
git commit -m "feat(appdeploy): 固定链部署历史写入（buildAndDeploy 四终态+panic兜底）

- INSERT 锚 GetOrCreateInstance 后（版本=计数器+1，覆盖 4 入口）
- build/run/原生失败+成功四终态 UPDATE；panic recover 兜底终结
- operator 尾参透传（Deploy/Promote/DeployCommit=用户名，发布中心=system）
- 写失败一律 Warn 不反噬主链

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: AI 链写入（aiDeploy INSERT + aiFail/aiFailWithRollback/成功 UPDATE）

**Files:**

- Modify: `platform/backend/internal/appdeploy/ai_engine.go` — `aiDeploy`（:127）加 `operator string` 尾参 + INSERT + 成功 UPDATE；`aiFail`（:314）/ `aiFailWithRollback`（:328）加 finish；panic 兜底
- Modify: `platform/backend/internal/appdeploy/handler.go` — Deploy 分流处（:1795）传 operator
- Test: `platform/backend/internal/appdeploy/deploy_history_test.go`（追加）；`platform/backend/internal/appdeploy/ai_engine_test.go`（2 处既有调用补参）

**Interfaces:**

- Consumes: Task 1 Store 方法、Task 3 `buildAndDeploy` 新签名（rollback 重放不经 buildAndDeploy，无依赖）。
- Produces: `func (h *Handler) aiDeploy(psID, aid, env, buildDir, operator string)`（终态签名）。

- [ ] **Step 1: 写失败集成测试（复用 ai_engine_test 的 fake 模式）**

追加到 `deploy_history_test.go`（import 补 `"os"` `"path/filepath"`）：

```go
// TestDeployHistory_AiChainSuccess AI 链成功：fake opencode + fake inspect → history 行
// engine=ai result=success（版本=1，镜像/端口为实态回填值）。
func TestDeployHistory_AiChainSuccess(t *testing.T) {
	aiInspect = func(ctx context.Context, h *Handler, container string) (bool, int, string) {
		return true, 9100, "appdeploy/ai-demo-test:v1"
	}
	defer func() { aiInspect = nil }()
	origRun := aiOpencodeRun
	aiOpencodeRun = fakeOpencode(t, true)
	defer func() { aiOpencodeRun = origRun }()

	h, _ := newHTTPHandler(t)
	ctx := context.Background()
	a := seedApp(t, h, "ps_1", "ai-demo", t.TempDir())
	h.aiDeploy("ps_1", a.ID, EnvTest, "", "ai-op")

	items, err := h.store.ListDeployHistoryByApp(ctx, a.ID, 20)
	if err != nil || len(items) != 1 {
		t.Fatalf("应恰 1 行: %v %v", items, err)
	}
	it := items[0]
	if it.Engine != "ai" || it.Result != "success" || it.Operator != "ai-op" || it.Version != 1 {
		t.Fatalf("AI 成功行: %+v", it)
	}
	if it.Image != "appdeploy/ai-demo-test:v1" || it.HostPort != 9100 {
		t.Fatalf("成功行应记实态: %+v", it)
	}
}
// TestDeployHistory_AiChainFailRollbackNotes AI 链失败：fake 失败 → history 行 failed，
// notes 含回滚记录（spec：回滚成功也记 failed，详情进 notes）。
func TestDeployHistory_AiChainFailRollbackNotes(t *testing.T) {
	aiInspect = func(ctx context.Context, h *Handler, container string) (bool, int, string) {
		return false, 0, ""
	}
	defer func() { aiInspect = nil }()
	origRun := aiOpencodeRun
	aiOpencodeRun = fakeOpencode(t, false)
	defer func() { aiOpencodeRun = origRun }()

	h, _ := newHTTPHandler(t)
	ctx := context.Background()
	a := seedApp(t, h, "ps_1", "hist-ai2", t.TempDir())
	h.aiDeploy("ps_1", a.ID, EnvTest, "", "ai-op")

	items, _ := h.store.ListDeployHistoryByApp(ctx, a.ID, 20)
	if len(items) != 1 || items[0].Result != "failed" || items[0].Engine != "ai" {
		t.Fatalf("AI 失败行: %+v", items)
	}
	if items[0].ErrorSummary == "" {
		t.Fatalf("失败行应记 error_summary: %+v", items[0])
	}
}
```

（`fakeOpencode` 的合法 result 写死 `appdeploy-ai-demo-test-v1`（ai_engine_test.go:63），应用名必须取 `ai-demo` 使 slug 匹配，否则五步验证 1 挂；newHTTPHandler 每 test Truncate 全表，与既有 TestAiDeploy_FullChain 同名应用不冲突。）

- [ ] **Step 2: 跑测试确认失败**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run TestDeployHistory_AiChain -v
```

预期：编译失败（`aiDeploy` 还是 4 参签名）。

- [ ] **Step 3: 实现 ai_engine.go 写入**

3a. **签名 + panic 兜底**（ai_engine.go:127-142）：加尾参 `operator string`；recover 块末尾补 finish（`histVer` 闭包变量同 Task 3 模式，声明在 recover 后、`ins.Version++` 前，赋值在 INSERT 前）：

```go
func (h *Handler) aiDeploy(psID, aid, env, buildDir, operator string) {
	ctx := context.Background()
	histVer := 0
	defer func() {
		if r := recover(); r != nil {
			// ……原有 markFailed + instance failed 代码不动……
			if histVer > 0 {
				if e := h.store.FinishDeployHistory(ctx, aid, env, histVer, "failed", truncateStr(fmt.Sprintf("[panic] AI 部署异常崩溃: %v", r), 200), "panic 兜底终结", "", 0); e != nil {
					zap.L().Warn("deploy_history panic 终结失败（不影响部署）", zap.String("app", aid), zap.Error(e))
				}
			}
		}
	}()
```

3b. **INSERT**（版本自增后，:167-169 `ins.Version++` / `version := ins.Version` 之后、端口预分配之前——端口满早退也应有历史行）：

```go
	ins.Version++
	version := ins.Version
	// P3 部署历史：开始行（AI 链版本精确已知）。best-effort。
	histVer = version
	if e := h.store.InsertDeployHistory(ctx, a.ID, env, version, "ai", operator, ""); e != nil {
		zap.L().Warn("deploy_history 插入失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
	}
```

3c. **成功终点**（函数尾 :310 `zap.L().Info("[appdeploy] AI 部署成功"...)` 之前）：

```go
	// P3 部署历史：成功终态（实态镜像/端口）。
	if e := h.store.FinishDeployHistory(ctx, a.ID, env, version, "success", "", "", ins.Image, ins.HostPort); e != nil {
		zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
	}
```

3d. **aiFail 加 finish**（:314 签名不变，函数末尾 markFailed 后）：

```go
func (h *Handler) aiFail(ctx context.Context, psID string, a *Application, ins *AppInstance, log *aiLog, reason string) {
	ins.Status = "failed"
	ins.LastError = reason
	ins.BuildLog = log.tail()
	_ = h.store.UpdateInstance(ctx, ins)
	h.markFailed(ctx, psID, a.ID, reason, ins.BuildLog)
	// P3 部署历史：失败终态（部署前失败，无回滚）。
	if e := h.store.FinishDeployHistory(ctx, a.ID, ins.Env, ins.Version, "failed", truncateStr(reason, 200), "", "", 0); e != nil {
		zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
	}
}
```

3e. **aiFailWithRollback 加 finish**（:328 函数末尾同样式；此时 reason 可能已带「（已自动回滚到 vN 服务…）」补写段——直接整段进 error_summary（truncateStr 200 截断），notes 固定记回滚标记）：

```go
	// P3 部署历史：失败终态（spec：回滚成功也记 failed，回滚详情进 notes——失败率不被回滚稀释）。
	if e := h.store.FinishDeployHistory(ctx, a.ID, ins.Env, ins.Version, "failed", truncateStr(reason, 200), "AI 链失败已触发自动回滚（详见 last_error/build_log）", "", 0); e != nil {
		zap.L().Warn("deploy_history 终结失败（不影响部署）", zap.String("app", a.ID), zap.Error(e))
	}
```

3f. **调用点**：

- handler.go:1795 → `go h.aiDeploy(psID, aid, env, buildDir, c.GetString(auth.CtxUserID))`
- ai_engine_test.go:90 → `h.aiDeploy("ps_1", a.ID, EnvTest, "", "tester")`
- ai_engine_test.go:125 → `h.aiDeploy("ps_1", a.ID, EnvTest, "", "tester")`

- [ ] **Step 4: 跑测试确认通过**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run 'TestDeployHistory|TestAiDeploy|TestEngineFor' -v
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/...
```

预期：新 2 测 + 既有 AI 测全 PASS，全包回归绿。

- [ ] **Step 5: Commit**

```bash
git add platform/backend/internal/appdeploy/ai_engine.go platform/backend/internal/appdeploy/handler.go platform/backend/internal/appdeploy/deploy_history_test.go platform/backend/internal/appdeploy/ai_engine_test.go
git commit -m "feat(appdeploy): AI 链部署历史写入（版本自增后 INSERT + 三终态 UPDATE）

- aiDeploy 加 operator 尾参；成功记实态镜像/端口
- aiFailWithRollback 记 failed+notes（回滚不稀释失败率）
- panic 兜底经 histVer 闭包变量终结

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: Detail 聚合扩展 + 统计 API

**Files:**

- Modify: `platform/backend/internal/appdeploy/detail.go` — `AppFullView` 加 `DeployHistory` 字段 + `Detail()` 聚合
- Modify: `platform/backend/internal/appdeploy/handler.go` — `Register` 加路由 + 新 `DeployStatsAPI` handler
- Test: `platform/backend/internal/appdeploy/deploy_history_test.go`（追加）

**Interfaces:**

- Consumes: Task 1 `ListDeployHistoryByApp` / `DeployStats`。
- Produces:
  - `AppFullView.DeployHistory []DeployHistoryItem \`json:"deploy_history"\``（最近 20 条）
  - `GET /api/v1/appdeploy/deploy-stats?days=30` → `httpx.OK(c, *DeployStatsResult)`（Task 6 前端按 `resp.data.engines[]` / `resp.data.daily[]` 消费）
  - `func (h *Handler) DeployStatsAPI(c *gin.Context)`（注册名与 Store 方法 DeployStats 区分）

- [ ] **Step 1: 写失败测试**

```go
// TestDetail_DeployHistorySection Detail 聚合含 deploy_history 节（最近 20 条 + 空归一 []）。
func TestDetail_DeployHistorySection(t *testing.T) {
	h, _ := newHTTPHandler(t)
	ctx := context.Background()
	a := seedApp(t, h, "ps_1", "hist-detail", t.TempDir())
	_ = h.store.InsertDeployHistory(ctx, a.ID, EnvTest, 1, "fixed", "yxt", "")
	_ = h.store.FinishDeployHistory(ctx, a.ID, EnvTest, 1, "success", "", "", "img:v1", 9100)

	d, err := h.store.Detail(ctx, "ps_1", a.ID)
	if err != nil || d == nil {
		t.Fatalf("detail: %v %v", d, err)
	}
	if len(d.DeployHistory) != 1 || d.DeployHistory[0].Version != 1 {
		t.Fatalf("detail 应含 1 条历史: %+v", d.DeployHistory)
	}

	// 空应用（无历史）→ 空切片非 nil
	b := seedApp(t, h, "ps_1", "no-hist", t.TempDir())
	d2, _ := h.store.Detail(ctx, "ps_1", b.ID)
	if d2.DeployHistory == nil || len(d2.DeployHistory) != 0 {
		t.Fatalf("无历史应空切片: %+v", d2.DeployHistory)
	}
}

// TestHandler_DeployStatsAPI 统计 API：days 钳制 + 空表 200 零值。
func TestHandler_DeployStatsAPI(t *testing.T) {
	h, _ := newHTTPHandler(t)
	r := newRouterWith(h)
	// 空表
	code, resp := doReq(t, r, http.MethodGet, "/api/v1/appdeploy/deploy-stats?days=30", nil)
	if code != 200 {
		t.Fatalf("空表应 200，得 %d %v", code, resp)
	}
	eng, _ := resp["data"].(map[string]interface{})["engines"].([]interface{})
	if len(eng) != 0 {
		t.Fatalf("空表应 0 引擎组: %v", resp)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/ -run 'TestDetail_DeployHistory|TestHandler_DeployStatsAPI' -v
```

预期：编译失败（`DeployHistory` 字段未定义 / 路由 404）。

- [ ] **Step 3: 实现**

detail.go `AppFullView` 结构体加字段（`DeployNeeds` 之后）：

```go
	DeployHistory []DeployHistoryItem `json:"deploy_history"` // P3 部署历史（最近 20 条，含在途）
```

`Detail()` 里 `d.Tasks` 聚合之后、`LoadDeployManifest` 之前加：

```go
	// P3：部署历史（最近 20 条；孤儿过滤；best-effort）
	d.DeployHistory, _ = s.ListDeployHistoryByApp(ctx, appID, 20)
```

norm 区（:148-156）加：

```go
	d.DeployHistory = norm(d.DeployHistory)
```

handler.go `Register`（:191 deploy-nodes 段之前）加：

```go
	r.GET("/appdeploy/deploy-stats", h.DeployStatsAPI) // P3：部署运营统计（按引擎分组）
```

handler.go 文件加 handler（放 `Stats` handler 之后）：

```go
// DeployStatsAPI 部署运营统计（P3）：近 N 天按引擎聚合成功率/耗时/失败 top/每日 trend。
//
// @Summary      部署统计
// @Tags         appdeploy
// @Produce      json
// @Param        days  query  int  false  "统计窗口天数（默认 30，钳制 1-90）"
// @Success      200   {object}  map[string]interface{}  "按引擎分组统计"
// @Security     BearerAuth
// @Router       /appdeploy/deploy-stats [get]
func (h *Handler) DeployStatsAPI(c *gin.Context) {
	days, _ := strconv.Atoi(c.Query("days"))
	if days == 0 {
		days = 30
	}
	res, err := h.store.DeployStats(c.Request.Context(), days)
	if err != nil {
		httpx.Err(c, 500, 50023, "部署统计查询失败: "+err.Error())
		return
	}
	httpx.OK(c, res)
}
```

（days 钳制 1–90 已在 Store.DeployStats 内做；handler 只补默认 30。）

- [ ] **Step 4: 跑测试确认通过**

```bash
cd platform/backend && go test -p 1 -count=1 ./internal/appdeploy/...
```

预期：全 PASS（含既有 Detail 测试——AppFullView 加字段不破坏既有断言）。

- [ ] **Step 5: Commit**

```bash
git add platform/backend/internal/appdeploy/detail.go platform/backend/internal/appdeploy/handler.go platform/backend/internal/appdeploy/deploy_history_test.go
git commit -m "feat(appdeploy): Detail 聚合部署历史 + 部署统计 API

- AppFullView.deploy_history 最近 20 条（含在途，孤儿过滤）
- GET /appdeploy/deploy-stats?days=30（钳制 1-90，空表零值）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 前端——详情页部署历史时间线 + 统计页 + 侧边栏入口

**Files:**

- Modify: `platform/frontend/app/applications/page.tsx` — Detail 类型加 `deploy_history`；详情弹窗「部署」列加可折叠时间线
- Create: `platform/frontend/app/ops/deploy-stats/page.tsx` — 统计页
- Modify: `platform/frontend/app/_components/sidebar.tsx` — `/ops` 项后加「部署统计」入口（或并入运维中心页内 tab——按下面 Step 1 的独立页方案）

**Interfaces:**

- Consumes: Task 5 的 `GET /api/v1/appdeploy/deploy-stats`（响应 `data.engines[]`：`engine/success/failed/avg_sec/med_sec/top_errors[]{fragment,count}`；`data.daily[]`：`day/engine/success/failed`）与 Detail 响应 `data.deploy_history[]`：`id/app_id/env/version/engine/result(''/success/failed)/operator/sha/image/host_port/duration_sec/error_summary/notes/created_at/finished_at`。

- [ ] **Step 1: 详情页时间线**

applications/page.tsx 的 `Detail` 类型（:78-129）`deploy_needs` 字段后加：

```tsx
  deploy_history: {
    id: number;
    env: string;
    version: number;
    engine: string; // fixed / ai
    result: string; // ''=在途 / success / failed
    operator: string;
    duration_sec?: number | null;
    error_summary?: string;
    notes?: string;
    created_at: string;
  }[];
```

详情弹窗「部署」列（:2083-2138，`发布（{detail.releases.length}）` 行之前）加可折叠时间线：

```tsx
{
  /* P3：部署历史时间线（最近 20 条，可折叠） */
}
{
  detail.deploy_history && detail.deploy_history.length > 0 && (
    <div className="mt-1">
      <details>
        <summary className="cursor-pointer text-text-muted">
          部署历史（{detail.deploy_history.length}）
        </summary>
        <div className="mt-1 max-h-48 space-y-0.5 overflow-y-auto">
          {detail.deploy_history.map((dh) => (
            <div key={dh.id} className="flex items-center gap-1 text-xs">
              <span className="text-text-muted">
                {new Date(dh.created_at).toLocaleString("zh-CN", { hour12: false })}
              </span>
              <span className="rounded bg-surface-2 px-1">v{dh.version}</span>
              <span title={dh.engine === "ai" ? "AI 引擎" : "固定引擎"}>
                {dh.engine === "ai" ? "🤖" : "🔧"}
              </span>
              <span
                className={
                  dh.result === "success"
                    ? "text-success"
                    : dh.result === "failed"
                      ? "text-danger"
                      : "text-warn"
                }
              >
                {dh.result === "success" ? "成功" : dh.result === "failed" ? "失败" : "部署中…"}
              </span>
              {dh.duration_sec != null && <span>{dh.duration_sec}s</span>}
              <span className="text-text-muted">{dh.operator}</span>
              {(dh.error_summary || dh.notes) && (
                <span
                  className="text-text-muted"
                  title={`${dh.error_summary ?? ""}${dh.notes ? "\n" + dh.notes : ""}`}
                >
                  ⓘ
                </span>
              )}
            </div>
          ))}
        </div>
      </details>
    </div>
  );
}
```

（时间行 hover `title` 展开 error_summary/notes——表格空间有限，不占常驻面积；spec「点开展开」以 `<details>` 折叠 + title 提示满足。）

- [ ] **Step 2: 统计页**

`platform/frontend/app/ops/deploy-stats/page.tsx`（仿 ops/page.tsx 的 fetch + Authorization 模式——**注意每处 fetch 都带 `Authorization: Bearer ${getAuthToken()}`**，教训见 notif 修复）：

```tsx
"use client";

import { useEffect, useState } from "react";
import { API_BASE_URL, getAuthToken } from "@/lib/api";

type ErrFreq = { fragment: string; count: number };
type EngineStats = {
  engine: string;
  success: number;
  failed: number;
  avg_sec: number;
  med_sec: number;
  top_errors: ErrFreq[];
};
type DailyCount = { day: string; engine: string; success: number; failed: number };
type StatsResp = { engines: EngineStats[]; daily: DailyCount[] };

export default function DeployStatsPage() {
  const [days, setDays] = useState(30);
  const [data, setData] = useState<StatsResp | null>(null);
  const [err, setErr] = useState("");

  useEffect(() => {
    setData(null);
    setErr("");
    fetch(`${API_BASE_URL}/appdeploy/deploy-stats?days=${days}`, {
      headers: { Authorization: `Bearer ${getAuthToken()}` },
    })
      .then((r) => r.json())
      .then((r) => {
        if (r.code !== 0) throw new Error(r.message || String(r.code));
        setData(r.data);
      })
      .catch((e) => setErr(String(e)));
  }, [days]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <h1 className="text-lg font-semibold">部署统计</h1>
        <select
          value={days}
          onChange={(e) => setDays(Number(e.target.value))}
          className="rounded border border-border bg-surface px-2 py-1 text-sm"
        >
          {[7, 14, 30, 60, 90].map((d) => (
            <option key={d} value={d}>
              近 {d} 天
            </option>
          ))}
        </select>
      </div>

      {err && <div className="text-danger">加载失败：{err}</div>}
      {!data && !err && <div className="text-text-muted">加载中…</div>}

      {data && data.engines.length === 0 && <div className="text-text-muted">窗口内无部署记录</div>}

      {data?.engines.map((e) => {
        const total = e.success + e.failed;
        const rate = total > 0 ? Math.round((e.success / total) * 100) : 0;
        return (
          <div key={e.engine} className="rounded-lg border border-border bg-surface p-4">
            <div className="flex items-baseline gap-3">
              <span className="font-medium">
                {e.engine === "ai" ? "🤖 AI 引擎" : "🔧 固定引擎"}
              </span>
              <span className="text-2xl font-bold text-success">{rate}%</span>
              <span className="text-sm text-text-muted">
                成功率（{e.success}/{total}）
              </span>
            </div>
            <div className="mt-2 text-sm text-text-muted">
              平均耗时 {e.avg_sec}s · 中位 {e.med_sec}s · 失败 {e.failed} 次
            </div>
            {e.top_errors.length > 0 && (
              <div className="mt-2">
                <div className="text-sm text-text-muted">失败 top 原因：</div>
                {e.top_errors.map((t) => (
                  <div key={t.fragment} className="text-sm">
                    <span className="text-danger">{t.count}</span> × {t.fragment}
                  </div>
                ))}
              </div>
            )}
          </div>
        );
      })}

      {data && data.daily.length > 0 && (
        <div className="rounded-lg border border-border bg-surface p-4">
          <div className="mb-2 font-medium">每日部署（新→旧）</div>
          <table className="w-full text-sm">
            <thead>
              <tr className="text-text-muted">
                <th className="text-left">日期</th>
                <th className="text-left">引擎</th>
                <th className="text-right">成功</th>
                <th className="text-right">失败</th>
              </tr>
            </thead>
            <tbody>
              {data.daily.map((d) => (
                <tr key={`${d.day}-${d.engine}`} className="border-t border-border">
                  <td>{d.day}</td>
                  <td>{d.engine === "ai" ? "🤖" : "🔧"}</td>
                  <td className="text-right text-success">{d.success}</td>
                  <td className="text-right text-danger">{d.failed}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 侧边栏入口**

sidebar.tsx NAV 数组 `/ops` 项（:10）后加：

```tsx
  { href: "/ops/deploy-stats", label: "部署统计", icon: "📈" },
```

- [ ] **Step 4: 构建验证**

```bash
cd platform/frontend && npm run build
```

预期：编译通过无类型错误（详情类型可空字段已按 `?` 声明）。

- [ ] **Step 5: Commit**

```bash
git add platform/frontend/app/applications/page.tsx platform/frontend/app/ops/deploy-stats/page.tsx platform/frontend/app/_components/sidebar.tsx
git commit -m "feat(frontend): 部署历史时间线 + 部署统计页

- 详情弹窗部署列可折叠时间线（vN 徽标+引擎图标+结果色+耗时+operator）
- /ops/deploy-stats 成功率大数字卡+失败 top+每日表（无图表库）
- 侧边栏「部署统计」入口

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: .28 部署 + e2e 验证

**Files:** 无代码（部署 + 浏览器/脚本验证）。产出 e2e 记录追加到本计划文件末尾。

**Interfaces:**

- Consumes: Task 1-6 全部（main 分支已合）。

**部署前置**：本任务开始前分支已按惯例合 main（executing 流程的收尾）。`.28` 操作走 `ssh -o PubkeyAcceptedAlgorithms=+ssh-rsa -o StrictHostKeyChecking=no -i ~/.ssh/miscode root@10.10.0.28`，compose 在 `/opt/anp/deploy`，**只动 `deploy_` 前缀容器**。

- [ ] **Step 1: 部署 .28**

```bash
# 本地（Windows Git Bash）打镜像传 .28 —— 与 P2 部署同流程
cd "D:/Projects/智源-ANP平台/platform/backend" && GOOS=linux GOARCH=amd64 go build -o /tmp/anp-backend ./cmd/server
# （前端 next build 产物同步——沿用既有 deploy 脚本/流程，见 deploy/README.md）
```

后：`.28` 上 `/opt/anp/deploy` 目录 `docker compose up -d deploy_backend deploy_frontend`（具体服务名按 deploy/README.md），健康探活 `curl -s http://127.0.0.1:8088/healthz`。

迁移自动跑（backend 启动时 db.Migrate）；验证表已建：

```bash
ssh ... root@10.10.0.28 "docker exec deploy_postgres_1 psql -U anp -d anp_dev -c '\\d deploy_history'"
```

- [ ] **Step 2: e2e——固定链一次成功部署**

浏览器（或 curl 带 admin token）：`POST /api/v1/project-spaces/<ps>/apps/<aid>/deploy {"env":"test","engine":"fixed"}` → 等 running → 详情页见时间线一行 `vN 🔧 成功 <耗时>s <operator>`；DB 复核 `SELECT version,engine,result,duration_sec,image,host_port FROM deploy_history ORDER BY id DESC LIMIT 1;` result=success 且 duration>0（**本机测不到的固定链 success 断言在此补上**）。

- [ ] **Step 3: e2e——AI 链一次部署**

`PUT /api/v1/config/deploy_engine` 值 `ai`（**必须走 API 刷缓存，不能直插 SQL**）→ deploy test → 时间线一行 `vN 🤖 …` → 统计页 `/ops/deploy-stats` 出现两引擎组与成功率 → **把 deploy_engine 改回 `fixed`**。

- [ ] **Step 4: e2e——孤儿行不进统计**

```bash
# 手造在途行后 backdate 成孤儿
ssh ... root@10.10.0.28 "docker exec deploy_postgres_1 psql -U anp -d anp_dev -c \"INSERT INTO deploy_history(app_id,env,version,engine,result,operator) VALUES('app_e2e_orphan','test',99,'ai','','e2e'); UPDATE deploy_history SET created_at=NOW()-interval '31 minutes' WHERE app_id='app_e2e_orphan';\""
```

→ 统计页/engine 组计数**不含**该行（总数与 Step 2/3 一致）→ 清理 `DELETE FROM deploy_history WHERE app_id='app_e2e_orphan';`。

- [ ] **Step 5: e2e 结论记录 + 收尾**

把四步结果（截图/SQL 输出摘要）记到本计划文件末尾「e2e 记录」节；更新 `.superpowers/sdd/progress.md` ledger；按惯例报告用户待浏览器确认项。

---

## Self-Review（已执行）

1. **Spec 覆盖**：§1 DDL→Task1；§2 四写点（固定 INSERT/三终态+panic、AI INSERT/三终态+panic）→Task3/4；§3.1 时间线→Task5+6；§3.2 统计 API+页→Task5+6；§4 错误处理（best-effort/空表/days 钳制）→Task1/2/5；§5 测试（单测/集成/AI fake/.28 e2e 含孤儿）→Task1/2/3/4/7；§6 决策已内化（回滚记 failed 进 notes、UPDATE 定位、不回填无任务需要——新表天然空起）。缺口：无。
2. **占位符扫描**：无 TBD/TODO/「适当处理」；Task 1 的 DeployStats 已是最终干净代码（FILTER 宽行直查，无死代码）。
3. **类型一致性**：`DeployHistoryItem` 字段在 Task 5（Detail）/Task 6（前端 type）一致；`DeployStatsResult.engines/daily` JSON tag 与前端 `engines/daily` 一致；`buildAndDeploy` 7 参签名在 Task 3 定义、Task 4 无新调用（rollback 直接走 deployer.Deploy）；`aiDeploy` 5 参签名 Task 4 定义、handler.go:1795 与 ai_engine_test 两处调用同步改；Task 4 测试应用名 `ai-demo` 与 fakeOpencode 写死的容器名/镜像匹配，断言与 aiInspect 注入值一致。

## e2e 记录（Task 7 执行时填写）

**执行时间**：2026-08-19（.28 部署 + API 级 e2e 全过）

### Step 1: .28 部署 ✓

- tar 同步 20 个改动文件（backend docs/appdeploy/migrations + frontend 4 文件）→ `docker-compose up --build -d backend/frontend`
- backend healthz 200；迁移 000037 自动执行：`\d deploy_history` 确认 15 列 + `idx_dephist_app`/`idx_dephist_stat` 两索引
- frontend :8088 = 200；容器内 `/app/.next/server/app/ops/deploy-stats/page.js` 存在；首页 grep「部署统计」命中（sidebar 入口上线）
- 冒烟：`GET /appdeploy/deploy-stats?days=30` 空表 → 200 `{engines:[],daily:[]}`（非 null 空数组）

### Step 2: 固定链成功部署 ✓（补上本机测不到的 success 断言）

- 夹具：ps_default 建 `hist-e2e-fixed`（app_2c7fd921-f8fc-4d6a-8），repo 写最小 Dockerfile(nginx:alpine)+index.html 并 git commit
- `POST .../deploy {"env":"test","engine":"fixed"}` → running
- Detail `deploy_history`：`v1 fixed 'success' admin dur=3s image=appdeploy/hist-e2e-fixed-test:v1 port=9103`（实态镜像/端口非空）
- DB 复核逐字段一致；应用实探 :9103 返回 fixture 页面
- 统计 API：fixed 组 success=1 avg=3s med=3s，daily 2026-08-19 fixed 1/0

### Step 3: AI 链部署 ✓

- `PUT /api/v1/config/deploy_engine {"value":"ai"}`（经 API 刷缓存，未直插 SQL）
- deploy 显式 engine=ai → preparing → running（真实 opencode+shim+五步验证全链）
- Detail：`v2 ai 'success' admin dur=38s image=...:v2 port=9106`；容器 `appdeploy-hist-e2e-fixed-test-v2` Up；:9106 实探 200
- 双引擎统计：fixed 1/3s + ai 1/38s 两组并存，daily 两行
- **deploy_engine 已改回 fixed**（PUT 值 fixed 确认返回）

### Step 4: 孤儿行不进统计 ✓

- 插入在途行 v99(engine=ai,result='')+backdate 31min → 统计前后对比：ai 组仍 success=1/failed=0，daily 仍 2 行 → **孤儿未泄漏**
- 已 `DELETE FROM deploy_history WHERE app_id='app_e2e_orphan'` 清理

### 遗留

- e2e 应用 `hist-e2e-fixed`（2 条 success 历史行）保留在 .28 作展示样本，可随时删（删应用会级联清历史）
- 浏览器人工确认项（用户）：详情弹窗时间线折叠/ⓘ 悬浮、`/ops/deploy-stats` 统计卡与窗口切换
