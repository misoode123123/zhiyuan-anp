# 交接摘要（2026-07-26）— 工作台三需求会话

> 本会话 context 已很大（四问题修复+①+③）。下次新 session 从这里接手。

## 分支与提交（fix/workbench-4issues，未推 main）

1. `90b4157` — 工作台四问题修复（首页向导/附件类型/派发幂等/任务失败可见）— **.28 已验证**
2. `f576ee7` — ①部署权限分离（dev 可部署 test 不可 prod）— **.28 已验证**（AC1/3/6 通过）
3. `fdf9560` — ③claude MVP 代码就绪（codews ttyd 改造，编译+test 通过）— **已部署验证（见下）**
4. （本次新提交）③ Task4-7 部署 + sysconfig NULL-description 启动崩溃修复 — **.28 已验证（AC1/3/5/7 通过）**

## .28 当前状态

- backend/frontend 跑①版本（含四问题修复 + ①RBAC）。
- ③**已部署验证**（Task4-7 完成）：backend 镜像已装 claude-code+ttyd，`/healthz/deep` 全 ok（claude/ttyd/opencode LookPath 通过）；claude 工作台 ttyd→智谱 GLM-4.6 调通（`claude -p` 注入 system_config env 返回 CLAUDE_OK）；opencode 回归正常（session_id/deep_url 正常）；AGENTS.md 已刷新生效。
- demo 用户 dev1/biz1/gate1 密码已统一为 admin123（为验证 RBAC，可沿用测③）。
- SSH：`ssh -i ~/.ssh/miscode root@10.10.0.28`；PG：`deploy_postgres_1` 库 anp/anp/anp。

## ③ claude MVP — 代码就绪，下一步 Task 4-7（按 plan）

plan：`docs/superpowers/plans/2026-07-26-多AI工具-claude-MVP.md`；spec：`docs/PRD/2026-07-26-多AI工具claude-codex-PRD.md`

已完成（Task 1-3，commit fdf9560）：

- `codews/tool.go`：Tool.Start 加 env 参数；ClaudeTool.Start = `ttyd -p <port> --writable claude --continue`；OpenCodeTool/CodexTool 适配
- `codews/manager.go`：Manager 注入 config.Store；toolEnv(claude) 注入 `ANTHROPIC_BASE_URL/AUTH_TOKEN(=zhipuai key)/MODEL`；ensureSession/DeepURL 仅 opencode
- `codews/coverage_test.go`：NewManager/stubTool 适配新签名
- `appdeploy/handler.go:70`：codews.NewManager(appDeployHost, configStore)
- `cmd/server/main.go`：种子加 `claude_base_url`/`claude_model`（key 复用 zhipuai_api_key）

**已完成（Task 4-7，本次提交）**：

- Task 4：`deploy/Dockerfile.backend` 加 `apk add --no-cache ttyd` + `npm i -g @anthropic-ai/claude-code`；`deploy/docker-compose.prod.yml` 加 `../data/claude:/root/.claude` volume；`server/healthcheck.go` LookPath 加 claude/ttyd（`/healthz/deep` 一查便知镜像装好）。
- Task 5：`appdeploy/handler.go:175` 注释"opencode"→"三工具"；RegisterChange 仅 opencode 调 SessionMessages（claude/codex 跳过对话，总结降级基于 diff）；InjectRequirement 非 opencode 返回"该工具不支持平台侧需求注入，请在终端手动操作"。
- Task 6：`applications/page.tsx` 去 claude/codex `*` 预留标记 + title"（*为预留）"。
- Task 7：scp 8 文件（③ Task1-3 的 tool/manager/main + 本次 handler/healthcheck/Dockerfile/compose/page）+ 重建 backend/frontend；手动插 system_config（claude_base_url/claude_model）。
- ⚠️ **部署踩坑（已修，见 [bug 文档](../../bugs/2026-07-26-system_config-NULL-description-启动崩溃.md)）**：手插两行 claude 配置未给 `description`（NULL）→ `Store.Load` 把可空 description 扫进 `string` 致 backend 启动 FATAL 崩溃循环。根因修：`sysconfig.go` Load/All 改 `COALESCE(description,'')`；数据补 description。**可空列 scan 必须容忍 NULL**。
- ⚠️ **验证期误操作（已恢复）**：AC 验证清理时误调 `DELETE /apps/snake`（本想停工作台进程），snake 应用被删；repo 源码在磁盘完好，已重建（new id app_bbd4467f）+ 部署 test 恢复 running。后续清理工作台只用 `pkill ttyd/opencode`，勿 DELETE app。
- AC 验证（.28 全通过）：AC1 claude ttyd→智谱 CLAUDE_OK；AC3 opencode 回归(session_id/deep_url 正常)；AC5 AGENTS.md 刷新生效；AC7 system_config 注入生效。AC4(多开发隔离)/AC6(--continue 恢复) 代码路径未变未单独测。

**前提已验证（.28）**：claude-code v2.1.220 可装、ttyd 1.7.4 可装、`claude -p` 配 `ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic`+智谱key 调通 GLM。⚠️ root 容器下 claude 不能配 `--dangerously-skip-permissions`（交互工作台用户在 ttyd 终端处理权限提示）。

## ②绩效记录 — 未开始

摸底已完成（conversation/codetask 缺 user_id、actor 用用户名非 user.id、codews 纯内存、零绩效统计）。spec 待写（顺序 ①→③→② 的最后）。

## 关键 memory

session-restart-per-feature（本偏好）、deploy-prod-10.10.0.28、pg-cut、no-sqlite-pg-only、autonomous-execution-preference、solution-doc-before-dev、verify-cross-frontend-backend。
