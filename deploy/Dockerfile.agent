# 智源 ANP AI 运行时（Python + zhipuai）
FROM python:3.10-slim
WORKDIR /app
COPY platform/agent-runtime/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY platform/agent-runtime/ .
EXPOSE 8001
CMD ["python", "-m", "agent_runtime.main"]
